// <!-- custom: file to set markdownlint config, i use it to disable some rules as advised by chatgpt/becomingthrough after asking it anyways etc and hinted showed in markdown lint docs that i didn't read as too tedious but that gave me the idea to ask chatgpt/becomingthrough helpful both or not or yes or etc anyways etc hopefully this config file and its lengthy but anyways etc xd or not xd or yes xd anyways etc config file is helpful as well but or not but or yes but anyways etc -->
// <!-- custom: use .jsonc instead of .json so we can comment the/this markdownlint config file anyways etc -->
{
	"default": true,
	"MD010": false, // <!-- custom: tabs are valid in fenced code blocks do not remove them, since i didn't easily/quickly found a rule specifically for tabs in fenced code blocks and this seems like a global rule, easier/safer to disable the rule entirely even though we would benefit slightly but less but anyways etc from tab checking globally, minus in fenced code blocks anyways etc -->
	"MD013": false, // <!-- custom: similarly some very long lines are valid in our code, generally for commented-out code but maybe not only (didn't check if there are exceptions to this is just if i may say a general explanation of me if i may say but anyways etc) but anyways etc, so show them as such anyways etc -->
	"MD033": false // <!-- custom: similarly or/and also anyways etc we use tags like <img src="./_1_AdvCiv-SAS/Images_In_General/misc_0.x/0.34_Techtree_ingame (1).JPG" alt="0.34_Techtree_ingame (1).JPG" width="250"></img> raw in our .md readme files, as this is the only way i found to reduce image width/size to say 250px instead of full size, an image markdown insert like ![img_military_tree_1](/_1_AdvCiv-SAS/Images_In_General/misc_0.x/0.43%20military%20tree_modified.png) doesn't seem to provide such an option (to reduce size in this case i mean anyways etc) or at least it doesn't seem functional, so until and if i ever find hopefully or not or yes or other or etc anyways etc such an option, i am fine to keep it as is in this case as this seems functional anyways etc, so disable the markdownlint rule instead anyways etc -->
}

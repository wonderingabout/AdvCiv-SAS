# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1619
# Commit: 6794398758ba34329fc0ba23e443aa4051773804
# History segment: pre-SAS AdvCiv history
# Parent used for diff: aa514d65a05fb12040546ff06ef6fb009b7d34f6
# Title: Trait category in Sevopedia repaired
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	3	Assets/Python/Contrib/Sevopedia/SevoPediaMain.py

[PATCHES]

diff --git a/Assets/Python/Contrib/Sevopedia/SevoPediaMain.py b/Assets/Python/Contrib/Sevopedia/SevoPediaMain.py
index 4f61fb913..1f40c15cd 100644
--- a/Assets/Python/Contrib/Sevopedia/SevoPediaMain.py
+++ b/Assets/Python/Contrib/Sevopedia/SevoPediaMain.py
@@ -637,7 +637,7 @@ class SevoPediaMain(CvPediaScreen.CvPediaScreen):
 		self.placeItems(WidgetTypes.WIDGET_PEDIA_DESCRIPTION, self.getTraitInfo)
 	
 	def getTraitList(self):
-		return self.getSortedList(gc.getNumNewConceptInfos(), self.getTraitInfo, True)
+		return self.getSortedList(gc.getNumNewConceptInfos(), self.getTraitInfo, True, False) # advc.004y: bCheckGraphicalOnly = False
 
 	def getTraitInfo(self, id):
 		info = gc.getNewConceptInfo(id)
@@ -919,12 +919,13 @@ class SevoPediaMain(CvPediaScreen.CvPediaScreen):
 	def isSortLists(self):
 		return AdvisorOpt.SevopediaSortItemList()
 
-	def getSortedList(self, numInfos, getInfo, noSort=False):
+	# advc.004y: bCheckGraphicalOnly flag added (for CvInfo types that don't have that element)
+	def getSortedList(self, numInfos, getInfo, noSort=False, bCheckGraphicalOnly = True):
 		list = []
 		for i in range(numInfos):
 			item = getInfo(i)
 			# advc.004y: GraphicalOnly check added
-			if item and not item.isGraphicalOnly():
+			if item and (not bCheckGraphicalOnly or not item.isGraphicalOnly()):
 				list.append((item.getDescription(), i))
 		if self.isSortLists() and not noSort:
 			list.sort()

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1422

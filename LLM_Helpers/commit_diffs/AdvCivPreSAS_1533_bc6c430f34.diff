# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1533
# Commit: bc6c430f34f4723c7847d6fed39945426161d10d
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 1d4fe90164f07c494a00bded6434a98cc66557ae
# Title: A few style/const changes; no 0% tech progress in Debug mode.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	5	Assets/Python/Screens/CvMainInterface.py
3	4	CvGameCoreDLL/CvDLLWidgetData.cpp
3	3	CvGameCoreDLL/CvGameCoreUtils.cpp
3	3	CvGameCoreDLL/CvGameCoreUtils.h
1	2	CvGameCoreDLL/CvInfos.h

[PATCHES]

diff --git a/Assets/Python/Screens/CvMainInterface.py b/Assets/Python/Screens/CvMainInterface.py
index 8ed4a51df..3fe1272d6 100644
--- a/Assets/Python/Screens/CvMainInterface.py
+++ b/Assets/Python/Screens/CvMainInterface.py
@@ -5335,11 +5335,12 @@ class CvMainInterface:
 						iProgressPercent = min(iProgressPercent, 99)
 						iProgressPercent = max(iProgressPercent, 0)
 				szTempBuffer += u" %d%%" % iProgressPercent
-				
-				szBuffer += szTempBuffer # Don't req. eCurrentResearch!=-1
-				if bAlignIcons:
-					# Pass along iProgressPercent instead of iTurnsLeft
-					scores.setResearch(eCurrentResearch, iProgressPercent)
+				# Don't req. eCurrentResearch!=-1 unless in Debug mode
+				if eCurrentResearch != -1 or not gc.getGame().isDebugMode():
+					szBuffer += szTempBuffer
+					if bAlignIcons:
+						# Pass along iProgressPercent instead of iTurnsLeft
+						scores.setResearch(eCurrentResearch, iProgressPercent)
 					# </advc.085>
 			# BUG (Dead Civs): ...end of indentation
 # BUG - Dead Civs - end

diff --git a/CvGameCoreDLL/CvDLLWidgetData.cpp b/CvGameCoreDLL/CvDLLWidgetData.cpp
index 49a5f5c7f..3623fcb02 100644
--- a/CvGameCoreDLL/CvDLLWidgetData.cpp
+++ b/CvGameCoreDLL/CvDLLWidgetData.cpp
@@ -4061,12 +4061,11 @@ void CvDLLWidgetData::parseScoreboardCheatText(CvWidgetDataStruct &widgetDataStr
 	
 	// double space if had any war plan
 	if (bHadAny)
-	{
 		szBuffer.append(NEWLINE);
-	}
 
-	// advc.104: K-Mod/BBAI war percentages aren't helpful for testing UWAI
-	if(getWPAI.isEnabled()) return;
+	// <advc.104> K-Mod/BBAI war percentages aren't helpful for testing UWAI
+	if(getWPAI.isEnabled())
+		return; // </advc.104>
 	
 	// calculate war percentages
 	float fOverallWarPercentage = 0;

diff --git a/CvGameCoreDLL/CvGameCoreUtils.cpp b/CvGameCoreDLL/CvGameCoreUtils.cpp
index 07d919fb2..a0d50125c 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.cpp
+++ b/CvGameCoreDLL/CvGameCoreUtils.cpp
@@ -61,7 +61,7 @@ double dMedian(vector<double>& distribution, bool bSorted) {
 	return (distribution[medianIndex] + distribution[medianIndex - 1]) / 2;
 }
 
-double dMean(vector<double>& distribution) {
+double dMean(vector<double> const& distribution) {
 
 	FAssert(!distribution.empty());
 	double r = 0;
@@ -70,7 +70,7 @@ double dMean(vector<double>& distribution) {
 	return r / distribution.size();
 }
 
-double dMax(vector<double>& distribution) {
+double dMax(vector<double> const& distribution) {
 
 	FAssert(!distribution.empty());
 	double r = distribution[0];
@@ -80,7 +80,7 @@ double dMax(vector<double>& distribution) {
 	return r;
 }
 
-double dMin(vector<double>& distribution) {
+double dMin(vector<double> const& distribution) {
 
 	FAssert(!distribution.empty());
 	double r = distribution[0];

diff --git a/CvGameCoreDLL/CvGameCoreUtils.h b/CvGameCoreDLL/CvGameCoreUtils.h
index 1b3e46471..83f2de527 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.h
+++ b/CvGameCoreDLL/CvGameCoreUtils.h
@@ -27,9 +27,9 @@ bool bernoulliSuccess(double pr, // 0 <= pr <= 1
 		char const* pszLog = "", bool bAsync = false,
 		int iData1 = MIN_INT, int iData2 = MIN_INT);
 double dMedian(std::vector<double>& distribution, bool bSorted = false);
-double dMean(std::vector<double>& distribution);
-double dMax(std::vector<double>& distribution);
-double dMin(std::vector<double>& distribution);
+double dMean(std::vector<double> const& distribution);
+double dMax(std::vector<double> const& distribution);
+double dMin(std::vector<double> const& distribution);
 // see e.g. Wikipedia: "percentile rank"
 double percentileRank(std::vector<double>& distribution, double score,
 		bool bSorted = false, // Is the distribution sorted (ascending)?

diff --git a/CvGameCoreDLL/CvInfos.h b/CvGameCoreDLL/CvInfos.h
index 49f994595..f009ce8ff 100644
--- a/CvGameCoreDLL/CvInfos.h
+++ b/CvGameCoreDLL/CvInfos.h
@@ -17,7 +17,6 @@
 #pragma warning( disable: 4127 )
 
 class CvXMLLoadUtility;
-class WarAndPeaceAI; // advc.104x
 
 //++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 //
@@ -3674,7 +3673,7 @@ public:
 	#endif
 	bool read(CvXMLLoadUtility* pXML);
 	//---------------------------------------PROTECTED MEMBER VARIABLES---------------------------------
-	friend WarAndPeaceAI; // advc.104x (for applyPersonalityWeight)
+	friend class WarAndPeaceAI; // advc.104x (for applyPersonalityWeight)
 protected:
 
 	int m_iWonderConstructRand;

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=4375

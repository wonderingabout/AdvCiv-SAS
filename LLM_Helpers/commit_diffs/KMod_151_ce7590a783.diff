# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 151
# Commit: ce7590a7834729617b3f29850deb67be01a2edb3
# History segment: K-Mod history
# Parent used for diff: c83b5e03e40e87ddbdd7626d30751abbd7c16d16
# Title: Fixed a bug in the new Priority Factor code for productivity buildings. Gave a boost to value of the first sources of espionage points.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	CvGameCoreDLL/CvCity.cpp
2	2	CvGameCoreDLL/CvCityAI.cpp
3	3	CvGameCoreDLL/CvPlayerAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 18a214de8..3170f0838 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -12580,7 +12580,8 @@ void CvCity::doReligion()
 	{
 		for (iI = 0; iI < GC.getNumReligionInfos(); iI++)
 		{
-			if (!isHasReligion((ReligionTypes)iI))
+			//if (!isHasReligion((ReligionTypes)iI))
+			if (!isHasReligion((ReligionTypes)iI) && GC.getGame().isReligionFounded((ReligionTypes)iI)) // K-Mod
 			{
 				if ((iI == GET_PLAYER(getOwnerINLINE()).getStateReligion()) || !(GET_PLAYER(getOwnerINLINE()).isNoNonStateReligionSpread()))
 				{

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index bf142924b..f4481d5e6 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -4717,7 +4717,7 @@ int CvCityAI::AI_buildingValueThreshold(BuildingTypes eBuilding, int iFocusFlags
 										if (iI == YIELD_PRODUCTION)
 										{
 											// priority += 2% per 1% in production increase. roughly. More when at war.
-											iPriorityFactor += std::min(100, iTempValue/std::max(1, (GET_TEAM(getTeam()).getAnyWarPlanCount(true) > 0 ? 2 : 3)*getYieldRate(YIELD_PRODUCTION)));
+											iPriorityFactor += std::min(100, (GET_TEAM(getTeam()).getAnyWarPlanCount(true) > 0 ? 300 : 200)*iTempValue/std::max(1, 5*getYieldRate(YIELD_PRODUCTION)));
 											iTempValue *= 3;
 											iTempValue /= 2;
 										}
@@ -4849,7 +4849,7 @@ int CvCityAI::AI_buildingValueThreshold(BuildingTypes eBuilding, int iFocusFlags
 						if (iI == YIELD_PRODUCTION)
 						{
 							// priority += 2% per 1% in production increase. roughly. More when at war.
-							iPriorityFactor += std::min(100, iTempValue/std::max(1, (GET_TEAM(getTeam()).getAnyWarPlanCount(true) > 0 ? 2 : 3)*getYieldRate(YIELD_PRODUCTION)));
+							iPriorityFactor += std::min(100, (GET_TEAM(getTeam()).getAnyWarPlanCount(true) > 0 ? 300 : 200)*iTempValue/std::max(1, 5*getYieldRate(YIELD_PRODUCTION)));
 							iTempValue *= 3;
 							iTempValue /= 2;
 						}

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 38cc5feaa..d3a026147 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -2453,9 +2453,9 @@ int CvPlayerAI::AI_commerceWeight(CommerceTypes eCommerce, CvCity* pCity) const
 			}
 			
 			// K-Mod
-			iAllTeamTotalPoints /= std::max(1, iTeamCount);
-			iWeight *= std::min(GET_TEAM(getTeam()).getEspionagePointsEver() + 2*iAllTeamTotalPoints, 6*iAllTeamTotalPoints);
-			iWeight /= std::max(12, 3 * iAllTeamTotalPoints);
+			iAllTeamTotalPoints /= std::max(1, iTeamCount); // Get the average total points
+			iWeight *= std::min(GET_TEAM(getTeam()).getEspionagePointsEver() + 2*iAllTeamTotalPoints + 36*GC.getGame().getGameTurn(), 6*iAllTeamTotalPoints);
+			iWeight /= std::max(1, 3 * iAllTeamTotalPoints);
 			if (AI_isDoStrategy(AI_STRATEGY_BIG_ESPIONAGE))
 			{
 				iWeight *= 2;

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2918

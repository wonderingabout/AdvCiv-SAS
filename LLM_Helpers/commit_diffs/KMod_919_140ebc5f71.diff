# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 919
# Commit: 140ebc5f71971f08f9bd9fc3650cf964393bb832
# History segment: K-Mod history
# Parent used for diff: ce64387415038e42d0117a6f3b89b3c005c94c1b
# Title: Fixed some minor issues with "make peace with other civ" trades.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	0	CvGameCoreDLL/CvDeal.cpp
18	0	CvGameCoreDLL/CvTeam.cpp
1	0	CvGameCoreDLL/CvTeam.h
3	2	CvGameCoreDLL/CvTeamAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvDeal.cpp b/CvGameCoreDLL/CvDeal.cpp
index a3cd64249..72b7aab30 100644
--- a/CvGameCoreDLL/CvDeal.cpp
+++ b/CvGameCoreDLL/CvDeal.cpp
@@ -875,6 +875,8 @@ bool CvDeal::startTrade(TradeData trade, PlayerTypes eFromPlayer, PlayerTypes eT
 		}
 		//GET_TEAM(GET_PLAYER(eFromPlayer).getTeam()).makePeace((TeamTypes)trade.m_iData);
 		GET_TEAM(GET_PLAYER(eFromPlayer).getTeam()).makePeace((TeamTypes)trade.m_iData, false); // K-Mod. (units will be bumped after the rest of the trade deals are completed.)
+		GET_TEAM(GET_PLAYER(eFromPlayer).getTeam()).signPeaceTreaty((TeamTypes)trade.m_iData); // K-Mod. Use a standard peace treaty rather than a simple cease-fire.
+		// K-Mod todo: this team should offer something fair to the peace-team if this teams endWarVal is higher.
 		break;
 
 	case TRADE_WAR:

diff --git a/CvGameCoreDLL/CvTeam.cpp b/CvGameCoreDLL/CvTeam.cpp
index a4fd1278e..3b744d115 100644
--- a/CvGameCoreDLL/CvTeam.cpp
+++ b/CvGameCoreDLL/CvTeam.cpp
@@ -1818,6 +1818,24 @@ void CvTeam::meet(TeamTypes eTeam, bool bNewDiplo)
 	}
 }
 
+// K-Mod
+void CvTeam::signPeaceTreaty(TeamTypes eTeam)
+{
+	TradeData item;
+	setTradeItem(&item, TRADE_PEACE_TREATY);
+
+	if (GET_PLAYER(getLeaderID()).canTradeItem(GET_TEAM(eTeam).getLeaderID(), item) && GET_PLAYER(GET_TEAM(eTeam).getLeaderID()).canTradeItem(getLeaderID(), item))
+	{
+		CLinkList<TradeData> ourList;
+		CLinkList<TradeData> theirList;
+
+		ourList.insertAtEnd(item);
+		theirList.insertAtEnd(item);
+
+		GC.getGameINLINE().implementDeal(getLeaderID(), (GET_TEAM(eTeam).getLeaderID()), &ourList, &theirList);
+	}
+}
+// K-Mod end
 
 void CvTeam::signOpenBorders(TeamTypes eTeam)
 {

diff --git a/CvGameCoreDLL/CvTeam.h b/CvGameCoreDLL/CvTeam.h
index 5f22dbb8a..7d9af4270 100644
--- a/CvGameCoreDLL/CvTeam.h
+++ b/CvGameCoreDLL/CvTeam.h
@@ -56,6 +56,7 @@ public:
 	//bool canContact(TeamTypes eTeam) const; // Exposed to Python
 	bool canContact(TeamTypes eTeam, bool bCheckWillingness = false) const; // K-Mod, Exposed to Python
 	void meet(TeamTypes eTeam, bool bNewDiplo);																		// Exposed to Python
+	void signPeaceTreaty(TeamTypes eTeam); // K-Mod
 	void signOpenBorders(TeamTypes eTeam);																				// Exposed to Python
 	void signDefensivePact(TeamTypes eTeam);																			// Exposed to Python
 	bool canSignDefensivePact(TeamTypes eTeam);

diff --git a/CvGameCoreDLL/CvTeamAI.cpp b/CvGameCoreDLL/CvTeamAI.cpp
index 18ab3c4b1..9adaa4f24 100644
--- a/CvGameCoreDLL/CvTeamAI.cpp
+++ b/CvGameCoreDLL/CvTeamAI.cpp
@@ -3801,9 +3801,10 @@ DenialTypes CvTeamAI::AI_makePeaceTrade(TeamTypes ePeaceTeam, TeamTypes eTeam) c
 	} */
 	// K-Mod
 	if (AI_refusePeace(ePeaceTeam))
-	{
 		return DENIAL_VICTORY;
-	}
+
+	if (!GET_PLAYER(getLeaderID()).canContactAndTalk(GET_TEAM(ePeaceTeam).getLeaderID()) || GET_TEAM(ePeaceTeam).AI_refusePeace(getID()))
+		return DENIAL_CONTACT_THEM;
 	// K-Mod end
 
 	return NO_DENIAL;

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=2923

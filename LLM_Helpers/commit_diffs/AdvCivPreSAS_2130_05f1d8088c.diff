# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2130
# Commit: 05f1d8088cc84d42595227f37175868238aedf7a
# History segment: pre-SAS AdvCiv history
# Parent used for diff: e13301fea35d6f25ecbf4d573559f4c32cd31e38
# Title: Fix likely bug in BtS memory manager (unused?)
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
9	4	CvGameCoreDLL/CvMemoryManager.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvMemoryManager.cpp b/CvGameCoreDLL/CvMemoryManager.cpp
index 5fa2e5711..ef81de591 100644
--- a/CvGameCoreDLL/CvMemoryManager.cpp
+++ b/CvGameCoreDLL/CvMemoryManager.cpp
@@ -112,7 +112,10 @@ void *__cdecl operator new(size_t size, char* pcFile, int iLine)
 
 void *__cdecl operator new[](size_t size, char* pcFile, int iLine)
 {
-	void* result = gDLL->newMem(size, pcFile, iLine);
+	FAssertMsg(false, "To check if operator new[](size_t*,char*,int) is ever used"); // advc.test
+	//void* result = gDLL->newMem(size, pcFile, iLine);
+	// advc.001: Looks like a copy-paste error
+	void* result = gDLL->newMemArray(size, pcFile, iLine);
 	memset(result, 0xDA, size); // advc.make
 	return result;
 }
@@ -124,7 +127,10 @@ void __cdecl operator delete(void* pvMem, char* pcFile, int iLine)
 
 void __cdecl operator delete[](void* pvMem, char* pcFile, int iLine)
 {
-	gDLL->delMem(pvMem, pcFile, iLine);
+	FAssertMsg(false, "To check if operator delete[](void*,char*,int) is ever used"); // advc.test
+	//gDLL->delMem(pvMem, pcFile, iLine);
+	// advc.001: Looks like a copy-paste error
+	gDLL->delMemArray(pvMem, pcFile, iLine);
 }
 
 
@@ -137,7 +143,7 @@ unsigned int memSize(void* a)
 {
 	return gDLL->memSize(a);
 }
-#endif // K-Mod
+#endif
 
 
 #ifdef MEMORY_TRACKING
@@ -252,7 +258,6 @@ CMemoryTrace::~CMemoryTrace()
 	if ( GetProcessMemoryInfo( GetCurrentProcess(), &pmc, sizeof(pmc)) )
 	{
 		char buffer[200];
-
 		sprintf(buffer, "function %s added %dK bytes, total now %dK\n", m_name, (int)(pmc.WorkingSetSize - m_start)/1024, (int)pmc.WorkingSetSize/1024);
 		OutputDebugString(buffer);
 	}

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1621

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4359
# Commit: b2708af7bf86f483748a7fa481f6a553b42333cc
# History segment: AdvCiv-SAS branch history
# Parent used for diff: dd243872ff1e7052c150056d89e71af83441ab87
# Title: Refactor: Rename "std11" namespace to "sequtil"
# Full commit message/metadata: see _1_AdvCiv-SAS/git_logs/git_log_anonymized_email_003_AdvCiv-SAS.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	CvGameCoreDLL/CvGame.cpp
1	1	CvGameCoreDLL/CvGameCoreUtils.h
1	1	CvGameCoreDLL/CvRandom.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 21877eb8c..26c0c16a3 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -1536,7 +1536,7 @@ void CvGame::rearrangeTeamStarts(/* advc.027: */ bool bOnlyWithinArea, scaled rI
 
 	std::vector<PlayerTypes> aeStartingLocs(MAX_CIV_PLAYERS);
 	// each player starting in own location
-	std11::iota(aeStartingLocs.begin(), aeStartingLocs.end(), (PlayerTypes)0);
+	sequtil::iota(aeStartingLocs.begin(), aeStartingLocs.end(), (PlayerTypes)0);
 
 	int iBestScore = getTeamClosenessScore(aaiDistances, aeStartingLocs);
 	bool bFoundSwap = true;

diff --git a/CvGameCoreDLL/CvGameCoreUtils.h b/CvGameCoreDLL/CvGameCoreUtils.h
index 24c903480..bf3875161 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.h
+++ b/CvGameCoreDLL/CvGameCoreUtils.h
@@ -35,7 +35,7 @@ class FAStar;
  have been too lazy to find a proper place for. */
 
 // advc:
-namespace std11
+namespace sequtil
 {
 // Erik: "Back-ported" from C++11
 template<class ForwardIt, class T>

diff --git a/CvGameCoreDLL/CvRandom.cpp b/CvGameCoreDLL/CvRandom.cpp
index 0bb728ea7..8989500cc 100644
--- a/CvGameCoreDLL/CvRandom.cpp
+++ b/CvGameCoreDLL/CvRandom.cpp
@@ -93,7 +93,7 @@ void CvRandom::shuffle(int* piShuffle, int iNum)
 // advc.enum:
 void CvRandom::shuffle(std::vector<int>& aiIndices)
 {
-	std11::iota(aiIndices.begin(), aiIndices.end(), 0);
+	sequtil::iota(aiIndices.begin(), aiIndices.end(), 0);
 	int const iSize = (int)aiIndices.size();
 	FAssertMsg(iSize > 0, "Shuffling empty vector; intended?");
 	for (int i = 0; i < iSize; i++)

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=1572

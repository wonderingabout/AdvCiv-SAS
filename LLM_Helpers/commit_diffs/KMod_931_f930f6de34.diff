# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 931
# Commit: f930f6de3494bb8c23a6c672403444b6540a354f
# History segment: K-Mod history
# Parent used for diff: 4693ac65cec3b0dfb3096e36cbdf0f3c4fd37b62
# Title: Fixed a minor bug in AI_transportGoTo which affected diagonal landings.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
9	1	CvGameCoreDLL/CvGameCoreUtils.h
1	2	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameCoreUtils.h b/CvGameCoreDLL/CvGameCoreUtils.h
index 4deeb7fc8..072cc953b 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.h
+++ b/CvGameCoreDLL/CvGameCoreUtils.h
@@ -152,11 +152,12 @@ inline int plotDistance(int iX1, int iY1, int iX2, int iY2)													// Expos
 	return (std::max(iDX, iDY) + (std::min(iDX, iDY) / 2));
 }
 
-// K-Mod, for convenience:
+// K-Mod, plot-to-plot alias for convenience:
 inline int plotDistance(const CvPlot* plot1, const CvPlot* plot2)
 {
 	return plotDistance(plot1->getX_INLINE(), plot1->getY_INLINE(), plot2->getX_INLINE(), plot2->getY_INLINE());
 }
+// K-Mod end
 
 // 3 | 3 | 3 | 3 | 3 | 3 | 3
 // -------------------------
@@ -178,6 +179,13 @@ inline int stepDistance(int iX1, int iY1, int iX2, int iY2)													// Expos
 	return std::max(xDistance(iX1, iX2), yDistance(iY1, iY2));
 }
 
+// K-Mod, plot-to-plot alias for convenience:
+inline int stepDistance(const CvPlot* plot1, const CvPlot* plot2)
+{
+	return stepDistance(plot1->getX_INLINE(), plot1->getY_INLINE(), plot2->getX_INLINE(), plot2->getY_INLINE());
+}
+// K-Mod end
+
 inline CvPlot* plotDirection(int iX, int iY, DirectionTypes eDirection)							// Exposed to Python
 {
 	if(eDirection == NO_DIRECTION)

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 1f6b10917..f4f30b783 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -17904,8 +17904,7 @@ bool CvUnitAI::AI_transportGoTo(CvPlot* pEndTurnPlot, CvPlot* pTargetPlot, int i
 		if (getGroup()->isAmphibPlot(pTargetPlot))
 		{
 			// If target is actually an amphibious landing from pEndTurnPlot, then set pEndTurnPlot = pTargetPlot so that we can land this turn.
-			if (pTargetPlot != pEndTurnPlot &&
-				plotDistance(pTargetPlot->getX_INLINE(), pTargetPlot->getY_INLINE(), pEndTurnPlot->getX_INLINE(), pEndTurnPlot->getY_INLINE()) == 1)
+			if (pTargetPlot != pEndTurnPlot && stepDistance(pTargetPlot, pEndTurnPlot) == 1)
 			{
 				pEndTurnPlot = pTargetPlot;
 			}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=2008

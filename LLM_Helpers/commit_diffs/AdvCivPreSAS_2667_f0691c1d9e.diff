# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2667
# Commit: f0691c1d9ef69fb580be92248988cbfa0d31dbd7
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 632364deceebd9a2988ce2c270d337b6d28013ff
# Title: Announce game name changes in multiplayer
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
18	0	Assets/XML/Text/CIV4GameText_advc.xml
9	5	CvGameCoreDLL/CvGame.cpp
1	0	CvGameCoreDLL/CvGame.h
7	0	CvGameCoreDLL/CvInitCore.cpp
15	0	CvGameCoreDLL/CvPlayer.cpp
1	0	CvGameCoreDLL/CvPlayer.h

[PATCHES]

diff --git a/Assets/XML/Text/CIV4GameText_advc.xml b/Assets/XML/Text/CIV4GameText_advc.xml
index 624aeb0ff..a2244075a 100644
--- a/Assets/XML/Text/CIV4GameText_advc.xml
+++ b/Assets/XML/Text/CIV4GameText_advc.xml
@@ -6392,4 +6392,22 @@
 		<Spanish>1 rival desconocido</Spanish>
 	</TEXT>
 
+	<!-- advc.135c -->
+	<TEXT>
+		<Tag>TXT_KEY_GAME_NAME_CHANGED</Tag>
+		<English>The game name has been changed from "%s1_OldName" to "%s2_NewName".</English>
+		<French>The game name has been changed from "%s1_OldName" to "%s2_NewName".</French>
+		<German>Der Spielname wurde ge�ndert von "%s1_OldName" in "%s2_NewName".</German>
+		<Italian>The game name has been changed from "%s1_OldName" to "%s2_NewName".</Italian>
+		<Spanish>The game name has been changed from "%s1_OldName" to "%s2_NewName".</Spanish>
+	</TEXT>
+	<TEXT>
+		<Tag>TXT_KEY_DEBUG_TOOLS_ENABLED</Tag>
+		<English>Debug tools have been enabled.</English>
+		<French>Debug tools have been enabled.</French>
+		<German>Debug tools have been enabled.</German>
+		<Italian>Debug tools have been enabled.</Italian>
+		<Spanish>Debug tools have been enabled.</Spanish>
+	</TEXT>
+
 </Civ4GameText>

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 588cc9206..3e1d7b015 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -5204,7 +5204,7 @@ void CvGame::updateDebugModeCache()
 	else m_bDebugModeCache = false;
 }
 
-// <advc.135c>
+// advc.135c:
 bool CvGame::isDebugToolsAllowed(bool bWB) const
 {
 	if(gDLL->getInterfaceIFace()->isInAdvancedStart())
@@ -5217,9 +5217,7 @@ bool CvGame::isDebugToolsAllowed(bool bWB) const
 			return false;
 		if(isHotSeat())
 			return true;
-		// (CvGame::getName isn't const)
-		CvWString const& szGameName = GC.getInitCore().getGameName();
-		return (szGameName.compare(L"chipotle") == 0);
+		return isGameNameEnableDebugTools(GC.getInitCore().getGameName());
 	}
 	if(bWB)
 	{
@@ -5227,7 +5225,13 @@ bool CvGame::isDebugToolsAllowed(bool bWB) const
 		return GC.getInitCore().getAdminPassword().empty();
 	}
 	return gDLL->getChtLvl() > 0;
-} // </advc.135c>
+}
+
+// advc.135c:
+bool CvGame::isGameNameEnableDebugTools(CvWString const& kGameName) const
+{
+	return (kGameName.compare(L"chipotle") == 0);
+}
 
 
 int CvGame::getPitbossTurnTime() const

diff --git a/CvGameCoreDLL/CvGame.h b/CvGameCoreDLL/CvGame.h
index 0e9f5cf2a..5495b2d31 100644
--- a/CvGameCoreDLL/CvGame.h
+++ b/CvGameCoreDLL/CvGame.h
@@ -421,6 +421,7 @@ public:
 	DllExport void toggleDebugMode();																				// Exposed to Python
 	DllExport void updateDebugModeCache();
 	bool isDebugToolsAllowed(bool bWB) const; // advc.135c
+	bool isGameNameEnableDebugTools(CvWString const& kGameName) const; // advc.135c
 	DllExport int getPitbossTurnTime() const;																			// Exposed to Python
 	DllExport void setPitbossTurnTime(int iHours);																			// Exposed to Python

diff --git a/CvGameCoreDLL/CvInitCore.cpp b/CvGameCoreDLL/CvInitCore.cpp
index f69c28ec6..d2042cbdb 100644
--- a/CvGameCoreDLL/CvInitCore.cpp
+++ b/CvGameCoreDLL/CvInitCore.cpp
@@ -1426,6 +1426,13 @@ void CvInitCore::setXMLCheck(PlayerTypes eID, CvString const& szXMLCheck)
 	Easier to track external calls in the debugger this way. */
 void CvInitCore::setGameName(CvWString const& szGameName)
 {
+	/*	<advc.135c> Changing the game name can be a step to enable debug tools.
+		Make sure that the other players are aware. */
+	if (getMultiplayer() && !m_szGameName.empty() && m_szGameName != szGameName &&
+		getActivePlayer() != NO_PLAYER)
+	{
+		GET_PLAYER(getActivePlayer()).announceGameNameChange(m_szGameName, szGameName);
+	} // </advc.135c>
 	m_szGameName = szGameName;
 }

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index 4467c5d42..874a140be 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -19389,6 +19389,21 @@ bool CvPlayer::hasSpaceshipArrived() const
 	return GET_TEAM(getTeam()).hasSpaceshipArrived();
 }
 
+// advc.135c: (see call location CvInitCore::setGameName)
+void CvPlayer::announceGameNameChange(CvWString szOldName, CvWString szNewName)
+{
+	gDLL->UI().addMessage(getID(), true, -1,
+			gDLL->getText("TXT_KEY_GAME_NAME_CHANGED",
+			szOldName.GetCString(), szNewName.GetCString()),
+			NULL, MESSAGE_TYPE_MINOR_EVENT);
+	if (GC.getGame().isGameNameEnableDebugTools(szNewName))
+	{
+		gDLL->UI().addMessage(getID(), true, -1,
+				gDLL->getText("TXT_KEY_DEBUG_TOOLS_ENABLED"),
+				NULL, MESSAGE_TYPE_MAJOR_EVENT);
+	}
+}
+
 // advc.210:
 void CvPlayer::checkAlert(int iAlertID, bool bSilent)
 {

diff --git a/CvGameCoreDLL/CvPlayer.h b/CvGameCoreDLL/CvPlayer.h
index 4361a9be0..3d2549e63 100644
--- a/CvGameCoreDLL/CvPlayer.h
+++ b/CvGameCoreDLL/CvPlayer.h
@@ -1391,6 +1391,7 @@ public:
 	DllExport void cheat(bool bCtrl, bool bAlt, bool bShift);
 	DllExport const CvArtInfoUnit* getUnitArtInfo(UnitTypes eUnit, int iMeshGroup = 0) const;
 	DllExport bool hasSpaceshipArrived() const;
+	void announceGameNameChange(CvWString szOldName, CvWString szNewName); // advc.135c
 	// <advc.003u>
 	__forceinline CvPlayerAI& AI()
 	{	//return *static_cast<CvPlayerAI*>(const_cast<CvPlayer*>(this));

[PATCH COVERAGE]
patchFiles=6 embeddedFiles=6 omittedFiles=0 embeddedPatchBytes=5159

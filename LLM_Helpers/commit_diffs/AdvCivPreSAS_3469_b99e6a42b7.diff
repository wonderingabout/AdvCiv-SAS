# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3469
# Commit: b99e6a42b73f2426fe285a16696312181fa44453
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 41517b1a5da89d85b13c92a1f62069fbf423428b
# Title: Misc. refactoring, comment changes
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	4	Assets/XML/GlobalDefines_devel.xml
1	1	CvGameCoreDLL/CvInfo_GameOption.cpp
1	1	CvGameCoreDLL/CvPlayerAI.cpp
1	1	CvGameCoreDLL/CvUnitAI.cpp
1	1	CvGameCoreDLL/UWAIAgent.cpp
2	2	CvGameCoreDLL/UWAIAgent.h

[PATCHES]

diff --git a/Assets/XML/GlobalDefines_devel.xml b/Assets/XML/GlobalDefines_devel.xml
index 75a4d8d19..ba0094e32 100644
--- a/Assets/XML/GlobalDefines_devel.xml
+++ b/Assets/XML/GlobalDefines_devel.xml
@@ -19,11 +19,12 @@
     </Define>
 
 	<!-- [advc.104]
-		 If UWAI reports are enabled (see above), no details about a AI war plan
+		 If UWAI reports are enabled (see above), no details about an AI war plan
 		 are logged unless that war plan has greater utility than this
-		 threshold. There's a separate threshold for war plans against
-		 human players. Recommended: -10/-50; use sth. like -1000/-1000 to log
-		 details always. -->
+		 threshold. Exception: Details abound ongoing wars (i.e. when considering
+		 peace) are logged regardless of the threshold.
+		 There's a separate threshold for war plans against human players.
+		 Recommended: -10/-50; use sth. like -1000/-1000 to log details always. -->
     <Define>
         <DefineName>UWAI_REPORT_THRESH</DefineName>
         <iDefineIntVal>-10</iDefineIntVal>

diff --git a/CvGameCoreDLL/CvInfo_GameOption.cpp b/CvGameCoreDLL/CvInfo_GameOption.cpp
index 81120bca3..88354e096 100644
--- a/CvGameCoreDLL/CvInfo_GameOption.cpp
+++ b/CvGameCoreDLL/CvInfo_GameOption.cpp
@@ -451,7 +451,7 @@ bool CvGameSpeedInfo::read(CvXMLLoadUtility* pXML)
 	pXML->GetChildXmlValByName(&m_iGoldenAgePercent, "iGoldenAgePercent");
 	// <advc>
 	FAssertMsg(m_iResearchPercent >= 100 ?
-			(m_iGoldenAgePercent > m_iResearchPercent / 3 && m_iGoldenAgePercent <= m_iResearchPercent) :
+			(m_iGoldenAgePercent * 3 > m_iResearchPercent && m_iGoldenAgePercent <= m_iResearchPercent) :
 			(3 * m_iGoldenAgePercent < 4 * m_iResearchPercent && m_iGoldenAgePercent >= m_iResearchPercent),
 			"The Golden Age modifier gets used for various adjustments; unusual values should be avoided."); // </advc>
 	pXML->GetChildXmlValByName(&m_iHurryPercent, "iHurryPercent");

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 175e92c33..61c953387 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -21067,7 +21067,7 @@ void CvPlayerAI::AI_proposeWarTrade(PlayerTypes eHireling)
 		iDeclareWarTradeRand ++;
 	}*/
 	// <advc.161>
-	if(iMinAtWarCounter == MAX_INT)
+	if (iMinAtWarCounter == MAX_INT)
 		return;
 	iDeclareWarTradeRand = iDeclareWarTradeRand / 10 + std::min(10, iMinAtWarCounter);
 	{

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 6aa4ec8ad..a3d4959b0 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -21424,7 +21424,7 @@ int CvUnitAI::AI_stackOfDoomExtra() const
 		rMult /= rTrainMod;
 	}
 	// A little extra for naval assault
-	if(getArea().getAreaAIType(kOurTeam.getID()) == AREAAI_ASSAULT)
+	if (getArea().getAreaAIType(kOurTeam.getID()) == AREAAI_ASSAULT)
 		rMult += fixp(0.225);
 	if(kOurTeam.AI_getNumWarPlans(WARPLAN_TOTAL) <= 0)
 		rMult *= fixp(0.85);

diff --git a/CvGameCoreDLL/UWAIAgent.cpp b/CvGameCoreDLL/UWAIAgent.cpp
index 3fd4cb7f4..bfcce1325 100644
--- a/CvGameCoreDLL/UWAIAgent.cpp
+++ b/CvGameCoreDLL/UWAIAgent.cpp
@@ -2040,7 +2040,7 @@ void UWAI::Team::respondToRebuke(TeamTypes eTarget, bool bPrepare)
 		kAgent.AI_isSneakAttackReady(eTarget)))
 	{
 		return;
-		}
+	}
 	if (!bPrepare && !kAgent.canDeclareWar(eTarget))
 		return;
 	FAssert(GET_TEAM(eTarget).isHuman());

diff --git a/CvGameCoreDLL/UWAIAgent.h b/CvGameCoreDLL/UWAIAgent.h
index f611c591d..6f0ef532b 100644
--- a/CvGameCoreDLL/UWAIAgent.h
+++ b/CvGameCoreDLL/UWAIAgent.h
@@ -185,8 +185,8 @@ public:
 	scaled confidenceFromPastWars(TeamTypes eTarget) const;
 	// LeaderHead-derived personality values that aren't cached. More in UWAICache.
 	/*	A measure of how paranoid our leader is, based on EspionageWeight and
-		protective trait. EspionageWeight is between 50 (Gandhi) and 150 (Stalin).
-		Return value is between 0.5 and 1.8.
+		protective trait. EspionageWeight is between 50 (Gandhi) and 140 (Stalin).
+		Return value is between 0.5 and 1.6.
 		"Paranoia" would be a better name, but that already means sth. else
 		(related to the Alert AI strategy). */
 	scaled distrustRating() const;

[PATCH COVERAGE]
patchFiles=6 embeddedFiles=6 omittedFiles=0 embeddedPatchBytes=4132

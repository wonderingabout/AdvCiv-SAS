# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 639
# Commit: 08469361b807bd4c5f09af2f1d54670210035d94
# History segment: K-Mod history
# Parent used for diff: 178bb429857dddee052439c52cd5b029a5312a9c
# Title: Fixed a minor bug in CvPlot::getNonObsoleteBonusType. (It gave access to resources that were revealed if the player had a city on the resource, regardless of...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	5	CvGameCoreDLL/CvPlot.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index 4652546ba..824a3d9e1 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -5608,6 +5608,9 @@ BonusTypes CvPlot::getNonObsoleteBonusType(TeamTypes eTeam, bool bCheckConnected
 		{
 			// note: this checks whether the bonus is connected for the owner of the plot, from the point of view of eTeam.
 			TeamTypes ePlotTeam = getTeam();
+			if (ePlotTeam == NO_TEAM || !GET_TEAM(ePlotTeam).isHasTech((TechTypes)GC.getBonusInfo(eBonus).getTechCityTrade()))
+				return NO_BONUS;
+
 			// note: this function is used inside CvPlot::updatePlotGroupBonuses, which is called during CvPlot::setImprovementType
 			// between when the improvement is changed and the revealed improvement type is updated...
 			// therefore when eTeam == ePlotTeam, we use the real improvement, not the revealed one.
@@ -5615,12 +5618,8 @@ BonusTypes CvPlot::getNonObsoleteBonusType(TeamTypes eTeam, bool bCheckConnected
 
 			FAssert(ePlotTeam != eTeam || eImprovement == getImprovementType());
 
-			if (ePlotTeam == NO_TEAM
-				? eImprovement != NO_IMPROVEMENT && !GC.getImprovementInfo(eImprovement).isImprovementBonusTrade(eBonus)
-				: !isCity() && !GET_TEAM(ePlotTeam).doesImprovementConnectBonus(eImprovement, eBonus))
-			{
+			if (!isCity() && !GET_TEAM(ePlotTeam).doesImprovementConnectBonus(eImprovement, eBonus))
 				return NO_BONUS;
-			}
 		}
 		// K-Mod end
 	}

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1440

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3540
# Commit: 68c59d5ec1c93b534d073b56843a24bb418f618c
# History segment: pre-SAS AdvCiv history
# Parent used for diff: c9685ee6de2ea83c4efd58215bc362375241647f
# Title: Assert game option IDs hardcoded in EXE aren't changed
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	2	CvGameCoreDLL/CvEnums.h
2	0	CvGameCoreDLL/CvInitCore.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvEnums.h b/CvGameCoreDLL/CvEnums.h
index 6190f979f..5c061ad9a 100644
--- a/CvGameCoreDLL/CvEnums.h
+++ b/CvGameCoreDLL/CvEnums.h
@@ -574,15 +574,17 @@ ENUM_START(Commerce, COMMERCE)
 	COMMERCE_ESPIONAGE,
 ENUM_END(Commerce, COMMERCE)
 
+/*	advc: Enumerators 0 and 7 probably mustn't change. The Custom Game screen
+	(in the EXE) acts on those options. */
 ENUM_START(GameOption, GAMEOPTION)
-	GAMEOPTION_ADVANCED_START,
+	GAMEOPTION_ADVANCED_START = 0,
 	GAMEOPTION_SPAH, // advc.250b: Taking the spot of NO_CITY_RAZING
 	GAMEOPTION_NO_CITY_FLIPPING,
 	GAMEOPTION_NO_FLIPPING_AFTER_CONQUEST, // advc.101
 	GAMEOPTION_NO_BARBARIANS,
 	GAMEOPTION_RAGING_BARBARIANS,
 	GAMEOPTION_AGGRESSIVE_AI,
-	GAMEOPTION_LEAD_ANY_CIV,
+	GAMEOPTION_LEAD_ANY_CIV = 7,
 	GAMEOPTION_RANDOM_PERSONALITIES,
 	GAMEOPTION_PICK_RELIGION,
 	GAMEOPTION_NO_TECH_TRADING,

diff --git a/CvGameCoreDLL/CvInitCore.h b/CvGameCoreDLL/CvInitCore.h
index b4fc02316..2d2b9d4f4 100644
--- a/CvGameCoreDLL/CvInitCore.h
+++ b/CvGameCoreDLL/CvInitCore.h
@@ -403,5 +403,7 @@ protected:
 	assertion). Just make sure that new data members are added in the right place. */
 BOOST_STATIC_ASSERT(sizeof(CvInitCore) ==
 		(sizeof(ArrayEnumMap<PlayerTypes,bool>) > 4 ? 444 : 420));
+// advc: Custom Game screen probably relies on this
+BOOST_STATIC_ASSERT(GAMEOPTION_LEAD_ANY_CIV == 7 && GAMEOPTION_ADVANCED_START == 0);
 
 #endif

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1407

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 607
# Commit: f93860079ab3ad24b59dc05b73b6aa27eef744c7
# History segment: K-Mod history
# Parent used for diff: 957abd0d0388780ac976af0781bdbf00c9b56917
# Title: Made some changes to when AI_setChooseProductionDirty is used for human players. - pushing a build order to a city will call AI_setChooseProductionDirty(fals...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	CvGameCoreDLL/CvCity.cpp
4	0	CvGameCoreDLL/CvMessageData.cpp
2	2	CvGameCoreDLL/CvPlayer.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 4c8bf97a3..6190dcb56 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -5415,7 +5415,8 @@ void CvCity::setPopulation(int iNewValue)
 			  ((getPopulation() == 1) && (iOldPopulation > 1))
 			  || ((getPopulation() > iOldPopulation) && (GET_PLAYER(getOwnerINLINE()).getNumCities() <= 2)))
 		{
-			if (!isHuman())
+			//if (!isHuman())
+			if (!isHuman() || isProductionAutomated()) // K-Mod
 			{
 				AI_setChooseProductionDirty(true);
 			}

diff --git a/CvGameCoreDLL/CvMessageData.cpp b/CvGameCoreDLL/CvMessageData.cpp
index c675afbcd..ca0cace1f 100644
--- a/CvGameCoreDLL/CvMessageData.cpp
+++ b/CvGameCoreDLL/CvMessageData.cpp
@@ -182,6 +182,10 @@ void CvNetPushOrder::Execute()
 		if (pCity != NULL)
 		{
 			pCity->pushOrder(m_eOrder, m_iData, -1, m_bAlt, !(m_bShift || m_bCtrl), m_bShift);
+			// K-Mod. We don't want automated cities to overrules our production choice.
+			if (pCity->isHuman())
+				pCity->AI_setChooseProductionDirty(false);
+			// K-Mod end
 		}
 
 		if (GC.getGameINLINE().getActivePlayer() == m_ePlayer)

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index f466875ed..5e423dcad 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -14962,7 +14962,7 @@ bool CvPlayer::doEspionageMission(EspionageMissionTypes eMission, PlayerTypes eT
 				bSomethingHappened = true;
 				bShowExplosion = true;
 				// K-Mod
-				if (!isHuman())
+				if (!isHuman() || pCity->isProductionAutomated())
 					pCity->AI_setChooseProductionDirty(true);
 				// K-Mod end
 			}
@@ -15008,7 +15008,7 @@ bool CvPlayer::doEspionageMission(EspionageMissionTypes eMission, PlayerTypes eT
 				bSomethingHappened = true;
 				bShowExplosion = true;
 				// K-Mod
-				if (!isHuman())
+				if (!isHuman() || pCity->isProductionAutomated())
 					pCity->AI_setChooseProductionDirty(true);
 				// K-Mod end
 			}

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=1948

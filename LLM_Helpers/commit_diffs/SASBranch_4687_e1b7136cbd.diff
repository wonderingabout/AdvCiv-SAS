# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4687
# Commit: e1b7136cbdb1f90f0613ff53e07373f438a62a08
# History segment: AdvCiv-SAS branch history
# Parent used for diff: 7f28ac7ebd63c4178b1440e78db64f607ef6602f
# Title: change call order and function definitions (reorder, same code otherwise) to match intuitively building the page panels from top left to bottom right, in sev...
# Full commit message/metadata: see _1_AdvCiv-SAS/git_logs/git_log_anonymized_email_003_AdvCiv-SAS.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
21	13	Assets/Python/Contrib/Sevopedia/SevoPediaLeader.py

[PATCHES]

diff --git a/Assets/Python/Contrib/Sevopedia/SevoPediaLeader.py b/Assets/Python/Contrib/Sevopedia/SevoPediaLeader.py
index b96dcc34e..dcbcd7ed0 100644
--- a/Assets/Python/Contrib/Sevopedia/SevoPediaLeader.py
+++ b/Assets/Python/Contrib/Sevopedia/SevoPediaLeader.py
@@ -21,6 +21,8 @@ localText = CyTranslator()
 
 class SevoPediaLeader:
 
+
+
 	def __init__(self, main):
 		self.iLeader = -1
 		self.top = main
@@ -66,6 +68,7 @@ class SevoPediaLeader:
 		self.H_AI_PERSONALITY = self.top.B_PEDIA_PAGE - 65
 
 
+
 	def interfaceScreen(self, iLeader):
 		self.iLeader = iLeader
 		screen = self.top.getScreen()
@@ -75,11 +78,13 @@ class SevoPediaLeader:
 		self.leaderWidget = self.top.getNextWidgetName()
 		screen.addLeaderheadGFC(self.leaderWidget, self.iLeader, AttitudeTypes.ATTITUDE_PLEASED, self.X_LEADERHEAD, self.Y_LEADERHEAD, self.W_LEADERHEAD, self.H_LEADERHEAD, WidgetTypes.WIDGET_GENERAL, -1, -1)
 
-		self.placeHistory()
+		# <!-- custom: change call order to match filling from top left to bottom right, more
+		# intuitive this way perhaps, anyways, -->
+		self.placeCiv()
 		self.placeCivic()
 		self.placeReligion()
-		self.placeCiv()
 		self.placeTraits()
+		self.placeHistory()
 		self.placeAIPersonalityPanel(iLeader)
 
 
@@ -93,17 +98,6 @@ class SevoPediaLeader:
 
 
 
-	def placeTraits(self):
-		screen = self.top.getScreen()
-		panelName = self.top.getNextWidgetName()
-		screen.addPanel(panelName, localText.getText("TXT_KEY_PEDIA_TRAITS", ()), "", True, False, self.X_TRAITS, self.Y_TRAITS, self.W_TRAITS, self.H_TRAITS, PanelStyles.PANEL_STYLE_BLUE50)
-		listName = self.top.getNextWidgetName()
-		# advc.001: Civ search moved into a static method
-		szSpecialText = CyGameTextMgr().parseLeaderTraits(self.iLeader, SevoPediaLeader.getCiv(self.iLeader), False, True)
-		szSpecialText = szSpecialText[1:]
-		screen.addMultilineText(listName, szSpecialText, self.X_TRAITS+5, self.Y_TRAITS+30, self.W_TRAITS-10, self.H_TRAITS-35, WidgetTypes.WIDGET_GENERAL, -1, -1, CvUtil.FONT_LEFT_JUSTIFY)
-
-
 	# advc.001 (from Taurus): Static for use by SevoPediaMain; body cut from placeTraits.
 	@staticmethod
 	def getCiv(iLeader):
@@ -118,6 +112,7 @@ class SevoPediaLeader:
 		return iLeaderCiv # </advc.001>
 
 
+
 	def placeCivic(self):		
 		screen = self.top.getScreen()
 		panelName = self.top.getNextWidgetName()
@@ -129,6 +124,7 @@ class SevoPediaLeader:
 			screen.addMultilineText(listName, szCivicText, self.X_CIVIC+5, self.Y_CIVIC+30, self.W_CIVIC-10, self.H_CIVIC-10, WidgetTypes.WIDGET_GENERAL, -1, -1, CvUtil.FONT_LEFT_JUSTIFY)
 
 
+
 	def placeReligion(self):		
 		screen = self.top.getScreen()
 		panelName = self.top.getNextWidgetName()
@@ -140,6 +136,18 @@ class SevoPediaLeader:
 
 
 
+	def placeTraits(self):
+		screen = self.top.getScreen()
+		panelName = self.top.getNextWidgetName()
+		screen.addPanel(panelName, localText.getText("TXT_KEY_PEDIA_TRAITS", ()), "", True, False, self.X_TRAITS, self.Y_TRAITS, self.W_TRAITS, self.H_TRAITS, PanelStyles.PANEL_STYLE_BLUE50)
+		listName = self.top.getNextWidgetName()
+		# advc.001: Civ search moved into a static method
+		szSpecialText = CyGameTextMgr().parseLeaderTraits(self.iLeader, SevoPediaLeader.getCiv(self.iLeader), False, True)
+		szSpecialText = szSpecialText[1:]
+		screen.addMultilineText(listName, szSpecialText, self.X_TRAITS+5, self.Y_TRAITS+30, self.W_TRAITS-10, self.H_TRAITS-35, WidgetTypes.WIDGET_GENERAL, -1, -1, CvUtil.FONT_LEFT_JUSTIFY)
+
+
+
 	def placeHistory(self):
 		screen = self.top.getScreen()
 		panelName = self.top.getNextWidgetName()

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=3547

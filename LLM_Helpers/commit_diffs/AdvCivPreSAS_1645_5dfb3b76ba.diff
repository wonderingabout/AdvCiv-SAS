# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1645
# Commit: 5dfb3b76ba34a249549d08d4ed695aa249c70858
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 3a78c0725b322c039618e2fb32d7b81c1eabdf5d
# Title: Defines for internal profiler moved to Makefile.project
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	2	CvGameCoreDLL/Project/Makefile
1	0	CvGameCoreDLL/Project/Makefile.project

[PATCHES]

diff --git a/CvGameCoreDLL/Project/Makefile b/CvGameCoreDLL/Project/Makefile
index 97a7bfa87..50d8027fe 100644
--- a/CvGameCoreDLL/Project/Makefile
+++ b/CvGameCoreDLL/Project/Makefile
@@ -19,7 +19,8 @@
 #   difficult to associate error messages with a translation unit; but don't need
 #   a slow version of every config.]
 # YOURMOD2 and YOURLOGS (see under "Paths").
-# Internal profiler enabled (FP_PROFILE_ENABLE, USE_INTERNAL_PROFILER).
+# PROJECT_Profile_CFLAGS added to Makefile.project. So that the internal profiler
+# can be enabled from there.
 # CvGameCoreDLL.rc used only for final release.
 # Removed unused /DCVGAMECOREDLL_EXPORTS from Debug_DEFINES
 # devolution (Erik) has added the target 'Final_Release' with
@@ -106,6 +107,7 @@ CUSTOM_LDFLAGS =
 
 PRECOMPILE_DEPENDENCIES =
 PROJECT_CFLAGS =
+PROJECT_Profile_CFLAGS =
 PROJECT_LDFLAGS =
 SOURCE_DIR =
 
@@ -132,6 +134,8 @@ SUBDIRPATH15 = ../
 !ENDIF
 !IF [ECHO PROJECT_LDFLAGS = >> Makefile.project]
 !ENDIF
+!IF [ECHO PROJECT_Profile_CFLAGS = >> Makefile.project]
+!ENDIF
 !IF [ECHO PRECOMPILE_DEPENDENCIES = >> Makefile.project]
 !ENDIF
 !ENDIF
@@ -244,7 +248,7 @@ CFLAGS  = $(Release_CFLAGS) /DFASSERT_ENABLE
 LDFLAGS = $(Release_LDFLAGS)
 !ENDIF
 !IF "$(TARGET)" == "Profile"
-CFLAGS  = $(Release_CFLAGS) /Z7 /DFP_PROFILE_ENABLE /DUSE_INTERNAL_PROFILER
+CFLAGS  = $(Release_CFLAGS) $(PROJECT_Profile_CFLAGS) /Z7
 LDFLAGS = $(Debug_LDFLAGS)
 !ENDIF
 !IF "$(TARGET)" == "Final_Release"

diff --git a/CvGameCoreDLL/Project/Makefile.project b/CvGameCoreDLL/Project/Makefile.project
index 384b2409d..e2da28472 100644
--- a/CvGameCoreDLL/Project/Makefile.project
+++ b/CvGameCoreDLL/Project/Makefile.project
@@ -1,4 +1,5 @@
 SOURCE_DIR = ..
 PROJECT_CFLAGS = /WX
+PROJECT_Profile_CFLAGS = /DFP_PROFILE_ENABLE /DUSE_INTERNAL_PROFILER
 PROJECT_LDFLAGS = /DEF:CvGameCoreDLL.def
 PRECOMPILE_DEPENDENCIES =

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1868

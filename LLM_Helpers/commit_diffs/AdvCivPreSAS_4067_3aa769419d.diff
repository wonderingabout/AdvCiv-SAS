# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4067
# Commit: 3aa769419d5efe301d8cf16c05c55fd7c287f522
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 35d656466529978b2a34c9fdddb2547587f72c1c
# Title: Fix unequal start point assignment (SPaH option)
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	2	CvGameCoreDLL/CvPlayer.cpp
2	0	CvGameCoreDLL/StartPointsAsHandicap.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index 8542a2b2b..5e9f4e7a6 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -944,8 +944,10 @@ void CvPlayer::initFreeUnits()
 	// </advc.027>
 	if (kGame.isOption(GAMEOPTION_ADVANCED_START) &&
 		(!isHuman() || !kGame.isOption(GAMEOPTION_SPAH))) // advc.250b
-	{
-		int iPoints = kGame.getNumAdvancedStartPoints();
+	{	// <advc.250b> Don't overwrite pts. already assigned by SPaH
+		int iPoints = getAdvancedStartPoints();
+		if (iPoints < 0) // </advc.250b>
+			iPoints = kGame.getNumAdvancedStartPoints();
 		// advc.250b (comment): Disabled through Handicap XML
 		iPoints *= GC.getInfo(getHandicapType()).getAdvancedStartPointsMod();
 		iPoints /= 100;

diff --git a/CvGameCoreDLL/StartPointsAsHandicap.cpp b/CvGameCoreDLL/StartPointsAsHandicap.cpp
index a68ee5797..69d0e4543 100644
--- a/CvGameCoreDLL/StartPointsAsHandicap.cpp
+++ b/CvGameCoreDLL/StartPointsAsHandicap.cpp
@@ -148,6 +148,8 @@ void StartPointsAsHandicap::gatherCivs() {
 bool StartPointsAsHandicap::assignPoints() {
 
 	int pointsEntered = GC.getInitCore().getNumAdvancedStartPoints();
+	// (This value should not be used in a SPaH game)
+	GC.getInitCore().setNumAdvancedStartPoints(-1);
 	FAssert(pointsEntered > 0);
 	report += CvString::format("Points entered: %d\n", pointsEntered);
 	bool flatDistrib = (pointsEntered < 10000);

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1398

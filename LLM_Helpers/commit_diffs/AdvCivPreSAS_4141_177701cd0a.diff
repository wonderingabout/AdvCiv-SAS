# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4141
# Commit: 177701cd0a0f4a295a59b4c7bf2d1c8143853763
# History segment: pre-SAS AdvCiv history
# Parent used for diff: cbcca407a90c523a22a4e2cbe43fce376787823f
# Title: Allow vassal/ master to help suppress 3rd-party revolts
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	1	CvGameCoreDLL/CvCity.cpp
14	4	CvGameCoreDLL/CvUnit.cpp
1	0	CvGameCoreDLL/CvUnit.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index c46248481..14f74f4b2 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -4278,8 +4278,12 @@ int CvCity::cultureGarrison(PlayerTypes ePlayer) const
 	int iGarrison = 0; // was 1
 	FOR_EACH_UNIT_IN(pUnit, getPlot())
 	{
-		if (pUnit->getTeam() == getTeam()) // advc.184: Had been counting all units
+		// <advc.184> Had been counting all units
+		if (pUnit->getTeam() != TEAMID(ePlayer) &&
+			pUnit->isGarrisonInTeamCity()) // </advc.184>
+		{
 			iGarrison += pUnit->garrisonStrength();
+		}
 	}
 	/*if (atWar(TEAMID(ePlayer), getTeam()))
 		iGarrison *= 2;*/ // advc.023: commented out

diff --git a/CvGameCoreDLL/CvUnit.cpp b/CvGameCoreDLL/CvUnit.cpp
index ea6f2c0a2..edc1dd0e6 100644
--- a/CvGameCoreDLL/CvUnit.cpp
+++ b/CvGameCoreDLL/CvUnit.cpp
@@ -7100,10 +7100,20 @@ bool CvUnit::isNoUnitCapture() const
 
 bool CvUnit::isMilitaryHappiness() const
 {
-	return m_pUnitInfo->isMilitaryHappiness() &&
-			/* <advc.184> */ getPlot().isCity() &&
-			(getPlot().getTeam() == getTeam() ||
-			getPlot().getTeam() == GET_TEAM(getTeam()).getMasterTeam()); // </advc.184>
+	return (m_pUnitInfo->isMilitaryHappiness() &&
+			isGarrisonInTeamCity()); // advc.184
+}
+
+// advc.184: Whether it is available for suppressing unhappiness and revolts
+bool CvUnit::isGarrisonInTeamCity() const
+{
+	if (!getPlot().isCity())
+		return false;
+	CvTeam const& kCityTeam = GET_TEAM(getPlot().getTeam());
+	if (isInvisible(kCityTeam.getID(), false))
+		return false;
+	return (kCityTeam.getMasterTeam() == getTeam() ||
+			kCityTeam.getID() == GET_TEAM(getTeam()).getMasterTeam());
 }
 
 /*	advc.101: Replacing iCultureGarrison in XML, which increases too slowly

diff --git a/CvGameCoreDLL/CvUnit.h b/CvGameCoreDLL/CvUnit.h
index 3d3a6f9ad..9d40677fe 100644
--- a/CvGameCoreDLL/CvUnit.h
+++ b/CvGameCoreDLL/CvUnit.h
@@ -392,6 +392,7 @@ public:
 		return m_pUnitInfo->isRivalTerritory();
 	}
 	bool isMilitaryHappiness() const;																		// Exposed to Python
+	bool isGarrisonInTeamCity() const; // advc.184
 	int garrisonStrength() const; // advc.101
 	bool isInvestigate() const																				// Exposed to Python
 	{

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2203

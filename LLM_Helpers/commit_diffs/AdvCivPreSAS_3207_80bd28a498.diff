# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3207
# Commit: 80bd28a498fffdc2fb52cf75a573456f1b963b3a
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 1eed14555a8a6becf38254e14824ed5b1d9ea382
# Title: Clean up the Barbarian unit placement commit
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	3	CvGameCoreDLL/BarbarianWeightMap.cpp
1	1	CvGameCoreDLL/BarbarianWeightMap.h
13	13	CvGameCoreDLL/CvGameCoreUtils.cpp
0	2	CvGameCoreDLL/Project/AdvCiv.vcxproj

[PATCHES]

diff --git a/CvGameCoreDLL/BarbarianWeightMap.cpp b/CvGameCoreDLL/BarbarianWeightMap.cpp
index 3c6f025d8..8dbf088cd 100644
--- a/CvGameCoreDLL/BarbarianWeightMap.cpp
+++ b/CvGameCoreDLL/BarbarianWeightMap.cpp
@@ -27,14 +27,13 @@ int BarbarianWeightMap::get(CvPlot const& kPlot) const
 				break;
 		}
 		if (iYieldScore < iTargetYieldScore)
-		{	/*	A fairly big decrease. Note that strength memory will have
+		{	/*	A fairly big decrease. Note that activity memory will have
 				a bit of a leveling effect. */
 			iWeight /= 5;
 		}
 	} // </advc.300>
 	iWeight *= std::max(0, 100
-			/*	The higher the memory value of nearby Barbarian defeats,
-				the less likely are new Barbarians. */
+			// Past activity discourages future activity
 			- m_activityMap.get(kPlot));
 	iWeight /= 100;
 	return iWeight;

diff --git a/CvGameCoreDLL/BarbarianWeightMap.h b/CvGameCoreDLL/BarbarianWeightMap.h
index 888837ac2..37bf6972d 100644
--- a/CvGameCoreDLL/BarbarianWeightMap.h
+++ b/CvGameCoreDLL/BarbarianWeightMap.h
@@ -3,7 +3,7 @@
 #define BARBARIAN_WEIGHT_MAP_H
 
 /*	advc.304: Memory of Barbarian activity and (based on that past activity)
-	weighted probability distribution for plots where Barbarians get placed. */
+	weight distribution for future Barbarian activity. */
 
 #include "CvMap.h"

diff --git a/CvGameCoreDLL/CvGameCoreUtils.cpp b/CvGameCoreDLL/CvGameCoreUtils.cpp
index 02a03465b..871c8b242 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.cpp
+++ b/CvGameCoreDLL/CvGameCoreUtils.cpp
@@ -800,38 +800,38 @@ int intHash(std::vector<int> const& kInputs, PlayerTypes ePlayer)
 }
 
 // create an array of shuffled numbers
-int* shuffle(int iNum, CvRandom& rand)
+int* shuffle(int iNum, CvRandom& kRand)
 {
 	int* piShuffle = new int[iNum];
-	shuffleArray(piShuffle, iNum, rand);
+	shuffleArray(piShuffle, iNum, kRand);
 	return piShuffle;
 }
 
 
-void shuffleArray(int* piShuffle, int iNum, CvRandom& rand)
+void shuffleArray(int* piShuffle, int iNum, CvRandom& kRand)
 {
-	for (int iI = 0; iI < iNum; iI++)
-		piShuffle[iI] = iI;
+	for (int i = 0; i < iNum; i++)
+		piShuffle[i] = i;
 
-	for (int iI = 0; iI < iNum; iI++)
+	for (int i = 0; i < iNum; i++)
 	{
-		int iJ = (rand.get(iNum - iI, NULL) + iI);
-		if (iI != iJ)
+		int j = (kRand.get(iNum - i, NULL) + i);
+		if (i != j)
 		{
-			int iTemp = piShuffle[iI];
-			piShuffle[iI] = piShuffle[iJ];
-			piShuffle[iJ] = iTemp;
+			int iTemp = piShuffle[i];
+			piShuffle[i] = piShuffle[j];
+			piShuffle[j] = iTemp;
 		}
 	}
 }
 
 // advc.enum: Caller needs to set the vector size
-void shuffleVector(std::vector<int>& aiIndices, CvRandom& rand)
+void shuffleVector(std::vector<int>& aiIndices, CvRandom& kRand)
 {
 	std11::iota(aiIndices.begin(), aiIndices.end(), 0);
 	int const iSize = (int)aiIndices.size();
 	for (int i = 0; i < iSize; i++)
-		std::swap(aiIndices[i], aiIndices[rand.get(iSize - i, NULL) + i]);
+		std::swap(aiIndices[i], aiIndices[kRand.get(iSize - i, NULL) + i]);
 }

diff --git a/CvGameCoreDLL/Project/AdvCiv.vcxproj b/CvGameCoreDLL/Project/AdvCiv.vcxproj
index 07792ec3f..7c5435c12 100644
--- a/CvGameCoreDLL/Project/AdvCiv.vcxproj
+++ b/CvGameCoreDLL/Project/AdvCiv.vcxproj
@@ -596,8 +596,6 @@ nmake clean /NOLOGO</NMakeCleanCommandLine>
   <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug-fast|Win32'">
     <LocalDebuggerEnvironment>_NO_DEBUG_HEAP=1</LocalDebuggerEnvironment>
     <DebuggerFlavor>WindowsLocalDebugger</DebuggerFlavor>
-    <LibraryPath>$(VCInstallDir)lib;$(VCInstallDir)atlmfc\lib;$(WindowsSdkDir)lib;$(FrameworkSDKDir)\lib;C:\Program Files (x86)\Visual Leak Detector\lib\Win32</LibraryPath>
-    <ExecutablePath>$(VCInstallDir)bin;$(WindowsSdkDir)bin\NETFX 4.0 Tools;$(WindowsSdkDir)bin;$(VSInstallDir)Common7\Tools\bin;$(VSInstallDir)Common7\tools;$(VSInstallDir)Common7\ide;$(ProgramFiles)\HTML Help Workshop;$(FrameworkSDKDir)\bin;$(MSBuildToolsPath32);$(VSInstallDir);$(SystemRoot)\SysWow64;$(FxCopDir);$(PATH);</ExecutablePath>
   </PropertyGroup>
   <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug-opt|Win32'">
     <LocalDebuggerEnvironment>_NO_DEBUG_HEAP=1</LocalDebuggerEnvironment>

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=4120

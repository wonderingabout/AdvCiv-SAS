# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1047
# Commit: dcfd9d8ad6dcba9cada1a4a4e104ca7a9a1cef0c
# History segment: K-Mod history
# Parent used for diff: 6c3985289750698553acb829528fdffd4fb60017
# Title: Corrected a problem which caused XML comments to break in some places.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
18	9	CvGameCoreDLL/CvXMLLoadUtilitySet.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvXMLLoadUtilitySet.cpp b/CvGameCoreDLL/CvXMLLoadUtilitySet.cpp
index 550de83bb..621d8de7b 100644
--- a/CvGameCoreDLL/CvXMLLoadUtilitySet.cpp
+++ b/CvGameCoreDLL/CvXMLLoadUtilitySet.cpp
@@ -2074,7 +2074,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(int **ppiList, const TCHAR* szRoot
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = FindInInfoClass(szTextVal);
 
@@ -2143,7 +2144,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(bool **ppbList, const TCHAR* szRoo
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = FindInInfoClass(szTextVal);
 							if (iIndexVal != -1)
@@ -2211,7 +2213,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(float **ppfList, const TCHAR* szRo
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = FindInInfoClass(szTextVal);
 							if (iIndexVal != -1)
@@ -2279,7 +2282,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(CvString **ppszList, const TCHAR*
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = FindInInfoClass(szTextVal);
 							if (iIndexVal != -1)
@@ -2347,7 +2351,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(int **ppiList, const TCHAR* szRoot
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = GC.getTypesEnum(szTextVal);
 							if (iIndexVal != -1)
@@ -2416,7 +2421,8 @@ void CvXMLLoadUtility::SetVariableListTagPairForAudioScripts(int **ppiList, cons
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal =	GC.getTypesEnum(szTextVal);
 							if (iIndexVal != -1)
@@ -2488,7 +2494,8 @@ void CvXMLLoadUtility::SetVariableListTagPairForAudioScripts(int **ppiList, cons
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal = FindInInfoClass(szTextVal);
 							if (iIndexVal != -1)
@@ -2560,7 +2567,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(bool **ppbList, const TCHAR* szRoo
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal =	GC.getTypesEnum(szTextVal);
 							if (iIndexVal != -1)
@@ -2628,7 +2636,8 @@ void CvXMLLoadUtility::SetVariableListTagPair(CvString **ppszList, const TCHAR*
 				{
 					for (i=0;i<iNumSibs;i++)
 					{
-						if (GetChildXmlVal(szTextVal))
+						//if (GetChildXmlVal(szTextVal))
+						if (SkipToNextVal() && GetChildXmlVal(szTextVal)) // K-Mod. (without this, a comment in the xml could break this)
 						{
 							iIndexVal =	GC.getTypesEnum(szTextVal);
 							if (iIndexVal != -1)

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=4089

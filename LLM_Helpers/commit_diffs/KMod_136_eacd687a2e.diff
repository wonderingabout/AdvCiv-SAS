# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 136
# Commit: eacd687a2ef3097c9511816c74b4c3a30d7a5ac3
# History segment: K-Mod history
# Parent used for diff: 0b8e6dd0841119b51d616c6b3a255ea914e0ea46
# Title: Fixed a divide-by-zero bug in my new tech value modifier function. Fixed a problem with the new finacial trouble check. Made a minor adjustment to the city g...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
25	22	CvGameCoreDLL/CvCityAI.cpp
3	1	CvGameCoreDLL/CvPlayerAI.cpp
1	1	CvGameCoreDLL/CvTeamAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index 61dd9a2ad..9e5fd2144 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -10736,36 +10736,39 @@ void CvCityAI::AI_buildGovernorChooseProduction()
 		}
 	}
 	
-	//worker
-	if (!bDanger && GET_PLAYER(getOwner()).AI_isPrimaryArea(area())) // if its not a primary area, let the player ship one in.
+	if (GET_PLAYER(getOwner()).AI_isPrimaryArea(area())) // if its not a primary area, let the player ship units in.
 	{
-		int iExistingWorkers = GET_PLAYER(getOwner()).AI_totalAreaUnitAIs(area(), UNITAI_WORKER);
-		int iNeededWorkers = GET_PLAYER(getOwner()).AI_neededWorkers(area());
-
-		if (iExistingWorkers < (iNeededWorkers + 1)/2) // I don't want to build more workers than the player actually wants.
+		//worker
+		if (!bDanger)
 		{
-			int iOdds = 100;
-			iOdds *= iNeededWorkers - iExistingWorkers;
-			iOdds /= std::max(1, iNeededWorkers);
-			iOdds *= 50 + iBestBuildingValue;
-			iOdds /= 50 + 5 * iBestBuildingValue;
+			int iExistingWorkers = GET_PLAYER(getOwner()).AI_totalAreaUnitAIs(area(), UNITAI_WORKER);
+			int iNeededWorkers = GET_PLAYER(getOwner()).AI_neededWorkers(area());
 
-			if (AI_chooseUnit(UNITAI_WORKER, iOdds))
+			if (iExistingWorkers < (iNeededWorkers + 1)/2) // I don't want to build more workers than the player actually wants.
 			{
-				return;
+				int iOdds = 100;
+				iOdds *= iNeededWorkers - iExistingWorkers;
+				iOdds /= std::max(1, iNeededWorkers);
+				iOdds *= 50 + iBestBuildingValue;
+				iOdds /= 50 + 5 * iBestBuildingValue;
+
+				if (AI_chooseUnit(UNITAI_WORKER, iOdds))
+				{
+					return;
+				}
 			}
 		}
-	}
 
-	//military
-	if (GET_TEAM(getTeam()).getAtWarCount(true) > 0 && (bDanger || iBestBuildingValue < 20))
-	{
-		int iOdds = 100;
-		iOdds *= 50 + iBestBuildingValue;
-		iOdds /= 50 + 10 * iBestBuildingValue;
-		if (AI_chooseUnit(NO_UNITAI, iOdds))
+		//military
+		if (GET_TEAM(getTeam()).getAtWarCount(true) > 0 && (bDanger || iBestBuildingValue < 20))
 		{
-			return;
+			int iOdds = 100;
+			iOdds *= 50 + iBestBuildingValue;
+			iOdds /= 50 + 10 * iBestBuildingValue;
+			if (AI_chooseUnit(NO_UNITAI, iOdds))
+			{
+				return;
+			}
 		}
 	}

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index dc5b96a6b..0ab56e3bd 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -4393,7 +4393,9 @@ bool CvPlayerAI::AI_isFinancialTrouble() const
 		// Really we just want to know how much of our commerce slider is used for paying the bills.
 		/* original bts code
 		int iNetCommerce = 1 + getCommerceRate(COMMERCE_GOLD) + getCommerceRate(COMMERCE_RESEARCH) + std::max(0, getGoldPerTurn()); */
-		int iNetCommerce = 1 + 100 * (getCommerceRate(COMMERCE_GOLD) + std::max(0, getGoldPerTurn())) / std::max(1, getCommercePercent(COMMERCE_GOLD));
+		if (getCommercePercent(COMMERCE_GOLD) == 0)
+			return false; // I can't think of any reason to run 0% gold when short on money...
+		int iNetCommerce = 1 + 100 * (getCommerceRate(COMMERCE_GOLD) + std::max(0, getGoldPerTurn())) / std::max(1, getCommercePercent(COMMERCE_GOLD)); // std::max... paranoia.
 		// K-Mod end
 /************************************************************************************************/
 /* UNOFFICIAL_PATCH                       06/11/09                       jdog5000 & DanF5771    */

diff --git a/CvGameCoreDLL/CvTeamAI.cpp b/CvGameCoreDLL/CvTeamAI.cpp
index f176a296d..3c0ed7e7f 100644
--- a/CvGameCoreDLL/CvTeamAI.cpp
+++ b/CvGameCoreDLL/CvTeamAI.cpp
@@ -1504,7 +1504,7 @@ int CvTeamAI::AI_knownTechValModifier(TechTypes eTech) const
 		}
 	}
 
-	return 50 * (iCivsMet - iTechCivs) / iCivsMet;
+	return 50 * (iCivsMet - iTechCivs) / std::max(1, iCivsMet);
 }
 // K-Mod end

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=3769

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2077
# Commit: dfaaf3510472640c6c96c3decacfa568af811d50
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 7911b9bc9f87a2a2385e6308a79e1cda0134d5be
# Title: ScaledInt: Add rand (PRNG) function
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
8	3	CvGameCoreDLL/ScaledInt.h

[PATCHES]

diff --git a/CvGameCoreDLL/ScaledInt.h b/CvGameCoreDLL/ScaledInt.h
index 51eca75d9..7ce3304e6 100644
--- a/CvGameCoreDLL/ScaledInt.h
+++ b/CvGameCoreDLL/ScaledInt.h
@@ -171,15 +171,20 @@ public:
 		return r;
 	}
 
+	// Result in the half-open interval [0, 1)
+	static ScaledInt rand(CvRandom& kRand, TCHAR const* pszLog)
+	{
+		ScaledInt r;
+		r.m_i = static_cast<IntType>(kRand.get(static_cast<uint>(iSCALE), pszLog));
+		return r;
+	}
 	/*	See intHash (CvGameCoreUtils.h) about the parameters.
 		Result in the half-open interval [0, 1). */
 	static ScaledInt hash(std::vector<int> const& x, PlayerTypes ePlayer = NO_PLAYER)
 	{
 		CvRandom rng;
 		rng.init(::intHash(x, ePlayer));
-		ScaledInt r;
-		r.m_i = static_cast<IntType>(rng.get(static_cast<uint>(iSCALE)));
-		return r;
+		return rand(rng, NULL);
 	}
 	// For hashing just a single input
 	static inline ScaledInt hash(int x, PlayerTypes ePlayer = NO_PLAYER)

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=920

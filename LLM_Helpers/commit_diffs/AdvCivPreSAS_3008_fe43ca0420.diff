# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3008
# Commit: fe43ca0420ba16c07896a1586a9b371b7b91182f
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 0e5f763de41fbec37faff1f8ba49e8e14cc4028d
# Title: Option to show air unit capacity in city bar hover
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
3	0	Assets/Config/BUG Main Interface.xml
1	0	Assets/Python/BUG/Tabs/BugMapOptionsTab.py
-	-	Assets/Res/Fonts/GameFont_75.tga
11	0	Assets/XML/Text/BUG Main Interface Options.xml
11	0	Assets/XML/Text/CIV4GameText_advc.xml
21	7	CvGameCoreDLL/CvGameTextMgr.cpp

[PATCHES]

diff --git a/Assets/Config/BUG Main Interface.xml b/Assets/Config/BUG Main Interface.xml
index eac26870f..12a2b313a 100644
--- a/Assets/Config/BUG Main Interface.xml	
+++ b/Assets/Config/BUG Main Interface.xml	
@@ -192,6 +192,9 @@
 			<!-- advc.101 -->
 			<option id="RevoltHelp" key="Revolt Help" 
 					type="boolean" default="True"/>
+			<!-- advc.187 -->
+			<option id="AirCapacity" key="Air Capacity" 
+					type="boolean" default="True"/>
 
 			<!-- Build/Action Icon Size by EmperorFool -->
 			<list   id="BuildIconSize" key="Build Icon Size"

diff --git a/Assets/Python/BUG/Tabs/BugMapOptionsTab.py b/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
index d8d04c26a..1eec878ac 100644
--- a/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
+++ b/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
@@ -56,6 +56,7 @@ class BugMapOptionsTab(BugOptionsTab.BugOptionsTab):
 		#self.addCheckbox(screen, center, "MiscHover__LatLongCoords")
 		#self.addCheckbox(screen, center, "MiscHover__PartialBuilds")
 		self.addCheckbox(screen, center, "MainInterface__RevoltHelp") # advc.101
+		self.addCheckbox(screen, center, "MainInterface__AirCapacity") # advc.187
 		self.addSpacer(screen, center, "SpacerCenter1") # advc.004
 		# <advc.002f>
 		self.addLabel(screen, center, "CityIcons", "City Billboards:")

diff --git a/Assets/Res/Fonts/GameFont_75.tga b/Assets/Res/Fonts/GameFont_75.tga
index b2a2ec209..e21482f26 100644
Binary files a/Assets/Res/Fonts/GameFont_75.tga and b/Assets/Res/Fonts/GameFont_75.tga differ

diff --git a/Assets/XML/Text/BUG Main Interface Options.xml b/Assets/XML/Text/BUG Main Interface Options.xml
index ef1d6d8e7..ed9538b0e 100644
--- a/Assets/XML/Text/BUG Main Interface Options.xml	
+++ b/Assets/XML/Text/BUG Main Interface Options.xml	
@@ -890,6 +890,17 @@
 		<English>If checked, help text about revolts is shown when hovering over city tiles; otherwise, it's shown when hovering over city bars.</English>
 		<German>Zeigt Hilfetext zu Revolten an, wenn der Mauszeiger auf ein Stadtfeld bewegt wird. Ist diese Option deaktiviert, informiert stattdessen der Hilfetext zum Stadt-Balken �ber Revolten.</German>
 	</TEXT>
+	<!-- advc.187 -->
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__AIRCAPACITY_TEXT</Tag>
+		<English>Air Unit Capacity on Tile</English>
+		<German>Kapazit�t f�r Lufteinheiten</German>
+	</TEXT>
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__AIRCAPACITY_HOVER</Tag>
+		<English>If checked, the air unit capacity is shown when hovering over a (city or fort) tile with a friendly air unit; otherwise, the air unit capacity is shown (only for proper cities) when hovering over a city bar.</English>
+		<German>Zeigt die Kapazit�t f�r Lufteinheiten an, wenn der Mauszeiger auf ein (Stadt- oder Festungs-) Feld bewegt wird, auf dem sich freundliche Lufteinheiten befinden. Ist diese Option deaktiviert, wird die Kapazit�t stattdessen im Hilfetext zum Stadt-Balken (also nur f�r Stadtfelder) angegeben.</German>
+	</TEXT>
 
 	<!-- advc.095 -->
 	<TEXT>

diff --git a/Assets/XML/Text/CIV4GameText_advc.xml b/Assets/XML/Text/CIV4GameText_advc.xml
index 2fd4923a8..87c466a36 100644
--- a/Assets/XML/Text/CIV4GameText_advc.xml
+++ b/Assets/XML/Text/CIV4GameText_advc.xml
@@ -6704,4 +6704,15 @@
 		<Spanish>+%d1_Change [ICON_UNHAPPY]: "&#161;El mundo os considera un ser vil!" (%d2 [NUM2:turno:turnos])</Spanish>
 	</TEXT>
 
+	<!-- advc.187: From CIV4GameText_BTS.xml. Use BULL's airport icon
+		 (which AdvCiv adds to GameFont_75.tga for this purpose). -->
+	<TEXT>
+		<Tag>TXT_KEY_CITY_BAR_AIR_UNIT_CAPACITY</Tag>
+		<English>[ICON_AIRPORT] Capacity %d1/%d2</English>
+		<French>Capacit&#233; [ICON_AIRPORT] %d1/%d2</French>
+		<German>[ICON_AIRPORT]-Kapazit&#228;t %d1/%d2</German>
+		<Italian>Spazio di [ICON_AIRPORT]: %d1/%d2</Italian>
+		<Spanish>Capacidad de [ICON_AIRPORT]: %d1/%d2</Spanish>
+	</TEXT>
+
 </Civ4GameText>

diff --git a/CvGameCoreDLL/CvGameTextMgr.cpp b/CvGameCoreDLL/CvGameTextMgr.cpp
index 3df63dd6f..6389e84db 100644
--- a/CvGameCoreDLL/CvGameTextMgr.cpp
+++ b/CvGameCoreDLL/CvGameTextMgr.cpp
@@ -2898,7 +2898,18 @@ void CvGameTextMgr::setPlotHelp(CvWStringBuffer& szString, CvPlot const& kPlot)
 	TeamTypes const eActiveTeam = kGame.getActiveTeam();
 	PlayerTypes const eActivePlayer = kGame.getActivePlayer();
 	CvWString szTempBuffer;
-
+	// <advc.187>
+	if (kPlot.isActiveVisible(false))
+	{
+		int iAirUnits = kPlot.countNumAirUnits(eActiveTeam);
+		if (gDLL->UI().getInterfaceMode() == INTERFACEMODE_REBASE ||
+			(iAirUnits > 0 && BUGOption::isEnabled("MainInterface__AirCapacity", true)))
+		{
+			szString.append(gDLL->getText("TXT_KEY_CITY_BAR_AIR_UNIT_CAPACITY",
+					iAirUnits, kPlot.airUnitSpaceAvailable(eActiveTeam) + iAirUnits));
+			szString.append(NEWLINE);
+		}
+	} // </advc.187>
 	PlayerTypes const eRevealedOwner = kPlot.getRevealedOwner(eActiveTeam, true);
 	if (eRevealedOwner != NO_PLAYER ||
 		// advc.099f:
@@ -5354,13 +5365,15 @@ void CvGameTextMgr::setCityBarHelp(CvWStringBuffer &szString, CvCity* pCity)
 				GET_PLAYER(pCity->getOwner()).greatPeopleThreshold(false)));
 	}
 	{
-		TeamTypes const eActiveTeam = GC.getGame().getActiveTeam();
-		int const iUnits = pCity->getPlot().countNumAirUnits(eActiveTeam);
-		if (iUnits > 0 && pCity->getAirUnitCapacity(eActiveTeam) > 0)
+		TeamTypes eActiveTeam = GC.getGame().getActiveTeam();
+		int iAirUnits = kCity.getPlot().countNumAirUnits(eActiveTeam);
+		if (iAirUnits > 0 &&
+			// advc.187: Now shown in plot help by default
+			!BUGOption::isEnabled("MainInterface__AirCapacity", true))
 		{
 			szString.append(NEWLINE);
 			szString.append(gDLL->getText("TXT_KEY_CITY_BAR_AIR_UNIT_CAPACITY",
-					iUnits, pCity->getAirUnitCapacity(eActiveTeam)));
+					iAirUnits, kCity.getAirUnitCapacity(eActiveTeam)));
 		}
 	}
 	szString.append(NEWLINE);
@@ -19766,8 +19779,9 @@ void CvGameTextMgr::getRebasePlotHelp(CvPlot const& kPlot,
 	bool const bFull = (iUnits >= pCity->getAirUnitCapacity(eActiveTeam));
 	if (bFull)
 		szHelp += CvWString::format(SETCOLR, TEXT_COLOR("COLOR_WARNING_TEXT"));
-	szHelp +=  NEWLINE + gDLL->getText("TXT_KEY_CITY_BAR_AIR_UNIT_CAPACITY",
-			iUnits, pCity->getAirUnitCapacity(eActiveTeam));
+	// advc.187: Handled by setPlotHelp now
+	/*szHelp += NEWLINE + gDLL->getText("TXT_KEY_CITY_BAR_AIR_UNIT_CAPACITY",
+			iUnits, pCity->getAirUnitCapacity(eActiveTeam));*/
 	if (bFull)
 		szHelp += ENDCOLR;
 	szHelp += NEWLINE;

[PATCH COVERAGE]
patchFiles=6 embeddedFiles=6 omittedFiles=0 embeddedPatchBytes=6380

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 506
# Commit: b1da97ec81216b315b002bf77e958ba4fde028b4
# History segment: K-Mod history
# Parent used for diff: d50f7e58ded98daef427e79b1cd4a91caba021d7
# Title: Removed a strange rule from the original BtS code which disallowed the AI from loading into boats on the coast. (They were only allowed to load from cities a...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	CvGameCoreDLL/CvUnit.cpp
1	1	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvUnit.cpp b/CvGameCoreDLL/CvUnit.cpp
index 1d735b885..bc9805007 100644
--- a/CvGameCoreDLL/CvUnit.cpp
+++ b/CvGameCoreDLL/CvUnit.cpp
@@ -2547,7 +2547,8 @@ bool CvUnit::canMoveInto(const CvPlot* pPlot, bool bAttack, bool bDeclareWar, bo
 		{
 			if (!pPlot->isCity() || 0 == GC.getDefineINT("LAND_UNITS_CAN_ATTACK_WATER_CITIES"))
 			{
-				if (bIgnoreLoad || !isHuman() || plot()->isWater() || !canLoad(pPlot))
+				//if (bIgnoreLoad || !isHuman() || plot()->isWater() || !canLoad(pPlot))
+				if (bIgnoreLoad || plot()->isWater() || !canLoad(pPlot)) // K-Mod. (AI might want to load into a boat on the coast)
 				{
 					return false;
 				}

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 881951a4a..debdab499 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -15081,7 +15081,7 @@ CvCity* CvUnitAI::AI_pickTargetCity(int iFlags, int iMaxPathTurns, bool bHuntBar
 									if (transport_path.GeneratePath(pLoopCity->plot()))
 									{
 										// faster by boat
-										FAssert(transport_path.GetPathTurns() + iLoadTurns < iPathTurns);
+										FAssert(transport_path.GetPathTurns() + iLoadTurns <= iPathTurns);
 										iPathTurns = transport_path.GetPathTurns() + iLoadTurns;
 									}
 								}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1293

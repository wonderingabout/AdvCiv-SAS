# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 6108
# Commit: 404d43461d2c67af87862341b7609ac882782b0e
# History segment: AdvCiv-SAS branch history
# Parent used for diff: ad20f152bd488a871c26a72c4471e5d3beba63cd
# Title: Expand SASGameRecord text maps with terrain and evolving feature layers
# Full commit message/metadata: see _SNAPSHOT_CONTEXT/git_log_since_tracked_advciv_sas_log.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
-	-	Assets/CvGameCoreDLL.dll
15	5	Assets/XML/GlobalDefines_advciv_sas.xml
131	32	CvGameCoreDLL/SASGameRecordLog.cpp
4	1	README.md
1	1	_1_AdvCiv-SAS/Docs/README_Main_Changes_Guide.md
-	-	_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG
-	-	_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG
-	-	_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG
-	-	_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG
-	-	_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG
37858	41948	_1_AdvCiv-SAS/SASGameRecord_log/SASGameRecord_example.log
 create mode 100644 _1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG
 create mode 100644 _1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG
 create mode 100644 _1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG

[PATCHES]

[PATCH OMITTED] file=a/Assets/CvGameCoreDLL.dll b/Assets/CvGameCoreDLL.dll reason=binary/generated-file changedLines=0 bytes=176

diff --git a/Assets/XML/GlobalDefines_advciv_sas.xml b/Assets/XML/GlobalDefines_advciv_sas.xml
index 594cc0ef7..dc57a7fcd 100644
--- a/Assets/XML/GlobalDefines_advciv_sas.xml
+++ b/Assets/XML/GlobalDefines_advciv_sas.xml
@@ -1510,7 +1510,7 @@ Created as part of AdvCiv-SAS improvements
 		<DefineName>SAS_GAME_RECORD_LOG_USE_TIMESTAMPED_FILENAME</DefineName>
 		<iDefineIntVal>1</iDefineIntVal>
 	</Define>
-	<!-- custom: Maximum map-content character columns (excluding the two pipe frame characters) and rows of each one-time level-3 SASGameRecord ASCII map layer. Generated maps are evenly reduced with one scale so horizontal SAS_Longworld and possible vertical/tower maps preserve their aspect ratio. Native plot resolution requires enough width for each plot's repeated horizontal characters below; set either maximum to 0 to omit the ASCII preview without disabling other level-3 rows. (GPT-5.6-Sol) -->
+	<!-- custom: Maximum map-content character columns (excluding the two pipe frame characters) and rows of each level-3 SASGameRecord ASCII map layer. Geography and terrain are normally stable and therefore recorded once at setup/load; exact later exceptions remain available through coordinate-bearing map-change rows. Features are recorded initially because they affect settling, movement, health, and yields, then features and political borders repeat at full interval and victory snapshots because they change frequently. Generated maps are evenly reduced with one scale so horizontal SAS_Longworld and possible vertical/tower maps preserve their aspect ratio. Native plot resolution requires enough width for each plot's repeated horizontal characters below; set either maximum to 0 to omit all ASCII previews without disabling other level-3 rows. (GPT-5.6-Sol) -->
 	<Define>
 		<DefineName>SAS_GAME_RECORD_MAP_ASCII_MAX_WIDTH</DefineName>
 		<iDefineIntVal>160</iDefineIntVal>
@@ -1524,17 +1524,27 @@ Created as part of AdvCiv-SAS improvements
 		<DefineName>SAS_GAME_RECORD_MAP_ASCII_HORIZONTAL_CHARS_PER_CELL</DefineName>
 		<iDefineIntVal>2</iDefineIntVal>
 	</Define>
-	<!-- custom: Geography-map symbols in this order: open water, lake, flat land, hills, peaks, mostly-water coast, balanced coast, mostly-land coast. Use comma-separated single printable characters; because literal whitespace and delimiter punctuation are fragile in XML, SPACE, COMMA, COLON, and SEMICOLON are recognized names. Symbols must be unique within this layer, and pipe is reserved for the picture frame. Invalid palettes produce a clear game-record configuration-error row and omit the map instead of silently changing symbols. (GPT-5.6-Sol) -->
+	<!-- custom: Geography-map symbols in this order: open water, lake, flat land, hills, peaks, mostly-water mixed land, balanced water/land, mostly-land mixed water, ice. The three mixed symbols describe resampled shoreline cells, not TERRAIN_COAST; Geography intentionally shows broad land/water and plot shape rather than shallow-water terrain. Use comma-separated single printable characters; because literal whitespace and delimiter punctuation are fragile in XML, SPACE, COMMA, COLON, and SEMICOLON are recognized names. Symbols must be unique within this layer, and pipe is reserved for the picture frame. Invalid palettes produce a clear game-record configuration-error row and omit the map instead of silently changing symbols. (GPT-5.6-Sol) -->
 	<Define>
 		<DefineName>SAS_GAME_RECORD_MAP_ASCII_GEOGRAPHY_SYMBOLS</DefineName>
-		<DefineTextVal>SPACE,~,.,h,^,COMMA,COLON,SEMICOLON</DefineTextVal>
+		<DefineTextVal>SPACE,~,.,h,^,COMMA,COLON,SEMICOLON,#</DefineTextVal>
 	</Define>
-	<!-- custom: Political-map symbols in this order: unowned water, unowned land, mixed unowned water/land, civilization city, Barbarian city, mixed civilization/Barbarian cities, multiple starting players, Barbarian territory. It uses the same comma-separated character and named-token rules as the geography palette, and symbols must be unique within this layer. (GPT-5.6-Sol) -->
+	<!-- custom: Terrain-map symbols corresponding in order to every TerrainInfo loaded by the mod, followed by an ice override. Ice is technically a feature, but it is not ordinarily removable, blocks naval paths, and forms recognizable strategic geography. One character cannot show both ice and its underlying Coast/Ocean terrain, so # replaces * or blank water because blocked access is strategically decisive. The legend records the exact symbol-to-type mapping; resampled cells show their dominant terrain unless ice covers at least half the cell. The number of symbols must exactly match the runtime terrain count plus one so reordered or added XML types cannot silently produce a misleading map. (GPT-5.6-Sol) -->
+	<Define>
+		<DefineName>SAS_GAME_RECORD_MAP_ASCII_TERRAIN_SYMBOLS</DefineName>
+		<DefineTextVal>g,p,d,t,s,*,SPACE,^,h,#</DefineTextVal>
+	</Define>
+	<!-- custom: Feature-map symbols in this order: featureless water, featureless land, then every FeatureInfo loaded by the mod. Blank water and dotted land preserve the continent silhouette without overlaying another layer. The legend records the exact symbol-to-type mapping, and resampled cells show their dominant feature state. The same named-token and validation rules as the geography palette apply. (GPT-5.6-Sol) -->
+	<Define>
+		<DefineName>SAS_GAME_RECORD_MAP_ASCII_FEATURE_SYMBOLS</DefineName>
+		<DefineTextVal>SPACE,.,#,j,o,f,F,X</DefineTextVal>
+	</Define>
+	<!-- custom: Political-map symbols in this order: unowned water, unowned land, mixed unowned water/land, civilization city, Barbarian city, mixed civilization/Barbarian cities, multiple starting players, Barbarian territory. Keep this layer purely political: Coast or ice overriding a player symbol would hide owned water, while showing them only on unowned plots would make physical geography inconsistent as borders expand. Terrain, Geography, and Features preserve that context separately. It uses the same comma-separated character and named-token rules as the geography palette, and symbols must be unique within this layer. (GPT-5.6-Sol) -->
 	<Define>
 		<DefineName>SAS_GAME_RECORD_MAP_ASCII_POLITICAL_SYMBOLS</DefineName>
 		<DefineTextVal>SPACE,.,COLON,@,!,*,+,?</DefineTextVal>
 	</Define>
-	<!-- custom: Direct player-symbol string with exactly MAX_CIV_PLAYERS unique printable characters that do not reuse either map layer's symbols. The default has 48 symbols for AdvCiv-SAS and deliberately omits lowercase h used for hills. (GPT-5.6-Sol) -->
+	<!-- custom: Direct player-symbol string with exactly MAX_CIV_PLAYERS unique printable characters that do not reuse political-layer symbols. Separate layers may reuse intuitive characters. The default has 48 symbols for AdvCiv-SAS. (GPT-5.6-Sol) -->
 	<Define>
 		<DefineName>SAS_GAME_RECORD_MAP_ASCII_PLAYER_SYMBOLS</DefineName>
 		<DefineTextVal>0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgijklm</DefineTextVal>

diff --git a/CvGameCoreDLL/SASGameRecordLog.cpp b/CvGameCoreDLL/SASGameRecordLog.cpp
index be38bbd2d..0722985e3 100644
--- a/CvGameCoreDLL/SASGameRecordLog.cpp
+++ b/CvGameCoreDLL/SASGameRecordLog.cpp
@@ -2573,18 +2573,23 @@ static void logSASGameRecordGeography()
 
 enum
 {
-	SAS_MAP_ASCII_GEOGRAPHY_SYMBOL_COUNT = 8,
+	SAS_MAP_ASCII_GEOGRAPHY_SYMBOL_COUNT = 9,
 	SAS_MAP_ASCII_POLITICAL_SYMBOL_COUNT = 8
 };
 
+// <!-- custom: Keep each layer's palette independent. Ice deliberately has separately configurable Geography, Terrain-override, and Features symbols so a mod-mod can choose how it reads in each picture rather than inheriting one presentation everywhere. (GPT-5.6-Sol) -->
 struct SASGameRecordMapAsciiPalette
 {
 	bool bValid;
 	char acGeography[SAS_MAP_ASCII_GEOGRAPHY_SYMBOL_COUNT];
 	char acPolitical[SAS_MAP_ASCII_POLITICAL_SYMBOL_COUNT];
+	std::vector<char> acTerrain;
+	std::vector<char> acFeatures;
 	CvString szPlayers;
 	CvString szGeographyDefine;
 	CvString szPoliticalDefine;
+	CvString szTerrainDefine;
+	CvString szFeatureDefine;
 	CvString szError;
 };
 
@@ -2636,8 +2641,12 @@ static SASGameRecordMapAsciiPalette const& getSASGameRecordMapAsciiPalette()
 		return kPalette;
 	bInitialized = true;
 	kPalette.bValid = false;
+	kPalette.acTerrain.resize(GC.getNumTerrainInfos() + 1);
+	kPalette.acFeatures.resize(GC.getNumFeatureInfos() + 2);
 	if (!parseSASGameRecordMapAsciiPaletteDefine("SAS_GAME_RECORD_MAP_ASCII_GEOGRAPHY_SYMBOLS", SAS_MAP_ASCII_GEOGRAPHY_SYMBOL_COUNT, kPalette.acGeography, kPalette.szGeographyDefine, kPalette.szError) ||
-			!parseSASGameRecordMapAsciiPaletteDefine("SAS_GAME_RECORD_MAP_ASCII_POLITICAL_SYMBOLS", SAS_MAP_ASCII_POLITICAL_SYMBOL_COUNT, kPalette.acPolitical, kPalette.szPoliticalDefine, kPalette.szError))
+			!parseSASGameRecordMapAsciiPaletteDefine("SAS_GAME_RECORD_MAP_ASCII_POLITICAL_SYMBOLS", SAS_MAP_ASCII_POLITICAL_SYMBOL_COUNT, kPalette.acPolitical, kPalette.szPoliticalDefine, kPalette.szError) ||
+			!parseSASGameRecordMapAsciiPaletteDefine("SAS_GAME_RECORD_MAP_ASCII_TERRAIN_SYMBOLS", (int)kPalette.acTerrain.size(), &kPalette.acTerrain[0], kPalette.szTerrainDefine, kPalette.szError) ||
+			!parseSASGameRecordMapAsciiPaletteDefine("SAS_GAME_RECORD_MAP_ASCII_FEATURE_SYMBOLS", (int)kPalette.acFeatures.size(), &kPalette.acFeatures[0], kPalette.szFeatureDefine, kPalette.szError))
 		return kPalette;
 	kPalette.szPlayers = GC.getDefineSTRING("SAS_GAME_RECORD_MAP_ASCII_PLAYER_SYMBOLS");
 	if ((int)kPalette.szPlayers.length() != MAX_CIV_PLAYERS)
@@ -2659,14 +2668,7 @@ static SASGameRecordMapAsciiPalette const& getSASGameRecordMapAsciiPalette()
 			kPalette.szError.Format("SAS_GAME_RECORD_MAP_ASCII_PLAYER_SYMBOLS repeats symbol ASCII %d", (int)ucSymbol);
 			return kPalette;
 		}
-		for (int iSymbol = 0; iSymbol < SAS_MAP_ASCII_GEOGRAPHY_SYMBOL_COUNT; iSymbol++)
-		{
-			if (ucSymbol == (unsigned char)kPalette.acGeography[iSymbol])
-			{
-				kPalette.szError.Format("SAS_GAME_RECORD_MAP_ASCII_PLAYER_SYMBOLS player %d reuses geography symbol ASCII %d", iPlayer, (int)ucSymbol);
-				return kPalette;
-			}
-		}
+		// <!-- custom: Separate layers may reuse intuitive symbols; only political symbols share a picture with player symbols. (GPT-5.6-Sol) -->
 		for (int iSymbol = 0; iSymbol < SAS_MAP_ASCII_POLITICAL_SYMBOL_COUNT; iSymbol++)
 		{
 			if (ucSymbol == (unsigned char)kPalette.acPolitical[iSymbol])
@@ -2693,10 +2695,40 @@ static void appendSASGameRecordMapAsciiSymbol(CvString& szRow, char cSymbol)
 	szRow += acSymbol;
 }
 
+static void appendSASGameRecordMapAsciiLegendEntry(CvString& szLegend, char cSymbol, char const* szType)
+{
+	CvString szEntry;
+	if (cSymbol == ' ') szEntry.Format("SPACE=%s", szType);
+	else szEntry.Format("%c=%s", cSymbol, szType);
+	if (!szLegend.empty()) szLegend += ";";
+	szLegend += szEntry;
+}
+
+static CvString getSASGameRecordMapAsciiTerrainLegend(SASGameRecordMapAsciiPalette const& kPalette)
+{
+	CvString szLegend;
+	for (int iTerrain = 0; iTerrain < GC.getNumTerrainInfos(); iTerrain++)
+		appendSASGameRecordMapAsciiLegendEntry(szLegend, kPalette.acTerrain[iTerrain], getSASGameRecordTerrainType((TerrainTypes)iTerrain));
+	appendSASGameRecordMapAsciiLegendEntry(szLegend, kPalette.acTerrain[GC.getNumTerrainInfos()], "FEATURE_ICE_OVERRIDE");
+	return szLegend;
+}
+
+static CvString getSASGameRecordMapAsciiFeatureLegend(SASGameRecordMapAsciiPalette const& kPalette)
+{
+	CvString szLegend;
+	appendSASGameRecordMapAsciiLegendEntry(szLegend, kPalette.acFeatures[0], "NO_FEATURE_WATER");
+	appendSASGameRecordMapAsciiLegendEntry(szLegend, kPalette.acFeatures[1], "NO_FEATURE_LAND");
+	for (int iFeature = 0; iFeature < GC.getNumFeatureInfos(); iFeature++)
+		appendSASGameRecordMapAsciiLegendEntry(szLegend, kPalette.acFeatures[iFeature + 2], getSASGameRecordFeatureType((FeatureTypes)iFeature));
+	return szLegend;
+}
+
 static char getSASGameRecordMapAsciiGeographySymbol(CvMap const& kMap, SASGameRecordMapAsciiPalette const& kPalette, int iMinX, int iMaxX, int iMinY, int iMaxY)
 {
+	static FeatureTypes const eIce = (FeatureTypes)GC.getDefineINT("COLD_FEATURE");
 	int iWater = 0;
 	int iLake = 0;
+	int iIce = 0;
 	int iLand = 0;
 	int iHills = 0;
 	int iPeaks = 0;
@@ -2709,6 +2741,7 @@ static char getSASGameRecordMapAsciiGeographySymbol(CvMap const& kMap, SASGameRe
 			{
 				iWater++;
 				if (kPlot.isLake()) iLake++;
+				if (kPlot.getFeatureType() == eIce) iIce++;
 			}
 			else
 			{
@@ -2718,6 +2751,8 @@ static char getSASGameRecordMapAsciiGeographySymbol(CvMap const& kMap, SASGameRe
 			}
 		}
 	}
+	// <!-- custom: Ice can form strategically important sea barriers in ordinary polar bands and in unconventional shapes on maps such as Peirce. Exact previews mark every ice plot; when resampling, require an ice-majority water cell so one plot does not hide a much larger mixed area. (GPT-5.6-Sol) -->
+	if (iIce > 0 && 2 * iIce >= iWater && iWater >= iLand) return kPalette.acGeography[8];
 	if (iLand <= 0)
 		return (iLake == iWater ? kPalette.acGeography[1] : kPalette.acGeography[0]);
 	if (iWater > 0)
@@ -2732,8 +2767,63 @@ static char getSASGameRecordMapAsciiGeographySymbol(CvMap const& kMap, SASGameRe
 	return kPalette.acGeography[2];
 }
 
+static char getSASGameRecordMapAsciiTerrainSymbol(CvMap const& kMap, SASGameRecordMapAsciiPalette const& kPalette, int iMinX, int iMaxX, int iMinY, int iMaxY)
+{
+	static FeatureTypes const eIce = (FeatureTypes)GC.getDefineINT("COLD_FEATURE");
+	static std::vector<int> aiCounts;
+	aiCounts.assign(GC.getNumTerrainInfos(), 0);
+	int iIce = 0;
+	for (int iY = iMinY; iY < iMaxY; iY++)
+	{
+		for (int iX = iMinX; iX < iMaxX; iX++)
+		{
+			CvPlot const& kPlot = *kMap.plot(iX, iY);
+			if (kPlot.getFeatureType() == eIce) iIce++;
+			TerrainTypes const eTerrain = kPlot.getTerrainType();
+			if (eTerrain >= 0 && eTerrain < GC.getNumTerrainInfos()) aiCounts[eTerrain]++;
+		}
+	}
+	// <!-- custom: Ice is technically a feature, but it is not ordinarily removable and blocks naval paths, so it reads as part of the strategic surface alongside Coast, land and peaks. One character cannot show both ice and its underlying Coast/Ocean terrain; prefer ice because blocked access is the strategically decisive state. Show exact ice at native resolution and an ice-majority cell after resampling; geography and features retain it for their separate shape/history purposes. (GPT-5.6-Sol) -->
+	int const iPlotCount = (iMaxX - iMinX) * (iMaxY - iMinY);
+	if (iIce > 0 && 2 * iIce >= iPlotCount) return kPalette.acTerrain[GC.getNumTerrainInfos()];
+	TerrainTypes eDominant = NO_TERRAIN;
+	int iDominantCount = 0;
+	for (int iTerrain = 0; iTerrain < GC.getNumTerrainInfos(); iTerrain++)
+	{
+		if (aiCounts[iTerrain] > iDominantCount)
+		{
+			eDominant = (TerrainTypes)iTerrain;
+			iDominantCount = aiCounts[iTerrain];
+		}
+	}
+	return (eDominant == NO_TERRAIN ? '?' : kPalette.acTerrain[eDominant]);
+}
+
+static char getSASGameRecordMapAsciiFeatureSymbol(CvMap const& kMap, SASGameRecordMapAsciiPalette const& kPalette, int iMinX, int iMaxX, int iMinY, int iMaxY)
+{
+	static std::vector<int> aiCounts;
+	aiCounts.assign(GC.getNumFeatureInfos() + 2, 0);
+	for (int iY = iMinY; iY < iMaxY; iY++)
+	{
+		for (int iX = iMinX; iX < iMaxX; iX++)
+		{
+			CvPlot const& kPlot = *kMap.plot(iX, iY);
+			FeatureTypes const eFeature = kPlot.getFeatureType();
+			aiCounts[eFeature == NO_FEATURE ? (kPlot.isWater() ? 0 : 1) : eFeature + 2]++;
+		}
+	}
+	// <!-- custom: Distinguishing featureless land from featureless water preserves the continent silhouette, so this changing feature layer remains readable without mentally overlaying the static geography map. (GPT-5.6-Sol) -->
+	int iDominant = 0;
+	for (int iFeature = 1; iFeature < (int)aiCounts.size(); iFeature++)
+	{
+		if (aiCounts[iFeature] > aiCounts[iDominant]) iDominant = iFeature;
+	}
+	return kPalette.acFeatures[iDominant];
+}
+
 static char getSASGameRecordMapAsciiPoliticalSymbol(CvMap const& kMap, SASGameRecordMapAsciiPalette const& kPalette, std::vector<PlayerTypes> const& aeStartingPlayers, int iMinX, int iMaxX, int iMinY, int iMaxY)
 {
+	// <!-- custom: Keep this layer purely political. Coast or ice overriding a player symbol would hide owned water, while showing them only on unowned plots would make physical geography inconsistent as borders expand; Terrain/Geography/Features preserve that context separately. (GPT-5.6-Sol) -->
 	int aiOwnerPlots[MAX_PLAYERS] = { 0 };
 	PlayerTypes eStartingPlayer = NO_PLAYER;
 	bool bMultipleStartingPlayers = false;
@@ -2757,7 +2847,7 @@ static char getSASGameRecordMapAsciiPoliticalSymbol(CvMap const& kMap, SASGameRe
 				if (pCity->isBarbarian()) bBarbarianCity = true;
 				else bCivilizationCity = true;
 			}
-			PlayerTypes const ePlotStartingPlayer = aeStartingPlayers[kMap.plotNum(iX, iY)];
+			PlayerTypes const ePlotStartingPlayer = (aeStartingPlayers.empty() ? NO_PLAYER : aeStartingPlayers[kMap.plotNum(iX, iY)]);
 			if (ePlotStartingPlayer != NO_PLAYER)
 			{
 				if (eStartingPlayer == NO_PLAYER) eStartingPlayer = ePlotStartingPlayer;
@@ -2787,9 +2877,11 @@ static char getSASGameRecordMapAsciiPoliticalSymbol(CvMap const& kMap, SASGameRe
 }
 
 // <!-- custom: A bounded text map gives external LLMs and text-only reviewers the broad spatial relationships that aggregate landmass statistics cannot show. Fit the generated map, rather than the selected XML world size, into the tunable box with one scale so Tiny maps remain exact while horizontal SAS_Longworld and possible vertical/tower maps preserve their shapes.
-// Monospace characters are usually much taller than wide, so repeat each map cell horizontally by a tunable amount. Keep metadata outside the pipe-framed drawing rows so humans and LLMs can parse each layer as one uninterrupted picture; detailed plot/city facts continue through the structured rows elsewhere. (GPT-5.6-Sol) -->
-static void logSASGameRecordMapAscii()
+// Monospace characters are usually much taller than wide, so repeat each map cell horizontally by a tunable amount. Keep metadata outside the pipe-framed drawing rows so humans and LLMs can parse each layer as one uninterrupted picture.
+// Geography and terrain are normally stable enough to record once at setup/load; structured map-change rows preserve later exceptions. Record features initially because jungle, forest, flood plains, oases, and ice affect settling, movement, health, and yields; repeat features and political borders at level-3 snapshots to show Forest/Jungle clearing and regrowth, fallout, expansion, conquest, and collapse. (GPT-5.6-Sol) -->
+static void logSASGameRecordMapAscii(bool bIncludeStaticLayers, char const* szReason)
 {
+	int const iLayerCount = (bIncludeStaticLayers ? 4 : 2);
 	int const iMaxWidth = getSASGameRecordMapAsciiMaxWidth();
 	int const iMaxHeight = getSASGameRecordMapAsciiMaxHeight();
 	if (iMaxWidth <= 0 || iMaxHeight <= 0)
@@ -2825,8 +2917,9 @@ static void logSASGameRecordMapAscii()
 	}
 	int const iOutputWidth = iPreviewWidth * iHorizontalCharsPerCell;
 	bool const bMarkStartingPlots = (GC.getGame().getElapsedGameTurns() == 0);
-	std::vector<PlayerTypes> aeStartingPlayers(kMap.numPlots(), NO_PLAYER);
+	std::vector<PlayerTypes> aeStartingPlayers;
 	CvString szPlayerSymbols;
+	if (bMarkStartingPlots) aeStartingPlayers.assign(kMap.numPlots(), NO_PLAYER);
 	for (int iPlayer = 0; iPlayer < MAX_CIV_PLAYERS; iPlayer++)
 	{
 		PlayerTypes const ePlayer = (PlayerTypes)iPlayer;
@@ -2835,24 +2928,27 @@ static void logSASGameRecordMapAscii()
 			continue;
 		if (bMarkStartingPlots && kPlayer.getStartingPlot() != NULL)
 			aeStartingPlayers[kMap.plotNum(kPlayer.getStartingPlot()->getX(), kPlayer.getStartingPlot()->getY())] = ePlayer;
-		// <!-- custom: Copy each display name before calling the next getter because CvInitCore localized-name getters share temporary storage; passing both getters directly left the human player name empty in a confirming game record. (GPT-5.6-Sol) -->
-		CvWString const szCivilizationName = kPlayer.getCivilizationShortDescription(0);
-		CvWString const szPlayerName = kPlayer.getName(0);
 		CvString szPlayerSymbol;
-		szPlayerSymbol.Format("%c=%d:%S(%S)", getSASGameRecordMapAsciiPlayerSymbol(ePlayer, kPalette), iPlayer, szCivilizationName.GetCString(), szPlayerName.GetCString());
+		szPlayerSymbol.Format("%c=%d", getSASGameRecordMapAsciiPlayerSymbol(ePlayer, kPalette), iPlayer);
 		if (!szPlayerSymbols.empty()) szPlayerSymbols += ";";
 		szPlayerSymbols += szPlayerSymbol;
 	}
 	CvString const szPlayerSymbolsQuoted = getSASGameRecordQuoted(getSASGameRecordOrDash(szPlayerSymbols).GetCString());
-	logSASGameRecord("GAME_RECORD_MAP_ASCII_BEGIN turn=%d source=%dx%d previewCells=%dx%d outputCharacters=%dx%d maxCharacters=%dx%d horizontalCharactersPerCell=%d aspectRatioPreserved=1 resampled=%d wrapX=%d wrapY=%d topRowFirst=1 rowFrame=PIPE",
-			GC.getGame().getGameTurn(), iSourceWidth, iSourceHeight, iPreviewWidth, iPreviewHeight, iOutputWidth, iPreviewHeight, iMaxWidth, iMaxHeight, iHorizontalCharsPerCell, iPreviewWidth != iSourceWidth || iPreviewHeight != iSourceHeight, kMap.isWrapX(), kMap.isWrapY());
-	logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=GEOGRAPHY palette=%s order=water,lake,flat_or_mostly_flat_land,mostly_hills,mostly_peaks,mostly_water_mixed_land,balanced_water_land,mostly_land_mixed_water",
-		getSASGameRecordQuoted(kPalette.szGeographyDefine.GetCString()).GetCString());
-	logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=POLITICAL palette=%s order=unowned_water,unowned_land,mixed_unowned_water_land,civilization_city,Barbarian_city,civilization_and_Barbarian_cities,multiple_starting_players,Barbarian_territory playerSymbolFormat=SYMBOL=PLAYER_ID:CIV(PLAYER_NAME) playerSymbols=%s startingPlotsMarked=%d",
-		getSASGameRecordQuoted(kPalette.szPoliticalDefine.GetCString()).GetCString(), szPlayerSymbolsQuoted.GetCString(), bMarkStartingPlots);
-	for (int iLayer = 0; iLayer < 2; iLayer++)
-	{
-		char const* szLayer = (iLayer == 0 ? "GEOGRAPHY" : "POLITICAL");
+	logSASGameRecord("GAME_RECORD_MAP_ASCII_BEGIN turn=%d reason=%s layers=%d source=%dx%d previewCells=%dx%d outputCharacters=%dx%d maxCharacters=%dx%d horizontalCharactersPerCell=%d aspectRatioPreserved=1 resampled=%d wrapX=%d wrapY=%d topRowFirst=1 rowFrame=PIPE",
+			GC.getGame().getGameTurn(), szReason, iLayerCount, iSourceWidth, iSourceHeight, iPreviewWidth, iPreviewHeight, iOutputWidth, iPreviewHeight, iMaxWidth, iMaxHeight, iHorizontalCharsPerCell, iPreviewWidth != iSourceWidth || iPreviewHeight != iSourceHeight, kMap.isWrapX(), kMap.isWrapY());
+	if (bIncludeStaticLayers)
+	{
+		logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=GEOGRAPHY palette=%s order=water,lake,flat_or_mostly_flat_land,mostly_hills,mostly_peaks,mostly_water_mixed_land,balanced_water_land,mostly_land_mixed_water,ice", getSASGameRecordQuoted(kPalette.szGeographyDefine.GetCString()).GetCString());
+		logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=TERRAIN palette=%s symbolTypes=%s resampledCell=dominant_type", getSASGameRecordQuoted(kPalette.szTerrainDefine.GetCString()).GetCString(), getSASGameRecordQuoted(getSASGameRecordMapAsciiTerrainLegend(kPalette).GetCString()).GetCString());
+	}
+	logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=FEATURES palette=%s symbolTypes=%s resampledCell=dominant_type", getSASGameRecordQuoted(kPalette.szFeatureDefine.GetCString()).GetCString(), getSASGameRecordQuoted(getSASGameRecordMapAsciiFeatureLegend(kPalette).GetCString()).GetCString());
+	// <!-- custom: GAME_RECORD_PLAYER_SETUP already stores each player ID's quoted civilization and display name. Keep this repeated map legend to unambiguous SYMBOL=PLAYER_ID pairs so user-controlled punctuation in names cannot break an embedded mini-format or duplicate long names at every snapshot. (ChatGPT-5.5 + GPT-5.6-Sol) -->
+	logSASGameRecord("GAME_RECORD_MAP_ASCII_LEGEND layer=POLITICAL palette=%s order=unowned_water,unowned_land,mixed_unowned_water_land,civilization_city,Barbarian_city,civilization_and_Barbarian_cities,multiple_starting_players,Barbarian_territory playerSymbolFormat=SYMBOL=PLAYER_ID playerSymbols=%s playerDetailsRows=GAME_RECORD_PLAYER_SETUP startingPlotsMarked=%d",
+			getSASGameRecordQuoted(kPalette.szPoliticalDefine.GetCString()).GetCString(), szPlayerSymbolsQuoted.GetCString(), bMarkStartingPlots);
+	for (int iLayer = 0; iLayer < 4; iLayer++)
+	{
+		if (iLayer < 2 && !bIncludeStaticLayers) continue;
+		char const* szLayer = (iLayer == 0 ? "GEOGRAPHY" : iLayer == 1 ? "TERRAIN" : iLayer == 2 ? "FEATURES" : "POLITICAL");
 		logSASGameRecord("GAME_RECORD_MAP_ASCII_LAYER_BEGIN layer=%s rows=%d columns=%d sourceYTop=%d sourceYBottom=0", szLayer, iPreviewHeight, iOutputWidth, iSourceHeight - 1);
 		for (int iRow = 0; iRow < iPreviewHeight; iRow++)
 		{
@@ -2864,9 +2960,11 @@ static void logSASGameRecordMapAscii()
 			{
 				int const iMinX = (iPreviewX * iSourceWidth) / iPreviewWidth;
 				int const iMaxX = ((iPreviewX + 1) * iSourceWidth) / iPreviewWidth;
-				char const cSymbol = (iLayer == 0 ?
-						getSASGameRecordMapAsciiGeographySymbol(kMap, kPalette, iMinX, iMaxX, iMinY, iMaxY) :
-						getSASGameRecordMapAsciiPoliticalSymbol(kMap, kPalette, aeStartingPlayers, iMinX, iMaxX, iMinY, iMaxY));
+				char cSymbol;
+				if (iLayer == 0) cSymbol = getSASGameRecordMapAsciiGeographySymbol(kMap, kPalette, iMinX, iMaxX, iMinY, iMaxY);
+				else if (iLayer == 1) cSymbol = getSASGameRecordMapAsciiTerrainSymbol(kMap, kPalette, iMinX, iMaxX, iMinY, iMaxY);
+				else if (iLayer == 2) cSymbol = getSASGameRecordMapAsciiFeatureSymbol(kMap, kPalette, iMinX, iMaxX, iMinY, iMaxY);
+				else cSymbol = getSASGameRecordMapAsciiPoliticalSymbol(kMap, kPalette, aeStartingPlayers, iMinX, iMaxX, iMinY, iMaxY);
 				for (int iRepeat = 0; iRepeat < iHorizontalCharsPerCell; iRepeat++)
 					appendSASGameRecordMapAsciiSymbol(szPlots, cSymbol);
 			}
@@ -2875,7 +2973,7 @@ static void logSASGameRecordMapAscii()
 		}
 		logSASGameRecord("GAME_RECORD_MAP_ASCII_LAYER_END layer=%s", szLayer);
 	}
-	logSASGameRecord("GAME_RECORD_MAP_ASCII_END turn=%d layers=2 rowsPerLayer=%d", GC.getGame().getGameTurn(), iPreviewHeight);
+	logSASGameRecord("GAME_RECORD_MAP_ASCII_END turn=%d reason=%s layers=%d rowsPerLayer=%d", GC.getGame().getGameTurn(), szReason, iLayerCount, iPreviewHeight);
 }
 
 static void logSASGameRecordInitialContext()
@@ -2885,7 +2983,7 @@ static void logSASGameRecordInitialContext()
 	// <!-- custom: Player/team IDs appear throughout the record, but live-player counts do not reveal where ordinary civilization slots end and the special Barbarian slots begin. Record the fixed DLL boundaries once at setup so external analysis can interpret every later ID correctly. (GPT-5.6-Sol) -->
 	logSASGameRecord("GAME_RECORD_SLOT_CONSTANTS MAX_CIV_PLAYERS=%d MAX_PLAYERS=%d BARBARIAN_PLAYER=%d MAX_CIV_TEAMS=%d MAX_TEAMS=%d BARBARIAN_TEAM=%d NO_PLAYER=%d NO_TEAM=%d", MAX_CIV_PLAYERS, MAX_PLAYERS, BARBARIAN_PLAYER, MAX_CIV_TEAMS, MAX_TEAMS, BARBARIAN_TEAM, NO_PLAYER, NO_TEAM);
 	logSASGameRecordGeography();
-	if (gGameRecordLogLevel >= 3) logSASGameRecordMapAscii();
+	if (gGameRecordLogLevel >= 3) logSASGameRecordMapAscii(true, "initialContext");
 	if (gGameRecordLogLevel >= 2) logSASGameRecordAttitudeLegend();
 	for (int iI = 0; iI < MAX_CIV_PLAYERS; iI++)
 	{
@@ -4945,6 +5043,7 @@ static void logSASGameRecordSnapshot(int iGameTurn, char const* szReason)
 			kSystemSnapshot.iProcessWorkingSetKB, kSystemSnapshot.iProcessPeakWorkingSetKB, kSystemSnapshot.iProcessPagefileUsageKB, kSystemSnapshot.iSystemMemoryLoadPercent, kSystemSnapshot.iProcessAvailableVirtualMB);
 	g_uiSASGameRecordPreviousSnapshotTime = uiSnapshotTime;
 	logSASGameRecordRunStatus(szReason);
+	if (gGameRecordLogLevel >= 3) logSASGameRecordMapAscii(false, szReason);
 	if (gGameRecordLogLevel >= 2)
 	{
 		logSASGameRecordMapBonusTotals(iGameTurn);

diff --git a/README.md b/README.md
index 3fa013a06..a03e24419 100644
--- a/README.md
+++ b/README.md
@@ -923,7 +923,7 @@ BBAI_LOG_SETTINGS SAS_BBAI_LOG_ENABLE=1 SAS_BBAI_LOG_USE_TIMESTAMPED_FILENAME=1
 
 ### SASGameRecord log
 
-`SASGameRecord_*.log` is a separate compact game record for autoplay, AI-strength, and cheap wall-clock performance review. It gives high-level context such as initial map/landmass geography and bonus/yield context, economy, expansion, city and battle history, synthetic whole-war outcomes, autoplay start/end, player appearance/elimination, run status, worked plots, unit composition including `UnitCombat` shares, Barbarian cities and pressure, diplomacy, exploration, environmental and map changes, project-victory progress, compact air/missile/nuclear posture, per-city air-base capacity and city-defense state, synthetic city-bombard sequences, and detailed Barbarian positions/combat and air-strike/interception/plot-bomb actions at level 3, and game state, which is useful on its own and also helps an LLM interpret detailed BBAI decision traces. Level 3 also includes compact text-map previews of geography and player positions so broad spatial relationships can be understood without screenshots.
+`SASGameRecord_*.log` is a separate compact game record for autoplay, AI-strength, and cheap wall-clock performance review. It gives high-level context such as initial map/landmass geography and bonus/yield context, economy, expansion, city and battle history, synthetic whole-war outcomes, autoplay start/end, player appearance/elimination, run status, worked plots, unit composition including `UnitCombat` shares, Barbarian cities and pressure, diplomacy, exploration, environmental and map changes, project-victory progress, compact air/missile/nuclear posture, per-city air-base capacity and city-defense state, synthetic city-bombard sequences, and detailed Barbarian positions/combat and air-strike/interception/plot-bomb actions at level 3, and game state, which is useful on its own and also helps an LLM interpret detailed BBAI decision traces. Level 3 also includes compact initial geography/terrain pictures and changing feature/political pictures so spatial, environmental, and territorial changes can be understood without screenshots.
 
 Free-text values such as city, player, leader, civ, map-script, and log-file names are quoted and escaped so names with spaces remain parser-friendly. It is currently an all-player diagnostic record and can contain spoilers, so it is not a spoiler-free player-advice export.
 
@@ -952,6 +952,9 @@ GAME_RECORD_ACTION turn=198 type=PLAYER_ELIMINATED player=9 team=9 civ=CIVILIZAT
 
 <img src="./_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG" alt="SASGameRecord_text_art_map_1" width="250"></img>
 <img src="./_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG" alt="SASGameRecord_text_art_map_2" width="250"></img>
+<img src="./_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG" alt="SASGameRecord_text_art_map_3" width="250"></img>
+<img src="./_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG" alt="SASGameRecord_text_art_map_4" width="250"></img>
+<img src="./_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG" alt="SASGameRecord_text_art_map_5" width="250"></img>
 
 ## CuCuGS

diff --git a/_1_AdvCiv-SAS/Docs/README_Main_Changes_Guide.md b/_1_AdvCiv-SAS/Docs/README_Main_Changes_Guide.md
index e86c35629..a0aa927ef 100644
--- a/_1_AdvCiv-SAS/Docs/README_Main_Changes_Guide.md
+++ b/_1_AdvCiv-SAS/Docs/README_Main_Changes_Guide.md
@@ -182,7 +182,7 @@ Some features are not supported in AdvCiv-SAS; e.g., non-English languages, pre-
 - (Requires AdvCiv-SAS 6100+) It also records compact production and military interval flows plus exact civic, effective state-religion, and war-plan transitions. (Requires AdvCiv-SAS 6101+) Technology-acquisition rows also identify whether each technology came from research, trade, a free choice, a goody hut, a Great Person, espionage, a random event, technology sharing, a team merge, inheritance, Advanced Start, Barbarian research, game setup, debug tools, or an unknown Python/script source. (Requires AdvCiv-SAS 6102+) Each log also identifies whether the session is a new or loaded single-player, scenario, network, Hotseat, PBEM, Pitboss, or replay game, including its multiplayer turn options. (Requires AdvCiv-SAS 6103+) It also synthesizes one summary for every war between civilization teams, including declaration and peace context, elapsed turns, war-success changes, units destroyed and their total production cost, city-plot battle wins, and cities/population captured. Wars already active when a save is loaded are explicitly partial, while wars still active at victory or autoplay end remain marked ongoing rather than being misreported as peace. A full turn-474 test matched all 115 declarations to exactly 91 ordinary-peace summaries, 4 capitulations, 13 state-change/elimination summaries, and 7 wars still ongoing at victory.
 - (Requires AdvCiv-SAS 6105+) Setup context also records the calendar, starting turn/year, and each player's actual assigned color with its primary RGB values, which helps connect the text record to maps and screenshots. When the game is won, one final row per civilization pairs its ordinary score with Civ4's normalized final score. (Requires AdvCiv-SAS 6104+) Full interval and victory snapshots also record UTC to milliseconds, total real milliseconds since the log session began, and milliseconds since the previous snapshot. This makes autoplay speed and late-game slowdown directly comparable without external timing or timestamp subtraction; real time includes pauses and user interaction. Anonymous process-memory, system memory-load percentage, available 32-bit address space, and exact foreground/minimized samples can be disabled separately without disabling timing or gameplay records. A separate privacy/detail level defaults to display context (resolution, fullscreen/windowed state, and graphics options), Windows versus Wine/Proton, and pointer width; it can be lowered to hide these or raised to add logical processor count and total RAM.
 - (Requires AdvCiv-SAS 6106+) Barbarian activity is also preserved without forcing ordinary civilization economy/diplomacy rows onto the Barbarian player. Interval snapshots record Barbarian cities, animals, unit types/roles, and how many units are in Barbarian, unowned, or civilization-owned territory; concise city rows show their development and defenses. At level 3, exact positions, fog/goody-hut/cargo spawn causes, and Barbarian combat outcomes make pressure on nearby civilizations traceable. In a confirming full run, these rows captured 84 spawns, 182 combats, Barbarian city development, and eventual eradication, while adding only about 350-400 rows to an 18.5 MB record.
-- (Requires AdvCiv-SAS 6107+) **Level-3 text-map previews:** `SASGameRecord` includes separate geography and political pictures so humans and LLMs can immediately see landmass shapes, islands, narrow passages, and player positions instead of reconstructing them from coordinates. Clean fixed-width rows preserve the generated map's proportions in horizontal, square, or vertical maps; the political legend directly maps each symbol to its player ID, civilization, and displayed player name. XML defines control the picture bounds, monospace-width correction, and validated symbols.
+- (Requires AdvCiv-SAS 6107+) **Level-3 text-map previews:** `SASGameRecord` includes separate geography, terrain, feature, and political pictures so humans and LLMs can immediately see landmass shapes, islands, ice barriers, narrow passages, environmental cover, and player positions instead of reconstructing them from coordinates. The terrain picture distinguishes Coast from Ocean and overlays ice: one character cannot show both ice and its underlying water terrain, and blocked access matters more strategically. Geography shows broad land/water and plot shapes plus ice barriers without duplicating Coast terrain. The political picture intentionally remains ownership-only, because physical symbols would either hide owned water or appear inconsistently only outside borders. Normally stable geography and terrain appear once at setup/load, with exact later exceptions preserved by coordinate-bearing map-change rows. The initial feature picture shows how jungle, forest, flood plains, oases, and ice affect settling, movement, health, and yields; feature and political pictures then repeat at each full interval and victory snapshot to show forest/jungle clearing and regrowth, late-game fallout, territorial expansion, conquest, and collapse across the run. Blank water and dotted featureless land keep each feature picture readable without overlaying the geography map. Separate structured rows directly record player pollution, its sources, city health, and global warming; the maps supply the spatial context when terrain or features change. Clean fixed-width rows preserve the generated map's proportions in horizontal, square, or vertical maps; legends map configurable symbols to terrain/feature types and political symbols to player IDs, whose setup rows provide the civilization and displayed player name. XML defines control the picture bounds, monospace-width correction, and validated symbols.
 - (Requires AdvCiv-SAS 6022+) Note: it currently contains spoilers: a spoiler-free implementation was considered, so the human/active player can give to an LLM or such their play summary and get LLM or such suggestions or feedback/analysis, but it would require an extensive rework, so currently this is not supported; therefore, `SASGameRecord` log should as of now not be treated as a spoiler-free player-advice export. Note 2: depending on `SAS_GAME_RECORD_INTERVAL_TURNS_UNSCALED_GAMESPEED` value, the last turn may not appear in the log: for example, an autoplay run of 200 turns ends at T200 and thus does not seem to show the T200 data, only up to T190, so autoplay at least 201 turns to show T200 for example if you have this issue. For stronger LLM analysis, also provide compact source context when possible; for example, the AdvCiv-SAS light source ZIP from [`make_light_source_zip.py`](/LLM_Helpers/README.md#make_light_source_zippy) includes useful source/XML/docs/helper context and full `SASGameRecord` examples while staying compact enough to upload and share. See also the full raw [SASGameRecord example log](/_1_AdvCiv-SAS/SASGameRecord_log/SASGameRecord_example.log), and [README.md (DLL Logging)](/README.md#dll-logging).
 
 ### 48 Civs DLL

diff --git a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG
index 020f98e4f..c019b17a2 100644
Binary files a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG and b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_1.PNG differ

diff --git a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG
index e86ca50ae..8f84b4314 100644
Binary files a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG and b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_2.PNG differ

diff --git a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG
new file mode 100644
index 000000000..a20bf66b8
Binary files /dev/null and b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_3.PNG differ

diff --git a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG
new file mode 100644
index 000000000..43cc69451
Binary files /dev/null and b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_4.PNG differ

diff --git a/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG
new file mode 100644
index 000000000..b6c0f102c
Binary files /dev/null and b/_1_AdvCiv-SAS/Images/LLM/SASGameRecord_text_art_map_5.PNG differ

[PATCH OMITTED] file=a/_1_AdvCiv-SAS/SASGameRecord_log/SASGameRecord_example.log b/_1_AdvCiv-SAS/SASGameRecord_log/SASGameRecord_example.log reason=known-generated/noisy-history changedLines=79806 bytes=24448777

[PATCH COVERAGE]
patchFiles=11 embeddedFiles=9 omittedFiles=2 embeddedPatchBytes=39874

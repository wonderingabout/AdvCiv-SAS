# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2549
# Commit: 01798c41953908b6351fd59e28d1db3caf3c34a4
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 9546cb3f887af3b7c52fcbae38992366110a8f4b
# Title: Enum type for Fractal flags
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
3	3	CvGameCoreDLL/AIStrategies.h
8	8	CvGameCoreDLL/BitUtil.h
63	90	CvGameCoreDLL/CvFractal.cpp
22	11	CvGameCoreDLL/CvFractal.h
1	1	CvGameCoreDLL/CvGameCoreDLL.h
7	6	CvGameCoreDLL/CvMap.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/AIStrategies.h b/CvGameCoreDLL/AIStrategies.h
index de835bcbb..7a63a046e 100644
--- a/CvGameCoreDLL/AIStrategies.h
+++ b/CvGameCoreDLL/AIStrategies.h
@@ -84,9 +84,9 @@ enum AIVictoryStage // advc: replacing preprocessor defines
 	AI_CITY_ROLE_LICHPIN            =	(1 << 11),	//this city must not fall
 };
 // <advc.enum>
-OVERRIDE_BITMASK_OPERATORS(AICityRole)*/
-OVERRIDE_BITMASK_OPERATORS(AIStrategy)
-OVERRIDE_BITMASK_OPERATORS(AIVictoryStage)
+OVERLOAD_BITWISE_OPERATORS(AICityRole)*/
+OVERLOAD_BITWISE_OPERATORS(AIStrategy)
+OVERLOAD_BITWISE_OPERATORS(AIVictoryStage)
 // </advc.enum>
 
 #endif // AI_DEFINES_H

diff --git a/CvGameCoreDLL/BitUtil.h b/CvGameCoreDLL/BitUtil.h
index c308429e1..f65f7ef8d 100644
--- a/CvGameCoreDLL/BitUtil.h
+++ b/CvGameCoreDLL/BitUtil.h
@@ -68,35 +68,35 @@ namespace BitUtil // advc: Don't want these in the global namespace (for now)
 };
 
 // advc.enum (not from WtP): Macro for (somehwat) type-safe bitmasks
-#define OVERRIDE_BITMASK_OPERATORS(EnumType) \
-	inline EnumType operator|(EnumType eLeft, EnumType eRight) \
+#define OVERLOAD_BITWISE_OPERATORS(EnumType) \
+	__forceinline EnumType operator|(EnumType eLeft, EnumType eRight) \
 	{ \
 		int iLeft = eLeft, iRight = eRight; \
 		return static_cast<EnumType>(iLeft | iRight); \
 	} \
-	inline EnumType operator|=(EnumType& eLeft, EnumType eRight) \
+	__forceinline EnumType operator|=(EnumType& eLeft, EnumType eRight) \
 	{ \
 		return eLeft = (eLeft | eRight); \
 	} \
-	inline EnumType operator&(EnumType eLeft, EnumType eRight) \
+	__forceinline EnumType operator&(EnumType eLeft, EnumType eRight) \
 	{ \
 		int iLeft = eLeft, iRight = eRight; \
 		return static_cast<EnumType>(iLeft & iRight); \
 	} \
-	inline EnumType operator&=(EnumType& eLeft, EnumType eRight) \
+	__forceinline EnumType operator&=(EnumType& eLeft, EnumType eRight) \
 	{ \
 		return eLeft = (eLeft & eRight); \
 	} \
-	inline EnumType operator^(EnumType eLeft, EnumType eRight) \
+	__forceinline EnumType operator^(EnumType eLeft, EnumType eRight) \
 	{ \
 		int iLeft = eLeft, iRight = eRight; \
 		return static_cast<EnumType>(iLeft ^ iRight); \
 	} \
-	inline EnumType operator^=(EnumType& eLeft, EnumType eRight) \
+	__forceinline EnumType operator^=(EnumType& eLeft, EnumType eRight) \
 	{ \
 		return eLeft = (eLeft ^ eRight); \
 	} \
-	inline EnumType operator~(EnumType e) \
+	__forceinline EnumType operator~(EnumType e) \
 	{ \
 		int i = e; \
 		return static_cast<EnumType>(~i); \

diff --git a/CvGameCoreDLL/CvFractal.cpp b/CvGameCoreDLL/CvFractal.cpp
index bca31dc90..fbe8cb36a 100644
--- a/CvGameCoreDLL/CvFractal.cpp
+++ b/CvGameCoreDLL/CvFractal.cpp
@@ -36,15 +36,17 @@ void CvFractal::reset()
 	m_iFracYExp = -1;
 	m_iXs = -1;
 	m_iYs = -1;
-	m_iFlags = 0;
+	m_eFlags = NO_PREDICATES;
 	m_iFracX = -1;
 	m_iFracY = -1;
-	m_iXInc = m_iYInc = -1; // advc: Risky not to initialize these
+	m_iXInc = m_iYInc = -1; // advc: better initialize
 }
 
-void CvFractal::fracInit(int iNewXs, int iNewYs, int iGrain, CvRandom& random, int iFlags, CvFractal* pRifts, int iFracXExp/*=7*/, int iFracYExp/*=6*/)
+void CvFractal::fracInit(int iNewXs, int iNewYs, int iGrain, CvRandom& random,
+	int iFlags, CvFractal* pRifts, int iFracXExp/*=7*/, int iFracYExp/*=6*/)
 {
-	fracInitInternal(iNewXs, iNewYs, iGrain, random, NULL, -1, iFlags, pRifts, iFracXExp, iFracYExp);
+	fracInitInternal(iNewXs, iNewYs, iGrain, random, NULL, -1,
+			(Predicates)iFlags, pRifts, iFracXExp, iFracYExp);
 }
 
 // pbyHints should be a 1d array of bytes representing a 2d array
@@ -53,29 +55,27 @@ void CvFractal::fracInit(int iNewXs, int iNewYs, int iGrain, CvRandom& random, i
 // where minExp = std::min(iFracXExp, iFracYExp)
 // Note above that an extra value is required in a dimension in which the map does not wrap.
 
-void CvFractal::fracInitHinted(int iNewXs, int iNewYs, int iGrain, CvRandom& random, byte* pbyHints, int iHintsLength, int iFlags, CvFractal* pRifts, int iFracXExp/*=7*/, int iFracYExp/*=6*/)
+void CvFractal::fracInitHinted(int iNewXs, int iNewYs, int iGrain, CvRandom& random,
+	byte* pbyHints, int iHintsLength, int iFlags, CvFractal* pRifts,
+	int iFracXExp/*=7*/, int iFracYExp/*=6*/)
 {
-	int iFlagsNonPolar = iFlags & (~FRAC_POLAR);
-	fracInitInternal(iNewXs, iNewYs, iGrain, random, pbyHints, iHintsLength, iFlagsNonPolar, pRifts, iFracXExp, iFracYExp);
+	Predicates eFlagsNonPolar = ((Predicates)iFlags) & (~FRAC_POLAR);
+	fracInitInternal(iNewXs, iNewYs, iGrain, random, pbyHints, iHintsLength,
+			eFlagsNonPolar, pRifts, iFracXExp, iFracYExp);
 }
 
-void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& random, byte* pbyHints, int iHintsLength, int iFlags, CvFractal* pRifts, int iFracXExp, int iFracYExp)
+void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& random,
+	byte* pbyHints, int iHintsLength, Predicates eFlags, CvFractal* pRifts,
+	int iFracXExp, int iFracYExp)
 {
 	PROFILE("CvFractal::fracInit()");
 
-	int iX, iY;
-	int iI;
-
 	reset();
 
 	if (iFracXExp < 0)
-	{
 		iFracXExp = DEFAULT_FRAC_X_EXP;
-	}
 	if (iFracYExp < 0)
-	{
 		iFracYExp = DEFAULT_FRAC_Y_EXP;
-	}
 
 	m_iFracXExp = iFracXExp;
 	m_iFracYExp = iFracYExp;
@@ -84,10 +84,10 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 
 	// Init m_aaiFrac to all zeroes:
 	m_aaiFrac = new int*[m_iFracX + 1];
-	for (iX = 0; iX < m_iFracX + 1; iX++)
+	for (int iX = 0; iX < m_iFracX + 1; iX++)
 	{
 		m_aaiFrac[iX] = new int[m_iFracY + 1];
-		for (iY = 0; iY < m_iFracY + 1; iY++)
+		for (int iY = 0; iY < m_iFracY + 1; iY++)
 		{
 			m_aaiFrac[iX][iY] = 0;
 		}
@@ -95,15 +95,15 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 
 	m_iXs = iNewXs;
 	m_iYs = iNewYs;
-	m_iFlags = iFlags;
+	m_eFlags = eFlags;
 	m_iXInc = ((m_iFracX * FLOAT_PRECISION) / m_iXs);
 	m_iYInc = ((m_iFracY * FLOAT_PRECISION) / m_iYs);
 
 	int iMinExp = std::min(m_iFracXExp, m_iFracYExp);
 	int iSmooth = range(iMinExp - iGrain, 0, iMinExp);
 
-	int iHintsWidth = (1 << (m_iFracXExp - iSmooth)) + ((m_iFlags & FRAC_WRAP_X) ? 0 : 1);
-	int iHintsHeight = (1 << (m_iFracYExp - iSmooth)) + ((m_iFlags & FRAC_WRAP_Y) ? 0 : 1);
+	int iHintsWidth = (1 << (m_iFracXExp - iSmooth)) + ((m_eFlags & FRAC_WRAP_X) ? 0 : 1);
+	int iHintsHeight = (1 << (m_iFracYExp - iSmooth)) + ((m_eFlags & FRAC_WRAP_Y) ? 0 : 1);
 
 	FAssertMsg(pbyHints == NULL || iHintsLength == iHintsWidth*iHintsHeight, "pbyHints is the wrong size!");
 
@@ -113,62 +113,61 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 	{
 		int iScreen = 0;  // This screens out already marked spots in m_aaiFrac[][];
 
-		for (iI = 0; iI <= iPass; iI++)
+		for (int i = 0; i <= iPass; i++)
 		{
-			iScreen |= (1 << iI);
+			iScreen |= (1 << i);
 		}
 
-		if (m_iFlags & FRAC_WRAP_Y)
+		if (m_eFlags & FRAC_WRAP_Y)
 		{
-			for (iX = 0; iX < m_iFracX + 1; iX++)
+			for (int iX = 0; iX < m_iFracX + 1; iX++)
 			{
 				m_aaiFrac[iX][m_iFracY] = m_aaiFrac[iX][0];
 			}
 		}
-		else if (m_iFlags & FRAC_POLAR)
+		else if (m_eFlags & FRAC_POLAR)
 		{
-			for (iX = 0; iX < m_iFracX + 1; iX++)
+			for (int iX = 0; iX < m_iFracX + 1; iX++)
 			{
 				m_aaiFrac[iX][   0    ] = iPolarHeight;
 				m_aaiFrac[iX][m_iFracY] = iPolarHeight;
 			}
 		}
 
-		if (m_iFlags & FRAC_WRAP_X)
+		if (m_eFlags & FRAC_WRAP_X)
 		{
-			for (iY = 0; iY < m_iFracY + 1; iY++)
+			for (int iY = 0; iY < m_iFracY + 1; iY++)
 			{
 				m_aaiFrac[m_iFracX][iY] = m_aaiFrac[0][iY];
 			}
 		}
-		else if (m_iFlags & FRAC_POLAR)
+		else if (m_eFlags & FRAC_POLAR)
 		{
-			for (iY = 0; iY < m_iFracY + 1; iY++)
+			for (int iY = 0; iY < m_iFracY + 1; iY++)
 			{
 				m_aaiFrac[   0    ][iY] = iPolarHeight;
 				m_aaiFrac[m_iFracX][iY] = iPolarHeight;
 			}
 		}
 
-		if (m_iFlags & FRAC_CENTER_RIFT)
+		if (m_eFlags & FRAC_CENTER_RIFT)
 		{
-			if (m_iFlags & FRAC_WRAP_Y)
+			if (m_eFlags & FRAC_WRAP_Y)
 			{
-				for (iX = 0; iX < m_iFracX + 1; iX++)
+				for (int iX = 0; iX < m_iFracX + 1; iX++)
 				{
-					for (iY = 0; iY < (m_iFracY / 6); iY++)
+					for (int iY = 0; iY < (m_iFracY / 6); iY++)
 					{
 						m_aaiFrac[iX][        iY         ] /= (abs((m_iFracY / 12) - iY) + 1);
 						m_aaiFrac[iX][(m_iFracY / 2) + iY] /= (abs((m_iFracY / 12) - iY) + 1);
 					}
 				}
 			}
-
-			if (m_iFlags & FRAC_WRAP_X)
+			if (m_eFlags & FRAC_WRAP_X)
 			{
-				for (iY = 0; iY < m_iFracY + 1; iY++)
+				for (int iY = 0; iY < m_iFracY + 1; iY++)
 				{
-					for (iX = 0; iX < (m_iFracX / 6); iX++)
+					for (int iX = 0; iX < (m_iFracX / 6); iX++)
 					{
 						m_aaiFrac[        iX         ][iY] /= (abs((m_iFracX / 12) - iX) + 1);
 						m_aaiFrac[(m_iFracX / 2) + iX][iY] /= (abs((m_iFracX / 12) - iX) + 1);
@@ -177,12 +176,14 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 			}
 		}
 
-		for (iX = 0; iX < (m_iFracX >> iPass) + ((m_iFlags & FRAC_WRAP_X) ? 0 : 1); iX++)
+		for (int iX = 0; iX < (m_iFracX >> iPass) +
+			((m_eFlags & FRAC_WRAP_X) ? 0 : 1); iX++)
 		{
 			gDLL->callUpdater();
-			for (iY = 0; iY < (m_iFracY >> iPass) + ((m_iFlags & FRAC_WRAP_Y) ? 0 : 1); iY++)
+			for (int iY = 0; iY < (m_iFracY >> iPass) +
+				((m_eFlags & FRAC_WRAP_Y) ? 0 : 1); iY++)
 			{
-				if ((iPass == iSmooth))// If this is the first, pass, set the initial random spots
+				if (iPass == iSmooth)// If this is the first, pass, set the initial random spots
 				{
 					if (pbyHints == NULL)
 					{
@@ -248,15 +249,13 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 	}
 
 	if (pRifts)
-	{
 		tectonicAction(pRifts);  //  Assumes FRAC_WRAP_X is on.
-	}
 
-	if (m_iFlags & FRAC_INVERT_HEIGHTS)
+	if (m_eFlags & FRAC_INVERT_HEIGHTS)
 	{
-		for (iX = 0; iX < m_iFracX; iX++)
+		for (int iX = 0; iX < m_iFracX; iX++)
 		{
-			for (iY = 0; iY < m_iFracY; iY++)
+			for (int iY = 0; iY < m_iFracY; iY++)
 			{
 				m_aaiFrac[iX][iY] = (255 - m_aaiFrac[iX][iY]);
 			}
@@ -267,18 +266,14 @@ void CvFractal::fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& r
 
 int CvFractal::getHeight(int iX, int iY)
 {
-	FAssertMsg(0 <= iX && iX < m_iXs, "iX out of range");
-	FAssertMsg(0 <= iY && iY < m_iYs, "iY out of range");
+	FAssertBounds(0, m_iXs, iX);
+	FAssertBounds(0, m_iYs, iY);
 	int iLowX = ((m_iXInc * iX) / FLOAT_PRECISION);
 	if (iLowX > m_iFracX - 1)
-	{
 		iLowX = m_iFracX - 1;	// clamp so that iLowX+1 doesn't overrun array
-	}
 	int iLowY = ((m_iYInc * iY) / FLOAT_PRECISION);
 	if (iLowY > m_iFracY - 1)
-	{
 		iLowY = m_iFracY - 1;	// clamp so that iLowY+1 doesn't overrun array
-	}
 	int iErrX = ((m_iXInc * iX) - (iLowX * FLOAT_PRECISION));
 	int iErrY = ((m_iYInc * iY) - (iLowY * FLOAT_PRECISION));
 
@@ -287,46 +282,30 @@ int CvFractal::getHeight(int iX, int iY)
 	iSum += ((                  iErrX) * (FLOAT_PRECISION - iErrY) * m_aaiFrac[iLowX + 1][iLowY    ]);
 	iSum += ((FLOAT_PRECISION - iErrX) * (                  iErrY) * m_aaiFrac[iLowX    ][iLowY + 1]);
 	iSum += ((                  iErrX) * (                  iErrY) * m_aaiFrac[iLowX + 1][iLowY + 1]);
-
 	iSum /= (FLOAT_PRECISION * FLOAT_PRECISION);
 
 	int iHeight = range(iSum, 0, 255);
-
-	if (m_iFlags & FRAC_PERCENT)
-	{
-		int iI = ((iHeight * 100) >> 8);
-
-		return iI;
-	}
-	else
-	{
-		return iHeight;
-	}
+	if (m_eFlags & FRAC_PERCENT)
+		return (iHeight * 100) >> 8;
+	return iHeight;
 }
 
 
 int CvFractal::getHeightFromPercent(int iPercent)
 {
-	PROFILE("CvFractal::getHeightFromPercent()");
-	int iEstimate;
-	int iLowerBound;
-	int iUpperBound;
-	int iSum;
-	int iX, iY;
-
-	iLowerBound = 0;
-	iUpperBound = 255;
+	PROFILE_FUNC();
 
 	iPercent = range(iPercent, 0, 100);
-	iEstimate = 255 * iPercent / 100;
 
+	int iEstimate = 255 * iPercent / 100;
+	int iLowerBound = 0;
+	int iUpperBound = 255;
 	while (iEstimate != iLowerBound)
 	{
-		iSum = 0;
-
-		for (iX = 0; iX < m_iFracX; iX++)
+		int iSum = 0;
+		for (int iX = 0; iX < m_iFracX; iX++)
 		{
-			for (iY = 0; iY < m_iFracY; iY++)
+			for (int iY = 0; iY < m_iFracY; iY++)
 			{
 				if (m_aaiFrac[iX][iY] < iEstimate)
 				{
@@ -345,7 +324,6 @@ int CvFractal::getHeightFromPercent(int iPercent)
 			iEstimate = (iUpperBound + iLowerBound) / 2;
 		}
 	}
-
 	return iEstimate;
 }
 
@@ -395,27 +373,22 @@ void CvFractal::tectonicAction(CvFractal* pRifts)  //  Assumes FRAC_WRAP_X is on
 int CvFractal::yieldX(int iBadX)  //  Assumes FRAC_WRAP_X is on.
 {
 	if (iBadX < 0)
-	{
 		return (iBadX + m_iFracX);
-	}
-
 	if (iBadX >= m_iFracX)
-	{
 		return (iBadX - m_iFracX);
-	}
-
 	return iBadX;
 }
 
-// <advc.tsl> Wrap this in a protected function so that subclasses can override it
-int CvFractal::polarHeight() {
-
+// advc.tsl: Wrap this in a protected function so that subclasses can override it
+int CvFractal::polarHeight()
+{
 	/*  No easy way to let map scripts set this because the code that exposes
 		CvFractal (and CvFractal::FracVals) is in the EXE. */
-	if(GC.getInitCore().getMapScriptName().compare(L"Fractal") == 0) {
+	if(GC.getInitCore().getMapScriptName().compare(L"Fractal") == 0)
+	{
 		/*  A power of 2 probably has no advantage here, but 64 also happens to work
 			pretty well. The closer to 0 this is set, the wider the polar water bands. */
 		return (1 << 6);
 	}
 	return 0;
-} // </advc.tsl>
+}

diff --git a/CvGameCoreDLL/CvFractal.h b/CvGameCoreDLL/CvFractal.h
index 1c7569c70..06819c925 100644
--- a/CvGameCoreDLL/CvFractal.h
+++ b/CvGameCoreDLL/CvFractal.h
@@ -11,17 +11,23 @@ class CvFractal
 
 public:
 
-	enum DllExport FracVals		// Exposed to Python
+	// Exposed to Python
+	/*	advc: ... through the EXE, i.e. the values shouldn't be changed in the DLL
+		b/c that wouldn't affect the values in Python.
+		Renaming the enum doesn't hurt though. */
+	enum /*FracVals*/ Predicates
 	{
-		FRAC_WRAP_X					= (0x0001),
-		FRAC_WRAP_Y					= (0x0002),
-		FRAC_PERCENT				= (0x0004),  //  Returns raw data as a percent.
-		FRAC_POLAR					= (0x0008),  //  Sets polar regions to zero.
-		FRAC_CENTER_RIFT		= (0x0010),  //  Draws rift in center of world, too.
-		FRAC_INVERT_HEIGHTS	= (0x0020),  //  Draws inverts the heights
-		DEFAULT_FRAC_X_EXP = 7,
-		DEFAULT_FRAC_Y_EXP = 6
+		NO_PREDICATES				= 0,
+		FRAC_WRAP_X					= (1 << 0),
+		FRAC_WRAP_Y					= (1 << 1),
+		FRAC_PERCENT				= (1 << 2),  //  Returns raw data as a percent.
+		FRAC_POLAR					= (1 << 3),  //  Sets polar regions to zero.
+		FRAC_CENTER_RIFT			= (1 << 4),  //  Draws rift in center of world, too.
+		FRAC_INVERT_HEIGHTS			= (1 << 5),  //  Draws inverts the heights
+		// <avdc.enum> Move ..._EXP out and give the enum bitmask operators (see end of file)
 	};
+	static int const DEFAULT_FRAC_X_EXP = 7;
+	static int const DEFAULT_FRAC_Y_EXP = 6; // </advc.enum>
 
 	//  Smoothness varies with 2^grain.
 	//  Totally random is grain = min (iFracXExp, iFracYExp).
@@ -45,7 +51,7 @@ protected:
 	// advc.003k (warning): It's not safe to add data members to this class!
 	int m_iXs;
 	int m_iYs;
-	int m_iFlags;
+	Predicates m_eFlags; // advc.enum (was int)
 
 	int m_iFracX;
 	int m_iFracY;
@@ -57,7 +63,10 @@ protected:
 
 	int **m_aaiFrac; //[FRAC_X + 1][FRAC_Y + 1];
 
-	void fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& random, byte* pbyHints, int iHintsLength, int iFlags, CvFractal* pRifts, int iFracXExp, int iFracYExp);
+	void fracInitInternal(int iNewXs, int iNewYs, int iGrain, CvRandom& random,
+			byte* pbyHints, int iHintsLength,
+			Predicates eFlags, // advc.enum
+			CvFractal* pRifts, int iFracXExp, int iFracYExp);
 	void tectonicAction(CvFractal* pRifts);
 	int yieldX(int iBadX);
 	int polarHeight(); // advc.tsl
@@ -65,4 +74,6 @@ protected:
 
 BOOST_STATIC_ASSERT(sizeof(CvFractal) == 44); // advc.003k
 
+OVERLOAD_BITWISE_OPERATORS(CvFractal::Predicates) // advc.enum
+
 #endif

diff --git a/CvGameCoreDLL/CvGameCoreDLL.h b/CvGameCoreDLL/CvGameCoreDLL.h
index eda643ac8..f7a2100b1 100644
--- a/CvGameCoreDLL/CvGameCoreDLL.h
+++ b/CvGameCoreDLL/CvGameCoreDLL.h
@@ -96,6 +96,7 @@ __forceinline float MaxFloat() { return DWtoF(0x7f7fffff); }
 #include "FFreeListTrashArray.h" // advc.003s: includes FreeListTraversal.h
 #include "LinkedList.h"
 #include "CvString.h"
+#include "BitUtil.h" // advc.enum
 #include "CvDefines.h"
 #include "CvEnums.h" // includes CvInfoEnum.h
 /*  advc: Smaller numbers may already crash the EXE; the DLL assumes in some places
@@ -109,7 +110,6 @@ BOOST_STATIC_ASSERT(MAX_PLAYERS < MAX_CHAR && MAX_TEAMS < MAX_CHAR);
 #include "CvGameCoreUtils.h"
 #include "ScaledNum.h"
 #include "CvGlobals.h"
-#include "BitUtil.h" // advc.enum: For EnumMap, but will have other uses too.
 #include "EnumMap2D.h" // advc.enum: Includes EnumMap.h
 #include "CvPythonCaller.h" // advc.003y
 #include "CvDLLLogger.h" // advc.003t

diff --git a/CvGameCoreDLL/CvMap.cpp b/CvGameCoreDLL/CvMap.cpp
index 09121554f..f9991c26f 100644
--- a/CvGameCoreDLL/CvMap.cpp
+++ b/CvGameCoreDLL/CvMap.cpp
@@ -661,15 +661,16 @@ CvArea* CvMap::findBiggestArea(bool bWater)
 
 int CvMap::getMapFractalFlags() const
 {
-	int wrapX = 0;
+	CvFractal::Predicates eWrapX = CvFractal::NO_PREDICATES;
 	if (isWrapX())
-		wrapX = (int)CvFractal::FRAC_WRAP_X;
+		eWrapX = CvFractal::FRAC_WRAP_X;
 
-	int wrapY = 0;
+	CvFractal::Predicates eWrapY = CvFractal::NO_PREDICATES;
 	if (isWrapY())
-		wrapY = (int)CvFractal::FRAC_WRAP_Y;
-
-	return (wrapX | wrapY);
+		eWrapY = CvFractal::FRAC_WRAP_Y;
+	/*	(advc.enum: Convert to int. It's only used in Python anyway, and
+		don't want to include CvFractal.h in CvMap.h.) */
+	return (eWrapX | eWrapY);
 }
 
 // Check plots for wetlands or seaWater. Returns true if found

[PATCH COVERAGE]
patchFiles=6 embeddedFiles=6 omittedFiles=0 embeddedPatchBytes=17612

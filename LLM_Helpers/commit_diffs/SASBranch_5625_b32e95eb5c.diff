# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 5625
# Commit: b32e95eb5c8e54152dd9c2c05b019b20e5ed968e
# History segment: AdvCiv-SAS branch history
# Parent used for diff: e6c0ad69a80920f610e2d74d3b6b2ff9b7e72199
# Title: Avoid unnecessary rewrites (GPT-5.3-Codex) Before writing, compare generated key block to current block; skip write if identical. Optionally store last appli...
# Full commit message/metadata: see _1_AdvCiv-SAS/git_logs/git_log_anonymized_email_003_AdvCiv-SAS.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	2	Assets/Python/BUG/BugEventManager.py
16	4	Assets/Python/SASBillboardScale.py
1	0	Long_Comments/CIV4DetailManager_for_comments.xml

[PATCHES]

diff --git a/Assets/Python/BUG/BugEventManager.py b/Assets/Python/BUG/BugEventManager.py
index 871ba99d3..53cc8b40e 100644
--- a/Assets/Python/BUG/BugEventManager.py
+++ b/Assets/Python/BUG/BugEventManager.py
@@ -662,9 +662,8 @@ def configure(logging=None, noLogEvents=None):
 	g_eventManager.addEventHandler("gameUpdate", g_eventManager.onGameUpdate)
 	# --
 
-	# custom: scale city billboard based on SAS font body level (Claude code Opus 4.6)
+	# <!-- custom: scale city billboard based on SAS font body level (Claude code Opus 4.6) -->
 	SASBillboardScale.apply()
-	# --
 
 def hookupPreGameStartEvent():
 	BugUtil.extend(preGameStart, "CvAppInterface", "preGameStart")

diff --git a/Assets/Python/SASBillboardScale.py b/Assets/Python/SASBillboardScale.py
index 3f75c3bd2..82b93c18b 100644
--- a/Assets/Python/SASBillboardScale.py
+++ b/Assets/Python/SASBillboardScale.py
@@ -39,7 +39,7 @@ _DETAIL_MANAGER_REL_PATH = os.path.join("Assets", "XML", "Misc", "CIV4DetailMana
 
 
 def _getMultiplierPercent(iFontBody):
-	"""Read SAS_BILLBOARD_SCALE_PERCENT_FONT_<N> for the given font level."""
+	# read SAS_BILLBOARD_SCALE_PERCENT_FONT_<N> for the given font level.
 	if iFontBody < 1 or iFontBody > 4:
 		return 100
 	szDefine = "SAS_BILLBOARD_SCALE_PERCENT_FONT_%d" % iFontBody
@@ -51,7 +51,7 @@ def _getMultiplierPercent(iFontBody):
 
 
 def _buildScaledKeys(iPercent):
-	"""Return list of formatted <Key> lines with scaled values."""
+	# return formatted CITYBILLBOARD_SCALE <Key> lines using the given percent multiplier.
 	fMult = iPercent / 100.0
 	lines = []
 	for (iDist, fBase) in _BASE_BILLBOARD_KEYS:
@@ -61,7 +61,7 @@ def _buildScaledKeys(iPercent):
 
 
 def _rewriteDetailManagerXML(szPath, scaledKeyLines):
-	"""Replace the CITYBILLBOARD_SCALE Key entries in the XML file."""
+	# replace CITYBILLBOARD_SCALE key lines in CIV4DetailManager.xml; skip file write when unchanged.
 	try:
 		f = open(szPath, "r")
 		content = f.read()
@@ -74,6 +74,18 @@ def _rewriteDetailManagerXML(szPath, scaledKeyLines):
 	pattern = r"(<Name>CITYBILLBOARD_SCALE</Name>\s*\n)((?:\s*<Key>[^<]*</Key>\s*\n)+)"
 	replacement = r"\1" + "\n".join(scaledKeyLines) + "\n"
 
+	match = re.search(pattern, content)
+	if match is None:
+		BugUtil.error("SASBillboardScale: CITYBILLBOARD_SCALE block not found in %s", szPath)
+		return False
+
+	currentKeys = [line.strip() for line in match.group(2).splitlines() if line.strip()]
+	targetKeys = [line.strip() for line in scaledKeyLines]
+	# If already in the desired state, do nothing to avoid needless disk churn.
+	if currentKeys == targetKeys:
+		BugUtil.info("SASBillboardScale: CITYBILLBOARD_SCALE already up to date in %s", szPath)
+		return True
+
 	newContent, count = re.subn(pattern, replacement, content)
 	if count == 0:
 		BugUtil.error("SASBillboardScale: CITYBILLBOARD_SCALE block not found in %s", szPath)
@@ -91,7 +103,7 @@ def _rewriteDetailManagerXML(szPath, scaledKeyLines):
 
 
 def apply():
-	"""Read font body level, compute billboard scale, rewrite XML."""
+	# apply define-driven CITYBILLBOARD_SCALE keys for the active SAS_UI_FONT_BODY.
 	iFontBody = getSASUIFontBody()
 	iPercent = _getMultiplierPercent(iFontBody)

diff --git a/Long_Comments/CIV4DetailManager_for_comments.xml b/Long_Comments/CIV4DetailManager_for_comments.xml
index afe428560..befc39c31 100644
--- a/Long_Comments/CIV4DetailManager_for_comments.xml
+++ b/Long_Comments/CIV4DetailManager_for_comments.xml
@@ -98,6 +98,7 @@
 		<!-- advc.002b: Was (700, 1.5), (2500, 1.0), (6000, 0.67).
 			 Some 10% bigger for the medium distances to accommodate large
 			 displays better. More enlargement close up. -->
+		<!-- custom: See also Assets/Python/SASBillboardScale.py for define-driven CITYBILLBOARD_SCALE profile application on load. (GPT-5.3-Codex) -->
 		<Key>500,    1.72</Key>
 		<Key>2500,   1.18</Key>
 		<Key>3800,   1.06</Key>

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=3858

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4020
# Commit: 8e00e6824d91878d5cc06f09ea52780cdf619bbc
# History segment: pre-SAS AdvCiv history
# Parent used for diff: cbab7130e03e9b2fea71cb7a2347e060867f2997
# Title: Fix a sign error in AI-human war trades check
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
7	4	CvGameCoreDLL/UWAIAgent.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/UWAIAgent.cpp b/CvGameCoreDLL/UWAIAgent.cpp
index b9a79095a..5f4f0cc6a 100644
--- a/CvGameCoreDLL/UWAIAgent.cpp
+++ b/CvGameCoreDLL/UWAIAgent.cpp
@@ -1632,20 +1632,23 @@ DenialTypes UWAI::Team::declareWarTrade(TeamTypes eTarget, TeamTypes eSponsor) c
 					{
 						int iMemberTradeVal=-1;
 						itAgentMember->uwai().canTradeAssets(
-								utilityToTradeVal(iUtilityThresh).round(),
+								utilityToTradeVal(-iUtilityThresh).round(),
 								itSponsorMember->getID(), &iMemberTradeVal);
 						iHumanTradeVal = std::max(iHumanTradeVal, iMemberTradeVal);
 					}
 				}
 				iUtilityThresh = std::max(iUtilityThresh,
 						- (tradeValToUtility(iHumanTradeVal) +
-						// Add 5 for gold that the human might be able to procure
+						// For gold that the human might be able to procure
 						((GET_TEAM(eSponsor).isGoldTrading() ||
 						kAgent.isGoldTrading() ||
 						// Or they could ask nicely
 						(GET_TEAM(eSponsor).isAtWar(eTarget) &&
-						kAgent.AI_getAttitude(eSponsor) >=
-						ATTITUDE_PLEASED)) ? 5 : 0)).round());
+						kAgent.AI_getAttitude(eSponsor) >= ATTITUDE_PLEASED)) ?
+						(GC.getGame().isOption(GAMEOPTION_NO_TECH_TRADING) ? 6 : 4) : 0) +
+						// For tech that the human might get access to soon
+						(GET_TEAM(eSponsor).isTechTrading() ||
+						kAgent.isTechTrading() ? 4 : 0)).round());
 			}
 			if (iU > iUtilityThresh)
 				return NO_DENIAL;

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1419

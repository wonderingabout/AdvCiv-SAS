# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 473
# Commit: bc499ecb57f1feff460f917b0ad460d1b3f0294d
# History segment: K-Mod history
# Parent used for diff: 77cae1f06ca436930f37576ec2040e41e2c746c3
# Title: Fixed a problem which caused automated units to repeated cancel and reorder their commands. Automated units will no longer attempt to be rescued using AI_han...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	0	CvGameCoreDLL/CvGameCoreUtils.cpp
2	1	CvGameCoreDLL/CvSelectionGroup.cpp
3	1	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameCoreUtils.cpp b/CvGameCoreDLL/CvGameCoreUtils.cpp
index 4b9a2989e..4826f88a8 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.cpp
+++ b/CvGameCoreDLL/CvGameCoreUtils.cpp
@@ -2505,6 +2505,12 @@ void getActivityTypeString(CvWString& szString, ActivityTypes eActivityType)
 	case ACTIVITY_SENTRY: szString = L"ACTIVITY_SENTRY"; break;
 	case ACTIVITY_INTERCEPT: szString = L"ACTIVITY_INTERCEPT"; break;
 	case ACTIVITY_MISSION: szString = L"ACTIVITY_MISSION"; break;
+// K-Mod. There were some missing activity strings...
+#define case_string(x) case x: szString = L#x; break;
+	case_string(ACTIVITY_PATROL)
+	case_string(ACTIVITY_PLUNDER)
+#undef case_string
+// K-Mod end
 
 	default: szString = CvWString::format(L"UNKNOWN_ACTIVITY(%d)", eActivityType); break;
 	}

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index a92a32adf..b787f3bf1 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -521,7 +521,8 @@ void CvSelectionGroup::autoMission()
 			if (!isBusy())
 			{
 				bool bVisibleHuman = false;
-				if (isHuman())
+				//if (isHuman())
+				if (!AI_isControlled()) // K-Mod. (otherwise the automation will just reissue commands immediately after they are cleared, resulting in an infinite loop.)
 				{
 					for (CLLNode<IDInfo>* pUnitNode = headUnitNode(); pUnitNode != NULL; pUnitNode = nextUnitNode(pUnitNode))
 					{

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index e0d281185..02f1967b3 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -20654,11 +20654,13 @@ bool CvUnitAI::AI_retreatToCity(bool bPrimary, bool bAirlift, int iMaxPath)
 // If we're on the coast, wait to be rescued!
 bool CvUnitAI::AI_handleStranded(int iFlags)
 {
-	FAssert(!isHuman());
 	FAssert(!isCargo());
 
 	PROFILE_FUNC();
 
+	if (isHuman())
+		return false;
+
 	const CvPlayerAI& kOwner = GET_PLAYER(getOwnerINLINE());
 
 	// return false if the group is not stranded.

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2014

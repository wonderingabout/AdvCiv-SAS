# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 381
# Commit: 45b7403be256bda41fe273ce9cff6063a7435c29
# History segment: K-Mod history
# Parent used for diff: 4120ef83fb47d37460d3804a1de9cdd6ea31f578
# Title: Fixed a problem with the anyPlotDanger function. Made a few minor tweaks for consistency.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	CvGameCoreDLL/CvPlayerAI.cpp
1	5	CvGameCoreDLL/CvSelectionGroup.cpp
1	1	CvGameCoreDLL/CvSelectionGroupAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 6103202bc..1fab5edac 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -4130,7 +4130,8 @@ bool CvPlayerAI::AI_getAnyPlotDanger(CvPlot* pPlot, int iRange, bool bTestMoves)
 	TeamTypes eTeam = getTeam();
 	//bool bCheckBorder = (!isHuman() && !pPlot->isCity());
 	// K-Mod. I don't want auto-workers on the frontline. Cities need to be excluded for some legacy AI code.
-	bool bCheckBorder = !pPlot->isCity();
+	bool bCheckBorder = !pPlot->isCity() && (!isHuman() || pPlot->plotCount(PUF_canDefend, -1, -1, getID(), NO_TEAM) == 0);
+	// K-Mod end
 
 	
 	if( bCheckBorder )

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index 8748dec4a..dacfae523 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -4588,12 +4588,8 @@ void CvSelectionGroup::CachePathValidityResult(CvPlot* pFromPlot, bool cachedRes
 void CvSelectionGroup::resetPath() const
 {
 	lastPathGeneratedFor = NULL; // K-Mod
-#ifdef KMOD_PATH_FINDER
-	//path_finder.Reset(); // note. the K-Mod finder doesn't need resetting in all the same places. So I'm not using this function.
-	//gDLL->getFAStarIFace()->ForceReset(&GC.getPathFinder()); // for side-by-side testing.
-#else
+	//path_finder.Reset(); // note. the K-Mod finder doesn't need resetting in all the same places.
 	gDLL->getFAStarIFace()->ForceReset(&GC.getPathFinder());
-#endif
 }

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.cpp b/CvGameCoreDLL/CvSelectionGroupAI.cpp
index a07e395d5..debd227bf 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.cpp
+++ b/CvGameCoreDLL/CvSelectionGroupAI.cpp
@@ -276,7 +276,7 @@ bool CvSelectionGroupAI::AI_update()
 				break;
 			}
 
-			//resetPath(); // disabled by K-Mod (testing)
+			resetPath();
 
 			if (pHeadUnit->AI_update())
 			{

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=1880

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 479
# Commit: 97d3f6c18baec8fdd17286a37e3809427e40805a
# History segment: K-Mod history
# Parent used for diff: bfc3c3cca7b436618312687ef166cd1a43ebe024
# Title: Made a hackish change to the heroic gesture event and the great mediator event so that these events do not trigger when one of the players has non NO_DENIAL...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
22	0	Assets/Python/EntryPoints/CvRandomEventInterface.py

[PATCHES]

diff --git a/Assets/Python/EntryPoints/CvRandomEventInterface.py b/Assets/Python/EntryPoints/CvRandomEventInterface.py
index a00b7978e..fcbf4966b 100644
--- a/Assets/Python/EntryPoints/CvRandomEventInterface.py
+++ b/Assets/Python/EntryPoints/CvRandomEventInterface.py
@@ -1625,6 +1625,17 @@ def canTriggerHeroicGesture(argsList):
 	if gc.getTeam(player.getTeam()).AI_getWarSuccess(destPlayer.getTeam()) <= 0:
 		return false
 
+	# K-Mod. an ugly hack to prevent the AI from signing a peace deal when it really doesn't want to.
+	tradeData = TradeData()
+	tradeData.ItemType = TradeableItems.TRADE_PEACE_TREATY
+
+	if player.getTradeDenial(kTriggeredData.eOtherPlayer, tradeData) != DenialTypes.NO_DENIAL:
+		return false
+
+	if destPlayer.getTradeDenial(kTriggeredData.ePlayer, tradeData) != DenialTypes.NO_DENIAL:
+		return false
+	# K-Mod end
+
 	return true
 
 def doHeroicGesture2(argsList):
@@ -1695,6 +1706,17 @@ def canTriggerGreatMediator(argsList):
 	if gc.getTeam(player.getTeam()).AI_getAtWarCounter(destPlayer.getTeam()) < 10:
 		return false
 
+	# K-Mod. an ugly hack to prevent the AI from signing a peace deal when it really doesn't want to.
+	tradeData = TradeData()
+	tradeData.ItemType = TradeableItems.TRADE_PEACE_TREATY
+
+	if player.getTradeDenial(kTriggeredData.eOtherPlayer, tradeData) != DenialTypes.NO_DENIAL:
+		return false
+
+	if destPlayer.getTradeDenial(kTriggeredData.ePlayer, tradeData) != DenialTypes.NO_DENIAL:
+		return false
+	# K-Mod end
+
 	return true
 
 def doGreatMediator2(argsList):

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1526

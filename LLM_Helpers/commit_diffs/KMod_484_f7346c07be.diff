# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 484
# Commit: f7346c07be0c40e3179b6e16a02713b3a98ba8ad
# History segment: K-Mod history
# Parent used for diff: 3a58ca6261b84e97663a7c33f72d99d8ba18c164
# Title: Fixed a potential crash bug in the new automation code in CvGame::updateMoves CvSelectionGroup::readyToAuto now returns true for automated groups that are re...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
12	5	CvGameCoreDLL/CvGame.cpp
2	1	CvGameCoreDLL/CvSelectionGroup.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 266bedf9c..c4b397020 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -7203,14 +7203,21 @@ void CvGame::updateMoves()
 					for(pLoopSelectionGroup = player.firstSelectionGroup(&iLoop); pLoopSelectionGroup; pLoopSelectionGroup = player.nextSelectionGroup(&iLoop))
 					{
 						pLoopSelectionGroup->autoMission();
-						// K-Mod. Here's where we do the AI for automated units.
-						if (player.isHuman() && pLoopSelectionGroup->AI_update())
+					}
+					// K-Mod. Here's where we do the AI for automated units.
+					// Note, we can't do AI_update and autoMission in the same loop, because either one might delete the group - and thus cause the other to crash.
+					if (player.isHuman())
+					{
+						for (pLoopSelectionGroup = player.firstSelectionGroup(&iLoop); pLoopSelectionGroup; pLoopSelectionGroup = player.nextSelectionGroup(&iLoop))
 						{
-							FAssert(player.hasBusyUnit());
-							break;
+							if (pLoopSelectionGroup->AI_update())
+							{
+								FAssert(player.hasBusyUnit());
+								break;
+							}
 						}
-						// K-Mod end
 					}
+					// K-Mod end
 
 					if (!(player.hasBusyUnit()))
 					{

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index 66e3cc634..76fa38c36 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -4064,7 +4064,8 @@ bool CvSelectionGroup::readyToMove(bool bAny)
 
 bool CvSelectionGroup::readyToAuto()
 {
-	return (canAllMove() && (headMissionQueueNode() != NULL));
+	//return (canAllMove() && (headMissionQueueNode() != NULL));
+	return (canAllMove() && (headMissionQueueNode() != NULL || isAutomated()));
 }

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1744

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 240
# Commit: f81196c8558277e575ddfb3a36a7b6d836f1fc50
# History segment: K-Mod history
# Parent used for diff: 40ee9283c85cb7acaf9517a55c692af299779b5a
# Title: Increased the volume of the loss-of-religion sound. Adjusted AI_follow to only do the attack checks on the first follow unit (because its the same check for...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	Assets/xml/Audio/Audio2DScripts.xml
3	1	CvGameCoreDLL/CvSelectionGroupAI.cpp
2	1	CvGameCoreDLL/CvUnit.h
7	4	CvGameCoreDLL/CvUnitAI.cpp
2	1	CvGameCoreDLL/CvUnitAI.h

[PATCHES]

diff --git a/Assets/xml/Audio/Audio2DScripts.xml b/Assets/xml/Audio/Audio2DScripts.xml
index 751cc0f7f..6bd7e7b49 100644
--- a/Assets/xml/Audio/Audio2DScripts.xml
+++ b/Assets/xml/Audio/Audio2DScripts.xml
@@ -11257,8 +11257,8 @@
 		<ScriptID>AS2D_BLIGHT</ScriptID>
 		<SoundID>SND_BLIGHT</SoundID>
 		<SoundType>GAME_INTERFACE</SoundType>
-		<iMinVolume>60</iMinVolume>
-		<iMaxVolume>60</iMaxVolume>
+		<iMinVolume>90</iMinVolume>
+		<iMaxVolume>90</iMaxVolume>
 		<iPitchChangeDown>0</iPitchChangeDown>
 		<iPitchChangeUp>0</iPitchChangeUp>
 		<iMinLeftPan>-1</iMinLeftPan>

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.cpp b/CvGameCoreDLL/CvSelectionGroupAI.cpp
index 680eec47e..039592602 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.cpp
+++ b/CvGameCoreDLL/CvSelectionGroupAI.cpp
@@ -305,6 +305,7 @@ bool CvSelectionGroupAI::AI_update()
 			if (!m_bGroupAttack)
 			{
 				pEntityNode = headUnitNode();
+				bool bFirst = true; // K-Mod
 
 				while ((pEntityNode != NULL) && readyToMove(true))
 				{
@@ -315,12 +316,13 @@ bool CvSelectionGroupAI::AI_update()
 					{
 						resetPath();
 
-						if (pLoopUnit->AI_follow())
+						if (pLoopUnit->AI_follow(bFirst))
 						{
 							bFollow = true;
 							break;
 						}
 					}
+					bFirst = false; // K-Mod
 				}
 			}

diff --git a/CvGameCoreDLL/CvUnit.h b/CvGameCoreDLL/CvUnit.h
index ab4d82b4f..6b8235410 100644
--- a/CvGameCoreDLL/CvUnit.h
+++ b/CvGameCoreDLL/CvUnit.h
@@ -759,7 +759,8 @@ public:
 	virtual void AI_uninit() = 0;
 	virtual void AI_reset(UnitAITypes eUnitAI = NO_UNITAI) = 0;
 	virtual bool AI_update() = 0;
-	virtual bool AI_follow() = 0;
+	//virtual bool AI_follow() = 0;
+	virtual bool AI_follow(bool bFirst = true) = 0; // K-Mod
 	virtual void AI_upgrade() = 0;
 	virtual void AI_promote() = 0;
 	virtual int AI_groupFirstVal() = 0;

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 99515b2de..04a7fbba5 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -429,7 +429,10 @@ bool CvUnitAI::AI_update()
 
 
 // Returns true if took an action or should wait to move later...
-bool CvUnitAI::AI_follow()
+// K-Mod added 'bFirst'. bFirst should be "true" if this is the first unit in the group to use this follow function.
+// the point is that there are some calculations and checks in here which only depend on the group, not the unit
+// so for efficiency, we should only check them once.
+bool CvUnitAI::AI_follow(bool bFirst)
 {
 	if (AI_followBombard())
 	{
@@ -453,9 +456,9 @@ bool CvUnitAI::AI_follow()
 		}
 	} */
 	// K-Mod, I've changed attack-follow code so that it will only attack with a single unit, not the whole group.
-	if (AI_cityAttack(1, 70, true))
+	if (bFirst && AI_cityAttack(1, 70, true))
 		return true;
-	if (AI_anyAttack(1, 70, 2, true, true))
+	if (bFirst && AI_anyAttack(1, 70, 2, true, true))
 		return true;
 	//
 
@@ -487,7 +490,7 @@ bool CvUnitAI::AI_follow()
 			}
 		}
 	} */
-	// K-Mod. AI_foundRange is AI. It doesn't always found when we want to, and it has the potential to found when we don't!
+	// K-Mod. AI_foundRange is bad AI. It doesn't always found when we want to, and it has the potential to found when we don't!
 	// So I've replaced it.
 	if (AI_foundFollow())
 		return true;

diff --git a/CvGameCoreDLL/CvUnitAI.h b/CvGameCoreDLL/CvUnitAI.h
index 943f92c66..9d089808b 100644
--- a/CvGameCoreDLL/CvUnitAI.h
+++ b/CvGameCoreDLL/CvUnitAI.h
@@ -22,7 +22,8 @@ public:
 	void AI_reset(UnitAITypes eUnitAI = NO_UNITAI);
 
 	bool AI_update();
-	bool AI_follow();
+	//bool AI_follow();
+	bool AI_follow(bool bFirst = true); // K-Mod
 /************************************************************************************************/
 /* BETTER_BTS_AI_MOD                      04/05/10                                jdog5000      */
 /*                                                                                              */

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=3899

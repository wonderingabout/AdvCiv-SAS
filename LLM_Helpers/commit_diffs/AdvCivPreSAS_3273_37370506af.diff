# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3273
# Commit: 37370506af5c6cba7bb7824e1d684eee343672b0
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 5054120b06e7fdb08b29ccfae78d55d1d0462f6e
# Title: Force-inline GET_PLAYER, GET_TEAM
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	CvGameCoreDLL/CvMap.h
4	0	CvGameCoreDLL/CvPlayerAI.h
4	0	CvGameCoreDLL/CvTeamAI.h
0	3	CvGameCoreDLL/FAssert.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvMap.h b/CvGameCoreDLL/CvMap.h
index 16071e580..b78bb313e 100644
--- a/CvGameCoreDLL/CvMap.h
+++ b/CvGameCoreDLL/CvMap.h
@@ -444,7 +444,7 @@ public: // advc: made several functions const
 		FAssert(isPlot(iX, iY)); // advc: Assertion added
 		return &(m_pMapPlots[plotNum(iX, iY)]);
 	} // <advc.inl> Even faster and less confusingly named; replacing the above in most places.
-	__forceinline CvPlot& getPlot(int x, int y) const
+	__inline CvPlot& getPlot(int x, int y) const
 	{
 		FAssert(isPlot(x, y));
 		return m_pMapPlots[plotNum(x, y)];

diff --git a/CvGameCoreDLL/CvPlayerAI.h b/CvGameCoreDLL/CvPlayerAI.h
index 253de3ed3..48fb93ded 100644
--- a/CvGameCoreDLL/CvPlayerAI.h
+++ b/CvGameCoreDLL/CvPlayerAI.h
@@ -22,6 +22,10 @@ class UWAICity; // advc.104d
 class CvPlayerAI : public CvPlayer
 {
 public:
+
+#ifdef _DEBUG
+	__forceinline // Annoying to step into by accident
+#endif
 	// advc.003u: Renamed from getPlayer
 	static CvPlayerAI& AI_getPlayer(PlayerTypes ePlayer)
 	{

diff --git a/CvGameCoreDLL/CvTeamAI.h b/CvGameCoreDLL/CvTeamAI.h
index ead3f796d..46f297dd8 100644
--- a/CvGameCoreDLL/CvTeamAI.h
+++ b/CvGameCoreDLL/CvTeamAI.h
@@ -19,6 +19,10 @@
 class CvTeamAI : public CvTeam
 {
 public:
+
+#ifdef _DEBUG
+	__forceinline // Annoying to step into by accident
+#endif
 	// advc.003u: Renamed from getTeam
 	static CvTeamAI& AI_getTeam(TeamTypes eTeam)
 	{

diff --git a/CvGameCoreDLL/FAssert.h b/CvGameCoreDLL/FAssert.h
index b353cdeff..63a587ba4 100644
--- a/CvGameCoreDLL/FAssert.h
+++ b/CvGameCoreDLL/FAssert.h
@@ -13,9 +13,6 @@
 #endif
 
 #ifdef FASSERT_ENABLE
-/*  advc.make: Inlining functions with assertions could be a bad choice. Let's
-	at least not force-inline. */
-#define __forceinline __inline
 
 #ifdef WIN32

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=1767

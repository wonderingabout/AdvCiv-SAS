# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2121
# Commit: 5ed2f154bf439d3c2f9ffae66c66396545b5b39e
# History segment: pre-SAS AdvCiv history
# Parent used for diff: b97cfdb996c988c6fdfaa75593cc0229c3ea47b6
# Title: Partly revert no-trading tech cost modifier tweak
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
3	3	Assets/XML/GlobalDefines_advc.xml
1	1	CvGameCoreDLL/CvTeam.cpp

[PATCHES]

diff --git a/Assets/XML/GlobalDefines_advc.xml b/Assets/XML/GlobalDefines_advc.xml
index b92c9c2db..6ee1e315e 100644
--- a/Assets/XML/GlobalDefines_advc.xml
+++ b/Assets/XML/GlobalDefines_advc.xml
@@ -971,13 +971,13 @@
 
 	<!-- advc.550d: This modifier is applied to tech costs when playing
 		 with the "No Tech Trading" option. The effect of the modifier is
-		 reduced in the early and late game. Recommended: -28, meaning that
-		 tech costs are taken times 100% - 28% = 72%. This multiplier gets
+		 reduced in the early and late game. Recommended: -30, meaning that
+		 tech costs are taken times 100% - 30% = 70%. This multiplier gets
 		 further adjusted to the tech era and map size. Use 0 for BtS
 		 behavior (tech costs unaffected by the "No Tech Trading" option). -->
 	<Define>
 		<DefineName>TECH_COST_NOTRADE_MODIFIER</DefineName>
-		<iDefineIntVal>-28</iDefineIntVal>
+		<iDefineIntVal>-30</iDefineIntVal>
 	</Define>
 
 	<!-- [advc.034] The number of turns with Open Borders after making peace

diff --git a/CvGameCoreDLL/CvTeam.cpp b/CvGameCoreDLL/CvTeam.cpp
index dc80380a8..add2b0e7a 100644
--- a/CvGameCoreDLL/CvTeam.cpp
+++ b/CvGameCoreDLL/CvTeam.cpp
@@ -2134,7 +2134,7 @@ int CvTeam::getResearchCost(TechTypes eTech, bool bGlobalModifiers, bool bTeamSi
 					GC.getDefineINT("TECH_COST_NOTRADE_MODIFIER"));
 			scaled rNoTradeAdjustment =
 					(rTECH_COST_NOTRADE_MODIFIER + per100(5) *
-					(eTechEra - scaled(3)).abs().pow(fixp(1.5))) *
+					(eTechEra - fixp(2.5)).abs().pow(fixp(1.5))) *
 					scaled::clamp(scaled(kWorld.getDefaultPlayers() - 2,
 					11 - eTechEra), 0, fixp(8/3.));
 			rNoTradeAdjustment.decreaseTo(0); // No Tech Trading can only lower tech costs

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1695

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1907
# Commit: 9ac9676e1418c7b925e3183f9f5fa641be9e796f
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 35eeb3b76aa42bb5a69d1fdf9561399324c5504a
# Title: Make ANY_AGENT_STATUS the default predicate for AgentIterator
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	CvGameCoreDLL/AgentIterator.h
1	1	CvGameCoreDLL/AgentIteratorTest.cpp
0	1	CvGameCoreDLL/AgentPredicates.h
2	2	CvGameCoreDLL/CvGame.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/AgentIterator.h b/CvGameCoreDLL/AgentIterator.h
index f5759206c..54c235728 100644
--- a/CvGameCoreDLL/AgentIterator.h
+++ b/CvGameCoreDLL/AgentIterator.h
@@ -266,7 +266,7 @@ private:
 #undef _bADD_BARBARIANS
 
 
-template<AgentStatusPredicate eSTATUS = ALIVE, AgentRelationPredicate eRELATION = ANY_AGENT_RELATION,
+template<AgentStatusPredicate eSTATUS = ANY_AGENT_STATUS, AgentRelationPredicate eRELATION = ANY_AGENT_RELATION,
 		bool bSYNCRAND_ORDER = false>
 class PlayerIter : public AgentIterator<CvPlayerAI, eSTATUS, eRELATION, bSYNCRAND_ORDER>
 {
@@ -281,7 +281,7 @@ public:
 	}
 };
 
-template<AgentStatusPredicate eSTATUS = ALIVE, AgentRelationPredicate eRELATION = ANY_AGENT_RELATION,
+template<AgentStatusPredicate eSTATUS = ANY_AGENT_STATUS, AgentRelationPredicate eRELATION = ANY_AGENT_RELATION,
 		bool bSYNCRAND_ORDER = false>
 class TeamIter : public AgentIterator<CvTeamAI, eSTATUS, eRELATION, bSYNCRAND_ORDER>
 {

diff --git a/CvGameCoreDLL/AgentIteratorTest.cpp b/CvGameCoreDLL/AgentIteratorTest.cpp
index f39a98575..478dd216e 100644
--- a/CvGameCoreDLL/AgentIteratorTest.cpp
+++ b/CvGameCoreDLL/AgentIteratorTest.cpp
@@ -83,7 +83,7 @@ int TestAgentIterator(int iSamples = 1)
 
 		{
 			TSC_PROFILE("ALIVE PlayerIter");
-			for (PlayerIter<> it; it.hasNext(); ++it)
+			for (PlayerIter<ALIVE> it; it.hasNext(); ++it)
 			{
 				CvPlayer const& kPlayer = *it;
 				r += kPlayer.getID();

diff --git a/CvGameCoreDLL/AgentPredicates.h b/CvGameCoreDLL/AgentPredicates.h
index e748e23d7..569e1bcd9 100644
--- a/CvGameCoreDLL/AgentPredicates.h
+++ b/CvGameCoreDLL/AgentPredicates.h
@@ -15,7 +15,6 @@
 enum AgentStatusPredicate
 {
 	ANY_AGENT_STATUS,
-	ANY_STATUS = ANY_AGENT_STATUS, // ANY_AGENT_STATUS is too verbose after all
 	EVER_ALIVE,
 	//NEVER_ALIVE, // Iterating over _only_ dead agents should rarely be needed
 	//DEFEATED,

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index e286ed0c5..c73d94a1e 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -9318,7 +9318,7 @@ void CvGame::onAllGameDataRead()
 	// </advc.opt>
 	GC.getAgents().gameStart(true); // advc.agent
 	// <advc.003m>
-	for (TeamIter<ANY_STATUS> it; it.hasNext(); ++it)
+	for (TeamIter<> it; it.hasNext(); ++it)
 	{
 		if (it->getNumWars() < 0 || it->getNumWars(false, false) < 0 ||
 			it->getNumWars(true, true) < 0)
@@ -9326,7 +9326,7 @@ void CvGame::onAllGameDataRead()
 			it->finalizeInit();
 		}
 	} // </advc.003m>  <advc.opt>
-	for (TeamIter<ANY_STATUS> it; it.hasNext(); ++it)
+	for (TeamIter<> it; it.hasNext(); ++it)
 	{
 		FOR_EACH_ENUM(WarPlan)
 		{

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=2613

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2066
# Commit: ffe04c3efd56da3c5142468b4e05cd12fd522d51
# History segment: pre-SAS AdvCiv history
# Parent used for diff: cad8437a40951a6b39619bd5ca46b44799f11ddd
# Title: Avoid some const_casts
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
3	2	CvGameCoreDLL/CvCityAI.cpp
3	3	CvGameCoreDLL/CvCityAI.h
3	2	CvGameCoreDLL/CvEventReporter.cpp
1	1	CvGameCoreDLL/CvEventReporter.h
4	4	CvGameCoreDLL/CvGame.h
2	4	CvGameCoreDLL/CvGameInterface.cpp
7	2	CvGameCoreDLL/CvInfo_Civilization.cpp
3	1	CvGameCoreDLL/CvInfo_Civilization.h
3	2	CvGameCoreDLL/CvPlayer.cpp
2	1	CvGameCoreDLL/CvPlayer.h
2	2	CvGameCoreDLL/CvPlayerAI.cpp
2	2	CvGameCoreDLL/CvPlayerAI.h
1	1	CvGameCoreDLL/CvPythonCaller.cpp
1	3	CvGameCoreDLL/CvSelectionGroup.cpp
0	3	CvGameCoreDLL/CvStatistics.h
2	2	CvGameCoreDLL/CvTalkingHeadMessage.cpp
1	1	CvGameCoreDLL/CvTalkingHeadMessage.h
2	2	CvGameCoreDLL/CvTeam.cpp
2	2	CvGameCoreDLL/CvTeam.h
4	2	CvGameCoreDLL/PlotRadiusIterator.h
1	2	CvGameCoreDLL/RiseFall.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index 112a2c2f5..987a1f616 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -5550,9 +5550,10 @@ int CvCityAI::AI_buildingValue(BuildingTypes eBuilding, int iFocusFlags,
 	// constructionValue cache
 	if (bUseConstructionValueCache && !bConstCache)
 	{
-		// please don't hate me for using const_cast.
+		// please don't hate me for using const cast.
 		// The const-correctness was my idea in the first place, but the original code doesn't make it easy for me.
-		const_cast<std::vector<int>&>(m_aiConstructionValue)[eBuildingClass] = iValue;
+		// advc: now mutable
+		m_aiConstructionValue[eBuildingClass] = iValue;
 	}
 	// K-Mod end

diff --git a/CvGameCoreDLL/CvCityAI.h b/CvGameCoreDLL/CvCityAI.h
index e40e95f5b..651ae439b 100644
--- a/CvGameCoreDLL/CvCityAI.h
+++ b/CvGameCoreDLL/CvCityAI.h
@@ -195,9 +195,9 @@ protected:
 	mutable int* m_iCachePlayerClosenessTurn;
 	mutable int* m_iCachePlayerClosenessDistance;
 	mutable int m_iNeededFloatingDefenders;
-	mutable int m_iNeededFloatingDefendersCacheTurn; // </advc>
-
-	std::vector<int> m_aiConstructionValue; // K-Mod. (cache)
+	mutable int m_iNeededFloatingDefendersCacheTurn;
+	mutable std::vector<int> m_aiConstructionValue; // K-Mod. (cache)
+	// </advc>
 
 	void AI_doDraft(bool bForce = false);
 	void AI_doHurry(bool bForce = false);

diff --git a/CvGameCoreDLL/CvEventReporter.cpp b/CvGameCoreDLL/CvEventReporter.cpp
index 751a0bf29..db915b7c3 100644
--- a/CvGameCoreDLL/CvEventReporter.cpp
+++ b/CvGameCoreDLL/CvEventReporter.cpp
@@ -392,8 +392,9 @@ void CvEventReporter::playerGoldTrade(PlayerTypes eFromPlayer, PlayerTypes eToPl
 {
 	m_kPythonEventMgr.reportPlayerGoldTrade(eFromPlayer, eToPlayer, iAmount);
 }
-// <advc.make> To get rid of the K-Mod friend declaration in the header
-CvPlayerRecord const* CvEventReporter::getPlayerRecord(PlayerTypes ePlayer) const
+/*	<advc.make> To get rid of the K-Mod friend declaration in the header.
+	Not const because CvStatistics performs lazy initialization of player records. */
+CvPlayerRecord const* CvEventReporter::getPlayerRecord(PlayerTypes ePlayer)
 {
 	return m_kStatistics.getPlayerRecord(ePlayer);
 } // </advc.make>

diff --git a/CvGameCoreDLL/CvEventReporter.h b/CvGameCoreDLL/CvEventReporter.h
index 50730d26a..9970fc110 100644
--- a/CvGameCoreDLL/CvEventReporter.h
+++ b/CvGameCoreDLL/CvEventReporter.h
@@ -119,7 +119,7 @@ public:
 	void setPlayerAlive(PlayerTypes ePlayerID, bool bNewValue);
 	void playerChangeStateReligion(PlayerTypes ePlayerID, ReligionTypes eNewReligion, ReligionTypes eOldReligion);
 	void playerGoldTrade(PlayerTypes eFromPlayer, PlayerTypes eToPlayer, int iAmount);
-	CvPlayerRecord const* getPlayerRecord(PlayerTypes ePlayer) const; // advc.make
+	CvPlayerRecord const* getPlayerRecord(PlayerTypes ePlayer); // advc.make
 
 	DllExport void chat(CvWString szString);

diff --git a/CvGameCoreDLL/CvGame.h b/CvGameCoreDLL/CvGame.h
index 93c4d4a85..d0c6665fe 100644
--- a/CvGameCoreDLL/CvGame.h
+++ b/CvGameCoreDLL/CvGame.h
@@ -581,10 +581,10 @@ public:
 
 	// advc.inl: const version, inline
 	DllExport inline CvDeal* getDeal(int iID)																	// Exposed to Python
-	{	CvGame const& kThis = *this;
-		return const_cast<CvDeal*>(kThis.getDeal(iID));
+	{
+		return m_deals.getAt(iID);
 	}
-	CvDeal const* getDeal(int iID) const
+	inline CvDeal const* getDeal(int iID) const
 	{
 		return m_deals.getAt(iID);
 	}
@@ -891,7 +891,7 @@ protected:
 		the other for CvDLLWidgetData::parseTradeItem. */
 	CLinkList<DealItemData> m_currentDeals;
 	CLinkList<DealItemData> m_currentDealsWidget;
-	bool m_bShowingCurrentDeals;
+	mutable bool m_bShowingCurrentDeals;
 	// </advc.072>
 
 	CvRandom m_mapRand;

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index afcacae72..a6a289f91 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -2655,8 +2655,6 @@ void CvGame::handleDiplomacySetAIComment(DiploCommentTypes eComment) const
 			gDLL->sendImplementDealMessage(eOtherPlayer, &playerList, &loopPlayerList);
 		}
 	}
-	// <advc.072>
-	const_cast<CvGame* const>(this)->m_bShowingCurrentDeals =
-			(eComment == GC.getAIDiploCommentType("CURRENT_DEALS"));
-	// </advc.072>
+	// advc.072:
+	m_bShowingCurrentDeals = (eComment == GC.getAIDiploCommentType("CURRENT_DEALS"));
 }

diff --git a/CvGameCoreDLL/CvInfo_Civilization.cpp b/CvGameCoreDLL/CvInfo_Civilization.cpp
index 85d35305a..72eee7475 100644
--- a/CvGameCoreDLL/CvInfo_Civilization.cpp
+++ b/CvGameCoreDLL/CvInfo_Civilization.cpp
@@ -1811,10 +1811,15 @@ CvDiplomacyInfo::~CvDiplomacyInfo()
 	m_pResponses.clear();
 }
 
-const CvDiplomacyResponse& CvDiplomacyInfo::getResponse(int iNum) const
+CvDiplomacyResponse const& CvDiplomacyInfo::getResponse(int iNum) const
 {
-	return *(m_pResponses[iNum]);
+	return *m_pResponses[iNum];
 }
+// <advc.705>
+CvDiplomacyResponse& CvDiplomacyInfo::getResponse_(int iNum)
+{
+	return *m_pResponses[iNum];
+} // </advc.705>
 
 int CvDiplomacyInfo::getNumResponses() const
 {

diff --git a/CvGameCoreDLL/CvInfo_Civilization.h b/CvGameCoreDLL/CvInfo_Civilization.h
index 5ffb1ce38..dff90ec0e 100644
--- a/CvGameCoreDLL/CvInfo_Civilization.h
+++ b/CvGameCoreDLL/CvInfo_Civilization.h
@@ -450,7 +450,9 @@ public: // advc: All the const functions are exposed to Python
 	~CvDiplomacyInfo();
 	//void uninit(); // advc: Merged into destructor
 
-	const CvDiplomacyResponse& getResponse(int iNum) const;
+	CvDiplomacyResponse const& getResponse(int iNum) const;
+	// advc.705: (underscore avoids ambiguity in Python export)
+	CvDiplomacyResponse& getResponse_(int iNum);
 	int getNumResponses() const;
 
 	bool getCivilizationTypes(int i, int j) const;

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index 389fade2e..3f2d4b79d 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -13109,7 +13109,8 @@ void CvPlayer::updateEspionageHistory(int iTurn, int iBestEspionage)
 const CvPlayerRecord* CvPlayer::getPlayerRecord() const
 {
 	return CvEventReporter::getInstance().
-			// advc.make: CvEventReporter::getPlayerRecord added
+			/*	advc.make: CvEventReporter::getPlayerRecord added.
+				We're no longer a friend of CvEventReporter. */
 			/*m_kStatistics.*/getPlayerRecord(getID());
 }
 // K-Mod end
@@ -20899,7 +20900,7 @@ void CvPlayer::buildTradeTable(PlayerTypes eOtherPlayer, CLinkList<TradeData>& o
 						if(eCancelPlayer == eOtherPlayer && eCancelBonus == j)
 						{
 							bValid = true;
-							const_cast<CvPlayer*>(this)->m_cancelingExport.deleteNode(pNode);
+							m_cancelingExport.deleteNode(pNode);
 							break;
 						}
 					}

diff --git a/CvGameCoreDLL/CvPlayer.h b/CvGameCoreDLL/CvPlayer.h
index c03fce727..5fa54ae95 100644
--- a/CvGameCoreDLL/CvPlayer.h
+++ b/CvGameCoreDLL/CvPlayer.h
@@ -1436,7 +1436,8 @@ protected:  // <advc.210>
 	// <advc.106b>
 	std::vector<CvTalkingHeadMessage*> m_aMajorMsgs;
 	int m_iNewMessages; // </advc.106b>
-	CLinkList<std::pair<PlayerTypes,BonusTypes> > m_cancelingExport; // advc.074
+	// advc.074:
+	mutable CLinkList<std::pair<PlayerTypes,BonusTypes> > m_cancelingExport;
 
 	CivicTypes* m_paeCivics;

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index a03ae30f1..f57f9c5a0 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -6565,7 +6565,7 @@ DiploCommentTypes CvPlayerAI::AI_getGreeting(PlayerTypes ePlayer) const
 					{	// If we haven't bragged before, do it promptly.
 						if (eBragUnit != m_aeLastBrag[ePlayer])
 						{
-							const_cast<CvPlayerAI*>(this)->m_aeLastBrag[ePlayer] = eBragUnit;
+							m_aeLastBrag[ePlayer] = eBragUnit;
 							return GC.getAIDiploCommentType("UNIT_BRAG");
 						} // else: Use BtS code below
 					} // Never brag about this unit to ePlayer
@@ -6582,7 +6582,7 @@ DiploCommentTypes CvPlayerAI::AI_getGreeting(PlayerTypes ePlayer) const
 		0 == GC.getASyncRand().get(4 /* <advc.079> */ +
 		(m_aeLastWarn[ePlayer] != eWorstEnemy ? -2 : 2)))
 	{
-		const_cast<CvPlayerAI*>(this)->m_aeLastWarn[ePlayer] = eWorstEnemy;
+		m_aeLastWarn[ePlayer] = eWorstEnemy;
 		// </advc.079>
 		if (GET_PLAYER(ePlayer).AI_hasTradedWithTeam(eWorstEnemy) &&
 			!atWar(TEAMID(ePlayer), eWorstEnemy))

diff --git a/CvGameCoreDLL/CvPlayerAI.h b/CvGameCoreDLL/CvPlayerAI.h
index e81256b5d..43852a4b4 100644
--- a/CvGameCoreDLL/CvPlayerAI.h
+++ b/CvGameCoreDLL/CvPlayerAI.h
@@ -669,8 +669,8 @@ protected:
 	int* m_aiGoldTradedTo;
 	int* m_aiAttitudeExtra;
 	// <advc.079>
-	UnitTypes m_aeLastBrag[MAX_CIV_PLAYERS];
-	TeamTypes m_aeLastWarn[MAX_CIV_PLAYERS]; // </advc.079>
+	mutable UnitTypes m_aeLastBrag[MAX_CIV_PLAYERS];
+	mutable TeamTypes m_aeLastWarn[MAX_CIV_PLAYERS]; // </advc.079>
 	int* m_aiBonusValue;
 	int* m_aiBonusValueTrade; // advc.036
 	int* m_aiUnitClassWeights;

diff --git a/CvGameCoreDLL/CvPythonCaller.cpp b/CvGameCoreDLL/CvPythonCaller.cpp
index e80f5f468..3b71e57a2 100644
--- a/CvGameCoreDLL/CvPythonCaller.cpp
+++ b/CvGameCoreDLL/CvPythonCaller.cpp
@@ -876,7 +876,7 @@ bool CvPythonCaller::doGoody(CvPlot const& kPlot, CvUnit const* pUnit, PlayerTyp
 	CyPlot* pyPlot = new CyPlot(kPlot);
 	argsList.add(m_python.makePythonObject(pyPlot));
 	// Note: pUnit can be NULL, but pyUnit can still be initialized (and is expected by Python).
-	CyUnit* pyUnit = new CyUnit(const_cast<CvUnit*>(pUnit));
+	CyUnit* pyUnit = new CyUnit(*pUnit);
 	argsList.add(m_python.makePythonObject(pyUnit));
 	call("doGoody", argsList, lResult);
 	delete pyPlot;

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index 869cf7639..1eeadc73d 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -3811,9 +3811,7 @@ CvPlot* CvSelectionGroup::getPathEndTurnPlot() const
 bool CvSelectionGroup::generatePath( const CvPlot* pFromPlot, const CvPlot* pToPlot, int iFlags, bool bReuse, int* piPathTurns, int iMaxPath) const
 {
 	// K-Mod - if I can stop the UI from messing with this pathfinder, I might be able to reduce OOS bugs.
-	// (note, the const-cast is just to get around the bad code from the original developers)
-	FAssert(const_cast<CvSelectionGroup*>(this)->AI_isControlled());
-	// K-Mod end
+	FAssert(AI_isControlled());
 
 	PROFILE("CvSelectionGroup::generatePath()");

diff --git a/CvGameCoreDLL/CvStatistics.h b/CvGameCoreDLL/CvStatistics.h
index b9ff84db1..f858a5775 100644
--- a/CvGameCoreDLL/CvStatistics.h
+++ b/CvGameCoreDLL/CvStatistics.h
@@ -155,9 +155,6 @@ public:
 	void goldenAge(PlayerTypes ePlayer);
 
 	DllExport CvPlayerRecord* getPlayerRecord(int iIndex);
-	// <advc.make> Need a (double) const version of this
-	inline CvPlayerRecord const* getPlayerRecord(int iIndex) const
-	{ return const_cast<CvStatistics*>(this)->getPlayerRecord(iIndex); } // </advc.make>
 
 	const char* getMapName() const;
 	EraTypes getEra() const;

diff --git a/CvGameCoreDLL/CvTalkingHeadMessage.cpp b/CvGameCoreDLL/CvTalkingHeadMessage.cpp
index ed05a52c3..1e6d9e83b 100644
--- a/CvGameCoreDLL/CvTalkingHeadMessage.cpp
+++ b/CvGameCoreDLL/CvTalkingHeadMessage.cpp
@@ -84,8 +84,8 @@ const CvString& CvTalkingHeadMessage::getSound() const
 {
 	/*  advc.106b: A hack that relies on the EXE triggering the sound after calling
 		getSound */
-	const_cast<CvTalkingHeadMessage*>(this)->bSoundPlayed = true;
-	return (m_szSound);
+	bSoundPlayed = true;
+	return m_szSound;
 }
 
 void CvTalkingHeadMessage::setSound(LPCTSTR pszSound)

diff --git a/CvGameCoreDLL/CvTalkingHeadMessage.h b/CvGameCoreDLL/CvTalkingHeadMessage.h
index ae82fde3b..a79b5a247 100644
--- a/CvGameCoreDLL/CvTalkingHeadMessage.h
+++ b/CvGameCoreDLL/CvTalkingHeadMessage.h
@@ -60,7 +60,7 @@ protected:
 	PlayerTypes m_eFromPlayer;
 	ChatTargetTypes m_eTarget;
 	bool m_bShown;
-	bool bSoundPlayed; // advc.106b
+	mutable bool bSoundPlayed; // advc.106b
 };
 
 #endif

diff --git a/CvGameCoreDLL/CvTeam.cpp b/CvGameCoreDLL/CvTeam.cpp
index 53063f38e..d364b555a 100644
--- a/CvGameCoreDLL/CvTeam.cpp
+++ b/CvGameCoreDLL/CvTeam.cpp
@@ -2893,8 +2893,8 @@ bool CvTeam::isAtWarExternal(TeamTypes eIndex) const
 		(b/c being at war shouldn't prevent AI-to-human peace offers). */
 	if (m_iPeaceOfferStage == 2 && m_eOfferingPeace == eIndex)
 	{
-		const_cast<CvTeam*>(this)->m_iPeaceOfferStage = 0;
-		const_cast<CvTeam*>(this)->m_eOfferingPeace = NO_TEAM;
+		m_iPeaceOfferStage = 0;
+		m_eOfferingPeace = NO_TEAM;
 		return false;
 	}
 	return isAtWar(eIndex);

diff --git a/CvGameCoreDLL/CvTeam.h b/CvGameCoreDLL/CvTeam.h
index 76ac119e1..a48e1a5f5 100644
--- a/CvGameCoreDLL/CvTeam.h
+++ b/CvGameCoreDLL/CvTeam.h
@@ -583,8 +583,8 @@ protected:
 	std::vector<int>* m_pavProjectArtTypes; // a vector for each type of project
 	std::vector<BonusTypes> m_aeRevealedBonuses;
 	// <advc.134a>
-	TeamTypes m_eOfferingPeace;
-	int m_iPeaceOfferStage;
+	mutable TeamTypes m_eOfferingPeace;
+	mutable int m_iPeaceOfferStage;
 	// </advc.134a>
 
 	// <dlph.26>

diff --git a/CvGameCoreDLL/PlotRadiusIterator.h b/CvGameCoreDLL/PlotRadiusIterator.h
index 39bff85d5..eff07465e 100644
--- a/CvGameCoreDLL/PlotRadiusIterator.h
+++ b/CvGameCoreDLL/PlotRadiusIterator.h
@@ -28,10 +28,12 @@ class SquareIterator
 public:
 	SquareIterator(CvPlot const& kCenter, int iRadius, bool bIncludeCenter = true)
 	{
-		/*	I've written a const and non-const version (as derived classes that
+		/*	I had written a const and non-const version (as derived classes that
 			set a "PlotType" template parameter), but CityPlotIterator and
 			AgentIterator don't have const versions either, and const-correctness
-			would make it harder to add more iterator classes in the future. */
+			would make it harder to add more iterator classes in the future.
+			So I've removed those derived classes.
+			Const-casting CvPlot should generally be safe. */
 		m_pNext = const_cast<CvPlot*>(&kCenter);
 		init(iRadius, bIncludeCenter);
 	}

diff --git a/CvGameCoreDLL/RiseFall.cpp b/CvGameCoreDLL/RiseFall.cpp
index ecc33fe85..99d43d7cf 100644
--- a/CvGameCoreDLL/RiseFall.cpp
+++ b/CvGameCoreDLL/RiseFall.cpp
@@ -1474,8 +1474,7 @@ CvDiplomacyResponse* RiseFall::findThanks() {
 	if(thanks == NULL)
 		return NULL;
 	for(int i = 0; i < thanks->getNumResponses(); i++) {
-		CvDiplomacyResponse const* r = &thanks->getResponse(i);
-		return const_cast<CvDiplomacyResponse*>(r);
+		return &thanks->getResponse_(i);
 	}
 	return NULL;
 }

[PATCH COVERAGE]
patchFiles=21 embeddedFiles=21 omittedFiles=0 embeddedPatchBytes=14386

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1181
# Commit: 6453e513f64ccc3927897657d0b474cddbcd44b6
# History segment: K-Mod history
# Parent used for diff: 46c71d84e0f914d8e8159c88d884fb2183d1f391
# Title: Increased chance of natural religion spread.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	Assets/xml/GlobalDefinesAlt.xml
12	1	CvGameCoreDLL/CvCity.cpp

[PATCHES]

diff --git a/Assets/xml/GlobalDefinesAlt.xml b/Assets/xml/GlobalDefinesAlt.xml
index 05bdfbbef..d8f35dcdc 100644
--- a/Assets/xml/GlobalDefinesAlt.xml
+++ b/Assets/xml/GlobalDefinesAlt.xml
@@ -119,7 +119,7 @@
 	</Define>
 	<Define>
 		<DefineName>RELIGION_SPREAD_DISTANCE_FACTOR</DefineName>
-		<iDefineIntVal>11</iDefineIntVal>
+		<iDefineIntVal>9</iDefineIntVal>
 	</Define>
 	<Define>
 		<DefineName>RELIGION_INFLUENCE_POPULATION_WEIGHT</DefineName>

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 7fd8ff202..b8ab9d1e8 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -13361,7 +13361,7 @@ void CvCity::doReligion()
 							iDivisor /= 100;
 
 							// now iDivisor is in the range [1, 1+iDistanceFactor] * iDivisorBase
-							// this is approximately in the range [4, 60], depending on what the xml value are. (the value currently being tested and tuned.)
+							// this is approximately in the range [5, 50], depending on what the xml value are. (the value currently being tested and tuned.)
 							iSpread /= iDivisor;
 							// K-Mod end
 
@@ -13378,6 +13378,17 @@ void CvCity::doReligion()
 		iRandThreshold *= 100;
 		iRandThreshold /= GC.getGameSpeedInfo(GC.getGameINLINE().getGameSpeedType()).getVictoryDelayPercent();
 
+		// K-Mod. Give a bonus for the first few cities.
+		/* {
+			int iReligionCities = GC.getGameINLINE().countReligionLevels(eLoopReligion);
+			if (iReligionCities < 3)
+			{
+				iRandThreshold *= 2 + iReligionCities;
+				iRandThreshold /= 1 + iReligionCities;
+			}
+		} */
+		//
+
 		if (GC.getGameINLINE().getSorenRandNum(GC.getDefineINT("RELIGION_SPREAD_RAND"), "Religion Spread") < iRandThreshold)
 		{
 			setHasReligion(eLoopReligion, true, true, true);

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1744

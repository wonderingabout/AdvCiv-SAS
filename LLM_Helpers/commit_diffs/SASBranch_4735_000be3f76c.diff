# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4735
# Commit: 000be3f76c4c4ebac3f45023b081303d7aa3787e
# History segment: AdvCiv-SAS branch history
# Parent used for diff: 01d6b62db5292bd470bb3b9c358668c171bf67d9
# Title: further simplify and generalize the shifting formula our expected output (expected_output_leaders_data_sample.py) and shifting assertions are still valid (ad...
# Full commit message/metadata: see _1_AdvCiv-SAS/git_logs/git_log_anonymized_email_003_AdvCiv-SAS.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
11	8	Assets/Python/Contrib/Sevopedia/ai_utils_shared_with_civ4.py

[PATCHES]

diff --git a/Assets/Python/Contrib/Sevopedia/ai_utils_shared_with_civ4.py b/Assets/Python/Contrib/Sevopedia/ai_utils_shared_with_civ4.py
index 2d5a87d83..3b56767a7 100644
--- a/Assets/Python/Contrib/Sevopedia/ai_utils_shared_with_civ4.py
+++ b/Assets/Python/Contrib/Sevopedia/ai_utils_shared_with_civ4.py
@@ -39,15 +39,17 @@ def get_shifted_values(min_val, value, max_val):
 	#	value = 24		->	shifted_value = 24 - 19 = 5
 	#	max_val = 53	->	shifted_max = 53 - 19 = 34
 	#
+	# example 8 (sanity check):
+	# 	min_val = 0		->	shifted_min = 0
+	#	value = 24		->	shifted_value = 24
+	#	max_val = 53	->	shifted_max = 53
+	#
 	# -->
-	if min_val != 0:
-		shifted_min = 0
-		shifted_value = value - min_val
-		shifted_max = max_val - min_val
-	else:
-		shifted_min = min_val
-		shifted_value = value
-		shifted_max = max_val
+
+	# <!-- custom: if min_val == 0, we return same values (min_val, value, max_val) as received -->
+	shifted_min = 0
+	shifted_value = value - min_val
+	shifted_max = max_val - min_val
 
 	return shifted_min, shifted_value, shifted_max
 
@@ -61,6 +63,7 @@ def test_expected_shifting_pre_normalize_to_100():
 		(-19, -11, 152, 0, 8, 171),	# 5
 		(-72, -55, 37, 0, 17, 109),	# (1.)6
 		(19, 24, 53, 0, 5, 34),		# 7
+		(0, 24, 53, 0, 24, 53),		# 8
 	)
 
 	for min_val, value, max_val, expected_shifted_min, expected_shifted_value, expected_shifted_max in vals_to_test:

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1383

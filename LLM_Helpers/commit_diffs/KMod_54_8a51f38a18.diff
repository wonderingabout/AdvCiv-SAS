# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 54
# Commit: 8a51f38a18e3dc357723b8a4b11a68359ca03503
# History segment: K-Mod history
# Parent used for diff: 31ffe5a061d41de73223c5323666484679216e4e
# Title: Decreased message stagger time from 3 seconds to 2 seconds. (experimental) Added a new decision-check for allowing the AI to counter-attack with collateral d...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	2	Assets/xml/GlobalDefinesAlt.xml
5	1	CvGameCoreDLL/CvCity.cpp
15	4	CvGameCoreDLL/CvCityAI.cpp
2	2	CvGameCoreDLL/CvPlayerAI.cpp
9	0	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/Assets/xml/GlobalDefinesAlt.xml b/Assets/xml/GlobalDefinesAlt.xml
index 5db1d0520..a886e0517 100644
--- a/Assets/xml/GlobalDefinesAlt.xml
+++ b/Assets/xml/GlobalDefinesAlt.xml
@@ -5,12 +5,14 @@
 <!-- -->
 <!-- Global Defines -->
 <Civ4Defines xmlns="x-schema:CIV4GlobalDefinesSchema.xml">
-<!-- Redefines -->
 	<Define>
 		<DefineName>BASE_REVOLT_OCCUPATION_TURNS</DefineName>
 		<iDefineIntVal>3</iDefineIntVal> <!-- increased from 2, because of changes that I've made to its useage -->
 	</Define>
-<!-- end of Redefines -->
+	<Define>
+		<DefineName>EVENT_MESSAGE_STAGGER_TIME</DefineName>
+		<iDefineIntVal>2</iDefineIntVal> <!-- decreased from 3 -->
+	</Define>
 	<Define>
 		<DefineName>FROZEN_TERRAIN</DefineName>
 		<DefineTextVal>TERRAIN_SNOW</DefineTextVal>

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 72243a90c..d35a8a3c2 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -5012,7 +5012,11 @@ int CvCity::culturePressureFactor() const
 			}
 		}
 	}
-	return iAnswer / iDivisor;
+	// dull the effects in the late-game.
+	iAnswer *= GC.getNumEraInfos();
+	iAnswer /= GET_PLAYER(getOwnerINLINE()).getCurrentEra() + GC.getNumEraInfos();
+
+	return std::max(100, iAnswer / iDivisor);
 }
 
 int CvCity::getNumBuilding(BuildingTypes eIndex) const

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index f7ea5eff9..3509bd417 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -2494,9 +2494,13 @@ void CvCityAI::AI_chooseProduction()
 	int iNumSpies = (kPlayer.AI_totalAreaUnitAIs(pArea, UNITAI_SPY));
 	int iNeededSpies = iNumCitiesInArea / 3;
 	iNeededSpies += isCapital() ? 1 : 0;
+	// K-Mod
+	if (kPlayer.AI_isDoStrategy(AI_STRATEGY_BIG_ESPIONAGE))
+		iNeededSpies *= 2;
 	if (iNumSpies < iNeededSpies)
 	{
-		if (AI_chooseUnit(UNITAI_SPY, 5 + 50 / (1 + iNumSpies)))
+		//if (AI_chooseUnit(UNITAI_SPY, 5 + 50 / (1 + iNumSpies)))
+		if (AI_chooseUnit(UNITAI_SPY, 50*iNeededSpies / (3*iNumSpies+iNeededSpies)))
 		{
 			return;
 		}
@@ -7731,6 +7735,7 @@ void CvCityAI::AI_doDraft(bool bForce)
 				if( bWait && GET_PLAYER(getOwnerINLINE()).AI_isDoStrategy(AI_STRATEGY_TURTLE) )
 				{
 					// Full out defensive
+					/* original bts code
 					if( bDanger || (getPopulation() >= std::max(5, getHighestPopulation() - 1)) )
 					{
 						bWait = false;
@@ -7738,7 +7743,11 @@ void CvCityAI::AI_doDraft(bool bForce)
 					else if( AI_countWorkedPoorTiles() >= 1 )
 					{
 						bWait = false;
-					}
+					}*/
+
+					// K-Mod: full out defensive indeed. We've already checked for happiness, and we're desperate for units.
+					// So lets just do it.
+					bWait = false;
 				}
 				
 				if( bWait && bDanger )
@@ -7756,9 +7765,11 @@ void CvCityAI::AI_doDraft(bool bForce)
 				if( bWait )
 				{
 					// Non-critical, only burn population if population is not worth much
-					if ((getConscriptAngerTimer() == 0) && (AI_countWorkedPoorTiles() > 1))
+					//if ((getConscriptAngerTimer() == 0) && (AI_countWorkedPoorTiles() > 1))
+					if ((getConscriptAngerTimer() == 0) && (AI_countWorkedPoorTiles() > 0)) // K-Mod
 					{
-						if( (getPopulation() >= std::max(5, getHighestPopulation() - 1)) )
+						//if( (getPopulation() >= std::max(5, getHighestPopulation() - 1)) )
+						// We're working poor tiles. What more do you want?
 						{
 							bWait = false;
 						}

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index f61b83df1..712ad670b 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -21067,9 +21067,9 @@ int CvPlayerAI::AI_getMinFoundValue() const
 
 	// The marginal cost increase is roughly equal to double the cost of a current city...
 	// But we're really going to have to fudge it anyway, because the city value is in arbitrary units
-	// lets just say each gold per turn is worth roughly 40 'value points'.
+	// lets just say each gold per turn is worth roughly 60 'value points'.
 	// In the future, this could be AI flavour based.
-	iValue += iNumCitiesPercent * getNumCities() * 40 / 100;
+	iValue += iNumCitiesPercent * getNumCities() * 60 / 100;
 	// K-Mod end
 	
 	return iValue;

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 56fe37573..38bdf69b4 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -2940,6 +2940,15 @@ void CvUnitAI::AI_attackCityMove()
 			{
 				return;
 			}
+			// K-Mod; sometimes you just need to blast them with collateral damage before they can do it to you!
+			// ... and please don't let them wipe out a massive stack just because we were too scared to fight back
+
+			// look. I don't want to spend too much time messing around with this. So let me just try an experimental heuristic... 
+			if (getGroup()->getNumUnits() > 1 &&
+				AI_anyAttack(1, 60/(getGroup()->getNumUnits()+1), 3+getGroup()->getNumUnits(), false))
+			{
+				return;
+			}
 		}
 	}

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=4935

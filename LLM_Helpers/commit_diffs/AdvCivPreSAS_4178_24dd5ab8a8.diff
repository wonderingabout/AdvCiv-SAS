# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4178
# Commit: 24dd5ab8a83633dd8aae47e246b27112538218c2
# History segment: pre-SAS AdvCiv history
# Parent used for diff: b9a4d92120119f02e19c290813ef548918b37d99
# Title: BUG option for lower screen update rate in between turns
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	0	Assets/Config/BUG Main Interface.xml
7	6	Assets/Python/BUG/Tabs/BugGeneralOptionsTab.py
12	0	Assets/XML/Text/BUG Main Interface Options.xml
6	0	Assets/XML/Text/BUG Options Screen.xml
110	84	CvGameCoreDLL/CvGame.cpp
1	0	CvGameCoreDLL/CvGame.h

[PATCHES]

diff --git a/Assets/Config/BUG Main Interface.xml b/Assets/Config/BUG Main Interface.xml
index b67e75cc7..cd9d0119c 100644
--- a/Assets/Config/BUG Main Interface.xml	
+++ b/Assets/Config/BUG Main Interface.xml	
@@ -120,6 +120,12 @@
 					type="boolean" default="False" 
 					get="isShowOnlyOnceProgress" dirtyBit="GameData"/>
 
+			<!-- advc.256: New option. NB: Changes to the values will require
+				 corresponding changes to CvGame::update. -->
+			<list   id="GraphicsUpdateRate" key="Graphics Update Rate" 
+					type="int" default="0" listType="int" 
+					values="12, 6, 4, 3, 2, 1, 0"/>
+
 			<!-- advc.092: New option -->
 			<option id="EnlargeHUD" key="Enlarge HUD" 
 					type="boolean" default="True"

diff --git a/Assets/Python/BUG/Tabs/BugGeneralOptionsTab.py b/Assets/Python/BUG/Tabs/BugGeneralOptionsTab.py
index 654427acf..a593a0d46 100644
--- a/Assets/Python/BUG/Tabs/BugGeneralOptionsTab.py
+++ b/Assets/Python/BUG/Tabs/BugGeneralOptionsTab.py
@@ -22,14 +22,15 @@ class BugGeneralOptionsTab(BugOptionsTab.BugOptionsTab):
 		left, center, right = self.addThreeColumnLayout(screen, column, "Top", True)
 		
 		self.createGreatPersonGeneralPanel(screen, left)
-		self.addSpacer(screen, left, "GeneralL1")
+		#self.addSpacer(screen, left, "GeneralL1") # advc.256: No more room for this
 		self.createTechSplashPanel(screen, left)
 		#self.createLeaderheadPanel(screen, left)
-		# <advc.092>
-		self.addSpacer(screen, left, "AboveEnlargeHUD1")
-		self.addSpacer(screen, left, "AboveEnlargeHUD2")
-		self.addCheckbox(screen, left, "MainInterface__EnlargeHUD")
-		# </advc.092>
+		# <advc.256>
+		self.addSpacer(screen, left, "AboveOverall")
+		self.addLabel(screen, left, "Overall", "Overall:")
+		self.addIntDropdown(screen, left, left, "MainInterface__GraphicsUpdateRate", True)
+		# </advc.256>
+		self.addCheckbox(screen, left, "MainInterface__EnlargeHUD") # advc.092
 		#self.createAutoSavePanel(screen, center)
 		self.createInfoPanePanel(screen, center)
 		# advc.004k (note): Should remove this spacer if the hide-command options are re-enabled

diff --git a/Assets/XML/Text/BUG Main Interface Options.xml b/Assets/XML/Text/BUG Main Interface Options.xml
index 7b83d6273..4cf858eec 100644
--- a/Assets/XML/Text/BUG Main Interface Options.xml	
+++ b/Assets/XML/Text/BUG Main Interface Options.xml	
@@ -229,6 +229,18 @@
 		<German>Die Fortschrittsbalken f�r [COLOR_HIGHLIGHT_TEXT]Gro�e Pers�nlichkeiten[COLOR_REVERT] and [COLOR_HIGHLIGHT_TEXT]Gro�e Gener�le[COLOR_REVERT] (falls �berhaupt aktiviert) erscheinen erst, wenn entsprechender Fortschritt besteht.</German>
 	</TEXT>
 
+	<!-- advc.256: New option -->
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__GRAPHICSUPDATERATE_TEXT</Tag>
+		<English>Screen Update Rate</English>
+		<German>Grafik-Update-Rate</German>
+	</TEXT>
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__GRAPHICSUPDATERATE_HOVER</Tag>
+		<English>The number of main screen updates per 3 seconds. Applies only during AI turns and never in multiplayer games. Recommended: 12, meaning 4 updates per second as in BtS, the fastest possible rate. Lower rates will speed up AI turns - probably by no more than 20% - at the expense of halting visuals.</English>
+		<German>Anzahl der Aktualisierungen des Hauptbildschirms pro 3 Sekunden. Betrifft nur KI-Z�ge und nur im Einzelspieler-Modus. Empfohlen: 12, also 4 Updates pro Sekunde wie In BtS, die h�chstm�gliche Anzahl. Niedrigere Werte verk�rzen die Dauer der KI-Z�ge etwas - vielleicht um bis zu 20% -, lassen das Bild aber stocken.</German>
+	</TEXT>
+
 	<!-- advc.092: New option -->
 	<TEXT>
 		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__ENLARGEHUD_TEXT</Tag>

diff --git a/Assets/XML/Text/BUG Options Screen.xml b/Assets/XML/Text/BUG Options Screen.xml
index a9b726e84..e9c187ffe 100644
--- a/Assets/XML/Text/BUG Options Screen.xml	
+++ b/Assets/XML/Text/BUG Options Screen.xml	
@@ -71,6 +71,12 @@
 		<Italian>Generale</Italian>
 		<Spanish>General</Spanish>
 	</TEXT>
+	<!-- advc.256 (Not a great label name ...) -->
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPTLABEL_OVERALL</Tag>
+		<English>Overall:</English>
+		<German>Insgesamt:</German>
+	</TEXT>
 	<TEXT>
 		<Tag>TXT_KEY_BUG_OPTLABEL_PROGRESSBARS</Tag>
 		<English>Progress Bars:</English>

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index e2c74867d..7f4345d7c 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -2842,93 +2842,119 @@ void CvGame::update()
 {
 	startProfilingDLL(false);
 	PROFILE_BEGIN("CvGame::update");
+	// <advc.256> Based on C2C, originally mostly from Rise of Mankind, I think.
+	CvPlayer const& kActivePlayer = GET_PLAYER(getActivePlayer());
+	int iSuccessiveUpdates = 1;
+	if (getTurnSlice() > 0) // Wait for BUG initialization
+	{
+		// Needs to match the value range set in XML
+		int const iDefaultGraphicsUpdateRate = 12;
+		int iGraphicsUpdateRate = BUGOption::getValue("MainInterface__GraphicsUpdateRate",
+				iDefaultGraphicsUpdateRate);
+		if (iGraphicsUpdateRate <= 0)
+			iSuccessiveUpdates = 128; // Not quite infinite, but a lot in a row.
+		else iSuccessiveUpdates = iDefaultGraphicsUpdateRate / iGraphicsUpdateRate;
+	}
+	if (isGameMultiPlayer() || !kActivePlayer.isAlive())
+		iSuccessiveUpdates = 1;
+	if (kActivePlayer.isTurnActive() &&
+		(!kActivePlayer.isAutoMoves() ||
+		!kActivePlayer.isOption(PLAYEROPTION_QUICK_MOVES)))
+	{
+		iSuccessiveUpdates = 1;
+	}
+	if (gDLL->GetWorldBuilderMode() && !isInAdvancedStart()) // As in BtS
+		iSuccessiveUpdates = 0;
+	for (int i = 0; i < iSuccessiveUpdates; i++)
+		updateUnprofiled(); // </advc.256>
+	PROFILE_END();
+	stopProfilingDLL(false);
+}
 
-	if (!gDLL->GetWorldBuilderMode() || isInAdvancedStart())
-	{
-		sendPlayerOptions();
+// advc.256: Cut from update
+void CvGame::updateUnprofiled()
+{
+	sendPlayerOptions();
 
-		// sample generic event
-		CyArgsList pyArgs;
-		pyArgs.add(getTurnSlice());
-		/*	advc.210: To prevent BUG alerts from being checked at the start of a
-			game turn. I've tried doing that through BugEventManager.py, but soon
-			gave up. Tagging advc.706 b/c it's especially important to supress
-			the update when R&F is enabled. */
-		if (!isInBetweenTurns())
-		{
-			CvEventReporter::getInstance().genericEvent("gameUpdate", pyArgs.makeFunctionArgs());
-			// <advc.003r>
-			for (int i = 0; i < NUM_UPDATE_TIMER_TYPES; i++)
-				handleUpdateTimer((UpdateTimerTypes)i); // </advc.003r>
-		}
-		if (getTurnSlice() == 0) // advc (note): Implies 0 elapsed game turns
-		{	// <advc.700> Delay initial auto-save until RiseFall is initialized
-			if (!isOption(GAMEOPTION_RISE_FALL) && // </advc.700>
-				m_iTurnLoadedFromSave != m_iElapsedGameTurns && // advc.044
-				// advc: Necessary now that re-gen resets turn slice
-				m_iMapRegens <= 0)
-			{
-				autoSave(true); // advc.106l
-			}
-		}
-		/*	<advc.004m> Slice 0 seems to be the earliest time when plot indicators
-			can be enabled w/o crashing. But it appears that, for some players,
-			the indicators do not actually appear then - race condition? Slice 1
-			doesn't seem to help either. Try 2? (It's a continuous count.) */
-		if (getTurnSlice() == 2 &&
-			// Leave it up to the savegame when loading an initial autosave
-			m_iTurnLoadedFromSave != m_iElapsedGameTurns)
-		{
-			if (BUGOption::isEnabled("MainInterface__StartWithResourceIcons", true))
-				gDLL->getEngineIFace()->setResourceLayer(true);
-		} // </advc.004m>
-		if (getNumGameTurnActive() == 0)
-		{
-			if (!isPbem() || !getPbemTurnSent())
-				doTurn();
-		}
-		updateScore();
-		updateWar();
-		updateMoves();
-		updateTimers();
-		updateTurnTimer();
-		AI().AI_updateAssignWork();
-		testAlive();
-		if (getAIAutoPlay() == 0 && !gDLL->GetAutorun() &&
-			getGameState() != GAMESTATE_EXTENDED)
-		{
-			if (countHumanPlayersAlive() == 0 &&
-				!isOption(GAMEOPTION_RISE_FALL)) // advc.707
-			{
-				setGameState(GAMESTATE_OVER);
-			}
-		}
-		changeTurnSlice(1);
-		/*	<advc.104l> Make sure that UI (specifically willing-to-talk indicator)
-			doesn't become outdated throughout a turn. Or at least not for long.
-			advc.085: Invalidating on every turn slice might slightly hurt
-			responsiveness when hovering over the scoreboard. (Not sure.) */
-		if (getTurnSlice() % 4 == 0)
-			AI().uwai().invalidateUICache(); // </advc.104l>
-		/*	<advc.004n> (Disassembly suggests that the EXE caches this info,
-			so this check is fast.) */
-		bool bCityScreenUp = gDLL->UI().isCityScreenUp();
-		if (bCityScreenUp != m_bCityScreenUp)
-		{
-			m_bCityScreenUp = bCityScreenUp;
-			onCityScreenChange();
-		} // </advc004n>
-		if (getActivePlayer() != NO_PLAYER && GET_PLAYER(getActivePlayer()).getAdvancedStartPoints() >= 0 &&
-			!gDLL->UI().isInAdvancedStart())
-		{
-			gDLL->UI().setInAdvancedStart(true);
-			gDLL->UI().setWorldBuilder(true);
-		} // <advc.705>
-		if (isOption(GAMEOPTION_RISE_FALL))
-			m_pRiseFall->restoreDiploText(); // </advc.705>
+	// sample generic event
+	CyArgsList pyArgs;
+	pyArgs.add(getTurnSlice());
+	/*	advc.210: To prevent BUG alerts from being checked at the start of a
+		game turn. I've tried doing that through BugEventManager.py, but soon
+		gave up. Tagging advc.706 b/c it's especially important to supress
+		the update when R&F is enabled. */
+	if (!isInBetweenTurns())
+	{
+		CvEventReporter::getInstance().genericEvent("gameUpdate", pyArgs.makeFunctionArgs());
+		// <advc.003r>
+		for (int i = 0; i < NUM_UPDATE_TIMER_TYPES; i++)
+			handleUpdateTimer((UpdateTimerTypes)i); // </advc.003r>
+	}
+	if (getTurnSlice() == 0) // advc (note): Implies 0 elapsed game turns
+	{	// <advc.700> Delay initial auto-save until RiseFall is initialized
+		if (!isOption(GAMEOPTION_RISE_FALL) && // </advc.700>
+			m_iTurnLoadedFromSave != m_iElapsedGameTurns && // advc.044
+			// advc: Necessary now that re-gen resets turn slice
+			m_iMapRegens <= 0)
+		{
+			autoSave(true); // advc.106l
+		}
+	}
+	/*	<advc.004m> Slice 0 seems to be the earliest time when plot indicators
+		can be enabled w/o crashing. But it appears that, for some players,
+		the indicators do not actually appear then - race condition? Slice 1
+		doesn't seem to help either. Try 2? (It's a continuous count.) */
+	if (getTurnSlice() == 2 &&
+		// Leave it up to the savegame when loading an initial autosave
+		m_iTurnLoadedFromSave != m_iElapsedGameTurns)
+	{
+		if (BUGOption::isEnabled("MainInterface__StartWithResourceIcons", true))
+			gDLL->getEngineIFace()->setResourceLayer(true);
+	} // </advc.004m>
+	if (getNumGameTurnActive() == 0)
+	{
+		if (!isPbem() || !getPbemTurnSent())
+			doTurn();
 	}
-	PROFILE_END();
-	stopProfilingDLL(false);
+	updateScore();
+	updateWar();
+	updateMoves();
+	updateTimers();
+	updateTurnTimer();
+	AI().AI_updateAssignWork();
+	testAlive();
+	if (getAIAutoPlay() == 0 && !gDLL->GetAutorun() &&
+		getGameState() != GAMESTATE_EXTENDED)
+	{
+		if (countHumanPlayersAlive() == 0 &&
+			!isOption(GAMEOPTION_RISE_FALL)) // advc.707
+		{
+			setGameState(GAMESTATE_OVER);
+		}
+	}
+	changeTurnSlice(1);
+	/*	<advc.104l> Make sure that UI (specifically willing-to-talk indicator)
+		doesn't become outdated throughout a turn. Or at least not for long.
+		advc.085: Invalidating on every turn slice might slightly hurt
+		responsiveness when hovering over the scoreboard. (Not sure.) */
+	if (getTurnSlice() % 4 == 0)
+		AI().uwai().invalidateUICache(); // </advc.104l>
+	/*	<advc.004n> (Disassembly suggests that the EXE caches this info,
+		so this check is fast.) */
+	bool bCityScreenUp = gDLL->UI().isCityScreenUp();
+	if (bCityScreenUp != m_bCityScreenUp)
+	{
+		m_bCityScreenUp = bCityScreenUp;
+		onCityScreenChange();
+	} // </advc004n>
+	if (getActivePlayer() != NO_PLAYER && GET_PLAYER(getActivePlayer()).getAdvancedStartPoints() >= 0 &&
+		!gDLL->UI().isInAdvancedStart())
+	{
+		gDLL->UI().setInAdvancedStart(true);
+		gDLL->UI().setWorldBuilder(true);
+	} // <advc.705>
+	if (isOption(GAMEOPTION_RISE_FALL))
+		m_pRiseFall->restoreDiploText(); // </advc.705>
 }

diff --git a/CvGameCoreDLL/CvGame.h b/CvGameCoreDLL/CvGame.h
index 9169de695..4199cb7f9 100644
--- a/CvGameCoreDLL/CvGame.h
+++ b/CvGameCoreDLL/CvGame.h
@@ -1200,6 +1200,7 @@ protected:
 
 	void verifyCivics();
 
+	void updateUnprofiled(); // advc.256
 	void updateWar();
 	void updateMoves();
 	void updateTimers();

[PATCH COVERAGE]
patchFiles=6 embeddedFiles=6 omittedFiles=0 embeddedPatchBytes=12285

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3003
# Commit: 10f402eaedb6382996cbaf22d4c94cd679ca2e53
# History segment: pre-SAS AdvCiv history
# Parent used for diff: e0a2a461a588d6e4c2f34c30318e5f6faca159c6
# Title: Add "Great People Rate Breakdown" from BULL
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	4	Assets/Config/BULL Misc Hovers.xml
1	0	Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
3	2	Assets/XML/Text/BULL Misc Hovers Options.xml
50	1	CvGameCoreDLL/CvGameTextMgr.cpp

[PATCHES]

diff --git a/Assets/Config/BULL Misc Hovers.xml b/Assets/Config/BULL Misc Hovers.xml
index c00f4d1f1..dadd90a66 100644
--- a/Assets/Config/BULL Misc Hovers.xml	
+++ b/Assets/Config/BULL Misc Hovers.xml	
@@ -68,11 +68,12 @@
 		
 		
 		<section id="City Screen">
-			<!-- advc.065: No longer optional -->
+			<!-- advc.065: No longer optional (default was False) -->
 			<!--option id="BaseCommerce" key="Base Commerce" 
-					type="boolean" default="False"/-->
-			<option id="GreatPeopleRateBreakdown" key="Great People Rate Breakdown" 
-					type="boolean" default="False"/>
+					type="boolean" default="True"/-->
+			<!-- advc.078: No longer optional (default was False) -->
+			<!--option id="GreatPeopleRateBreakdown" key="Great People Rate Breakdown" 
+					type="boolean" default="True"/-->
 					
 			<option id="BuildingSavedMaintenance" key="Building Saved Maintenance" 
 					type="boolean" default="False"/>

diff --git a/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py b/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
index 7acf1a4c5..66aa363e6 100644
--- a/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
+++ b/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
@@ -80,6 +80,7 @@ class BugCityScreenOptionsTab(BugOptionsTab.BugOptionsTab):
 		self.addLabel(screen, panel, "GreatPersonBar", "Great Person Bar:")
 		self.addCheckbox(screen, panel, "CityScreen__GreatPersonTurns")
 		self.addCheckbox(screen, panel, "CityScreen__GreatPersonInfo")
+		# advc.078 (note): Merged, but always enabled.
 		#self.addCheckbox(screen, panel, "MiscHover__GreatPeopleRateBreakdown")
 		
 	def createProductionQueuePanel(self, screen, panel):

diff --git a/Assets/XML/Text/BULL Misc Hovers Options.xml b/Assets/XML/Text/BULL Misc Hovers Options.xml
index 868d547f3..8539c35d2 100644
--- a/Assets/XML/Text/BULL Misc Hovers Options.xml	
+++ b/Assets/XML/Text/BULL Misc Hovers Options.xml	
@@ -270,7 +270,8 @@
 		<Italian>Quando � selezionato, nel testo a comparsa di ogni tipo di Commercio viene mostrato (quando necessario) il relativo subtotale (il totale prima dei modificatori).</Italian>
 		<Spanish>Muestra el subtotal para cada tipo de comercio en su texto flotante.</Spanish>
 	</TEXT>
-	<TEXT>
+	<!-- advc.078: Not optional -->
+	<!--TEXT>
 		<Tag>TXT_KEY_BUG_OPT_MISCHOVER__GREATPEOPLERATEBREAKDOWN_TEXT</Tag>
 		<English>Rate Breakdown</English>
 		<German>Aufschl�sselung der Rate</German>
@@ -283,7 +284,7 @@
 		<German>Zeigt im Tooltip die [ICON_GREATPEOPLE]/Runde f�r jede einzelne Art Gro�e Pers�nlichkeit an</German>
 		<Italian>Quando � selezionato, nel testo a comparsa di ogni tipo di Grande Personaggio viene mostrato il tasso individuale di [ICON_GREATPEOPLE] per turno.</Italian>
 		<Spanish>Permite consultar informaci�n individual de cada tipo de [ICON_GREATPEOPLE] en el texto flotante.</Spanish>
-	</TEXT>
+	</TEXT-->
 	
 	<!-- advc.004: German shortened -->
 	<TEXT>

diff --git a/CvGameCoreDLL/CvGameTextMgr.cpp b/CvGameCoreDLL/CvGameTextMgr.cpp
index 0df345ddb..c0465af51 100644
--- a/CvGameCoreDLL/CvGameTextMgr.cpp
+++ b/CvGameCoreDLL/CvGameTextMgr.cpp
@@ -17074,7 +17074,56 @@ void CvGameTextMgr::parseGreatPeopleHelp(CvWStringBuffer &szBuffer, CvCity const
 	if (iGPRate == 0 && !bBuildingAdditionalGreatPeople)
 	// BUG - Building Additional Great People - end
 		return;
-
+	// BULL - Great People Rate Breakdown - start (advc.078)
+	//if (BUGOption::isEnabled("MiscHover__GreatPeopleRateBreakdown", true))
+	{
+		bool bFirst = true;
+		{
+			int iRate = 0;
+			FOR_EACH_ENUM(Specialist)
+			{
+				iRate += (kCity.getSpecialistCount(eLoopSpecialist) +
+						kCity.getFreeSpecialistCount(eLoopSpecialist)) *
+						GC.getSpecialistInfo(eLoopSpecialist).getGreatPeopleRateChange();
+			}
+			if (iRate > 0)
+			{
+				if (bFirst)
+				{
+					szBuffer.append(SEPARATOR);
+					bFirst = false;
+				}
+				szBuffer.append(NEWLINE);
+				szBuffer.append(gDLL->getText("TXT_KEY_MISC_HELP_SPECIALIST_COMMERCE",
+						iRate, gDLL->getSymbolID(GREAT_PEOPLE_CHAR),
+						L"TXT_KEY_CONCEPT_SPECIALISTS"));
+			}
+		}
+		{
+			int iRate = 0;
+			FOR_EACH_ENUM2(Building, eBuilding)
+			{
+				if (kCity.getNumBuilding(eBuilding) > 0 &&
+					// advc (future-proofing):
+					!GET_TEAM(kCity.getTeam()).isObsoleteBuilding(eBuilding))
+				{
+					iRate += kCity.getNumBuilding(eBuilding) *
+							GC.getBuildingInfo(eBuilding).getGreatPeopleRateChange();
+				}
+			}
+			if (iRate > 0)
+			{
+				if (bFirst)
+				{
+					szBuffer.append(SEPARATOR);
+					bFirst = false;
+				}
+				szBuffer.append(NEWLINE);
+				szBuffer.append(gDLL->getText("TXT_KEY_MISC_HELP_BUILDING_COMMERCE",
+						iRate, gDLL->getSymbolID(GREAT_PEOPLE_CHAR)));
+			}
+		}
+	} // BULL - Great People Rate Breakdown - end
 	szBuffer.append(SEPARATOR);
 	szBuffer.append(NEWLINE);
 	// <advc.004> Skip base rate if final rate will be the same

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=4857

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3178
# Commit: 6347851abb0c73ece86138b6c204f7e00abf55ed
# History segment: pre-SAS AdvCiv history
# Parent used for diff: a9fde73b885a603fe65c776e530d274b506c21cd
# Title: Few more notes about memory debugging added to makefile
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
9	6	CvGameCoreDLL/Project/Makefile.project

[PATCHES]

diff --git a/CvGameCoreDLL/Project/Makefile.project b/CvGameCoreDLL/Project/Makefile.project
index 44c871409..1650de48a 100644
--- a/CvGameCoreDLL/Project/Makefile.project
+++ b/CvGameCoreDLL/Project/Makefile.project
@@ -3,12 +3,6 @@ SOURCE_DIR = ..
 PROJECT_CFLAGS = /WX /W4
 # Allow inline expansion of functions with an inline keyword even for debug builds
 PROJECT_Debug_CFLAGS = /Ob1
-# Should _not_ be set when inspecting disassembly as it will disable __forceinline
-#PROJECT_DebugOpt_CFLAGS = /DFASSERT_ENABLE /Oy-
-# Can use global optimizations along with debug symbols; it's just slow to compile.
-# (But not recommended for profiling with Very Sleepy; I've seen it e.g. claim
-# that _alldiv calls all sorts of GameCoreDLL functions.)
-#PROJECT_DebugOpt_CFLAGS = /GL
 # DLL-internal profiler: /DUSE_INTERNAL_PROFILER
 # Time Stamp Counter profiler: /DUSE_TSC_PROFILER
 # External profiler, e.g. Very Sleepy: No profile flags needed; profile builds always use Z7.
@@ -18,3 +12,12 @@ PROJECT_Debug_CFLAGS = /Ob1
 PROJECT_Profile_CFLAGS = /DPROFILE_AI_AUTO_PLAY
 PROJECT_LDFLAGS = /DEF:CvGameCoreDLL.def
 PRECOMPILE_DEPENDENCIES = 
+# Should use these extra flags for debugging (i.e. when debugging errors that
+# aren't reproducible with non-optimizing debug builds). Replacing
+# /O2 with just e.g. /Oi /Ot /Ob1 is also worth a try, or a debug build w/o /RTC1;
+# that would have to be done through the actual Makefile.
+#PROJECT_DebugOpt_CFLAGS = /DFASSERT_ENABLE /Oy-
+# Can use global optimizations along with debug symbols; it's just slow to compile.
+# (But not recommended for profiling with Very Sleepy; I've seen it e.g. claim
+# that _alldiv calls all sorts of GameCoreDLL functions.)
+#PROJECT_DebugOpt_CFLAGS = /GL

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1736

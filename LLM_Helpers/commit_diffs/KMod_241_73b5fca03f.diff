# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 241
# Commit: 73b5fca03f56262e82241696cea868c45e5a1231
# History segment: K-Mod history
# Parent used for diff: f81196c8558277e575ddfb3a36a7b6d836f1fc50
# Title: Changed the way K-Mod deals with the original BtS pathfinding 'reuse' bug. (This new way seems better to me, but I don't fully understand the nature of the b...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
10	5	CvGameCoreDLL/CvSelectionGroup.cpp
2	1	CvGameCoreDLL/CvSelectionGroup.h
1	1	CvGameCoreDLL/CvSelectionGroupAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index bc2c2b4f0..ca9791cad 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -34,6 +34,7 @@
 
 //	KOSHLING MOD - path validity cache, used only within a single
 //	call to GneratePath()
+const CvSelectionGroup* CvSelectionGroup::lastPathGeneratedFor = NULL; // K-Mod
 int CvSelectionGroup::m_cachedPathValidityFromPlotX = -2;
 int CvSelectionGroup::m_cachedPathValidityFromPlotY;
 bool CvSelectionGroup::m_cachedPathValidityResult;
@@ -4422,7 +4423,8 @@ bool CvSelectionGroup::generatePath( const CvPlot* pFromPlot, const CvPlot* pToP
 {
 	//	Path generation reuse is only ok for a given stack, not between stacks
 	//	so we reset whether to allow path reuse globally for each stack
-	static const CvSelectionGroup* lastPathGeneratedFor = NULL;
+	//static const CvSelectionGroup* lastPathGeneratedFor = NULL;
+	// K-Mod, moved to class scope.
 
 	PROFILE("CvSelectionGroup::generatePath()")
 
@@ -4446,9 +4448,11 @@ bool CvSelectionGroup::generatePath( const CvPlot* pFromPlot, const CvPlot* pToP
 
 	//	Koshling - the path generator screws up if you allow it to reuse info previosuly
 	//	generated by a different stack (it gets the move counts wrong potentially)
-	if ( this != lastPathGeneratedFor )
+	// K-Mod. originally this was fixed by setting bReuse = false.
+	// I've changed it to instead call resetPath - and only if lastPathGeneratedFor != NULL
+	if (lastPathGeneratedFor && lastPathGeneratedFor != this)
 	{
-		bReuse = false;
+		resetPath();
 	}
 
 	bSuccess = gDLL->getFAStarIFace()->GeneratePath(&GC.getPathFinder(), pFromPlot->getX_INLINE(), pFromPlot->getY_INLINE(), pToPlot->getX_INLINE(), pToPlot->getY_INLINE(), false, iFlags, bReuse);
@@ -4456,7 +4460,7 @@ bool CvSelectionGroup::generatePath( const CvPlot* pFromPlot, const CvPlot* pToP
 	//	Invalidate the cache again so that callers from outside the DLL to path generation don't
 	//	invoke a stale cache
 	m_cachedPathValidityFromPlotX = -2;	//	-2 blocks all attempts to use the cache
-	lastPathGeneratedFor = this;
+	//lastPathGeneratedFor = this;
 
 	if (piPathTurns != NULL)
 	{
@@ -4505,8 +4509,9 @@ void CvSelectionGroup::CachePathValidityResult(CvPlot* pFromPlot, bool cachedRes
 	}
 }
 
-void CvSelectionGroup::resetPath()
+void CvSelectionGroup::resetPath() const
 {
+	lastPathGeneratedFor = NULL; // K-Mod
 	gDLL->getFAStarIFace()->ForceReset(&GC.getPathFinder());
 }

diff --git a/CvGameCoreDLL/CvSelectionGroup.h b/CvGameCoreDLL/CvSelectionGroup.h
index aa45afd1d..f417c44c6 100644
--- a/CvGameCoreDLL/CvSelectionGroup.h
+++ b/CvGameCoreDLL/CvSelectionGroup.h
@@ -164,7 +164,7 @@ public:
 	CvPlot* getPathFirstPlot() const;																																		// Exposed to Python
 	CvPlot* getPathEndTurnPlot() const;																																	// Exposed to Python
 	bool generatePath( const CvPlot* pFromPlot, const CvPlot* pToPlot, int iFlags = 0, bool bReuse = false, int* piPathTurns = NULL) const;	// Exposed to Python
-	void resetPath();																																										// Exposed to Python
+	void resetPath() const;					// Exposed to Python
 
 	DllExport void clearUnits();
 	DllExport bool addUnit(CvUnit* pUnit, bool bMinimalChange);
@@ -278,6 +278,7 @@ public:
 	//	a crash in the main engine.  This is a bit untidy, but essentially fine due to the
 	//	single threaded nature of the application and the fact that cache validity is only
 	//	required across a single path generation call, which cannot interleave
+	static const CvSelectionGroup* lastPathGeneratedFor; // K-Mod
 	static int m_cachedPathValidityFromPlotX;
 	static int m_cachedPathValidityFromPlotY;
 	static bool m_cachedPathValidityResult;

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.cpp b/CvGameCoreDLL/CvSelectionGroupAI.cpp
index 039592602..19be5c6ba 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.cpp
+++ b/CvGameCoreDLL/CvSelectionGroupAI.cpp
@@ -314,7 +314,7 @@ bool CvSelectionGroupAI::AI_update()
 
 					if (pLoopUnit->canMove())
 					{
-						resetPath();
+						//resetPath(); // K-Mod disabled (this is now handled by generatePath)
 
 						if (pLoopUnit->AI_follow(bFirst))
 						{

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=4199

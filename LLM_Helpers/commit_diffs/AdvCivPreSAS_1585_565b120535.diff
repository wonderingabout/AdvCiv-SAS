# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1585
# Commit: 565b120535639a96fd3616fd9bb4f7b570331b1e
# History segment: pre-SAS AdvCiv history
# Parent used for diff: af24cee4ee4cccaa546943b0e8d055a6f0044e6c
# Title: A few changes for mod-mods
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
11	8	Assets/Config/Not Just Another Game Clock.xml
9	7	Assets/XML/GlobalDefines_advc.xml
6	1	CvGameCoreDLL/MilitaryBranch.cpp

[PATCHES]

diff --git a/Assets/Config/Not Just Another Game Clock.xml b/Assets/Config/Not Just Another Game Clock.xml
index b33e8a587..928a4dc4e 100644
--- a/Assets/Config/Not Just Another Game Clock.xml	
+++ b/Assets/Config/Not Just Another Game Clock.xml	
@@ -50,40 +50,43 @@
 			<!-- advc.068: Tech_Screen added to dirty-bits. Colors changed;
 				 were RED, YELLOW, GREEN, CYAN, BLUE (too difficult to read
 				 on the blue interface), MAGENTA, WHITE.
-				 YIELD_FOOD is an orange; see init.xml. -->
+				 YIELD_FOOD is an orange; see init.xml. Removed the unused custom getters and setters.
+				 Note that the list ids must correspond to the type tags in
+				 Civ4EraInfos.xml (b/c of the getEraColor function defined
+				 below) -->
 			<list   id="Color_ERA_ANCIENT" key="ERA_ANCIENT" 
 					type="color" default="COLOR_YIELD_FOOD" 
-					get="getAncientEraColor" set="setAncientEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Ancient Era" 
 					help="Color to use for the Ancient era."/>
 			<list   id="Color_ERA_CLASSICAL" key="ERA_CLASSICAL" 
 					type="color" default="COLOR_HIGHLIGHT_TEXT" 
-					get="getClassicalEraColor" set="setClassicalEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Classical Era" 
 					help="Color to use for the Classical era."/>
 			<list   id="Color_ERA_MEDIEVAL" key="ERA_MEDIEVAL" 
 					type="color" default="COLOR_UNIT_TEXT" 
-					get="getMedievalEraColor" set="setMedievalEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Medieval Era" 
 					help="Color to use for the Medieval era."/>
 			<list   id="Color_ERA_RENAISSANCE" key="ERA_RENAISSANCE" 
 					type="color" default="COLOR_GREEN" 
-					get="getRenaissanceEraColor" set="setRenaissanceEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Renaissance Era" 
 					help="Color to use for the Renaissance era."/>
 			<list   id="Color_ERA_INDUSTRIAL" key="ERA_INDUSTRIAL" 
 					type="color" default="COLOR_WARNING_TEXT" 
-					get="getIndustrialEraColor" set="setIndustrialEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Industrial Era" 
 					help="Color to use for the Industrial era."/>
 			<list   id="Color_ERA_MODERN" key="ERA_MODERN" 
 					type="color" default="COLOR_BUILDING_TEXT" 
-					get="getModernEraColor" set="setModernEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Modern Era" 
 					help="Color to use for the Modern era."/>
 			<list   id="Color_ERA_FUTURE" key="ERA_FUTURE" 
 					type="color" default="COLOR_MAGENTA" 
-					get="getFutureEraColor" set="getFutureEraColor" dirtyBit="GameData,Tech_Screen" 
+					dirtyBit="GameData,Tech_Screen" 
 					label="Future Era" 
 					help="Color to use for the Future era."/>
 			<accessor get="getEraColor" set="setEraColor" args="era" id="Color_%s"/>

diff --git a/Assets/XML/GlobalDefines_advc.xml b/Assets/XML/GlobalDefines_advc.xml
index 59c7d0fc5..651d6e64c 100644
--- a/Assets/XML/GlobalDefines_advc.xml
+++ b/Assets/XML/GlobalDefines_advc.xml
@@ -72,6 +72,15 @@
 		<iDefineIntVal>0</iDefineIntVal>
 	</Define>
 
+	<!-- advc.002h: 0 only hides the clouds. Their shadows are hidden through
+		 BML.fpk. Original value: 1. If BML.fpk is removed, then this value
+		 should be set back to 1; otherwise, there will be cloud shadows without
+		 clouds. -->
+	<Define>
+		<DefineName>RENDER_GLOBEVIEW_CLOUDS</DefineName>
+		<iDefineIntVal>0</iDefineIntVal>
+	</Define>
+
 	<!-- [advc.104, advc.019]
 		A value of 1 or greater enables K-Mod's AI for decisions on war
 		and peace (instead of UWAI), and disables the "Aggressive AI" option.
@@ -914,13 +923,6 @@
 		<iDefineIntVal>1</iDefineIntVal>
 	</Define>
 
-	<!-- [advc.002h] 0 only hides the clouds. Their shadows are hidden through
-		 BML.fpk. Original value: 1. -->
-	<Define>
-		<DefineName>RENDER_GLOBEVIEW_CLOUDS</DefineName>
-		<iDefineIntVal>0</iDefineIntVal>
-	</Define>
-	
 	<!-- [advc.550d] This modifier is applied to tech costs when playing
 		 with the "No Tech Trading" option. The effect of the modifier is
 		 reduced in the early and late game. Recommended: 28. Use 0 for BtS

diff --git a/CvGameCoreDLL/MilitaryBranch.cpp b/CvGameCoreDLL/MilitaryBranch.cpp
index f9a359157..bedb9f530 100644
--- a/CvGameCoreDLL/MilitaryBranch.cpp
+++ b/CvGameCoreDLL/MilitaryBranch.cpp
@@ -111,7 +111,8 @@ void MilitaryBranch::NuclearArsenal::updateTypicalUnit() {
 	for(int i = 0; i < GC.getNumUnitClassInfos(); i++) {
 		UnitTypes ut = (UnitTypes)(GC.getCivilizationInfo(
 				civ.getCivilizationType()).getCivilizationUnits(i));
-		if(ut == NO_UNIT) continue;
+		if(ut == NO_UNIT)
+			continue;
 		CvCity* capital = civ.getCapitalCity();
 		if(capital == NULL || !capital->canTrain(ut))
 			continue;
@@ -467,6 +468,8 @@ bool MilitaryBranch::Army::canTrainSiege() const {
 	for(int i = 0; i < GC.getNumUnitClassInfos(); i++) {
 		UnitTypes ut = (UnitTypes)(GC.getCivilizationInfo(
 				civ.getCivilizationType()).getCivilizationUnits(i));
+		if(ut == NO_UNIT)
+			continue;
 		CvUnitInfo const& u = GC.getUnitInfo(ut);
 		if(((u.getBombardRate() > 0 && u.getDomainType() == DOMAIN_LAND) ||
 				(u.getBombardRate() > 0 && u.getDomainType() == DOMAIN_AIR)) &&
@@ -484,6 +487,8 @@ bool MilitaryBranch::Army::canTrainCollateral() const {
 	for(int i = 0; i < GC.getNumUnitClassInfos(); i++) {
 		UnitTypes ut = (UnitTypes)(GC.getCivilizationInfo(
 				civ.getCivilizationType()).getCivilizationUnits(i));
+		if(ut == NO_UNIT)
+			continue;
 		CvUnitInfo const& u = GC.getUnitInfo(ut);
 		if(u.getCollateralDamage() > 0 &&
 				isValidDomain((DomainTypes)u.getDomainType()) &&

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=5768

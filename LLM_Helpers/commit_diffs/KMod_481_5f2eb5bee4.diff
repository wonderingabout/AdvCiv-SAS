# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 481
# Commit: 5f2eb5bee40cf4106c330c55fe018915b47c2c33
# History segment: K-Mod history
# Parent used for diff: b3af3db102ea7b7ed1ca522ff5c90e1ddc9b43b3
# Title: Fixed an original BtS problem which prevented units from being un-grouped before they were automated.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	0	CvGameCoreDLL/CvGameInterface.cpp
2	0	CvGameCoreDLL/CvPlayer.cpp
2	2	CvGameCoreDLL/CvUnit.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index 74884814b..15329bed8 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -1076,6 +1076,12 @@ void CvGame::selectionListGameNetMessage(int eMessage, int iData2, int iData3, i
 			}
 			else if (eMessage == GAMEMESSAGE_DO_COMMAND)
 			{
+				// K-Mod. When setting a unit to automate, we must be careful not to keep it grouped
+				if (iData2 == COMMAND_AUTOMATE && !gDLL->getInterfaceIFace()->mirrorsSelectionGroup())
+				{
+					selectionListGameNetMessage(GAMEMESSAGE_JOIN_GROUP);
+				}
+				// K-Mod end
 				pSelectedUnitNode = gDLL->getInterfaceIFace()->headSelectionListNode();
 
 				while (pSelectedUnitNode != NULL)

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index 00d7b7bfd..0f2608381 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -13136,6 +13136,8 @@ void CvPlayer::changeImprovementYieldChange(ImprovementTypes eIndex1, YieldTypes
 // XXX should pUnit be a CvSelectionGroup???
 void CvPlayer::updateGroupCycle(CvUnit* pUnit)
 {
+	PROFILE_FUNC();
+
 	CLLNode<IDInfo>* pUnitNode;
 	CLLNode<int>* pSelectionGroupNode;
 	CLLNode<int>* pBestSelectionGroupNode;

diff --git a/CvGameCoreDLL/CvUnit.cpp b/CvGameCoreDLL/CvUnit.cpp
index 73ad0e23c..6923b8665 100644
--- a/CvGameCoreDLL/CvUnit.cpp
+++ b/CvGameCoreDLL/CvUnit.cpp
@@ -3101,10 +3101,10 @@ bool CvUnit::canAutomate(AutomateTypes eAutomate) const
 		return false;
 	}
 
-	if (!isGroupHead())
+	/*if (!isGroupHead())
 	{
 		return false;
-	}
+	}*/ // disabled by K-Mod
 
 	switch (eAutomate)
 	{

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=1638

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3448
# Commit: bc9b8190da26fc77bf0345700955a1a023d66515
# History segment: pre-SAS AdvCiv history
# Parent used for diff: b02a397b492447e91b15633093888acc85581836
# Title: AI eval of Great Lighthouse considers planned city sites
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
27	3	CvGameCoreDLL/CvCityAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index 197c9f0a0..f79c5b4e6 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -4151,9 +4151,33 @@ int CvCityAI::AI_buildingValue(BuildingTypes eBuilding, int iFocusFlags,
 
 			iTempValue *= AI_yieldMultiplier(YIELD_COMMERCE);
 			iTempValue /= 100;
-
-			iTempValue += kBuilding.getCoastalTradeRoutes() * std::max((iCitiesTarget+1)/2,
-					kOwner.countNumCoastalCities()) * iGlobalTradeValue;
+			{
+				int const iCoastalTradeRoutes = kBuilding.getCoastalTradeRoutes();
+				if (iCoastalTradeRoutes != 0)
+				{
+					int const iCoastalCities = kOwner.countNumCoastalCities();
+					// <advc.131>
+					scaled rCoastalCitySites;
+					int const iCitySites = kOwner.AI_getNumCitySites();
+					for (int i = 0; i < iCitySites; i++)
+					{
+						if (kOwner.AI_getCitySite(i).isCoastalLand(-1))
+						{	/*	Less likely to claim lower-priority sites;
+								and it'll take longer. */
+							rCoastalCitySites += 1 - scaled(i, iCitySites);
+						}
+					}
+					// K-Mod formula:
+					//std::max((iCitiesTarget+1)/2, kOwner.countNumCoastalCities())
+					int iProjectedCoastalCities = (iCoastalCities + rCoastalCitySites +
+							/*	City placement will favor the coast more once we have
+								extra trade routes */
+							scaled(iCoastalTradeRoutes, 2)).uround();
+					// </advc.131>
+					iTempValue += iCoastalTradeRoutes * iProjectedCoastalCities *
+							iGlobalTradeValue;
+				}
+			}
 			// <advc.310>
 			iTempValue += kBuilding.getAreaTradeRoutes() *
 					// Same formula as for AreaHappiness (Notre Dame)

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1620

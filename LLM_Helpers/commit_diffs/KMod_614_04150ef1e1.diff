# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 614
# Commit: 04150ef1e1757a38600ac2790560c54b81e8967f
# History segment: K-Mod history
# Parent used for diff: 98f47bb14a42fa522a3885749d532ce6117004e8
# Title: Fixed some minor problems with how the AI's group attacks are handled and adjusted some of the code to make similar mistakes less likely in the future.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	1	CvGameCoreDLL/CvSelectionGroup.cpp
1	1	CvGameCoreDLL/CvSelectionGroup.h
6	15	CvGameCoreDLL/CvSelectionGroupAI.cpp
2	2	CvGameCoreDLL/CvSelectionGroupAI.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index 638e3c86e..2cecb554d 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -3268,6 +3268,8 @@ bool CvSelectionGroup::groupAttack(int iX, int iY, int iFlags, bool& bFailedAlre
 {
 	PROFILE_FUNC();
 
+	FAssert(!isBusy()); // K-Mod
+
 	CvPlot* pDestPlot = GC.getMapINLINE().plotINLINE(iX, iY);
 
 	// K-Mod. Rather than clearing the existing path data; use a temporary pathfinder.
@@ -3390,7 +3392,8 @@ bool CvSelectionGroup::groupAttack(int iX, int iY, int iFlags, bool& bFailedAlre
 							{
 								bFailedAlreadyFighting = true;
 							}
-							if (!pBestAttackUnit->isAttacking())
+							//if (!pBestAttackUnit->isCombat())
+							FAssert(!pBestAttackUnit->isCombat());
 							{
 								// we need to issue the attack order to start the attack
 								pBestAttackUnit->attack(pDestPlot, bStack);
@@ -3430,6 +3433,8 @@ void CvSelectionGroup::groupMove(CvPlot* pPlot, bool bCombat, CvUnit* pCombatUni
 {
 	//PROFILE_FUNC();
 
+	FAssert(!isBusy()); // K-Mod
+
 	CLLNode<IDInfo>* pUnitNode;
 	CvUnit* pLoopUnit;

diff --git a/CvGameCoreDLL/CvSelectionGroup.h b/CvGameCoreDLL/CvSelectionGroup.h
index b913b195f..1a07c1549 100644
--- a/CvGameCoreDLL/CvSelectionGroup.h
+++ b/CvGameCoreDLL/CvSelectionGroup.h
@@ -219,7 +219,7 @@ public:
 	// K-Mod end
 	virtual void AI_queueGroupAttack(int iX, int iY) = 0;
 	virtual void AI_cancelGroupAttack() = 0;
-	virtual bool AI_isGroupAttack() = 0;
+	virtual bool AI_isGroupAttack() const = 0; // K-Mod made const
 
 	virtual bool AI_isControlled() = 0;
 	virtual bool AI_isDeclareWar(const CvPlot* pPlot = NULL) = 0;

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.cpp b/CvGameCoreDLL/CvSelectionGroupAI.cpp
index a2b924caf..d1f069014 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.cpp
+++ b/CvGameCoreDLL/CvSelectionGroupAI.cpp
@@ -192,7 +192,8 @@ bool CvSelectionGroupAI::AI_update()
 	bDead = false;
 	
 	bool bFailedAlreadyFighting = false;
-	while ((m_bGroupAttack && !bFailedAlreadyFighting) || readyToMove())
+	//while ((m_bGroupAttack && !bFailedAlreadyFighting) || readyToMove())
+	while ((AI_isGroupAttack() && !isBusy()) || readyToMove()) // K-Mod
 	{
 		iTempHack++;
 		if (iTempHack > 100)
@@ -217,9 +218,9 @@ bool CvSelectionGroupAI::AI_update()
 		}
 
 		// if we want to force the group to attack, force another attack
-		if (m_bGroupAttack)
+		if (AI_isGroupAttack())
 		{			
-			m_bGroupAttack = false;
+			AI_cancelGroupAttack();
 
 			groupAttack(m_iGroupAttackX, m_iGroupAttackY, MOVE_DIRECT_ATTACK, bFailedAlreadyFighting);
 		}
@@ -252,7 +253,7 @@ bool CvSelectionGroupAI::AI_update()
 
 		// if no longer group attacking, and force separate is true, then bail, decide what to do after group is split up
 		// (UnitAI of head unit may have changed)
-		if (!m_bGroupAttack && AI_isForceSeparate())
+		if (!AI_isGroupAttack() && AI_isForceSeparate())
 		{
 			AI_separate();	// pointers could become invalid...
 			//return true;
@@ -267,7 +268,7 @@ bool CvSelectionGroupAI::AI_update()
 			bFollow = false;
 
 			// if we not group attacking, then check for follow action
-			if (!m_bGroupAttack)
+			if (!AI_isGroupAttack())
 			{
 				pEntityNode = headUnitNode();
 				// K-Mod note: I've rearranged a few things below, and added 'bFirst'.
@@ -637,16 +638,6 @@ void CvSelectionGroupAI::AI_queueGroupAttack(int iX, int iY)
 	m_iGroupAttackY = iY;
 }
 
-inline void CvSelectionGroupAI::AI_cancelGroupAttack()
-{
-	m_bGroupAttack = false;
-}
-
-inline bool CvSelectionGroupAI::AI_isGroupAttack()
-{
-	return m_bGroupAttack;
-}
-
 bool CvSelectionGroupAI::AI_isControlled()
 {
 	return (!isHuman() || isAutomated());

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.h b/CvGameCoreDLL/CvSelectionGroupAI.h
index 6ce1d4295..6eaaa54de 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.h
+++ b/CvGameCoreDLL/CvSelectionGroupAI.h
@@ -46,8 +46,8 @@ public:
 	int AI_sumStrength(const CvPlot* pAttackedPlot = NULL, DomainTypes eDomainType = NO_DOMAIN, bool bCheckCanAttack = false) const;
 	// K-Mod end
 	void AI_queueGroupAttack(int iX, int iY);
-	void AI_cancelGroupAttack();
-	bool AI_isGroupAttack();
+	inline void AI_cancelGroupAttack() { m_bGroupAttack = false; } // K-Mod (made inline)
+	inline bool AI_isGroupAttack() const { return m_bGroupAttack; } // K-Mod (made inline)
 
 	bool AI_isControlled();
 	bool AI_isDeclareWar(const CvPlot* pPlot = NULL);

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=4436

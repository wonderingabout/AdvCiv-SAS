# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2259
# Commit: 9557b2371c62b5b584a30309002206da91585dd6
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 6b0da4cb77731790e89dce52b67b4b798e1a732b
# Title: City trades: stricter attitude threshold at 20% nationality
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
11	4	Assets/XML/GlobalDefines_advc.xml
3	0	CvGameCoreDLL/CvGlobals.h
3	2	CvGameCoreDLL/CvPlayerAI.cpp

[PATCHES]

diff --git a/Assets/XML/GlobalDefines_advc.xml b/Assets/XML/GlobalDefines_advc.xml
index a27011abc..acf7d6e59 100644
--- a/Assets/XML/GlobalDefines_advc.xml
+++ b/Assets/XML/GlobalDefines_advc.xml
@@ -895,15 +895,22 @@
 	</Define>
 
 	<!-- advc.ctr: To receive a city through trade, a civ needs to have at
-		 least this percentage of tile culture in the city tile. Moreover,
-		 an AI civ applies a stricter attitude threshold when it has this much or
-		 more culture in a city and needs to decide whether to trade (not
-		 liberate) the city to another civ.
+		 least this percentage of culture in the city tile (nationality).
+		 (Additionally, the recipient needs to have at least half as much
+		 nationality as the city owner.)
 		 Recommended: 10 -->
 	<Define>
 		<DefineName>CITY_TRADE_CULTURE_THRESH</DefineName>
 		<iDefineIntVal>10</iDefineIntVal>
 	</Define>
+	<!-- When a city has this much nationality (city tile culture percentage)
+		 or more, then its AI owner applies a stricter attitude threshold
+		 when deciding whether to trade (not liberate) the city.
+		 Recommended: 20 (twice the CITY_TRADE_CULTURE_THRESH) -->
+	<Define>
+		<DefineName>NATIVE_CITY_CULTURE_THRESH</DefineName>
+		<iDefineIntVal>20</iDefineIntVal>
+	</Define>
 
 	<!-- [advc.030] Set this to 1 or greater (recommended) to treat plot groups
 		 that are separated by mountain peaks or shelf ice as separate map

diff --git a/CvGameCoreDLL/CvGlobals.h b/CvGameCoreDLL/CvGlobals.h
index 181e5a38c..11998c8d9 100644
--- a/CvGameCoreDLL/CvGlobals.h
+++ b/CvGameCoreDLL/CvGlobals.h
@@ -467,7 +467,10 @@ public:
 		DO(CITY_AIR_UNIT_CAPACITY) \
 		DO(COLLATERAL_COMBAT_DAMAGE) \
 		DO(TECH_COST_EXTRA_TEAM_MEMBER_MODIFIER) \
+		/* <advc.ctr> */ \
 		DO(CITY_TRADE_CULTURE_THRESH) \
+		DO(NATIVE_CITY_CULTURE_THRESH) \
+		/* </advc.ctr> */ \
 		DO(AI_OFFER_EXTRA_GOLD_PERCENT) \
 		DO(RECON_VISIBILITY_RANGE) \
 		DO(UNIT_VISIBILITY_RANGE) \

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 10b9338ba..bf4c4ac0c 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -12025,7 +12025,8 @@ DenialTypes CvPlayerAI::AI_cityTrade(CvCityAI const& kCity, PlayerTypes eToPlaye
 		{	// Trade with non-rivals if Friendly
 			if (getMasterTeam() == kToTeam.getMasterTeam())
 				iAttitudeThresh = ATTITUDE_PLEASED;
-			else return DENIAL_NEVER;
+			// "Against all we stand for" - when attitude threshold impossible to reach
+			else return DENIAL_FAVORITE_CIVIC;
 		}
 		if (iAttitude <= iAttitudeThresh)
 			return DENIAL_ATTITUDE;
@@ -12112,7 +12113,7 @@ AttitudeTypes CvPlayerAI::AI_cityTradeAttitudeThresh(CvCity const& kCity,
 		return ATTITUDE_FURIOUS;
 	CvLeaderHeadInfo const& kPersonality = GC.getInfo(getPersonalityType());
 	int r = (kCity.getPlot().calculateCulturePercent(getID()) >=
-			GC.getDefineINT(CvGlobals::CITY_TRADE_CULTURE_THRESH) ? 
+			GC.getDefineINT(CvGlobals::NATIVE_CITY_CULTURE_THRESH) ? 
 			kPersonality.getNativeCityRefuseAttitudeThreshold() :
 			kPersonality.getCityRefuseAttitudeThreshold());
 	return (AttitudeTypes)r;

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=3084

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 807
# Commit: ddde12c250fcb9c72c11b0b938f55d35e74aa362
# History segment: K-Mod history
# Parent used for diff: 18a4ef255fa4ebededa28f4cf92f28088052b70d
# Title: Minor changes. Tweaked spoils value.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	0	CvGameCoreDLL/CvGameCoreUtils.h
4	2	CvGameCoreDLL/CvTeamAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameCoreUtils.h b/CvGameCoreDLL/CvGameCoreUtils.h
index 1fb793669..4deeb7fc8 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.h
+++ b/CvGameCoreDLL/CvGameCoreUtils.h
@@ -200,6 +200,8 @@ inline CvPlot* plotXY(int iX, int iY, int iDX, int iDY)																// Expose
 	return GC.getMapINLINE().plotINLINE((iX + iDX), (iY + iDY));
 }
 
+inline CvPlot* plotXY(const CvPlot* pPlot, int iDX, int iDY) { return plotXY(pPlot->getX_INLINE(), pPlot->getY_INLINE(), iDX, iDY); } // K-Mod
+
 inline DirectionTypes directionXY(int iDX, int iDY)																		// Exposed to Python
 {
 	if ((abs(iDX) > DIRECTION_RADIUS) || (abs(iDY) > DIRECTION_RADIUS))

diff --git a/CvGameCoreDLL/CvTeamAI.cpp b/CvGameCoreDLL/CvTeamAI.cpp
index c09130315..4a667bbe1 100644
--- a/CvGameCoreDLL/CvTeamAI.cpp
+++ b/CvGameCoreDLL/CvTeamAI.cpp
@@ -1395,14 +1395,16 @@ int CvTeamAI::AI_warSpoilsValue(TeamTypes eTarget, WarPlanTypes eWarPlan) const
 			for (ReligionTypes i = (ReligionTypes)0; i < GC.getNumReligionInfos(); i=(ReligionTypes)(i+1))
 			{
 				if (pLoopCity->isHolyCity(i))
-					iCityValue += GC.getGameINLINE().countReligionLevels(i) / (pLoopCity->hasShrine(i) ? 1 : 2);
+					iCityValue += std::max(0, GC.getGameINLINE().countReligionLevels(i) / (pLoopCity->hasShrine(i) ? 1 : 2) - 4);
+				// note: the -4 at the end is mostly there to offset the 'wonder' value that will be added later.
+				// I don't want to double-count the value of the shrine, and the religion without the shrine isn't worth much anyway.
 			}
 
 			// corp HQ value
 			for (CorporationTypes i = (CorporationTypes)0; i < GC.getNumCorporationInfos(); i=(CorporationTypes)(i+1))
 			{
 				if (pLoopCity->isHeadquarters(i))
-					iCityValue += 2 * GC.getGameINLINE().countCorporationLevels(i);
+					iCityValue += std::max(0, 2 * GC.getGameINLINE().countCorporationLevels(i) - 4);
 			}
 
 			// wonders

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1885

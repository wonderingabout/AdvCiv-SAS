# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3927
# Commit: 01bb2cccd01b732fb9f63088d82b188ae0962f0c
# History segment: pre-SAS AdvCiv history
# Parent used for diff: d0d5b1d1f02d597fb05186070a4cef6a6faa19f9
# Title: Two small refactoring changes
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
16	16	CvGameCoreDLL/CvGameTextMgr.cpp
1	1	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameTextMgr.cpp b/CvGameCoreDLL/CvGameTextMgr.cpp
index f0853efa4..3e81d47b8 100644
--- a/CvGameCoreDLL/CvGameTextMgr.cpp
+++ b/CvGameCoreDLL/CvGameTextMgr.cpp
@@ -1672,24 +1672,24 @@ void CvGameTextMgr::setPlotListHelpPerOwner(CvWStringBuffer& szString,
 	uint uTotal = perOwner[iRogueIndex][ALL].size();
 	for(int i = 0; i < iRogueIndex; i++) // Rogue units don't get a heading
 	{
-		int iSize = perOwner[i][ALL].size();
-		if(iSize > 0)
-		{
-			//uTotal += iSize; // Not good enough; need to anticipate linewrap.
-			for(int j = 0; j < iSize; j++)
-			{
+		int const iSize = perOwner[i][ALL].size();
+		if (iSize <= 0)
+			continue;
+		iOwners++;
+		//uTotal += iSize; // Not good enough; need to anticipate linewrap.
+		for(int j = 0; j < iSize; j++)
+		{
+			uTotal++;
+			// Ad hoc heuristic for estimating the required (horizontal) space
+			int iSpaceValue = perOwner[i][ALL][j]->getName().length()
+					+ (8 * perOwner[i][ALL][j]->getExperience()) / 5;
+			if(perOwner[i][ALL][j]->getDamage() > 0)
+				iSpaceValue += 2;
+			if(iSpaceValue >= 30)
 				uTotal++;
-				// Ad hoc heuristic for estimating the required (horizontal) space
-				int iSpaceValue = perOwner[i][ALL][j]->getName().length()
-						+ (8 * perOwner[i][ALL][j]->getExperience()) / 5;
-				if(perOwner[i][ALL][j]->getDamage() > 0)
-					iSpaceValue += 2;
-				if(iSpaceValue >= 30)
-					uTotal++;
-			}
-			if(iSize > 1)
-				iHeadings++;
 		}
+		if(iSize > 1)
+			iHeadings++;
 	}
 	int iTotal = (int)uTotal;
 	CvUnit const& kCenterUnit = *kPlot.getCenterUnit();

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 7fee2f0dd..0dfab03ea 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -2939,7 +2939,7 @@ void CvUnitAI::AI_attackCityMove()
 	if (isBarbarian())
 		bReadyToAttack = (getGroup()->getNumUnits() >= 3);
 	// <advc.300>
-	if (!isBarbarian() && !bTurtle)
+	else if (!bTurtle)
 	{
 		int const iGroupSz = getGroup()->getNumUnits();
 		scaled const rOurEra = GET_PLAYER(getOwner()).AI_getCurrEraFactor();

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=2072

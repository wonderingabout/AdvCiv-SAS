# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 5868
# Commit: ea84ad64200432ec6a86af75d4ae71f7218d4798
# History segment: AdvCiv-SAS branch history
# Parent used for diff: 452bbb8183205039d2494b36c592b8b0b09080f6
# Title: fix ruff E713 (not x in y -> x not in y) done with the help of GPT-5.5-Thinking with regexp thanks
# Full commit message/metadata: see _1_AdvCiv-SAS/git_logs/git_log_anonymized_email_003_AdvCiv-SAS.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	Assets/Python/BUG/configobj.py
0	1	Assets/Python/Contrib/EventSigns.py
1	1	PrivateMaps/Archipelago.py
4	4	PrivateMaps/BTG_Cross.py
2	2	PrivateMaps/BTG_Grid.py
1	1	PrivateMaps/Equal_Islands_V2_beta.py
1	1	PrivateMaps/Islands.py
6	6	PrivateMaps/Planet_Generator_0_68.py

[PATCHES]

diff --git a/Assets/Python/BUG/configobj.py b/Assets/Python/BUG/configobj.py
index d6809226d..ee3d8c6ed 100644
--- a/Assets/Python/BUG/configobj.py
+++ b/Assets/Python/BUG/configobj.py
@@ -2460,7 +2460,7 @@ class ConfigObj(Section):
         for entry in spec_section:
             if entry == '__many__':
                 continue
-            if (not entry in section.scalars) or (entry in section.defaults):
+            if (entry not in section.scalars) or (entry in section.defaults):
                 # missing entries
                 # or entries from defaults
                 missing = True

diff --git a/Assets/Python/Contrib/EventSigns.py b/Assets/Python/Contrib/EventSigns.py
index 385bb8730..84b38864c 100644
--- a/Assets/Python/Contrib/EventSigns.py
+++ b/Assets/Python/Contrib/EventSigns.py
@@ -276,7 +276,6 @@ class MapSigns:
 		if not thisKey:
 			BugUtil.warn("MapSigns.storeSign() could not determine valid keyname for Plot %s." % (str(pPlot)))
 			return False
-		# <!-- custom: ruff E713: use "key not in x" instead of "not key in x". Credit: ChatGPT. (GPT-5.2-Codex (summarized)) -->
 		if thisKey not in self.plotDict:
 			self.plotDict[thisKey] = PlotSigns(pPlot)
 		self.plotDict[thisKey].setSign(ePlayer, szCaption)

diff --git a/PrivateMaps/Archipelago.py b/PrivateMaps/Archipelago.py
index f8186d25a..6633fb5ee 100644
--- a/PrivateMaps/Archipelago.py
+++ b/PrivateMaps/Archipelago.py
@@ -370,7 +370,7 @@ def assignStartingPlots():
 						iPlotYield += (iFertileCheck - 1)
 					iRegionNetYield += iPlotYield
 					if pPlot.isHills(): iRegionNetYield += 1 # Add a bonus point for Hills plots.
-					if not iArea in land_areas: # This plot is the first detected in its AreaID.
+					if iArea not in land_areas: # This plot is the first detected in its AreaID.
 						iNumLandAreas += 1
 						land_areas.append(iArea)
 						land_area_plots[iArea] = 1

diff --git a/PrivateMaps/BTG_Cross.py b/PrivateMaps/BTG_Cross.py
index 67468bcd6..a11ef82b9 100644
--- a/PrivateMaps/BTG_Cross.py
+++ b/PrivateMaps/BTG_Cross.py
@@ -800,7 +800,7 @@ def assignStartingPlots():
 						iPlotYield += (iFertileCheck - 1)
 					iRegionNetYield += iPlotYield
 					if pPlot.isHills(): iRegionNetYield += 1 # Add a bonus point for Hills plots.
-					if not iArea in land_areas: # This plot is the first detected in its AreaID.
+					if iArea not in land_areas: # This plot is the first detected in its AreaID.
 						iNumLandAreas += 1
 						land_areas.append(iArea)
 						land_area_plots[iArea] = 1
@@ -914,7 +914,7 @@ def assignStartingPlots():
 					playerID = assignLoop
 					reg = assignLoop % iNumRegions
 
-			if not reg in region_data:
+			if reg not in region_data:
 				if len(best_regions) > 0:
 					reg = best_regions[assignLoop % len(best_regions)]
 				else:
@@ -1012,7 +1012,7 @@ def assignStartingPlots():
 		if region_duplicated[0] in best_regions :
 					tpR = [region_duplicated[0]]
 					for item in best_regions :
-							if not item in tpR : tpR.append(item)
+							if item not in tpR : tpR.append(item)
 					best_regions = tpR
 		else :
 			print("you missed something there O.o")
@@ -1058,7 +1058,7 @@ def assignStartingPlots():
 					playerID = assignLoop
 					reg = assignLoop % iNumRegions
 
-			if not reg in region_data:
+			if reg not in region_data:
 				if len(best_regions) > 0:
 					reg = best_regions[assignLoop % len(best_regions)]
 				else:

diff --git a/PrivateMaps/BTG_Grid.py b/PrivateMaps/BTG_Grid.py
index 4ec43ffbc..ef4e5937d 100644
--- a/PrivateMaps/BTG_Grid.py
+++ b/PrivateMaps/BTG_Grid.py
@@ -992,7 +992,7 @@ def assignStartingPlots():
 						iPlotYield += (iFertileCheck - 1)
 					iRegionNetYield += iPlotYield
 					if pPlot.isHills(): iRegionNetYield += 1 # Add a bonus point for Hills plots.
-					if not iArea in land_areas: # This plot is the first detected in its AreaID.
+					if iArea not in land_areas: # This plot is the first detected in its AreaID.
 						iNumLandAreas += 1
 						land_areas.append(iArea)
 						land_area_plots[iArea] = 1
@@ -1182,7 +1182,7 @@ def assignStartingPlots():
 		if region_duplicated[0] in best_regions :
 					tpR = [region_duplicated[0]]
 					for item in best_regions :
-							if not item in tpR : tpR.append(item)
+							if item not in tpR : tpR.append(item)
 					best_regions = tpR
 		else :
 			print("you missed something there O.o")

diff --git a/PrivateMaps/Equal_Islands_V2_beta.py b/PrivateMaps/Equal_Islands_V2_beta.py
index 4b3edb26e..676b0e695 100644
--- a/PrivateMaps/Equal_Islands_V2_beta.py
+++ b/PrivateMaps/Equal_Islands_V2_beta.py
@@ -530,7 +530,7 @@ def assignStartingPlots():
 						iPlotYield += (iFertileCheck - 1)
 					iRegionNetYield += iPlotYield
 					if pPlot.isHills(): iRegionNetYield += 1 # Add a bonus point for Hills plots.
-					if not iArea in land_areas: # This plot is the first detected in its AreaID.
+					if iArea not in land_areas: # This plot is the first detected in its AreaID.
 						iNumLandAreas += 1
 						land_areas.append(iArea)
 						land_area_plots[iArea] = 1

diff --git a/PrivateMaps/Islands.py b/PrivateMaps/Islands.py
index 6ae13c78f..feee88797 100644
--- a/PrivateMaps/Islands.py
+++ b/PrivateMaps/Islands.py
@@ -530,7 +530,7 @@ def assignStartingPlots():
 						iPlotYield += (iFertileCheck - 1)
 					iRegionNetYield += iPlotYield
 					if pPlot.isHills(): iRegionNetYield += 1 # Add a bonus point for Hills plots.
-					if not iArea in land_areas: # This plot is the first detected in its AreaID.
+					if iArea not in land_areas: # This plot is the first detected in its AreaID.
 						iNumLandAreas += 1
 						land_areas.append(iArea)
 						land_area_plots[iArea] = 1

diff --git a/PrivateMaps/Planet_Generator_0_68.py b/PrivateMaps/Planet_Generator_0_68.py
index aed4406aa..21d24a959 100644
--- a/PrivateMaps/Planet_Generator_0_68.py
+++ b/PrivateMaps/Planet_Generator_0_68.py
@@ -1121,15 +1121,15 @@ class TileBuilder:
 					if (len(self.baseTerrain) == 0):
 						wtt = world[px,py]
 						if py > y:
-							if py < world._dimensions[0]-ps-1 and not wtt in self.tileType and not self._intersect(px,py):
+							if py < world._dimensions[0]-ps-1 and wtt not in self.tileType and not self._intersect(px,py):
 								list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 								c += 1
 						elif py < y:
-							if py > ps and not wtt in self.tileType and not self._intersect(px,py):
+							if py > ps and wtt not in self.tileType and not self._intersect(px,py):
 								list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 								c += 1
 						else:
-							if not wtt in self.tileType and not self._intersect(px,py):
+							if wtt not in self.tileType and not self._intersect(px,py):
 								list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 								c += 1
 					else:
@@ -1162,15 +1162,15 @@ class TileBuilder:
 			if (len(self.baseTerrain) == 0):
 				wtt = world[px,py]
 				if py > y:
-					if py < world._dimensions[0]-ps-1 and not wtt in self.tileType and not self._intersect(px,py):
+					if py < world._dimensions[0]-ps-1 and wtt not in self.tileType and not self._intersect(px,py):
 						list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 						c += 1
 				elif py < y:
-					if py > ps and not wtt in self.tileType and not self._intersect(px,py):
+					if py > ps and wtt not in self.tileType and not self._intersect(px,py):
 						list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 						c += 1
 				else:
-					if not wtt in self.tileType and not self._intersect(px,py):
+					if wtt not in self.tileType and not self._intersect(px,py):
 						list[world.getOffset([px,py])] = [px,py,scatt,scattdir,world.getOffset([x,y])]
 						c += 1
 			else:

[PATCH COVERAGE]
patchFiles=8 embeddedFiles=8 omittedFiles=0 embeddedPatchBytes=7774

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1022
# Commit: 0abeea3ba2260b630b00b4e646f6c451c7ab7524
# History segment: K-Mod history
# Parent used for diff: 308aa9a1564f8957929a3e18a98fde9ba1b04d1f
# Title: Fixed a mistake in the recent pathfinder bug-fix.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	CvGameCoreDLL/CvGameCoreUtils.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameCoreUtils.cpp b/CvGameCoreDLL/CvGameCoreUtils.cpp
index 8e2948b59..66ea805a1 100644
--- a/CvGameCoreDLL/CvGameCoreUtils.cpp
+++ b/CvGameCoreDLL/CvGameCoreUtils.cpp
@@ -2098,7 +2098,7 @@ int pathAdd(FAStarNode* parent, FAStarNode* node, int data, const void* pointer,
 		else
 		{
 			PROFILE("pathAdd - non-uniform cost");
-			// Move costs are even for units in this group.
+			// Move costs are uneven for units in this group.
 			// To be sure of the true movement cost for the group, we need to calculate the movement cost for each unit for every step in this turn.
 			std::vector<const CvPlot*> plot_list; // will be traversed in reverse order
 			plot_list.push_back(pToPlot);
@@ -2119,7 +2119,7 @@ int pathAdd(FAStarNode* parent, FAStarNode* node, int data, const void* pointer,
 				int iUnitMoves = bMaxMoves ? pLoopUnit->maxMoves() : pLoopUnit->movesLeft();
 				for (size_t i = plot_list.size()-1; i > 0; i--)
 				{
-					iUnitMoves -= plot_list[i]->movementCost(pLoopUnit, plot_list[i-1]);
+					iUnitMoves -= plot_list[i-1]->movementCost(pLoopUnit, plot_list[i]);
 					FAssert(iUnitMoves > 0 || i == 1);
 				}

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1159

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2675
# Commit: 2630a0c19053f139c13da4887784cf032f370421
# History segment: pre-SAS AdvCiv history
# Parent used for diff: a8397319a242e99eb7ae1a03a7d344b31ba2a607
# Title: Kill Barbarian garrison upon culture flip
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	CvGameCoreDLL/CvCity.cpp
5	2	CvGameCoreDLL/CvPlot.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 9f11cee63..900ada33b 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -697,8 +697,8 @@ void CvCity::damageGarrison(PlayerTypes eRevoltSource)
 {
 	FOR_EACH_UNIT_VAR_IN(pUnit, getPlot())
 	{
-		/*	advc.101: (Didn't matter in BtS because
-			Barbarian cities would flip on the first revolt) */
+		/*	advc.101: OK in BtS b/c Barbarian cities would flip on the first revolt.
+			Now killed only when the city flips through CvPlot::setOwner. */
 		/*if (pUnit->isBarbarian())
 			pUnit->kill(false, eRevoltSource);
 		else*/

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index 895ce55a2..b8e40089c 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -3824,8 +3824,11 @@ void CvPlot::setOwner(PlayerTypes eNewValue, bool bCheckUnits, bool bUpdatePlotG
 				if (pLoopUnit->isEnemy(pNewCity->getTeam(), *this))
 				{
 					FAssert(pLoopUnit->getTeam() != pNewCity->getTeam());
-					//pLoopUnit->kill(false, pNewCity->getTeam());
-					pLoopUnit->jumpToNearestValidPlot(); // advc.101: don't kill
+					/*	<advc.101> Kill only Barbarians (which no longer get killed
+						by CvCity::damageGarrison) */
+					if (pLoopUnit->isBarbarian())
+						pLoopUnit->kill(false, /*pNewCity->getPlayer()*/NO_PLAYER);
+					else pLoopUnit->jumpToNearestValidPlot(); // </advc.101>
 				}
 			}
 		}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1411

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3043
# Commit: ef51ddbbffbc9f0aabe744310de1d4ed9edd27e4
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 0edf3a61417f917f0f043bdfa7f9903d11b1493b
# Title: Skip tile-reveal from map trades during Auto Play profiling
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
12	1	CvGameCoreDLL/CvDeal.cpp
1	1	CvGameCoreDLL/Project/AdvCiv.vcxproj
3	1	CvGameCoreDLL/Project/Makefile.project

[PATCHES]

diff --git a/CvGameCoreDLL/CvDeal.cpp b/CvGameCoreDLL/CvDeal.cpp
index aa96ae61b..e4d4d594a 100644
--- a/CvGameCoreDLL/CvDeal.cpp
+++ b/CvGameCoreDLL/CvDeal.cpp
@@ -789,7 +789,18 @@ bool CvDeal::startTrade(TradeData trade, PlayerTypes eFromPlayer, PlayerTypes eT
 		break;
 
 	case TRADE_MAPS:
-	{
+	{	// <advc.003o>
+		#ifdef PROFILE_AI_AUTO_PLAY
+		/*	Implementing map trades for the active player is very slow due to
+			graphics updates, which, it seems could only be optimized in the EXE.
+			Don't want that to skew DLL profiling on AI Auto Play. */
+		if (GC.getGame().getAIAutoPlay() > 0 &&
+			GC.getGame().getActiveTeam() == TEAMID(eToPlayer))
+		{
+			break;
+		}
+		#endif
+		// </advc.003o>
 		PROFILE("CvDeal::startTrade.MAPS"); // advc
 		CvMap const& kMap = GC.getMap();
 		for (int i = 0; i < kMap.numPlots(); i++)

diff --git a/CvGameCoreDLL/Project/AdvCiv.vcxproj b/CvGameCoreDLL/Project/AdvCiv.vcxproj
index d6e3b87a2..707bdadc8 100644
--- a/CvGameCoreDLL/Project/AdvCiv.vcxproj
+++ b/CvGameCoreDLL/Project/AdvCiv.vcxproj
@@ -528,7 +528,7 @@ nmake precompile /NOLOGO
 bin\jom build /NOLOGO</NMakeReBuildCommandLine>
     <NMakeCleanCommandLine>set TARGET=Profile
 nmake clean /NOLOGO</NMakeCleanCommandLine>
-    <AdditionalOptions>/DWIN32 /D_WINDOWS /D_USRDLL /DNDEBUG /DFINAL_RELEASE /DUSE_DATA_DIR /D_CODE_EDITOR /DUSE_TSC_PROFILER /DUSE_INTERNAL_PROFILER</AdditionalOptions>
+    <AdditionalOptions>/DWIN32 /D_WINDOWS /D_USRDLL /DNDEBUG /DFINAL_RELEASE /DUSE_DATA_DIR /D_CODE_EDITOR /DUSE_TSC_PROFILER /DUSE_INTERNAL_PROFILER /DPROFILE_AI_AUTO_PLAY</AdditionalOptions>
   </PropertyGroup>
   <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug-fast|Win32'">
     <NMakePreprocessorDefinitions>WIN32;_DEBUG;$(NMakePreprocessorDefinitions)</NMakePreprocessorDefinitions>

diff --git a/CvGameCoreDLL/Project/Makefile.project b/CvGameCoreDLL/Project/Makefile.project
index 55447e5c5..40b6ec1d4 100644
--- a/CvGameCoreDLL/Project/Makefile.project
+++ b/CvGameCoreDLL/Project/Makefile.project
@@ -12,6 +12,8 @@ PROJECT_Debug_CFLAGS = /Ob1
 # Time Stamp Counter profiler: /DUSE_TSC_PROFILER
 # External profiler, e.g. Very Sleepy: No profile flags needed; profile builds always use Z7.
 # Standard Firaxis profiler (not sure if this can work w/o access to the EXE): /DFP_PROFILE_ENABLE
-PROJECT_Profile_CFLAGS = /DUSE_INTERNAL_PROFILER
+# The Auto Play flag disables graphics updates known to be rare and costly b/c they may
+#   skew profiling of the DLL. So far only affects map trades involving the active player.
+PROJECT_Profile_CFLAGS = /DPROFILE_AI_AUTO_PLAY
 PROJECT_LDFLAGS = /DEF:CvGameCoreDLL.def
 PRECOMPILE_DEPENDENCIES =

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2660

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2618
# Commit: bba7439f12a4f48bb8c33d2b90b6e54e8ba7a1cf
# History segment: pre-SAS AdvCiv history
# Parent used for diff: d95244177986b1508017957cb2e6523a0388bd09
# Title: Fix main map blackout upon drafting
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	2	CvGameCoreDLL/CvCity.cpp
2	2	CvGameCoreDLL/CvUnit.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 037953ce7..b15b1bac8 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -2775,10 +2775,13 @@ void CvCity::conscript()
 	if (pUnit != NULL)
 	{
 		if (GC.getGame().getActivePlayer() == getOwner() &&
-			 !CvPlot::isAllFog()) // advc.706
+			isHuman()) // advc.127, advc.706
 		{
-			gDLL->UI().lookAt(getPlot().getPoint(), CAMERALOOKAT_NORMAL); // K-Mod
+			/*	advc.001o: Conscription had caused the main map to black out.
+				The true cause of that problem may lie elsewhere, but switching
+				these two statements is at least a workaround. */
 			gDLL->UI().selectUnit(pUnit, true, false, true);
+			gDLL->UI().lookAt(getPlot().getPoint(), CAMERALOOKAT_NORMAL); // K-Mod
 		}
 		if (gCityLogLevel >= 2 && !isHuman()) logBBAI("      City %S does conscript of a %S at cost of %d pop, %d anger", getName().GetCString(), pUnit->getName().GetCString(), iPopChange, iAngerLength); // BETTER_BTS_AI_MOD, AI logging, 10/02/09, jdog5000
 	}

diff --git a/CvGameCoreDLL/CvUnit.cpp b/CvGameCoreDLL/CvUnit.cpp
index 69c415ef3..ac1a9b97b 100644
--- a/CvGameCoreDLL/CvUnit.cpp
+++ b/CvGameCoreDLL/CvUnit.cpp
@@ -5005,8 +5005,8 @@ bool CvUnit::found()
 {
 	if (!canFound(plot()))
 		return false;
-	if (GC.getGame().getActivePlayer() == getOwner()
-		&& !CvPlot::isAllFog()) // advc.706
+	if (GC.getGame().getActivePlayer() == getOwner() &&
+		isHuman()) // advc.127, advc.706
 	{
 		gDLL->UI().lookAt(getPlot().getPoint(), CAMERALOOKAT_NORMAL);
 	}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1527

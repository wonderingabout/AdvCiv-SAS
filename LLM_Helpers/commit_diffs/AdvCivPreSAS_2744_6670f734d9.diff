# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2744
# Commit: 6670f734d92dd60204b552fc6bb32c7d04e423b6
# History segment: pre-SAS AdvCiv history
# Parent used for diff: d4066d302e5ad3aefc3f3cf2fbf9d8e6bf667563
# Title: BUG option for disabling savemap key combination
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	0	Assets/Config/BUG Main Interface.xml
2	0	Assets/Python/BUG/Tabs/BugMapOptionsTab.py
5	5	Assets/Python/Contrib/AIAutoPlay.py
11	2	Assets/Python/Contrib/savemap.py
18	0	Assets/XML/Text/BUG Main Interface Options.xml

[PATCHES]

diff --git a/Assets/Config/BUG Main Interface.xml b/Assets/Config/BUG Main Interface.xml
index dd3a8463c..cbb877485 100644
--- a/Assets/Config/BUG Main Interface.xml	
+++ b/Assets/Config/BUG Main Interface.xml	
@@ -148,6 +148,12 @@
 				<change module="MinimapOptions" function="updateUnitsOnMinimap"/>
 			</option>
 
+			<!-- advc.savem: -->
+			<option id="EnableSavemap" key="Enable Savemap" 
+					type="boolean" default="True"
+					get="isSavemapEnabled">
+			</option>
+
 			<!-- Movement Points -->
 			<option id="UnitMovementPointsFraction" key="Unit Movement Points Fraction" 
 					type="boolean" default="True"

diff --git a/Assets/Python/BUG/Tabs/BugMapOptionsTab.py b/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
index e1ff220dd..0db31f034 100644
--- a/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
+++ b/Assets/Python/BUG/Tabs/BugMapOptionsTab.py
@@ -72,6 +72,8 @@ class BugMapOptionsTab(BugOptionsTab.BugOptionsTab):
 		# </advc.002a>
 		self.addLabel(screen, right, "Misc", "Misc:")
 		self.addCheckbox(screen, right, "EventSigns__Enabled")
+		# advc.savem
+		self.addCheckbox(screen, right, "MainInterface__EnableSavemap")
 		
 		#self.addCheckbox(screen, right, "Actions__IgnoreHarmlessBarbarians")

diff --git a/Assets/Python/Contrib/AIAutoPlay.py b/Assets/Python/Contrib/AIAutoPlay.py
index 55c356f83..2dd8e6fa9 100644
--- a/Assets/Python/Contrib/AIAutoPlay.py
+++ b/Assets/Python/Contrib/AIAutoPlay.py
@@ -267,12 +267,12 @@ class AIAutoPlay :
 				if theKey == int(InputTypes.KB_B):
 					self.bBenchmark = True # </advc>
 				self.toAIChooser()
-
-		if theKey == int(InputTypes.KB_M):
+		# advc.127: Disable this feature
+		#if theKey == int(InputTypes.KB_M):
 			# Toggle auto moves
-			if self.LOG_DEBUG:
-				CyInterface().addImmediateMessage("Moving your units...","")
-			game.setAIAutoPlay(1)
+		#	if self.LOG_DEBUG:
+		#		CyInterface().addImmediateMessage("Moving your units...","")
+		#	game.setAIAutoPlay(1)
 		if theKey == int(InputTypes.KB_O):
 			doRefortify( game.getActivePlayer() )

diff --git a/Assets/Python/Contrib/savemap.py b/Assets/Python/Contrib/savemap.py
index 7f54c5983..9c151665b 100644
--- a/Assets/Python/Contrib/savemap.py
+++ b/Assets/Python/Contrib/savemap.py
@@ -8,14 +8,23 @@ from CvPythonExtensions import *
 import os
 # <advc.savem>
 import BugPath
-import BugUtil # </advc.savem>
+import BugUtil
+import BugCore
+import CvEventInterface
+import BugEventManager
+# </advc.savem>
 
 gc = CyGlobalContext()
 # <advc.savem>
 pathnames = []
 
 def savemap(argsList=None):
-
+	# <advc.savem>
+	if gc.getGame().isNetworkMultiPlayer() and not CvEventInterface.getEventManager().isCheatsEnabled():
+		return
+	if not BugCore.game.MainInterface.isSavemapEnabled():
+		return
+	# </advc.savem>
 	# First choice: Mod folder
 	pathnames.append(str(BugPath.getModDir()) + "\\PrivateMaps\\")
 	# Under MyGames. Can't load it from there into AdvCiv w/o toggling NoCustomAssets.

diff --git a/Assets/XML/Text/BUG Main Interface Options.xml b/Assets/XML/Text/BUG Main Interface Options.xml
index e15b31301..f69e0bba1 100644
--- a/Assets/XML/Text/BUG Main Interface Options.xml	
+++ b/Assets/XML/Text/BUG Main Interface Options.xml	
@@ -429,6 +429,24 @@
 		<Spanish>If checked, outside their owners' borders units are shown as colored blips on the main screen minimap.</Spanish>
 	</TEXT>
 
+	<!-- advc.savem: -->
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__ENABLESAVEMAP_TEXT</Tag>
+		<English>Enable Savemap</English>
+		<French>Enable Savemap</French>
+		<German>Savemap-Funktion aktivieren</German>
+		<Italian>Enable Savemap</Italian>
+		<Spanish>Enable Savemap</Spanish>
+	</TEXT>
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__ENABLESAVEMAP_HOVER</Tag>
+		<English>If checked, [COLOR_HIGHLIGHT_TEXT]Alt+Shift+M[COLOR_REVERT] can be pressed at any point of a (non-network) game to export the map into a map script and save it to [COLOR_BUILDING_TEXT]AdvCiv\PrivateMaps[COLOR_REVERT] or (as a fallback) [COLOR_BUILDING_TEXT]My Games\Beyond the Sword\PublicMaps[COLOR_REVERT]. Disabling the key combination has no performance benefit, but makes sure that it isn't pressed by accident.</English>
+		<French>If checked, [COLOR_HIGHLIGHT_TEXT]Alt+Shift+M[COLOR_REVERT] can be pressed at any point of a (non-network) game to export the map into a map script and save it to [COLOR_BUILDING_TEXT]AdvCiv\PrivateMaps[COLOR_REVERT] or (as a fallback) [COLOR_BUILDING_TEXT]My Games\Beyond the Sword\PublicMaps[COLOR_REVERT]. Disabling the key combination has no performance benefit, but makes sure that it isn't pressed by accident.</French>
+		<German>Exportiert die aktuelle Karte als spielbares Python-Skript, wenn [COLOR_HIGHLIGHT_TEXT]Alt+Umschalt+M[COLOR_REVERT] gedr�ckt wird. Das Skript wird unter [COLOR_BUILDING_TEXT]AdvCiv\PrivateMaps[COLOR_REVERT] oder (Ausweichl�sung) [COLOR_BUILDING_TEXT]My Games\Beyond the Sword\PublicMaps[COLOR_REVERT] abgelegt. Das Deaktivieren der Tastenkombination bringt keine Zeitersparnis, stellt aber sicher, dass sie nicht versehentlich gedr�ckt wird. In Netzwerk-Spielen ist sie in jedem Fall deaktiviert.</German>
+		<Italian>If checked, [COLOR_HIGHLIGHT_TEXT]Alt+Shift+M[COLOR_REVERT] can be pressed at any point of a (non-network) game to export the map into a map script and save it to [COLOR_BUILDING_TEXT]AdvCiv\PrivateMaps[COLOR_REVERT] or (as a fallback) [COLOR_BUILDING_TEXT]My Games\Beyond the Sword\PublicMaps[COLOR_REVERT]. Disabling the key combination has no performance benefit, but makes sure that it isn't pressed by accident.</Italian>
+		<Spanish>If checked, [COLOR_HIGHLIGHT_TEXT]Alt+Shift+M[COLOR_REVERT] can be pressed at any point of a (non-network) game to export the map into a map script and save it to [COLOR_BUILDING_TEXT]AdvCiv\PrivateMaps[COLOR_REVERT] or (as a fallback) [COLOR_BUILDING_TEXT]My Games\Beyond the Sword\PublicMaps[COLOR_REVERT]. Disabling the key combination has no performance benefit, but makes sure that it isn't pressed by accident.</Spanish>
+	</TEXT>
+
 	<TEXT>
 		<Tag>TXT_KEY_BUG_OPT_MAININTERFACE__PROGRESSBARSTICKMARKS_TEXT</Tag>
 		<English>Tick Marks</English>

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=6097

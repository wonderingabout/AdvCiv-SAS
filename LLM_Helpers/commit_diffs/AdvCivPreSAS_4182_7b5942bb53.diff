# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4182
# Commit: 7b5942bb5343eb4310ac81e566fc449d8f0f6f81
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 2eddf22d1761aa426ebe581908a3143069ce542c
# Title: Remove unused preprocessor define from makefile
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	CvGameCoreDLL/Project/Makefile

[PATCHES]

diff --git a/CvGameCoreDLL/Project/Makefile b/CvGameCoreDLL/Project/Makefile
index d8ddcfdf2..ef487f8a7 100644
--- a/CvGameCoreDLL/Project/Makefile
+++ b/CvGameCoreDLL/Project/Makefile
@@ -24,7 +24,7 @@
 #   To make it easier to switch, temporarily, to /Zd, which links much faster.
 # PROJECT_Profile_CFLAGS, PROJECT_Debug_CFLAGS, PROJECT_DebugOpt_CFLAGS added to Makefile.project.
 # For selecting a profiler and for balancing speed vs. fidelity of debug builds.
-# Removed unused /DCVGAMECOREDLL_EXPORTS from Debug_DEFINES
+# Removed unused /DCVGAMECOREDLL_EXPORTS
 # devolution (Erik) has added the target 'Final_Release' with
 #   whole-program optimization. [To avoid confusion, I've removed the Firaxis flag
 #   "FINAL_RELEASE", which had merely disabled the OutputDebugString function from
@@ -88,7 +88,7 @@ FD="bin\fastdep.exe"
 
 #### DEFINES ####
 Debug_DEFINES=/DWIN32 /D_WINDOWS /D_USRDLL /D_DEBUG
-Release_DEFINES=/DWIN32 /D_WINDOWS /D_USRDLL /DCVGAMECOREDLL_EXPORTS /DNDEBUG
+Release_DEFINES=/DWIN32 /D_WINDOWS /D_USRDLL /DNDEBUG
 ## Optional: Copy the flags above into "Additional options" in NMAKE properties (Configuration Properties)
 ## to make VC++ aware of what is defined and what isn't, which affects the help popup and red lines.
 ## Use the Release_DEFINES for all targets except debug. Assert adds /DFASSERT_ENABLE,

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1343

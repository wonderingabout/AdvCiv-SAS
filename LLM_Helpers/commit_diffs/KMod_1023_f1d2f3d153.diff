# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1023
# Commit: f1d2f3d153e121adf82d2d181d2dca38a6254707
# History segment: K-Mod history
# Parent used for diff: 0abeea3ba2260b630b00b4e646f6c451c7ab7524
# Title: Reversed the effect of bPrereqReligion for units and buildings.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	CvGameCoreDLL/CvCity.cpp
2	1	CvGameCoreDLL/CvPlot.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 1060fbd59..fd43d6d39 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -1995,7 +1995,8 @@ bool CvCity::canConstruct(BuildingTypes eBuilding, bool bContinue, bool bTestVis
 
 	if (GC.getBuildingInfo(eBuilding).isPrereqReligion())
 	{
-		if (getReligionCount() > 0)
+		//if (getReligionCount() > 0)
+		if (getReligionCount() == 0) // K-Mod
 		{
 			return false;
 		}

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index b0f9b9f59..66246ae17 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -10051,7 +10051,8 @@ bool CvPlot::canTrain(UnitTypes eUnit, bool bContinue, bool bTestVisible) const
 
 	if (GC.getUnitInfo(eUnit).isPrereqReligion())
 	{
-		if (NULL == pCity || pCity->getReligionCount() > 0)
+		//if (NULL == pCity || pCity->getReligionCount() > 0)
+		if (NULL == pCity || pCity->getReligionCount() == 0) // K-Mod
 		{
 			return false;
 		}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=981

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1010
# Commit: e322e2ff8ee64183daf6781130c1498b888cca19
# History segment: K-Mod history
# Parent used for diff: 500fe480cc485bd8b6aeb0de807d6b1e87fded1c
# Title: Fixed a crash in the Hall of Fame screen from the original BtS code.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
8	0	Assets/Python/Screens/CvHallOfFameScreen.py

[PATCHES]

diff --git a/Assets/Python/Screens/CvHallOfFameScreen.py b/Assets/Python/Screens/CvHallOfFameScreen.py
index 4ca74828a..2ab7a94f8 100644
--- a/Assets/Python/Screens/CvHallOfFameScreen.py
+++ b/Assets/Python/Screens/CvHallOfFameScreen.py
@@ -63,6 +63,7 @@ class CvHallOfFameScreen:
 		self.DROPDOWN_SPACING_Y = 50
 								
 		self.nWidgetCount = 0
+		self.infoList = [] # K-Mod
 				
 		self.bAllowReplay = False
 		
@@ -244,6 +245,13 @@ class CvHallOfFameScreen:
 				
 		screen = self.getScreen()
 
+		# K-Mod. Delete old widgets.
+		for i in range(len(self.infoList)):
+			szButtonName = self.REPLAY_BUTTON_ID + str(i)
+			screen.deleteWidget(szButtonName)
+		screen.deleteWidget(self.TABLE_ID)
+		# K-Mod end
+		
 		screen.addTableControlGFC(self.TABLE_ID, 10, 2, 2 * self.DROPDOWN_SPACING_Y + self.DROPDOWN_Y, 1018, 545, True, True, 16, 16, TableStyles.TABLE_STYLE_STANDARD);
 		screen.enableSelect(self.TABLE_ID, False)
 		screen.enableSort(self.TABLE_ID)

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=961

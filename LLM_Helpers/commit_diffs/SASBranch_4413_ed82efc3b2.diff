# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4413
# Commit: ed82efc3b27b922fe85087ff8d2943a76cf7629e
# History segment: AdvCiv-SAS branch history
# Parent used for diff: ff73bdf88e63510df2d964caec4a59380318a636
# Title: Close BtS leak of rival cities in Fog of War (Go To mode)
# Full commit message/metadata: see _SNAPSHOT_CONTEXT/git_log_since_tracked_advciv_sas_log.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
11	0	CvGameCoreDLL/CvGameTextMgr.cpp
0	2	CvGameCoreDLL/CvUnit.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameTextMgr.cpp b/CvGameCoreDLL/CvGameTextMgr.cpp
index 4a143e0cc..0f673e637 100644
--- a/CvGameCoreDLL/CvGameTextMgr.cpp
+++ b/CvGameCoreDLL/CvGameTextMgr.cpp
@@ -3068,6 +3068,17 @@ void CvGameTextMgr::setPlotHelp(CvWStringBuffer& szString, CvPlot const& kPlot)
 		}
 	}
 	CvUnit const* pHeadSelectedUnit = gDLL->UI().getHeadSelectedUnit(); // advc
+	// advc.004h: (Could show founding border in Go To mode here like BtS does)
+	/*static CvPlot const* pPrevPlot = NULL;
+	if (&kPlot != pPrevPlot)
+	{
+		pPrevPlot = &kPlot;
+		if (pHeadSelectedUnit != NULL &&
+			gDLL->UI().getInterfaceMode() == INTERFACEMODE_GO_TO)
+		{
+			pHeadSelectedUnit->updateFoundingBorder();
+		}
+	}*/
 	// <advc.012>
 	TeamTypes const eDefTeam = (eRevealedOwner != NO_PLAYER ?
 			GET_PLAYER(eRevealedOwner).getTeam() :

diff --git a/CvGameCoreDLL/CvUnit.cpp b/CvGameCoreDLL/CvUnit.cpp
index 8d3876cef..8f10dbbec 100644
--- a/CvGameCoreDLL/CvUnit.cpp
+++ b/CvGameCoreDLL/CvUnit.cpp
@@ -2052,8 +2052,6 @@ void CvUnit::updateFoundingBorder(bool bForceClear) const
 	if (!GC.getGame().isFinalInitialized())
 		return;
 	int iMode = BUGOption::getValue("MainInterface__FoundingBorder", 2);
-	if(BUGOption::isEnabled("MainInterface__FoundingYields", false) && iMode == 1)
-		return; // BtS behavior
 	gDLL->getEngineIFace()->clearAreaBorderPlots(AREA_BORDER_LAYER_FOUNDING_BORDER);
 	if(bForceClear || iMode <= 0 || !isFound())
 		return;

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1441

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 679
# Commit: 9c580de1539c866491344d63a9d02cfc88dfedfd
# History segment: K-Mod history
# Parent used for diff: 4d52bbf50f684fa08d0e6f34c23d6229537098f4
# Title: Increased the barbarian tech rate from 3% per turn, to 4% per turn. The barbarians will no longer get any free research for the first half of their pre-spawn...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	1	Assets/xml/GlobalDefinesAlt.xml
2	2	CvGameCoreDLL/CvTeam.cpp

[PATCHES]

diff --git a/Assets/xml/GlobalDefinesAlt.xml b/Assets/xml/GlobalDefinesAlt.xml
index d82977d75..d8b3ffe1e 100644
--- a/Assets/xml/GlobalDefinesAlt.xml
+++ b/Assets/xml/GlobalDefinesAlt.xml
@@ -15,12 +15,16 @@
 	</Define>
 	<Define>
 		<DefineName>BASE_REVOLT_OCCUPATION_TURNS</DefineName>
-		<iDefineIntVal>3</iDefineIntVal> <!-- increased from 2, because of changes that I've made to its useage -->
+		<iDefineIntVal>3</iDefineIntVal> <!-- increased from 2, because of changes that I've made to its usage -->
 	</Define>
 	<Define>
 		<DefineName>EVENT_MESSAGE_STAGGER_TIME</DefineName>
 		<iDefineIntVal>2</iDefineIntVal> <!-- decreased from 3 -->
 	</Define>
+	<Define>
+		<DefineName>BARBARIAN_FREE_TECH_PERCENT</DefineName>
+		<iDefineIntVal>4</iDefineIntVal> <!-- increased from 3, to compensate for other changes -->
+	</Define>
 	<Define>
 		<DefineName>FROZEN_TERRAIN</DefineName>
 		<DefineTextVal>TERRAIN_SNOW</DefineTextVal>

diff --git a/CvGameCoreDLL/CvTeam.cpp b/CvGameCoreDLL/CvTeam.cpp
index 5d77f8405..f74bf168b 100644
--- a/CvGameCoreDLL/CvTeam.cpp
+++ b/CvGameCoreDLL/CvTeam.cpp
@@ -953,10 +953,10 @@ void CvTeam::doTurn()
 
 	if (isBarbarian())
 	{
-		// K-Mod. Delay the start of the barbarian research. (this is an experimental change, and is currently disabled)
+		// K-Mod. Delay the start of the barbarian research. (This is an experimental change. It is currently compensated by an increase in the barbarian tech rate.)
 		const CvPlayerAI& kBarbPlayer = GET_PLAYER(getLeaderID());
 		const CvGame& kGame = GC.getGameINLINE();
-		//if (kGame.getElapsedGameTurns() >= GC.getHandicapInfo(kGame.getHandicapType()).getBarbarianCreationTurnsElapsed() * GC.getGameSpeedInfo(kGame.getGameSpeedType()).getBarbPercent() / 400)
+		if (kGame.getElapsedGameTurns() >= GC.getHandicapInfo(kGame.getHandicapType()).getBarbarianCreationTurnsElapsed() * GC.getGameSpeedInfo(kGame.getGameSpeedType()).getBarbPercent() / 200)
 		{
 			for (TechTypes i = (TechTypes)0; i < GC.getNumTechInfos(); i = (TechTypes)(i+1))
 			{

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=2027

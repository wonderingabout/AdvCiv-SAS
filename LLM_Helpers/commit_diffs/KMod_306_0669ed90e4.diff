# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 306
# Commit: 0669ed90e41dabae54376e7fc3a5642a8d67ce52
# History segment: K-Mod history
# Parent used for diff: 1d99057535752f9793533bd41becda30431f45f5
# Title: Fixed a bug which caused Civ4 to crash if the player started a game, exited back to the menu, and then started a new game with Rapid Unit Cycling enabled. Re...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	1	CvGameCoreDLL/CvGameInterface.cpp
3	0	CvGameCoreDLL/CvPlayerAI.cpp
0	1	CvGameCoreDLL/CvSelectionGroup.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index 3c9de7c30..4ec612cd2 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -751,8 +751,11 @@ void CvGame::cycleSelectionGroups_delayed(int iDelay, bool bIncremental, bool bD
 {
 	PROFILE_FUNC(); // I'm just hoping that the python call doesn't hurt the respose times
 	// Only rapid-cycle when not doing auto-play.
+	// Also note, cycleSelectionGroups currently causes a crash if the game is not initialised.
+	// (and this function is indirectly called during the set of up a new game - so we currently need that init check.)
 	PlayerTypes eActive = getActivePlayer();
-	if (eActive != NO_PLAYER && GET_PLAYER(eActive).isHuman() &&
+	if (GC.getGameINLINE().isFinalInitialized() &&
+		eActive != NO_PLAYER && GET_PLAYER(eActive).isHuman() &&
 		getBugOptionBOOL("MainInterface__RapidUnitCycling", false, "RAPID_UNIT_CYCLING"))
 	{
 		if (!bDelayOnly)

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 5379b3f71..9045b2ee2 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -16353,6 +16353,9 @@ void CvPlayerAI::AI_doDiplo()
 															ourList.concatenate(ourCounter);
 															theirList.concatenate(theirCounter);
 															bDeal = true;
+															// K-Mod note: the following log call will miss the second name if it is a non-standard name (for example, a human player name).
+															// This is a problem in the way the getName function is implimented. Unfortunately, the heart of the problem is in the signature of
+															// some of the dllexport functions - and so it can't easily be fixed.
 															if (gTeamLogLevel >= 2) logBBAI("    %S makes a deal with %S using AI_counterPropose. (%d : %d)", getName(0), GET_PLAYER((PlayerTypes)iI).getName(0), ourList.getLength(), theirList.getLength());
 														}
 													}

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index e282f7e6b..479fdf114 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -1464,7 +1464,6 @@ void CvSelectionGroup::startMission()
 						if (pLoopUnit->espionage((EspionageMissionTypes)headMissionQueueNode()->m_data.iData1, headMissionQueueNode()->m_data.iData2))
 						{
 							bAction = true;
-							pLoopUnit->getGroup()->AI_setMissionAI(NO_MISSIONAI, 0, 0); // K-Mod: mission accomplished.
 						}
 						pUnitNode = NULL; // allow one unit at a time to do espionage
 						break;

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2578

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1339
# Commit: 2800d69a12c2d59963adf123d2c6f558c81c1978
# History segment: pre-SAS AdvCiv history
# Parent used for diff: d6445a4c0ba8148e0b49c90fa63d7e596b161741
# Title: Fixed a sound issue in change 106b:
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	1	Assets/Python/Contrib/Civ4lerts.py
2	1	Assets/Python/Contrib/MoreCiv4lerts.py
1	1	CvGameCoreDLL/AdvCiv4lerts.cpp
3	2	CvGameCoreDLL/CvDLLInterfaceIBase2.cpp
4	0	CvGameCoreDLL/CvGame.cpp
2	1	CvGameCoreDLL/CvPlayer.cpp
14	1	CvGameCoreDLL/CvTalkingHeadMessage.cpp
2	2	CvGameCoreDLL/CvTalkingHeadMessage.h

[PATCHES]

diff --git a/Assets/Python/Contrib/Civ4lerts.py b/Assets/Python/Contrib/Civ4lerts.py
index 4f89727c4..f2a13ac36 100644
--- a/Assets/Python/Contrib/Civ4lerts.py
+++ b/Assets/Python/Contrib/Civ4lerts.py
@@ -158,7 +158,8 @@ def addMessage(iPlayer, szString, szIcon, iFlashX=-1, iFlashY=-1, bOffArrow=Fals
 		return # </advc.706>
 	# advc.106c: Reduced time from LONG to normal
 	eventMessageTimeLong = gc.getDefineINT("EVENT_MESSAGE_TIME")
-	CyInterface().addMessage(iPlayer, True, eventMessageTimeLong,
+	# advc.106: Set bForce to False
+	CyInterface().addMessage(iPlayer, False, eventMessageTimeLong,
 							 szString, None, InterfaceMessageTypes.MESSAGE_TYPE_INFO, 
 							 szIcon, ColorTypes(-1),
 							 iFlashX, iFlashY, bOffArrow, bOnArrow)

diff --git a/Assets/Python/Contrib/MoreCiv4lerts.py b/Assets/Python/Contrib/MoreCiv4lerts.py
index 03faf4582..fd8aa1895 100644
--- a/Assets/Python/Contrib/MoreCiv4lerts.py
+++ b/Assets/Python/Contrib/MoreCiv4lerts.py
@@ -64,7 +64,8 @@ class AbstractMoreCiv4lertsEvent(object):
 			# the iActivePlayer (on the caller side) should always
 			# be iOwner. Tbd.: Remove the iPlayer attribute from all
 			# _addMessage... functions.
-			CyInterface().addMessage(self.iOwner, True, eventMessageTimeLong, szString, None, 0, szIcon, ColorTypes(iColor), iFlashX, iFlashY, bOffArrow, bOnArrow)
+			# advc.106: Set bForce to False
+			CyInterface().addMessage(self.iOwner, False, eventMessageTimeLong, szString, None, 0, szIcon, ColorTypes(iColor), iFlashX, iFlashY, bOffArrow, bOnArrow)
 
 class MoreCiv4lertsEvent( AbstractMoreCiv4lertsEvent):

diff --git a/CvGameCoreDLL/AdvCiv4lerts.cpp b/CvGameCoreDLL/AdvCiv4lerts.cpp
index 151c1ee27..603f0f065 100644
--- a/CvGameCoreDLL/AdvCiv4lerts.cpp
+++ b/CvGameCoreDLL/AdvCiv4lerts.cpp
@@ -47,7 +47,7 @@ void AdvCiv4lert::msg(CvWString s, LPCSTR icon, int x, int y, int goodOrBad) con
 	else if(goodOrBad < 0)
 		color = GC.getInfoTypeForString("COLOR_RED");
 	bool arrows = (icon != NULL);
-	gDLL->getInterfaceIFace()->addHumanMessage(ownerId, true,
+	gDLL->getInterfaceIFace()->addHumanMessage(ownerId, false,
 			GC.getEVENT_MESSAGE_TIME(), s, NULL,
 			force ? MESSAGE_TYPE_MAJOR_EVENT : MESSAGE_TYPE_INFO, // advc.127
 			icon, (ColorTypes)color, x, y, arrows, arrows);

diff --git a/CvGameCoreDLL/CvDLLInterfaceIBase2.cpp b/CvGameCoreDLL/CvDLLInterfaceIBase2.cpp
index 9b558bfd0..8b25ebe72 100644
--- a/CvGameCoreDLL/CvDLLInterfaceIBase2.cpp
+++ b/CvGameCoreDLL/CvDLLInterfaceIBase2.cpp
@@ -33,8 +33,9 @@ void CvDLLInterfaceIFaceBase::addHumanMessage(PlayerTypes ePlayer, bool bForce,
 					immediately, whereas bForce=false seems to delay the
 					announcement until the start of the next turn (until the
 					preceding event has been on display long enough?).
-					bForce=true should make Auto Play easier to follow. */
-					true, iLength, szString, pszSound, eType, NULL,
+					bForce=true should make Auto Play easier to follow.
+					Also set pszSound to NULL; no sounds during Auto Play. */
+					true, iLength, szString, NULL, eType, NULL,
 					//NO_COLOR, -1, -1, // (K-Mod)
 					/*  advc.127: Don't want stuff to flash during Auto Play,
 						but pszIcon=NULL is enough to prevent this. No need to

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index c9fee464a..c7b2fb910 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -9436,6 +9436,10 @@ void CvGame::read(FDataStreamBase* pStream)
 	pStream->Read(&m_iInitTech);
 	pStream->Read(&m_iInitWonders);
 	pStream->Read(&m_iAIAutoPlay);
+	/*  advc.127: m_iAIAutoPlay really shouldn't be stored in savegames.
+		Auto Play is off when a savegame is loaded, even if it's an autosave
+		created during Auto Play, so m_iAIAutoPlay needs to be 0. */
+	m_iAIAutoPlay = 0;
 	pStream->Read(&m_iGlobalWarmingIndex); // K-Mod
 	pStream->Read(&m_iGwEventTally); // K-Mod

diff --git a/CvGameCoreDLL/CvPlayer.cpp b/CvGameCoreDLL/CvPlayer.cpp
index 47b5d0194..a74960ac3 100644
--- a/CvGameCoreDLL/CvPlayer.cpp
+++ b/CvGameCoreDLL/CvPlayer.cpp
@@ -14803,7 +14803,8 @@ void CvPlayer::addMessage(const CvTalkingHeadMessage& message)
 			before postProcessBeginTurnEvents gets called. */
 		CvTalkingHeadMessage* copy = new CvTalkingHeadMessage(message.getTurn(),
 				message.getLength(), message.getDescription(),
-				NULL, // The sound is already played when the event triggers
+				// Don't play it twice
+				message.getSoundPlayed() ? NULL : message.getSound(),
 				MESSAGE_TYPE_MAJOR_EVENT, message.getIcon(), message.getFlashColor(),
 				message.getX(), message.getY(), message.getOffScreenArrows(),
 				message.getOnScreenArrows());

diff --git a/CvGameCoreDLL/CvTalkingHeadMessage.cpp b/CvGameCoreDLL/CvTalkingHeadMessage.cpp
index 42885bdb5..0ee984436 100644
--- a/CvGameCoreDLL/CvTalkingHeadMessage.cpp
+++ b/CvGameCoreDLL/CvTalkingHeadMessage.cpp
@@ -17,7 +17,8 @@ CvTalkingHeadMessage::CvTalkingHeadMessage(int iMessageTurn, int iLen, LPCWSTR p
 	m_eMessageType(eType),
 	m_eFromPlayer(NO_PLAYER),
 	m_eTarget(NO_CHATTARGET),
-	m_bShown(false)
+	m_bShown(false),
+	bSoundPlayed(false) // advc.106b
 {
 }
 
@@ -48,6 +49,9 @@ void CvTalkingHeadMessage::read(FDataStreamBase& stream)
 	stream.Read(&iType);
 	m_eTarget = (ChatTargetTypes)iType;
 	stream.Read(&m_bShown);
+	/*  advc.106b: I don't think we ever want to play a sound after loading
+		a savegame */
+	bSoundPlayed = true;
 }
 
 void CvTalkingHeadMessage::write(FDataStreamBase& stream) const
@@ -80,6 +84,9 @@ void CvTalkingHeadMessage::setDescription(CvWString pszDescription)
 
 const CvString& CvTalkingHeadMessage::getSound() const
 {
+	/*  advc.106b: A hack that relies on the EXE playing the sound after calling
+		getSound */
+	const_cast<CvTalkingHeadMessage*>(this)->bSoundPlayed = true;
 	return (m_szSound);
 }
 
@@ -267,3 +274,9 @@ void CvTalkingHeadMessage::setShown(bool bShown)
 {
 	m_bShown = bShown;
 }
+
+// <advc.106b>
+bool CvTalkingHeadMessage::getSoundPlayed() const
+{
+	return bSoundPlayed;
+} // </advc.106b>

diff --git a/CvGameCoreDLL/CvTalkingHeadMessage.h b/CvGameCoreDLL/CvTalkingHeadMessage.h
index 49588c96a..9ed28805d 100644
--- a/CvGameCoreDLL/CvTalkingHeadMessage.h
+++ b/CvGameCoreDLL/CvTalkingHeadMessage.h
@@ -41,7 +41,7 @@ public:
 	DllExport void setFromPlayer(PlayerTypes eFromPlayer);
 	DllExport bool getShown() const;
 	DllExport void setShown(bool bShown);
-
+	bool getSoundPlayed() const; // advc.106b
 	int getExpireTurn(bool human = true); // advc.700: Param added
 
 
@@ -60,7 +60,7 @@ protected:
 	PlayerTypes m_eFromPlayer;
 	ChatTargetTypes m_eTarget;
 	bool m_bShown;
-
+	bool bSoundPlayed; // advc.106b
 };
 
 #endif
\ No newline at end of file

[PATCH COVERAGE]
patchFiles=8 embeddedFiles=8 omittedFiles=0 embeddedPatchBytes=6639

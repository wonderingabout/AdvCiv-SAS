# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 671
# Commit: 51b40ab853bdbe71bee0eaa6728e712ef2ad0be8
# History segment: K-Mod history
# Parent used for diff: 844c29514a11a7d85e4d290541766ef0babfe563
# Title: Changed the functionality of double-clicking a stack of units, from grouping them all to waking them all. Changed the corresponding tip to reflect the new fu...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
9	0	Assets/xml/Text/K-Mod hints.xml
20	0	CvGameCoreDLL/CvGame.cpp
3	2	CvGameCoreDLL/CvGameInterface.cpp

[PATCHES]

diff --git a/Assets/xml/Text/K-Mod hints.xml b/Assets/xml/Text/K-Mod hints.xml
index 313698194..23d4da3e2 100644
--- a/Assets/xml/Text/K-Mod hints.xml	
+++ b/Assets/xml/Text/K-Mod hints.xml	
@@ -128,4 +128,13 @@
 	<Italian>I Barbari possono apparire in un qualsiasi spazio oscurato dalla nebbia di guerra. Non essere mai certo che uno spazio non visibile sia sicuro, anche se hai controllato durante il turno precedente!</Italian>
 	<Spanish>Los b&#225;rbaros pueden aparecer en cualquier lugar en el que una casilla est&#233; oscurecida por la niebla de la guerra, as&#237; que no supong&#225;is que una casilla que no pod&#225;is ver es segura, aunque la hay&#225;is comprobado en el turno anterior.</Spanish>
 </TEXT>
+<!-- replacement text for the double-click tip, because I've changed the functionality of double-click -->
+<TEXT>
+	<Tag>TXT_KEY_HINT_INTERFACE_6</Tag>
+	<English>Double clicking on a stack of units will wake them all up.</English>
+	<French>Double clicking on a stack of units will wake them all up.</French>
+	<German>Double clicking on a stack of units will wake them all up.</German>
+	<Italian>Double clicking on a stack of units will wake them all up.</Italian>
+	<Spanish>Double clicking on a stack of units will wake them all up.</Spanish>
+</TEXT>
 </Civ4GameText>
\ No newline at end of file

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index af3bcf860..2a57c92f9 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -2462,6 +2462,26 @@ void CvGame::selectGroup(CvUnit* pUnit, bool bShift, bool bCtrl, bool bAlt) cons
 	PROFILE_FUNC();
 
 	FAssertMsg(pUnit != NULL, "pUnit == NULL unexpectedly");
+	// K-Mod. A hack to change the functionality of double-click from select all to wake all.
+	if (bAlt && !bShift && !bCtrl && !GC.altKey())
+	{
+		// the caller says alt is pressed, but the computer says otherwise. Lets assume this is a double-click.
+		CvPlot* pUnitPlot = pUnit->plot();
+		CLLNode<IDInfo>* pUnitNode = pUnitPlot->headUnitNode();
+		while (pUnitNode != NULL)
+		{
+			CvUnit* pLoopUnit = ::getUnit(pUnitNode->m_data);
+			pUnitNode = pUnitPlot->nextUnitNode(pUnitNode);
+
+			if (pLoopUnit->getOwnerINLINE() == getActivePlayer() && pLoopUnit->isGroupHead() && pLoopUnit->isWaiting())
+			{
+				CvMessageControl::getInstance().sendDoCommand(pLoopUnit->getID(), COMMAND_WAKE, -1, -1, false);
+			}
+		}
+		gDLL->getInterfaceIFace()->selectUnit(pUnit, true, false, true);
+		return;
+	}
+	// K-Mod end
 
 	if (bAlt || bCtrl)
 	{

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index 056248884..710569c02 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -2322,7 +2322,8 @@ void CvGame::applyFlyoutMenu(const CvFlyoutMenuData& kItem)
 				{
 					CvUnit* pLoopUnit = ::getUnit(pUnitNode->m_data);
 					pUnitNode = pPlot->nextUnitNode(pUnitNode);
-					if (pLoopUnit->isGroupHead())
+					//if (pLoopUnit->isGroupHead())
+					if (pLoopUnit->isGroupHead() && pLoopUnit->getOwnerINLINE() == getActivePlayer()) // K-Mod
 					{
 						CvMessageControl::getInstance().sendDoCommand(pLoopUnit->getID(), COMMAND_WAKE, -1, -1, false);
 					}
@@ -2338,7 +2339,7 @@ void CvGame::applyFlyoutMenu(const CvFlyoutMenuData& kItem)
 				{
 					CvUnit* pLoopUnit = ::getUnit(pUnitNode->m_data);
 					pUnitNode = pPlot->nextUnitNode(pUnitNode);
-					if (pLoopUnit->isGroupHead())
+					if (pLoopUnit->isGroupHead() && pLoopUnit->getOwnerINLINE() == getActivePlayer()) // K-Mod
 					{
 						CvMessageControl::getInstance().sendPushMission(pLoopUnit->getID(), ((pLoopUnit->isFortifyable()) ? MISSION_FORTIFY : MISSION_SLEEP), -1, -1, 0, false);
 					}

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=3698

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 553
# Commit: 886bb414df0fa826c7869b001866a5e1956119f3
# History segment: K-Mod history
# Parent used for diff: 9bfa52ee151b3296dae5600740e10b0704442756
# Title: Reduced the disapperance probability for fallout from 5% per turn to 2.5% per turn. The destroy building espionage mission evaluation now uses bAllowRecusion...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	Assets/xml/Terrain/CIV4FeatureInfos.xml
3	1	CvGameCoreDLL/CvPlayerAI.cpp

[PATCHES]

diff --git a/Assets/xml/Terrain/CIV4FeatureInfos.xml b/Assets/xml/Terrain/CIV4FeatureInfos.xml
index 6c18cec2d..81977ff86 100644
--- a/Assets/xml/Terrain/CIV4FeatureInfos.xml
+++ b/Assets/xml/Terrain/CIV4FeatureInfos.xml
@@ -597,7 +597,7 @@
 			<iHealthPercent>-50</iHealthPercent>
 			<iDefense>0</iDefense>
 			<iAppearance>0</iAppearance>
-			<iDisappearance>500</iDisappearance>
+			<iDisappearance>250</iDisappearance>
 			<iGrowth>0</iGrowth>
 			<iTurnDamage>0</iTurnDamage>
 			<iWarmingDefense>0</iWarmingDefense>

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 068584792..0cd93adbe 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -13812,7 +13812,9 @@ int CvPlayerAI::AI_espionageVal(PlayerTypes eTargetPlayer, EspionageMissionTypes
 						{
 							//iValue += pCity->AI_buildingValue((BuildingTypes)iData);
 							// K-Mod
-							iValue += 2 * pCity->AI_buildingValue((BuildingTypes)iData);
+							// Note: I'm not allowing recursion in the building evaluation.
+							// This may cause the cached value to be inaccurate, but it doesn't really matter, because the building is already built!
+							iValue += 2 * pCity->AI_buildingValue((BuildingTypes)iData, 0, 0, false, false);
 							iValue *= 60 + kBuilding.getProductionCost();
 							iValue /= 100;
 						}

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1342

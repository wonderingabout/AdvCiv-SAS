# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2223
# Commit: 548f56a4468db60353a316b986546bee8d6b8b04
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 3861499f43011ac1057ad25f8c78ff52b72c0eab
# Title: Make the color of war trade alerts configurable
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
11	1	Assets/XML/Interface/CIV4ColorVals.xml
2	1	CvGameCoreDLL/AdvCiv4lerts.cpp

[PATCHES]

diff --git a/Assets/XML/Interface/CIV4ColorVals.xml b/Assets/XML/Interface/CIV4ColorVals.xml
index fcf408345..703c3d254 100644
--- a/Assets/XML/Interface/CIV4ColorVals.xml
+++ b/Assets/XML/Interface/CIV4ColorVals.xml
@@ -137,12 +137,22 @@
 			<fBlue>0.25</fBlue>
 			<fAlpha>1.00</fAlpha>
 		</ColorVal>
-		<ColorVal>
+		<!--ColorVal>
 			<Type>COLOR_CITY_BROWN</Type>
 			<fRed>0.70</fRed>
 			<fGreen>0.45</fGreen>
 			<fBlue>0.00</fBlue>
 			<fAlpha>1.00</fAlpha>
+		</ColorVal-->
+		<!-- advc.210a: White for now.
+			 Replacing the unused color above in order to stay compatible
+			 with the BtS replay format. -->
+		<ColorVal>
+			<Type>COLOR_WAR_TRADE_ALERT</Type>
+			<fRed>1.00</fRed>
+			<fGreen>1.00</fGreen>
+			<fBlue>1.00</fBlue>
+			<fAlpha>1.00</fAlpha>
 		</ColorVal>
 		<ColorVal>
 			<Type>COLOR_CITY_GREEN</Type>

diff --git a/CvGameCoreDLL/AdvCiv4lerts.cpp b/CvGameCoreDLL/AdvCiv4lerts.cpp
index cbefe04f0..c78a254ae 100644
--- a/CvGameCoreDLL/AdvCiv4lerts.cpp
+++ b/CvGameCoreDLL/AdvCiv4lerts.cpp
@@ -131,7 +131,8 @@ void WarTradeAlert::msg(TeamTypes warTeamId, std::vector<TeamTypes> victims, boo
 	AdvCiv4lert::msg(text, NULL,
 			// <advc.127b>
 			warTeam.getCapitalX(TEAMID(ownerId)),
-			warTeam.getCapitalY(TEAMID(ownerId))); // </advc.127b>
+			warTeam.getCapitalY(TEAMID(ownerId)), // </advc.127b>
+			GC.getColorType("WAR_TRADE_ALERT"));
 } // </advc.210a>
 
 // <advc.210b>

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1406

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1602
# Commit: aef1469368beaf90e79cefc8b3e3a22b1051bd51
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 13c22bc600e32d831f85b2f1299382109e7416cb
# Title: Increased grid size of Not-too-big-or-small map
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
6	6	PrivateMaps/not_too_Big_or_Small.py

[PATCHES]

diff --git a/PrivateMaps/not_too_Big_or_Small.py b/PrivateMaps/not_too_Big_or_Small.py
index 35105ab7f..a4198cbe7 100644
--- a/PrivateMaps/not_too_Big_or_Small.py
+++ b/PrivateMaps/not_too_Big_or_Small.py
@@ -18,12 +18,12 @@ def getGridSize(argsList):
 	if iWorldSize < 0:
 		return ()
 	sizeModifiers = {
-		WorldSizeTypes.WORLDSIZE_DUEL:		(0, -1),
-		WorldSizeTypes.WORLDSIZE_TINY:		(0, -2),
-		WorldSizeTypes.WORLDSIZE_SMALL:		(-1,-2),
-		WorldSizeTypes.WORLDSIZE_STANDARD:	(-2,-2),
-		WorldSizeTypes.WORLDSIZE_LARGE:		(-3,-2),
-		WorldSizeTypes.WORLDSIZE_HUGE:		(-5,-2)
+		WorldSizeTypes.WORLDSIZE_DUEL:		(0, 0),
+		WorldSizeTypes.WORLDSIZE_TINY:		(0, -1),
+		WorldSizeTypes.WORLDSIZE_SMALL:		(-1,-1),
+		WorldSizeTypes.WORLDSIZE_STANDARD:	(-2,-1),
+		WorldSizeTypes.WORLDSIZE_LARGE:		(-2,-2),
+		WorldSizeTypes.WORLDSIZE_HUGE:		(-4,-2)
 	}
 	wi = CyGlobalContext().getWorldInfo(iWorldSize)
 	return (sizeModifiers[iWorldSize][0] + wi.getGridWidth(), sizeModifiers[iWorldSize][1] + wi.getGridHeight())

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1005

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1486
# Commit: a4f06d6fcb94a6c1cbb2cc2817fe908e2a8ab81e
# History segment: pre-SAS AdvCiv history
# Parent used for diff: e44b359db224bf0b6bc373ebfdb2e6bb65e64eba
# Title: Capitulation offers, scoreboard icons, 2 bugfixes from Mongoose Mod
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
5	3	Assets/Python/BUG/Scoreboard.py
18	10	Assets/Python/Screens/CvMainInterface.py
4	3	Assets/xml/Text/Advanced Scoreboard Options.xml
8	6	CvGameCoreDLL/CvCity.cpp
7	4	CvGameCoreDLL/CvCityAI.cpp
2	7	CvGameCoreDLL/CvGame.cpp
1	1	CvGameCoreDLL/CvGame.h
2	1	CvGameCoreDLL/CvGameTextMgr.cpp
68	76	CvGameCoreDLL/CvPlayerAI.cpp
3	2	CvGameCoreDLL/CvPlayerAI.h
35	54	CvGameCoreDLL/CvPlot.cpp
66	111	CvGameCoreDLL/CvUnitAI.cpp
4	3	PrivateMaps/PerfectMongoose.py
-	-	manual_0.95c.odt

[PATCHES]

diff --git a/Assets/Python/BUG/Scoreboard.py b/Assets/Python/BUG/Scoreboard.py
index 63d8a27a4..5536fa2c5 100644
--- a/Assets/Python/BUG/Scoreboard.py
+++ b/Assets/Python/BUG/Scoreboard.py
@@ -255,13 +255,15 @@ class Scoreboard:
 	def setPower(self, value):
 		self._set(POWER, smallText(value))
 		
-	def setResearch(self, tech, turns):
+	def setResearch(self, tech, progress): # advc.085: Last param was turns
 		if (ScoreOpt.isShowResearchIcons()):
 			self._set(RESEARCH, tech)
 		else:
 			self._set(RESEARCH, smallText(gc.getTechInfo(tech).getDescription()))
-		if turns >= 0: # advc.004x
-			self._set(RESEARCH_TURNS, smallText(u"(%d)" % turns))
+		#if turns >= 0: # advc.004x
+		#	self._set(RESEARCH_TURNS, smallText(u"(%d)" % turns))
+		# advc.085: Replacing the two lines above
+		self._set(RESEARCH_TURNS, smallText(u" %d%%" % progress))
 		
 	def setEspionage(self):
 		self._set(ESPIONAGE)

diff --git a/Assets/Python/Screens/CvMainInterface.py b/Assets/Python/Screens/CvMainInterface.py
index 2c722f3c8..d2615439c 100644
--- a/Assets/Python/Screens/CvMainInterface.py
+++ b/Assets/Python/Screens/CvMainInterface.py
@@ -5255,8 +5255,8 @@ class CvMainInterface:
 														szBuffer = szBuffer + szTempBuffer
 														if (bAlignIcons):
 															scores.setReligion(szTempBuffer)
-													
-													if (bEspionage and gc.getTeam(eTeam).getEspionagePointsAgainstTeam(gc.getGame().getActiveTeam()) < gc.getTeam(gc.getGame().getActiveTeam()).getEspionagePointsAgainstTeam(eTeam)):
+													# advc.120h: Second condition was gc.getTeam(eTeam).getEspionagePointsAgainstTeam(gc.getGame().getActiveTeam()) < gc.getTeam(gc.getGame().getActiveTeam()).getEspionagePointsAgainstTeam(eTeam)
+													if bEspionage and gc.getPlayer(gc.getGame().getActivePlayer()).getEspionageSpendingWeightAgainstTeam(eTeam) > 0:
 														szTempBuffer = u"%c" %(gc.getCommerceInfo(CommerceTypes.COMMERCE_ESPIONAGE).getChar())
 														szBuffer = szBuffer + szTempBuffer
 														if (bAlignIcons):
@@ -5266,16 +5266,24 @@ class CvMainInterface:
 												if gc.getGame().isDebugMode() or (gc.getActivePlayer().canSeeResearch(ePlayer) and
 												(gc.getPlayer(ePlayer).getTeam() != gc.getGame().getActiveTeam() or gc.getTeam(gc.getGame().getActiveTeam()).getNumMembers() > 1)):
 												# K-Mod end
-													if (gc.getPlayer(ePlayer).getCurrentResearch() != -1):
+													eCurrentResearch = gc.getPlayer(ePlayer).getCurrentResearch() # advc.003
+													if (eCurrentResearch != -1):
+														szTempBuffer = u"-%s" %gc.getTechInfo(eCurrentResearch).getDescription()
 														# <advc.004x>
-														iTurnsLeft = gc.getPlayer(ePlayer).getResearchTurnsLeft(gc.getPlayer(ePlayer).getCurrentResearch(), True)
-														if iTurnsLeft < 0:
-															szTempBuffer = u"-%s" %(gc.getTechInfo(gc.getPlayer(ePlayer).getCurrentResearch()).getDescription())
-														else: # </advc.004x>
-															szTempBuffer = u"-%s (%d)" %(gc.getTechInfo(gc.getPlayer(ePlayer).getCurrentResearch()).getDescription(), iTurnsLeft)
-														szBuffer = szBuffer + szTempBuffer
+														#iTurnsLeft = gc.getPlayer(ePlayer).getResearchTurnsLeft(gc.getPlayer(ePlayer).getCurrentResearch(), True)
+														#if iTurnsLeft >= 0:
+														# </advc.004x>
+															#szTempBuffer = u"-%s (%d)" %(gc.getTechInfo(gc.getPlayer(ePlayer).getCurrentResearch()).getDescription(), iTurnsLeft)
+														# <advc.085> Replacing the above
+														iProgressPercent = (gc.getTeam(eTeam).getResearchProgress(eCurrentResearch) * 100) / gc.getTeam(eTeam).getResearchCost(eCurrentResearch)
+														iProgressPercent = min(iProgressPercent, 99)
+														iProgressPercent = max(iProgressPercent, 0)
+														szTempBuffer += u" %d%%" % iProgressPercent
+														# </advc.085>
+														szBuffer += szTempBuffer
 														if (bAlignIcons):
-															scores.setResearch(gc.getPlayer(ePlayer).getCurrentResearch(), iTurnsLeft)
+															# advc.085: Pass along iProgressPercent instead of iTurnsLeft
+															scores.setResearch(gc.getPlayer(ePlayer).getCurrentResearch(), iProgressPercent)
 												# BUG: ...end of indentation
 # BUG - Dead Civs - end
 # BUG - Power Rating - start

diff --git a/Assets/xml/Text/Advanced Scoreboard Options.xml b/Assets/xml/Text/Advanced Scoreboard Options.xml
index a59628ee5..ddd2efa44 100644
--- a/Assets/xml/Text/Advanced Scoreboard Options.xml	
+++ b/Assets/xml/Text/Advanced Scoreboard Options.xml	
@@ -380,12 +380,13 @@
 	<!-- advc.004: Revised (English, German); explanation for N (trade
 		 network) matches advc.124.
 		 dlph.30: Leader and civ icon
-		 advc.085: Mention the underscores -->
+		 advc.085: Mention the underscores
+		 advc.120h: Meaning of 'E' changed -->
 	<TEXT>
 		<Tag>TXT_KEY_BUG_OPT_SCORES__DISPLAYORDER_HOVER</Tag>
-		<English>The entered string determines which columns appear on the scoreboard and in which order. Available columns (legend):[NEWLINE][NEWLINE][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - Exclamation mark when refusing to talk[NEWLINE][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - Question mark for unmet rivals (multiplayer/ Debug mode)[NEWLINE][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Icon indicating war or active peace treaty[NEWLINE][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Score[NEWLINE][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - Score delta[NEWLINE][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - Rank[NEWLINE][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - City count[NEWLINE][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - Vassal icon if master of at least one vassal[NEWLINE][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - Player ID number (position in turn order)[NEWLINE][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Leader icon[NEWLINE][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Civilization icon[NEWLINE][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - The civilization's display name[NEWLINE][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - [ICON_ESPIONAGE] icon if you've accumulated more espionage points against them than vice versa[NEWLINE][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Power ratio[NEWLINE][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - Technology that is currently being researched[NEWLINE][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - The number of research turns left[NEWLINE][COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - [ICON_TRADE] icon if there is a trade connection with their capital[NEWLINE][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - [ICON_OPENBORDERS] icon if you have an Open Borders agreement with them[NEWLINE][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - [ICON_DEFENSIVEPACT] icon if you have a Defensive Pact with them[NEWLINE][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - Religion icon (state religion)[NEWLINE][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - Smiley indicating AI attitude toward you[NEWLINE][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - [ICON_ANGRYPOP] icon if you're their worst enemy[NEWLINE][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - (multiplayer) A star next to players who have already taken their turns[NEWLINE][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Network stats (ping)[NEWLINE][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - "OOS" warning next to players who are out of sync[NEWLINE][NEWLINE]To override the default spacing, place a number (positive or negative) in between two column codes.[NEWLINE][NEWLINE]Letters preceded by an underscore appear only on mouse-over and only if [COLOR_HIGHLIGHT_TEXT]Expand on Hover[COLOR_REVERT] is enabled.</English>
+		<English>The entered string determines which columns appear on the scoreboard and in which order. Available columns (legend):[NEWLINE][NEWLINE][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - Exclamation mark when refusing to talk[NEWLINE][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - Question mark for unmet rivals (multiplayer/ Debug mode)[NEWLINE][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Icon indicating war or active peace treaty[NEWLINE][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Score[NEWLINE][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - Score delta[NEWLINE][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - Rank[NEWLINE][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - City count[NEWLINE][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - Vassal icon if master of at least one vassal[NEWLINE][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - Player ID number (position in turn order)[NEWLINE][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Leader icon[NEWLINE][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Civilization icon[NEWLINE][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - The civilization's display name[NEWLINE][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - [ICON_ESPIONAGE] icon if you've set a positive weight on the Espionage screen[NEWLINE][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Power ratio[NEWLINE][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - Technology that is currently being researched[NEWLINE][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - The research progress as a percentage[NEWLINE][COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - [ICON_TRADE] icon if there is a trade connection with their capital[NEWLINE][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - [ICON_OPENBORDERS] icon if you have an Open Borders agreement with them[NEWLINE][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - [ICON_DEFENSIVEPACT] icon if you have a Defensive Pact with them[NEWLINE][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - Religion icon (state religion)[NEWLINE][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - Smiley indicating AI attitude toward you[NEWLINE][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - [ICON_ANGRYPOP] icon if you're their worst enemy[NEWLINE][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - (multiplayer) A star next to players who have already taken their turns[NEWLINE][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Network stats (ping)[NEWLINE][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - "OOS" warning next to players who are out of sync[NEWLINE][NEWLINE]To override the default spacing, place a number (positive or negative) in between two column codes.[NEWLINE][NEWLINE]Letters preceded by an underscore appear only on mouse-over and only if [COLOR_HIGHLIGHT_TEXT]Expand on Hover[COLOR_REVERT] is enabled.</English>
 		<French>Ceci d�termine l'ordre dans lequel les colonnes apparaissent.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Vous �tes en guerre contre cette civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Score de la civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - The change in the civ's score since previous turn.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - The civ's rank based on their score.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - The number of cities the civ owns.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - The civ has at least one vassal.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - The civ's player ID number.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Ic&#244;ne de chef[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Ic&#244;ne de la civilisation[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - Nom de la civilisation et/ou du dirigeant.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - Vous n'avez pas encore rencontr� cette civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - The civ refuses to talk to you.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - Votre pro rata d'espionnage avec cette civilisation est bon.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Pro rata du pouvoir de cette civilisation compar� � vous.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - La technologie que la civilisation recherche (vassaux, �quipe et espionnage).[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - Le nombre de tours qui restent � la recherche.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - Cette civilisation est connect�e � votre r�seau de commerce.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - Vous avez une accord de fronti�res ouvertes avec cette civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - Vous avez un pacte d�fensif avec cette civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - La religion nationale de cette civilisation.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - L'attitude de cette civilisation envers vous.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - You are the civ's worst enemy.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - Vous attendez que cette civilisation finisse son tour.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Statistique de R�seau (ping).[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - Le joueur est out-of-sync.[NEWLINE][NEWLINE]You can place a number (positive or negative) between column codes to override the default spacing.</French>
-		<German>Die Zeichenfolge bestimmt die Spalten der Punkteanzeige und ihre Reihenfolge:[NEWLINE][NEWLINE][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - Ausrufezeichen bei Gespr�chsverweigerung[NEWLINE][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - Fragezeichen vor unbekannten Gegnern (f�r Debug-Modus)[NEWLINE][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Kriegszustand oder Friedensvertrag[NEWLINE][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Punktestand[NEWLINE][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - Punktetrend[NEWLINE][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - Punkterang[NEWLINE][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - Anzahl der St�dte[NEWLINE][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - Vasallensymbol bei Hegemonie �ber wenigstens einen Vasallenstaat[NEWLINE][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - Interne Spieler-ID, die die Zugreihenfolge bestimmt[NEWLINE][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Staatsoberhaupt-Symbol[NEWLINE][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Zivilisationssymbol[NEWLINE][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - Anzeigename[NEWLINE][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - [ICON_ESPIONAGE]-Symbol, wenn ein Gegner weniger Spionagepunkte angesammelt hat als Sie[NEWLINE][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Machtverh�ltnis[NEWLINE][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - Derzeit erforschte Technologie[NEWLINE][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - Anzahl verbleibender Runden bis zur Erforschung[NEWLINE] [COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - [ICON_TRADE]-Symbol bei Handelsverbindung mit der gegnerischen Hauptstadt[NEWLINE][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - [ICON_OPENBORDERS]-Symbol bei Abkommen �ber offene Grenzen[NEWLINE][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - [ICON_DEFENSIVEPACT]-Symbol bei Verteidigungsb�ndnis[NEWLINE][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - Religionssymbol (Staatsreligion)[NEWLINE][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - Emoticon, das die Einstellung der KI anzeigt [NEWLINE][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - [ICON_ANGRYPOP]-Symbol, wenn Sie der Erzfeind Ihres Gegners sind[NEWLINE][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - (Mehrspielermodus) Sternchen bei Spielern, die bereits gezogen haben[NEWLINE][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Netzwerkstatistik (Ping)[NEWLINE][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - "out-of-sync"-Warnung wenn aus dem Takt[NEWLINE][NEWLINE]Zwischen den Spaltenk�rzeln k�nnen (positive und negative) Zahlen platziert werden, um Abst�nde einzustellen; ansonsten wird der Standardabstand verwendet.[NEWLINE][NEWLINE]Steht vor einem Buchstaben ein Unterstrich, dann erscheint die entspr. Spalte nur, wenn der Mauszeiger auf die Punkteanzeige bewegt wird und die Option [COLOR_HIGHLIGHT_TEXT]ausklappbare Spalten[COLOR_REVERT] aktiviert ist.</German>
+		<German>Die Zeichenfolge bestimmt die Spalten der Punkteanzeige und ihre Reihenfolge:[NEWLINE][NEWLINE][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - Ausrufezeichen bei Gespr�chsverweigerung[NEWLINE][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - Fragezeichen vor unbekannten Gegnern (f�r Debug-Modus)[NEWLINE][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Kriegszustand oder Friedensvertrag[NEWLINE][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Punktestand[NEWLINE][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - Punktetrend[NEWLINE][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - Punkterang[NEWLINE][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - Anzahl der St�dte[NEWLINE][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - Vasallensymbol bei Hegemonie �ber wenigstens einen Vasallenstaat[NEWLINE][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - Interne Spieler-ID, die die Zugreihenfolge bestimmt[NEWLINE][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Staatsoberhaupt-Symbol[NEWLINE][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Zivilisationssymbol[NEWLINE][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - Anzeigename[NEWLINE][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - [ICON_ESPIONAGE]-Symbol, falls Sie zur Zeit Spionagepunkte gegen den Gegner ansammeln[NEWLINE][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Machtverh�ltnis[NEWLINE][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - Derzeit erforschte Technologie[NEWLINE][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - Forschungsfortschritt (Prozentangabe)[NEWLINE] [COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - [ICON_TRADE]-Symbol bei Handelsverbindung mit der gegnerischen Hauptstadt[NEWLINE][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - [ICON_OPENBORDERS]-Symbol bei Abkommen �ber offene Grenzen[NEWLINE][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - [ICON_DEFENSIVEPACT]-Symbol bei Verteidigungsb�ndnis[NEWLINE][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - Religionssymbol (Staatsreligion)[NEWLINE][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - Emoticon, das die Einstellung der KI anzeigt [NEWLINE][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - [ICON_ANGRYPOP]-Symbol, wenn Sie der Erzfeind Ihres Gegners sind[NEWLINE][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - (Mehrspielermodus) Sternchen bei Spielern, die bereits gezogen haben[NEWLINE][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Netzwerkstatistik (Ping)[NEWLINE][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - "out-of-sync"-Warnung wenn aus dem Takt[NEWLINE][NEWLINE]Zwischen den Spaltenk�rzeln k�nnen (positive und negative) Zahlen platziert werden, um Abst�nde einzustellen; ansonsten wird der Standardabstand verwendet.[NEWLINE][NEWLINE]Steht vor einem Buchstaben ein Unterstrich, dann erscheint die entspr. Spalte nur, wenn der Mauszeiger auf die Punkteanzeige bewegt wird und die Option [COLOR_HIGHLIGHT_TEXT]ausklappbare Spalten[COLOR_REVERT] aktiviert ist.</German>
 		<Italian>Determina l'ordine in base al quale vengono visualizzate le colonne del Pannello del Punteggio:[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - sei in guerra o hai un trattato di pace con la Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - il Punteggio della Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - la variazione nel punteggio rispetto al turno precedente;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - La posizione della Civilt� in base al punteggio.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - Il munero delle citt� della Civilt�.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - mostra l'icona di una stella argentata se la Civilt� ha dei Vassalli;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - il numero di identificazione della Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Icona del leader[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Icona di civilt&#224;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - il Nome della Civilt�/del Leader della Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - non hai ancora incontrato questa Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - Il Leader della Civilt� rifiuta di parlarti.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - hai un Rapporto di Spionaggio positivo verso la Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - la Potenza della Civilt� rispetto alla tua (o viceversa);[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - la Tecnologia che la Civilt� sta ricercando (nota per Vassalli e Alleati o mediante Spionaggio);[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - il numero di Turni di Ricerca rimasti alla Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - la Civilt� � connessa alla tua rete commerciale;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - hai un accordo di Libero Scambio con la Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - hai un Patto Difensivo con la Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - la Religione di Stato della Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - l'atteggiamento della Civilt� verso di te;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - siete il Peggior Nemico della Civilt�;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - sei in attesa che questa Civilt� finisca il suo turno;[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - le statistiche di connessione del giocatore della Civilt� (ping);[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - il giocatore di questa Civilt� � "out-of-sync".[NEWLINE][NEWLINE]Un numero definisce la spaziatura (in pixel) che deve seguire la colonna precedente (se visualizzata), prevalendo sulla spaziatura di default; un numero negativo comporta il ravvicinamento (ed al limite anche la sovrapposizione) fra le due colonne.[NEWLINE]Gli spazi vengono ignorati, per cui possono essere utilizzati per semplificare la lettura della stringa di codici.</Italian>
 		<Spanish>Determina el orden en el que las distintas columnas aparecen.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]W[COLOR_REVERT] - Est�s en guerra o tienes un tratado de paz con esta civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]S[COLOR_REVERT] - Puntuaci�n de la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Z[COLOR_REVERT] - Cambios en puntuaci�n desde el �ltimo turno.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]K[COLOR_REVERT] - Rango de la civ basado en su puntuaci�n.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]Q[COLOR_REVERT] - N�mero de ciudades de la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]V[COLOR_REVERT] - La civ tiene al menos un vasallo.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]I[COLOR_REVERT] - ID de jugador de la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]F[COLOR_REVERT] - Icono de l&#237;der[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]G[COLOR_REVERT] - Icono de la civilizaci&#243;n[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]C[COLOR_REVERT] - Nombre de civ / l�der / jugador.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]?[COLOR_REVERT] - A�n no te has encontrado con la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]![COLOR_REVERT] - La civ se niega a hablar.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]E[COLOR_REVERT] - Tienes una proporci�n positiva de puntos de espionaje contra esta civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]P[COLOR_REVERT] - Proporci�n de fuerza de la civ comparada contigo.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]T[COLOR_REVERT] - Tecnolog�a que est� siendo investigada (vasallos, equipo y espionajes).[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]U[COLOR_REVERT] - N�mero de turnos de investigaci�n restantes.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]N[COLOR_REVERT] - La civ est� conectada a tu red de comercio.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]B[COLOR_REVERT] - Tienes apertura de fronteras con la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]D[COLOR_REVERT] - Tienes un acuerdo de protecci�n con la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]R[COLOR_REVERT] - Religi�n oficial de la civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]A[COLOR_REVERT] - Actitud de la civ hacia ti.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]H[COLOR_REVERT] - Eres el peor enemigo de esta civ.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]*[COLOR_REVERT] - Esta civ espera a que termines tu turno.[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]L[COLOR_REVERT] - Estado de la conexi�n de esta civ (ping)..[NEWLINE][ICON_BULLET][COLOR_HIGHLIGHT_TEXT]O[COLOR_REVERT] - Este jugador est� fuera de l�nea.[NEWLINE][NEWLINE]Puedes colocar un n�mero (positivo o negativo) entre c�digos de columna para reemplazar el espaciado por defecto.</Spanish>
 	</TEXT>

diff --git a/CvGameCoreDLL/CvCity.cpp b/CvGameCoreDLL/CvCity.cpp
index 9a33fd42e..4dd332ee5 100644
--- a/CvGameCoreDLL/CvCity.cpp
+++ b/CvGameCoreDLL/CvCity.cpp
@@ -14685,12 +14685,12 @@ void CvCity::getVisibleEffects(ZoomLevelTypes eCurZoom, std::vector<const TCHAR*
 
 void CvCity::getCityBillboardSizeIconColors(NiColorA& kDotColor, NiColorA& kTextColor) const
 {
+	PlayerColorTypes ePlayerColor = //GET_PLAYER(getOwnerINLINE()).getPlayerColor())
+		/*  advc.001: CvPlayer::getPlayerColor will return the Barbarian color
+			if the city owner hasn't been met (city revealed through map trade) */
+			GC.getInitCore().getColor(getOwnerINLINE());
 	NiColorA kPlayerColor = GC.getColorInfo((ColorTypes)GC.getPlayerColorInfo(
-			//GET_PLAYER(getOwnerINLINE()).getPlayerColor())
-			/*  advc.001: CvPlayer::getPlayerColor will return the Barbarian color
-				if the city owner hasn't been met (city revealed through map trade) */
-			GC.getInitCore().getColor(getOwnerINLINE()))
-			.getColorTypePrimary()).getColor();
+			ePlayerColor).getColorTypePrimary()).getColor();
 	NiColorA kGrowing;
 	kGrowing = NiColorA(0.73f,1,0.73f,1);
 	NiColorA kShrinking(1,0.73f,0.73f,1);
@@ -14729,7 +14729,9 @@ void CvCity::getCityBillboardSizeIconColors(NiColorA& kDotColor, NiColorA& kText
 	else
 	{
 		kDotColor = kPlayerColor;
-		NiColorA kPlayerSecondaryColor = GC.getColorInfo((ColorTypes) GC.getPlayerColorInfo(GET_PLAYER(getOwnerINLINE()).getPlayerColor()).getColorTypeSecondary()).getColor();
+		NiColorA kPlayerSecondaryColor = GC.getColorInfo((ColorTypes)
+				GC.getPlayerColorInfo(ePlayerColor). // advc.001
+				getColorTypeSecondary()).getColor();
 		kTextColor = kPlayerSecondaryColor;
 	}
 }

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index d9a6fae45..ce2750c43 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -3718,7 +3718,7 @@ int CvCityAI::AI_buildingValue(BuildingTypes eBuilding, int iFocusFlags,
 
 	CvPlayerAI& kOwner = GET_PLAYER(getOwnerINLINE());
 	CvTeamAI& kTeam = GET_TEAM(kOwner.getTeam()); // dlph.16
-	CvGame const& g = GC.getGame();
+	CvGame const& g = GC.getGameINLINE();
 	int iOwnerEra = kOwner.getCurrentEra();
 	CvBuildingInfo& kBuilding = GC.getBuildingInfo(eBuilding);
 	BuildingClassTypes eBuildingClass = (BuildingClassTypes) kBuilding.getBuildingClassType();
@@ -4359,9 +4359,12 @@ int CvCityAI::AI_buildingValue(BuildingTypes eBuilding, int iFocusFlags,
 
 			if (kBuilding.getFreeBonus() != NO_BONUS)
 			{
-				iValue += (kOwner.AI_bonusVal((BonusTypes)(kBuilding.getFreeBonus()), 1) *
-					((kOwner.getNumTradeableBonuses((BonusTypes)(kBuilding.getFreeBonus())) == 0) ? 2 : 1) *
-					(iNumCities + kBuilding.getNumFreeBonuses()));
+				iValue += kOwner.AI_bonusVal((BonusTypes)kBuilding.getFreeBonus(), 1) *
+						((kOwner.getNumTradeableBonuses(
+						(BonusTypes)kBuilding.getFreeBonus()) == 0) ? 2 : 1) *
+						(iNumCities + //kBuilding.getNumFreeBonuses()
+						// advc.001: Based on the Mongoose Mod changelog (15 Feb 2013)
+						g.getNumFreeBonuses(eBuilding));
 			}
 
 			if (kBuilding.getNoBonus() != NO_BONUS)

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 8ce64fdce..9cdec6f63 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -3684,16 +3684,11 @@ int CvGame::countKnownTechNumTeams(TechTypes eTech)
 }
 
 
-int CvGame::getNumFreeBonuses(BuildingTypes eBuilding)
+int CvGame::getNumFreeBonuses(BuildingTypes eBuilding) const
 {
 	if (GC.getBuildingInfo(eBuilding).getNumFreeBonuses() == -1)
-	{
 		return GC.getWorldInfo(GC.getMapINLINE().getWorldSize()).getNumFreeBuildingBonuses();
-	}
-	else
-	{
-		return GC.getBuildingInfo(eBuilding).getNumFreeBonuses();
-	}
+	else return GC.getBuildingInfo(eBuilding).getNumFreeBonuses();
 }

diff --git a/CvGameCoreDLL/CvGame.h b/CvGameCoreDLL/CvGame.h
index 306960313..6b49c040a 100644
--- a/CvGameCoreDLL/CvGame.h
+++ b/CvGameCoreDLL/CvGame.h
@@ -129,7 +129,7 @@ public:
 	int countTotalCivPower();																								// Exposed to Python
 	int countTotalNukeUnits();																							// Exposed to Python
 	int countKnownTechNumTeams(TechTypes eTech);														// Exposed to Python
-	int getNumFreeBonuses(BuildingTypes eBuilding);													// Exposed to Python
+	int getNumFreeBonuses(BuildingTypes eBuilding) const;	// advc.003: const										// Exposed to Python
 
 	int countReligionLevels(ReligionTypes eReligion) /* advc.003: */ const;							// Exposed to Python 
 	int calculateReligionPercent(ReligionTypes eReligion,						// Exposed to Python

diff --git a/CvGameCoreDLL/CvGameTextMgr.cpp b/CvGameCoreDLL/CvGameTextMgr.cpp
index 3bf2d610a..feff55863 100644
--- a/CvGameCoreDLL/CvGameTextMgr.cpp
+++ b/CvGameCoreDLL/CvGameTextMgr.cpp
@@ -1535,6 +1535,7 @@ void CvGameTextMgr::setPlotListHelpPerOwner(CvWStringBuffer& szString,
 	if(kPlot.getCenterUnit() == NULL)
 		return;
 	double fontFactor = 1;
+	// <advc.002b>
 	CvArtInfoMisc const* pTheme = ARTFILEMGR.getMiscArtInfo("DEFAULT_THEME_NAME");
 	if(pTheme != NULL && pTheme->getPath() != NULL) {
 		CvString szThemePath(pTheme->getPath());
@@ -1543,7 +1544,7 @@ void CvGameTextMgr::setPlotListHelpPerOwner(CvWStringBuffer& szString,
 			Only check if the AdvCiv theme has been removed: */
 		if(szThemePath.find("AdvCiv") == CvString::npos)
 			fontFactor = 14 / 12.0;
-	}
+	} // </advc.002b>
 	CvGame const& g = GC.getGameINLINE();
 	int iScreenHeight = g.getScreenHeight();
 	int iLineLimit = (iScreenHeight == 0 ? 25 :

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index c916845ff..d148caaee 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -1071,7 +1071,8 @@ void CvPlayerAI::AI_offerCapitulation(PlayerTypes civId) {
 	if(!GET_PLAYER(civId).isHuman()) {
 		GC.getGameINLINE().implementDeal(getID(), civId, &ourList, &theirList);
 		return;
-	}
+	} // advc.134a:
+	GET_PLAYER(civId).AI_counterPropose(getID(), ourList, theirList, true, false, 0.9);
 	AI_changeContactTimer(civId, CONTACT_PEACE_TREATY,
 			// advc.134a: Double the delay
 			2 * GC.getLeaderHeadInfo(getPersonalityType()).
@@ -11396,14 +11397,14 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 	// K-Mod end
 	// <advc.705>
 	CvGame& g = GC.getGameINLINE();
-	bool possibleCollusion = g.isOption(GAMEOPTION_RISE_FALL) &&
+	bool bPossibleCollusion = g.isOption(GAMEOPTION_RISE_FALL) &&
 			bHuman && g.getRiseFall().
 			isCooperationRestricted(getID()); // </advc.705>
 	if( !bVassalTrade )
 	{
 		if (kWeGive.getLength() == 0 && kTheyGive.getLength() > 0)
 		{	// <advc.705>
-			if(possibleCollusion && !g.getRiseFall().isSquareDeal(
+			if(bPossibleCollusion && !g.getRiseFall().isSquareDeal(
 					kWeGive, kTheyGive, getID())) {
 				g.getRiseFall().substituteDiploText(true);
 				return false;
@@ -11440,7 +11441,7 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 		is the value that we assign to what they _give_.) */
 	int iTheyReceive = kPlayer.AI_dealVal(getID(), &kWeGive, false, iChange);
 	// <advc.705>
-	int iPessimisticVal = (possibleCollusion ? g.getRiseFall().pessimisticDealVal(
+	int iPessimisticVal = (bPossibleCollusion ? g.getRiseFall().pessimisticDealVal(
 			getID(), iTheyReceive, kWeGive) : -1); // </advc.705>
 	int iWeReceive = AI_dealVal(ePlayer, &kTheyGive, false, iChange);
 	int iThreshold = -1; // advc.155: Declaration moved up
@@ -11479,91 +11480,78 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 				CvDeal::isVassalTributeDeal(&kWeGive))
 			return false; // </advc.133>
 		// <advc.130o>
-		bool demand = false;
-		bool accept = true;
-		bool vassal = kOurTeam.isVassal(kPlayer.getTeam()); // </advc.130o>
-		if(vassal && CvDeal::isVassalTributeDeal(&kWeGive))
-		{
+		bool bDemand = false;
+		bool bAccept = true;
+		bool bVassal = kOurTeam.isVassal(kPlayer.getTeam()); // </advc.130o>
+		if(bVassal && CvDeal::isVassalTributeDeal(&kWeGive)) {
 			if (AI_getAttitude(ePlayer, false) <= GC.getLeaderHeadInfo(getPersonalityType()).getVassalRefuseAttitudeThreshold()
-				//&& kOurTeam.getAtWarCount(true) == 0
-				&& !AI_isFocusWar() // advc.105: Replacing the above
-				&& TEAMREF(ePlayer).getDefensivePactCount() == 0)
-			{
+					//&& kOurTeam.getAtWarCount(true) == 0
+					&& !AI_isFocusWar() // advc.105: Replacing the above
+					&& TEAMREF(ePlayer).getDefensivePactCount() == 0) {
 				iTheyReceive *= (kOurTeam.getPower(false) + 10);
 				iTheyReceive /= (TEAMREF(ePlayer).getPower(false) + 10);
 			}
-			else
-			{
-				return true;
-			}
+			else return true;
 		}
-		else
-		{
+		else {
 			// <advc.130o> Moved up
-			int const mc = AI_getMemoryCount(ePlayer, MEMORY_MADE_DEMAND_RECENT);
-			if(mc > 0)
+			int iMemory = AI_getMemoryCount(ePlayer, MEMORY_MADE_DEMAND_RECENT);
+			if(iMemory > 0)
 				return false; // </advc.130o>
 			/*  <advc.130o> Was hard-coded, but the XML value is CAUTIOUS for all
 				leaders, so no functional change. */
-			AttitudeTypes const noGiveHelpThresh = (AttitudeTypes)GC.getLeaderHeadInfo(
+			AttitudeTypes eNoHelpThresh = (AttitudeTypes)GC.getLeaderHeadInfo(
 					getPersonalityType()).getNoGiveHelpAttitudeThreshold();
-			demand = (AI_getAttitude(ePlayer) <= noGiveHelpThresh);
-			if (demand) // </advc.130o>
-			{
+			bDemand = (AI_getAttitude(ePlayer) <= eNoHelpThresh);
+			if (bDemand) { // </advc.130o>
 				FAssert(bHuman);
-				if (kOurTeam.getPower(false) > ((TEAMREF(ePlayer).getPower(false) * 4) / 3))
-				{	// advc.130o: Don't return just yet
-					accept = false;
+				if (kOurTeam.getPower(false) > (TEAMREF(ePlayer).getPower(false) * 4) / 3) {
+					// advc.130o: Don't return just yet
+					bAccept = false;
 				}
 			} // <advc.144>
 			else { // <advc.130v>
-				if(AI_getAttitude(ePlayer, false) <= noGiveHelpThresh)
+				if(AI_getAttitude(ePlayer, false) <= eNoHelpThresh)
 					return false; // </advc.130v>
 				if(!bSameTeam && // advc.155
-						!vassal && // advc.130v
+						!bVassal && // advc.130v
 						::hash(GC.getGameINLINE().getGameTurn(), getID()) <
 						AI_prDenyHelp())
 					return false; // </advc.144>
 			}
 			// <advc.104m>
-			if(accept && demand && getWPAI.isEnabled() &&
-					!bSameTeam) // advc.155
-				accept = warAndPeaceAI().considerDemand(ePlayer, iTheyReceive);
+			if(bAccept && bDemand && getWPAI.isEnabled() && /* advc.155: */ !bSameTeam)
+				bAccept = warAndPeaceAI().considerDemand(ePlayer, iTheyReceive);
 			// </advc.104m>
 		}
 		// advc.130o: Do this only if UWAI hasn't already handled the offer
-		if(!demand || (!getWPAI.isEnabled() && accept)) {
-			iThreshold = (kOurTeam.AI_getHasMetCounter(kPlayer.getTeam()) + 50);
+		if(!bDemand || (!getWPAI.isEnabled() && bAccept)) {
+			iThreshold = kOurTeam.AI_getHasMetCounter(kPlayer.getTeam()) + 50;
 			iThreshold *= 2;
 			// <advc.155>
 			if(bSameTeam)
 				iThreshold *= 5; // </advc.155>
-			else if(!vassal) { // advc.130v
+			else if(!bVassal) { // advc.130v
 				if (TEAMREF(ePlayer).AI_isLandTarget(getTeam()))
-				{
 					iThreshold *= 3;
-				} 
 				iThreshold *= (TEAMREF(ePlayer).getPower(false) + 100);
 				iThreshold /= (kOurTeam.getPower(false) + 100);
 			} // <advc.155>
-			int randExtra = ::round((::hash(g.getGameTurn(), getID()) - 0.5) *
+			int iRandExtra = ::round((::hash(g.getGameTurn(), getID()) - 0.5) *
 					iThreshold * 0.2);
-			iThreshold += randExtra; // </advc.155>
+			iThreshold += iRandExtra; // </advc.155>
 			iThreshold -= kPlayer.AI_getPeacetimeGrantValue(getID());
-
-			accept = (iTheyReceive < iThreshold); // advc.130o: Don't return yet
+			bAccept = (iTheyReceive < iThreshold); // advc.130o: Don't return yet
 			// <advc.144>
-			if(accept && !demand &&
-					!vassal && // advc.130v
+			if(bAccept && !bDemand && /* advc.130v: */ !bVassal &&
 					getWPAI.isEnabled())
-				accept = warAndPeaceAI().considerGiftRequest(ePlayer, iTheyReceive);
+				bAccept = warAndPeaceAI().considerGiftRequest(ePlayer, iTheyReceive);
 			// </advc.144>
-		}
-		// <advc.130o>
-		if(demand) {
+		} // <advc.130o>
+		if(bDemand) {
 			/*  Moved here from CvPlayer::handleDiploEvent b/c that handler
 				can't distinguish whether the AI accepts a human demand */
-			if(accept) {
+			if(bAccept) {
 				/*  ePlayer is human; could still let the proxy AI remember,
 					but that gets confusing in R&F games when a human demands
 					tribute from a civ and later takes control of that civ.
@@ -11579,16 +11567,16 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 					with the MADE_DEMAND_RECENT memory. */
 			}
 		} // <advc.155> Previously handled by CvPlayer::handleDiploEvent
-		else if(accept && bSameTeam)
+		else if(bAccept && bSameTeam)
 			AI_changeMemoryCount(ePlayer, MEMORY_MADE_DEMAND_RECENT, 1);
 		// </advc.155>
-		return accept; // </advc.130o>
+		return bAccept; // </advc.130o>
 	} // <advc.036>
 	if(!AI_checkResourceLimits(kWeGive, kTheyGive, ePlayer, iChange))
 		return false; // </advc.036>
 	// <advc.705>
 	if(g.isOption(GAMEOPTION_RISE_FALL) && bHuman) {
-		if(possibleCollusion) {
+		if(bPossibleCollusion) {
 			double thresh = g.getRiseFall().dealThresh(isAnnualDeal(kWeGive));
 			if(iChange < 0)
 				thresh = std::min(thresh, std::max(0.4, thresh - 0.15));
@@ -11600,18 +11588,17 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 		if(!kOurTeam.isGoldTrading() && !TEAMREF(ePlayer).isGoldTrading())
 			iTheyReceive = ::round(0.9 * iTheyReceive);
 	} // </advc.705>
-	if (iChange < 0)
-	{
+	if (iChange < 0) {
 		//return (iTheirValue * 110 >= iOurValue * 100);
 		// <advc.133> Always 125 in K-Mod
-		int tolerance = (bHuman ? 145 : 155) - std::min(35, iDealAge);
+		int iTolerance = (bHuman ? 145 : 155) - std::min(35, iDealAge);
 		// <advc.155>
 		if(bSameTeam) {
 			if(iTheyReceive < iThreshold)
 				return true;
-			tolerance = 150;
+			iTolerance = 150;
 		} // </advc.155>
-		if(tolerance * // </advc.133>
+		if(iTolerance * // </advc.133>
 				iWeReceive >= iTheyReceive * 100) // K-Mod
 			return true;
 		/*  <advc.036> Need to make trades more stable in large games,
@@ -11619,7 +11606,7 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 			indefinitely -> randomize it. */
 		if(!bHuman)
 			return false;
-		double prCancel = (0.025 * tolerance) /
+		double prCancel = (0.025 * iTolerance) /
 				(GET_TEAM(kPlayer.getTeam()).getHasMetCivCount() *
 				iTheyReceive / (double)iWeReceive);
 		return (prCancel < ::hash(g.getGameTurn(), getID()));
@@ -11639,7 +11626,7 @@ bool CvPlayerAI::AI_considerOffer(PlayerTypes ePlayer,
 	if(iWeReceive >= iTheyReceive)
 		return true;
 	// <advc.036>
-	if(bHuman && iWeReceive + singleBonusTradeTolerance >= iTheyReceive &&
+	if(bHuman && iWeReceive + m_iSingleBonusTradeTolerance >= iTheyReceive &&
 			kWeGive.getLength() == 1 && kTheyGive.getLength() == 1 &&
 			kWeGive.head()->m_data.m_eItemType == TRADE_RESOURCES &&
 			kTheyGive.head()->m_data.m_eItemType == TRADE_RESOURCES)
@@ -11664,17 +11651,16 @@ bool maxValueCompare(const std::pair<TradeData*, int>& a, const std::pair<TradeD
 
 
 bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer, const CLinkList<TradeData>* pTheirList, const CLinkList<TradeData>* pOurList, CLinkList<TradeData>* pTheirInventory, CLinkList<TradeData>* pOurInventory, CLinkList<TradeData>* pTheirCounter, CLinkList<TradeData>* pOurCounter,
-		double leniency) const // advc.705: Applied to everything that's added to iTheirValue
+		double leniency) const // advc.705: Applied to everything that's added to iValueForThem
 {
 	PROFILE_FUNC(); // advc.003b
 	bool bTheirGoldDeal = AI_goldDeal(pTheirList);
 	bool bOurGoldDeal = AI_goldDeal(pOurList);
 
 	if (bOurGoldDeal && bTheirGoldDeal)
-	{
 		return false;
-	} /* <advc.036> Check trade denial. Should only be needed when renegotiating
-		 a canceled deal with a human. */
+	/*  <advc.036> Check trade denial. Should only be needed when renegotiating
+		a canceled deal with a human. */
 	if(GET_PLAYER(ePlayer).isHuman()) {
 		for(CLLNode<TradeData>* tdn = pOurList->head(); tdn != NULL;
 				tdn = pOurList->next(tdn)) {
@@ -11713,14 +11699,19 @@ bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer, const CLinkList<TradeDat
 		for (CLLNode<TradeData>* pNode = pTheirList->head(); !bEndWar && pNode; pNode = pTheirList->next(pNode))
 		{
 			if (CvDeal::isEndWar(pNode->m_data.m_eItemType))
+			{
 				bEndWar = true;
+				break;
+			}
 		}
 		for (CLLNode<TradeData>* pNode = pOurList->head(); !bEndWar && pNode; pNode = pOurList->next(pNode))
 		{
 			if (CvDeal::isEndWar(pNode->m_data.m_eItemType))
+			{
 				bEndWar = true;
+				break;
+			}
 		}
-
 		// see if someone is willing to surrender
 		if (!bEndWar)
 		{
@@ -11754,10 +11745,7 @@ bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer, const CLinkList<TradeDat
 					pOurCounter->insertAtEnd(item);
 				bEndWar = true;
 			}
-			else
-			{
-				return false; // if there is no peace, there will be no trade
-			}
+			else return false; // if there is no peace, there will be no trade
 		}
 	}
 	// <advc.132> Don't accept two civics of the same column.
@@ -11823,13 +11811,12 @@ bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer, const CLinkList<TradeDat
 	{	// <advc.003> Moved into auxiliary function
 		bDeal = AI_balanceDeal(bOurGoldDeal, pTheirInventory, ePlayer,
 			iValueForThem, iValueForUs, pTheirCounter, pOurList, 1, true,
-			// </advc.003> <advc.036>
+			// </advc.003>  <advc.036>
 			happyLeft, healthLeft, pTheirList->getLength());
 			// </advc.036>
 	}
 	else if (iValueForUs > iValueForThem)
-	{
-		// <advc.003> Moved into auxiliary function
+	{	// <advc.003> Moved into auxiliary function
 		bDeal = GET_PLAYER(ePlayer).AI_balanceDeal(bTheirGoldDeal, pOurInventory,
 				getID(), iValueForUs, iValueForThem, pOurCounter, pTheirList,
 				leniency, false, // </advc.003> <advc.036>
@@ -12122,7 +12109,7 @@ bool CvPlayerAI::AI_balanceDeal(bool bGoldDeal, CLinkList<TradeData> const* pInv
 			if(iItemValue > 0 && (bGenerous ||
 					iGreaterVal >= iSmallerVal + iItemValue
 					// <advc.036>
-					|| (iGreaterVal + singleBonusTradeTolerance >= iItemValue &&
+					|| (iGreaterVal + m_iSingleBonusTradeTolerance >= iItemValue &&
 					pCounter->getLength() <= 0 && iOtherListLength <= 0 &&
 					bSingleResource && ti == TRADE_RESOURCES))) {
 				std::pair<TradeData*,int> itemValue = std::make_pair(
@@ -12178,7 +12165,7 @@ bool CvPlayerAI::AI_balanceDeal(bool bGoldDeal, CLinkList<TradeData> const* pInv
 				if(iGreaterVal >= iSmallerVal + iItemValue
 						// <advc.036>
 						|| (bSingleResource && iGreaterVal +
-						singleBonusTradeTolerance >= iSmallerVal &&
+						m_iSingleBonusTradeTolerance >= iSmallerVal &&
 						pCounter->getLength() <= 0 && iOtherListLength <= 0 &&
 						it->first->m_eItemType == TRADE_RESOURCES)) {
 						// </advc.036>
@@ -12190,7 +12177,7 @@ bool CvPlayerAI::AI_balanceDeal(bool bGoldDeal, CLinkList<TradeData> const* pInv
 			}
 		}
 	} // <advc.036> Special treatment for one-for-one resource trades
-	if(bSingleResource && iGreaterVal - iSmallerVal <= singleBonusTradeTolerance &&
+	if(bSingleResource && iGreaterVal - iSmallerVal <= m_iSingleBonusTradeTolerance &&
 			((pCounter->getLength() <= 0 && iOtherListLength == 1 &&
 			pGoldPerTurnNode != NULL) ||
 			(pCounter->getLength() == 1 &&
@@ -12339,13 +12326,17 @@ int CvPlayerAI::AI_tradeValToGold(int iTradeVal, bool bOverpay, int iMaxGold,
 bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer,
 		CLinkList<TradeData>& kTheyGive,
 		CLinkList<TradeData>& kWeGive,
-		bool bTheyMayGiveMore, bool bWeMayGiveMore) const {
+		bool bTheyMayGiveMore, bool bWeMayGiveMore,
+		/*  advc.705: Multiplier for the value counted for the items of the
+			side that does not give more. */
+		double generosity) const {
 
 	PROFILE_FUNC();
 	FAssert(ePlayer != getID());
 	FAssertMsg(bTheyMayGiveMore || !bWeMayGiveMore, "Probably better to call "
 			"AI_counterPropose on ePlayer then");
 	FAssert(bTheyMayGiveMore || bWeMayGiveMore);
+	FAssert(generosity != 0);
 	CLinkList<TradeData> ourInventory, theirInventory, ourCounter, theirCounter;
 	if(bTheyMayGiveMore) {
 		CvPlayer const& kPlayer = GET_PLAYER(ePlayer);
@@ -12355,6 +12346,7 @@ bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer,
 		kPlayer.markTradeOffers(theirInventory, kTheyGive);
 		// "hide what should be excluded"
 		kPlayer.updateTradeList(getID(), theirInventory, kTheyGive, kWeGive);
+		generosity = 1 / generosity; // advc.705
 	}
 	if(bWeMayGiveMore) {
 		buildTradeTable(ePlayer, ourInventory);
@@ -12363,7 +12355,7 @@ bool CvPlayerAI::AI_counterPropose(PlayerTypes ePlayer,
 	}
 	if(AI_counterPropose(ePlayer, &kTheyGive, &kWeGive,
 			&theirInventory, &ourInventory,
-			&theirCounter, &ourCounter)) {
+			&theirCounter, &ourCounter, generosity)) {
 		FAssert(bWeMayGiveMore || ourCounter.getLength() == 0);
 		FAssert(bTheyMayGiveMore || theirCounter.getLength() == 0);
 		kWeGive.concatenate(ourCounter);

diff --git a/CvGameCoreDLL/CvPlayerAI.h b/CvGameCoreDLL/CvPlayerAI.h
index 407c8486b..48db1456e 100644
--- a/CvGameCoreDLL/CvPlayerAI.h
+++ b/CvGameCoreDLL/CvPlayerAI.h
@@ -681,7 +681,7 @@ protected:
 	bool m_abTheyFarAhead[MAX_CIV_PLAYERS];
 	bool m_abTheyBarelyAhead[MAX_CIV_PLAYERS]; // </advc.130c>
 	std::map<UnitClassTypes, int> m_GreatPersonWeights; // K-Mod
-	static int const singleBonusTradeTolerance = 20; // advc.036
+	static int const m_iSingleBonusTradeTolerance = 20; // advc.036
 	//mutable int* m_aiCloseBordersAttitudeCache;
 	std::vector<int> m_aiCloseBordersAttitudeCache; // K-Mod. (the original system was prone to mistakes.)
 
@@ -743,7 +743,8 @@ protected:
 	// <advc.003>
 	// Variant that writes the proposal into pTheirList and pOurList
 	bool AI_counterPropose(PlayerTypes ePlayer, CLinkList<TradeData>& kTheyGive,
-			CLinkList<TradeData>& kWeGive, bool bTheyMayGiveMore, bool bWeMayGiveMore) const;
+			CLinkList<TradeData>& kWeGive, bool bTheyMayGiveMore, bool bWeMayGiveMore,
+			double generosity = 1) const;
 	bool AI_balanceDeal(bool bGoldDeal, CLinkList<TradeData> const* pInventory,
 			PlayerTypes ePlayer, int& iGreaterVal, int& iSmallerVal,
 			CLinkList<TradeData>* pCounter,

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index b2ffc10b4..a7482cc80 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -1188,65 +1188,56 @@ bool CvPlot::isAdjacentPlotGroupConnectedBonus(PlayerTypes ePlayer, BonusTypes e
 }
 
 
-void CvPlot::updatePlotGroupBonus(bool bAdd)
+void CvPlot::updatePlotGroupBonus(bool bAdd)  // advc.003 style changes
 {
 	PROFILE_FUNC();
 
 	if (!isOwned())
-	{
 		return;
-	}
 
 	CvPlotGroup* pPlotGroup = getPlotGroup(getOwnerINLINE());
-	// <advc.003>
 	if(pPlotGroup == NULL)
-		return; // </advc.003>
+		return;
 
 	CvCity* pPlotCity = getPlotCity();
-
 	if (pPlotCity != NULL)
 	{
-		for (int iI = 0; iI < GC.getNumBonusInfos(); ++iI)
+		for (int iI = 0; iI < GC.getNumBonusInfos(); iI++)
 		{
-			if (!GET_TEAM(getTeam()).isBonusObsolete((BonusTypes)iI))
+			BonusTypes eLoopBonus = (BonusTypes)iI;
+			if (!GET_TEAM(getTeam()).isBonusObsolete(eLoopBonus))
 			{
-				pPlotGroup->changeNumBonuses(((BonusTypes)iI), (pPlotCity->getFreeBonus((BonusTypes)iI) * ((bAdd) ? 1 : -1)));
+				pPlotGroup->changeNumBonuses(eLoopBonus,
+						pPlotCity->getFreeBonus(eLoopBonus) * (bAdd ? 1 : -1));
 			}
 		}
-
 		if (pPlotCity->isCapital())
 		{
-			for (int iI = 0; iI < GC.getNumBonusInfos(); ++iI)
+			for (int iI = 0; iI < GC.getNumBonusInfos(); iI++)
 			{
-				pPlotGroup->changeNumBonuses(((BonusTypes)iI), (GET_PLAYER(getOwnerINLINE()).getBonusExport((BonusTypes)iI) * ((bAdd) ? -1 : 1)));
-				pPlotGroup->changeNumBonuses(((BonusTypes)iI), (GET_PLAYER(getOwnerINLINE()).getBonusImport((BonusTypes)iI) * ((bAdd) ? 1 : -1)));
+				BonusTypes eLoopBonus = (BonusTypes)iI;
+				pPlotGroup->changeNumBonuses(eLoopBonus,
+						GET_PLAYER(getOwnerINLINE()).getBonusExport(eLoopBonus) *
+						(bAdd ? -1 : 1));
+				pPlotGroup->changeNumBonuses(eLoopBonus,
+						GET_PLAYER(getOwnerINLINE()).getBonusImport(eLoopBonus) *
+						(bAdd ? 1 : -1));
 			}
 		}
 	}
 
 	/* original code
 	eNonObsoleteBonus = getNonObsoleteBonusType(getTeam());
-
-	if (eNonObsoleteBonus != NO_BONUS)
-	{
-		if (GET_TEAM(getTeam()).isHasTech((TechTypes)(GC.getBonusInfo(eNonObsoleteBonus).getTechCityTrade())))
-		{
-			if (isCity(true, getTeam()) ||
-				((getImprovementType() != NO_IMPROVEMENT) && GC.getImprovementInfo(getImprovementType()).isImprovementBonusTrade(eNonObsoleteBonus)))
-			{
+	if (eNonObsoleteBonus != NO_BONUS) {
+		if (GET_TEAM(getTeam()).isHasTech((TechTypes)(GC.getBonusInfo(eNonObsoleteBonus).getTechCityTrade()))) {
+			if (isCity(true, getTeam()) || ((getImprovementType() != NO_IMPROVEMENT) && GC.getImprovementInfo(getImprovementType()).isImprovementBonusTrade(eNonObsoleteBonus))) {
 				if ((pPlotGroup != NULL) && isBonusNetwork(getTeam()))
-				{
 					pPlotGroup->changeNumBonuses(eNonObsoleteBonus, ((bAdd) ? 1 : -1));
-				}
-			}
-		}
-	} */
+	} } } */
 	// K-Mod. I'm just trying to standardize the code to reduce the potential for mistakes. There are no functionality changes here.
 	BonusTypes eBonus = getNonObsoleteBonusType(getTeam(), true);
 	if (eBonus != NO_BONUS && pPlotGroup && isBonusNetwork(getTeam()))
-	{
 		pPlotGroup->changeNumBonuses(eBonus, bAdd ? 1 : -1);
-	}
 	// K-Mod end
 }
 
@@ -6100,20 +6091,16 @@ CvCity* CvPlot::getPlotCity() const
 }
 
 
-void CvPlot::setPlotCity(CvCity* pNewValue)
+void CvPlot::setPlotCity(CvCity* pNewValue)  // advc.003: style changes
 {
-	CvPlotGroup* pPlotGroup;
-	CvPlot* pLoopPlot;
-	int iI;
-
 	if(getPlotCity() == pNewValue)
 		return;
 
 	if (isCity())
 	{
-		for (iI = 0; iI < NUM_CITY_PLOTS; ++iI)
+		for (int iI = 0; iI < NUM_CITY_PLOTS; ++iI)
 		{
-			pLoopPlot = plotCity(getX_INLINE(), getY_INLINE(), iI);
+			CvPlot* pLoopPlot = plotCity(getX_INLINE(), getY_INLINE(), iI);
 
 			if (pLoopPlot != NULL)
 			{
@@ -6126,35 +6113,32 @@ void CvPlot::setPlotCity(CvCity* pNewValue)
 	updatePlotGroupBonus(false);
 	if (isCity())
 	{
-		pPlotGroup = getPlotGroup(getOwnerINLINE());
-
+		CvPlotGroup* pPlotGroup = getPlotGroup(getOwnerINLINE());
 		if (pPlotGroup != NULL)
 		{
 			FAssertMsg((0 < GC.getNumBonusInfos()), "GC.getNumBonusInfos() is not greater than zero but an array is being allocated in CvPlot::setPlotCity");
-			for (iI = 0; iI < GC.getNumBonusInfos(); ++iI)
+			for (int iI = 0; iI < GC.getNumBonusInfos(); iI++)
 			{
-				getPlotCity()->changeNumBonuses(((BonusTypes)iI), -(pPlotGroup->getNumBonuses((BonusTypes)iI)));
+				BonusTypes eLoopBonus = (BonusTypes)iI;
+				getPlotCity()->changeNumBonuses((eLoopBonus),
+						-pPlotGroup->getNumBonuses(eLoopBonus));
 			}
 		}
 	}
 	if (pNewValue != NULL)
-	{
 		m_plotCity = pNewValue->getIDInfo();
-	}
-	else
-	{
-		m_plotCity.reset();
-	}
+	else m_plotCity.reset();
 	if (isCity())
 	{
-		pPlotGroup = getPlotGroup(getOwnerINLINE());
-
+		CvPlotGroup* pPlotGroup = getPlotGroup(getOwnerINLINE());
 		if (pPlotGroup != NULL)
 		{
 			FAssertMsg((0 < GC.getNumBonusInfos()), "GC.getNumBonusInfos() is not greater than zero but an array is being allocated in CvPlot::setPlotCity");
-			for (iI = 0; iI < GC.getNumBonusInfos(); ++iI)
+			for (int iI = 0; iI < GC.getNumBonusInfos(); iI++)
 			{
-				getPlotCity()->changeNumBonuses(((BonusTypes)iI), pPlotGroup->getNumBonuses((BonusTypes)iI));
+				BonusTypes eLoopBonus = (BonusTypes)iI;
+				getPlotCity()->changeNumBonuses(eLoopBonus,
+						pPlotGroup->getNumBonuses(eLoopBonus));
 			}
 		}
 	}
@@ -6162,10 +6146,9 @@ void CvPlot::setPlotCity(CvCity* pNewValue)
 
 	if (isCity())
 	{
-		for (iI = 0; iI < NUM_CITY_PLOTS; ++iI)
+		for (int iI = 0; iI < NUM_CITY_PLOTS; ++iI)
 		{
-			pLoopPlot = plotCity(getX_INLINE(), getY_INLINE(), iI);
-
+			CvPlot* pLoopPlot = plotCity(getX_INLINE(), getY_INLINE(), iI);
 			if (pLoopPlot != NULL)
 			{
 				pLoopPlot->changeCityRadiusCount(1);
@@ -6173,10 +6156,8 @@ void CvPlot::setPlotCity(CvCity* pNewValue)
 			}
 		}
 	}
-
 	updateIrrigated();
 	updateYield();
-
 	updateMinimapColor();
 }

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index d08c9b50c..2766fca2e 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -12256,7 +12256,7 @@ bool CvUnitAI::AI_guardCitySite()
 			for(int dy = -1; dy <= 1; dy++) {
 				CvPlot* adj = m.plot(pBestGuardPlot->getX_INLINE() + dx,
 						pBestGuardPlot->getY_INLINE() + dy);
-				if(adj == NULL || adj->isImpassable())
+				if(adj == NULL || (adj->isImpassable() && !m_pUnitInfo->isCanMoveImpassable()))
 					continue;
 				int iGuardVal = adj->defenseModifier(getTeam(), true);
 				iGuardVal += adj->seeFromLevel(getTeam()) * 30;
@@ -20292,80 +20292,54 @@ bool CvUnitAI::AI_improvePlot(CvPlot* pPlot, BuildTypes eBuild)
 	
 }
 
-BuildTypes CvUnitAI::AI_betterPlotBuild(CvPlot* pPlot, BuildTypes eBuild)
+BuildTypes CvUnitAI::AI_betterPlotBuild(CvPlot* pPlot, BuildTypes eBuild)  // advc.003: some style changes
 {
 	FAssert(pPlot != NULL);
 	FAssert(eBuild != NO_BUILD);
 	bool bBuildRoute = false;
 	bool bClearFeature = false;
-	
 	FeatureTypes eFeature = pPlot->getFeatureType();
 	
 	CvBuildInfo& kOriginalBuildInfo = GC.getBuildInfo(eBuild);
-	
 	if (kOriginalBuildInfo.getRoute() != NO_ROUTE)
-	{
 		return eBuild;
-	}
 	
 	int iWorkersNeeded = AI_calculatePlotWorkersNeeded(pPlot, eBuild);
 
-	/********************************************************************************/
-	/* 	BETTER_BTS_AI_MOD						7/31/08				jdog5000	*/
-	/* 																			*/
-	/* 	Bugfix																	*/
-	/********************************************************************************/
 	//if ((pPlot->getBonusType() == NO_BONUS) && (pPlot->getWorkingCity() != NULL))
-	if ((pPlot->getNonObsoleteBonusType(getTeam()) == NO_BONUS) && (pPlot->getWorkingCity() != NULL))
-	{
+	// BETTER_BTS_AI_MOD, Bugfix, 7/31/08, jdog5000
+	if (pPlot->getNonObsoleteBonusType(getTeam()) == NO_BONUS &&
+			pPlot->getWorkingCity() != NULL)
 		iWorkersNeeded = std::max(1, std::min(iWorkersNeeded, pPlot->getWorkingCity()->AI_getWorkersHave()));
-	}
-	/********************************************************************************/
-	/* 	BETTER_BTS_AI_MOD						END								*/
-	/********************************************************************************/
 
 	if (eFeature != NO_FEATURE)
 	{
 		CvFeatureInfo& kFeatureInfo = GC.getFeatureInfo(eFeature);
 		if (kOriginalBuildInfo.isFeatureRemove(eFeature))
 		{
-			if ((kOriginalBuildInfo.getImprovement() == NO_IMPROVEMENT) || (!pPlot->isBeingWorked() || (kFeatureInfo.getYieldChange(YIELD_FOOD) + kFeatureInfo.getYieldChange(YIELD_PRODUCTION)) <= 0))
-			{
+			if (kOriginalBuildInfo.getImprovement() == NO_IMPROVEMENT ||
+					(!pPlot->isBeingWorked() ||
+					kFeatureInfo.getYieldChange(YIELD_FOOD) +
+					kFeatureInfo.getYieldChange(YIELD_PRODUCTION) <= 0))
 				bClearFeature = true;
-			}
 		}
-		
-		if ((kFeatureInfo.getMovementCost() > 1) && (iWorkersNeeded > 1))
-		{
+		if (kFeatureInfo.getMovementCost() > 1 && iWorkersNeeded > 1)
 			bBuildRoute = true;
-		}
 	}
-	/********************************************************************************/
-	/* 	BETTER_BTS_AI_MOD						7/31/08				jdog5000	*/
-	/* 																			*/
-	/* 	Bugfix																	*/
-	/********************************************************************************/
 	//if (pPlot->getBonusType() != NO_BONUS)
+	// BETTER_BTS_AI_MOD, Bugfix, 7/31/08, jdog5000: START
 	if (pPlot->getNonObsoleteBonusType(getTeam()) != NO_BONUS)
-	{
 		bBuildRoute = true;
-	}
 	else if (pPlot->isHills())
 	{
-		if ((GC.getHILLS_EXTRA_MOVEMENT() > 0) && (iWorkersNeeded > 1))
-		{
+		if (GC.getHILLS_EXTRA_MOVEMENT() > 0 && iWorkersNeeded > 1)
 			bBuildRoute = true;
-		}
-	}
-	/********************************************************************************/
-	/* 	BETTER_BTS_AI_MOD						END								*/
-	/********************************************************************************/
+	} // BETTER_BTS_AI_MOD: END
 
 	if (pPlot->getRouteType() != NO_ROUTE)
-	{
 		bBuildRoute = false;
-	}
 
+	int const NO_PLOTGROUP = FFreeList::INVALID_INDEX; // advc.003
 	BuildTypes eBestBuild = NO_BUILD;
 	int iBestValue = 0;
 	for (int iBuild = 0; iBuild < GC.getNumBuildInfos(); iBuild++)
@@ -20373,93 +20347,74 @@ BuildTypes CvUnitAI::AI_betterPlotBuild(CvPlot* pPlot, BuildTypes eBuild)
 		BuildTypes eBuild = ((BuildTypes)iBuild);
 		CvBuildInfo& kBuildInfo = GC.getBuildInfo(eBuild);
 
-		
 		RouteTypes eRoute = (RouteTypes)kBuildInfo.getRoute();
 		if ((bBuildRoute && (eRoute != NO_ROUTE)) || (bClearFeature && kBuildInfo.isFeatureRemove(eFeature)))
 		{
-			if (canBuild(pPlot, eBuild))
+			if (!canBuild(pPlot, eBuild))
+				continue;
+
+			int iValue = 10000;
+			if (bBuildRoute && eRoute != NO_ROUTE)
 			{
-				int iValue = 10000;
+				iValue *= (1 + GC.getRouteInfo(eRoute).getValue());
+				iValue /= 2;
+				//if (pPlot->getBonusType() != NO_BONUS)
+				//	BETTER_BTS_AI_MOD, Bugfix, 7/31/08, jdog5000
+				if (pPlot->getNonObsoleteBonusType(getTeam()) != NO_BONUS)
+					iValue *= 2;
 
-				if (bBuildRoute && (eRoute != NO_ROUTE))
+				if (pPlot->getWorkingCity() != NULL)
 				{
-					iValue *= (1 + GC.getRouteInfo(eRoute).getValue());
-					iValue /= 2;
-					
-					/********************************************************************************/
-					/* 	BETTER_BTS_AI_MOD						7/31/08				jdog5000	*/
-					/* 																			*/
-					/* 	Bugfix																	*/
-					/********************************************************************************/
-					//if if (pPlot->getBonusType() != NO_BONUS)
-					if (pPlot->getNonObsoleteBonusType(getTeam()) != NO_BONUS)
-					{
-						iValue *= 2;
-					}
-					/********************************************************************************/
-					/* 	BETTER_BTS_AI_MOD						END								*/
-					/********************************************************************************/
+					iValue *= 2 + iWorkersNeeded +
+							((pPlot->isHills() && iWorkersNeeded > 1) ?
+							2 * GC.getHILLS_EXTRA_MOVEMENT() : 0);
+					iValue /= 3;
+				}
+				ImprovementTypes eImprovement = (ImprovementTypes)kOriginalBuildInfo.getImprovement();
+				if (eImprovement != NO_IMPROVEMENT)
+				{
+					CvImprovementInfo const& kImprov = GC.getImprovementInfo(eImprovement);
+					int iRouteMultiplier =
+						100 * kImprov.getRouteYieldChanges(eRoute, YIELD_FOOD) +
+						100 * kImprov.getRouteYieldChanges(eRoute, YIELD_PRODUCTION) +
+						 60 * kImprov.getRouteYieldChanges(eRoute, YIELD_COMMERCE);
+					iValue *= 100 + iRouteMultiplier;
+					iValue /= 100;
+				}
+				int iPlotGroupId = NO_PLOTGROUP;
+				for (int iDirection = 0; iDirection < NUM_DIRECTION_TYPES; iDirection++)
+				{
+					CvPlot* pLoopPlot = plotDirection(pPlot->getX_INLINE(), pPlot->getY_INLINE(), (DirectionTypes)iDirection);
+					if (pLoopPlot == NULL)
+						continue;
+					if (!pPlot->isRiver() && pLoopPlot->getRouteType() == NO_ROUTE)
+						continue;
 
-					if (pPlot->getWorkingCity() != NULL)
-					{
-						iValue *= 2 + iWorkersNeeded + ((pPlot->isHills() && (iWorkersNeeded > 1)) ? 2 * GC.getHILLS_EXTRA_MOVEMENT() : 0);
-						iValue /= 3;
-					}
-					ImprovementTypes eImprovement = (ImprovementTypes)kOriginalBuildInfo.getImprovement();
-					if (eImprovement != NO_IMPROVEMENT)
-					{
-						int iRouteMultiplier = ((GC.getImprovementInfo(eImprovement).getRouteYieldChanges(eRoute, YIELD_FOOD)) * 100);
-						iRouteMultiplier += ((GC.getImprovementInfo(eImprovement).getRouteYieldChanges(eRoute, YIELD_PRODUCTION)) * 100);
-						iRouteMultiplier += ((GC.getImprovementInfo(eImprovement).getRouteYieldChanges(eRoute, YIELD_COMMERCE)) * 60);
-						iValue *= 100 + iRouteMultiplier;
-						iValue /= 100;
-					}
+					CvPlotGroup* pLoopGroup = pLoopPlot->getPlotGroup(getOwnerINLINE());
+					if (pLoopGroup == NULL || pLoopGroup->getID() == NO_PLOTGROUP)
+						continue;
 
-					int iPlotGroupId = -1;
-					for (int iDirection = 0; iDirection < NUM_DIRECTION_TYPES; iDirection++)
+					if (pLoopGroup->getID() != iPlotGroupId
+							// advc.001: Based on Mongoose Mod changelog (12-14 Dec 2012)
+							&& iPlotGroupId != NO_PLOTGROUP)
 					{
-						CvPlot* pLoopPlot = plotDirection(pPlot->getX_INLINE(), pPlot->getY_INLINE(), (DirectionTypes)iDirection);
-						if (pLoopPlot != NULL)
-						{
-							if (pPlot->isRiver() || (pLoopPlot->getRouteType() != NO_ROUTE))
-							{
-								CvPlotGroup* pLoopGroup = pLoopPlot->getPlotGroup(getOwnerINLINE());
-								if (pLoopGroup != NULL)
-								{
-									if (pLoopGroup->getID() != -1)
-									{
-										if (pLoopGroup->getID() != iPlotGroupId)
-										{
-											//This plot bridges plot groups, so route it.
-											iValue *= 4;
-											break;
-										}
-										else
-										{
-											iPlotGroupId = pLoopGroup->getID();
-										}
-									}
-								}
-							}
-						}
+						//This plot bridges plot groups, so route it.
+						iValue *= 4;
+						break;
 					}
+					else iPlotGroupId = pLoopGroup->getID();
 				}	
-
-				iValue /= (kBuildInfo.getTime() + 1);
-
-				if (iValue > iBestValue)
-				{
-					iBestValue = iValue;
-					eBestBuild = eBuild;
-				}
+			}
+			iValue /= (kBuildInfo.getTime() + 1);
+			if (iValue > iBestValue)
+			{
+				iBestValue = iValue;
+				eBestBuild = eBuild;
 			}
 		}
 	}
-	
 	if (eBestBuild == NO_BUILD)
-	{
 		return eBuild;
-	}
 	return eBestBuild;	
 }

diff --git a/PrivateMaps/PerfectMongoose.py b/PrivateMaps/PerfectMongoose.py
index cbf07f519..0c3720669 100644
--- a/PrivateMaps/PerfectMongoose.py
+++ b/PrivateMaps/PerfectMongoose.py
@@ -389,11 +389,12 @@ class MapConstants:
 
 		#The following values are used for assigning starting locations. For now,
 		#they have the same ratio that is found in CvPlot::getFoundValue
-		# <advc.021b> Was 20/40/10. Using the values from Civ 4 Reimagined instead, except for food, now valued at 23 (40 in Civ 4 Reimagined). Don't want to rule out steppe starts when there isn't enough room elsewhere. Let the normalization code worry about lack of food.
+		# <advc.021b> Was 10/40/20. Using the values from Civ 4 Reimagined instead, except for food, now valued at 23 (40 in Civ 4 Reimagined). Don't want to rule out steppe starts when there isn't enough room elsewhere. Let the normalization code worry about lack of food.
+		# Mongoose Mod uses 16/16/8 according to the changelog from 19 Jan 2013
 		# (Now that I've replaced PotentialValue with AI_foundValue in one important spot, these coefficients should be less important. They're still used though.)
-		self.CommerceValue   = 15
-		self.ProductionValue = 30
 		self.FoodValue       = 23
+		self.ProductionValue = 30
+		self.CommerceValue   = 15
 		# </advc.021b>
 
 		#Coastal cities are important, how important is determined by this

[PATCH OMITTED] file=a/manual_0.95c.odt b/manual_0.95c.odt reason=binary/generated-file changedLines=0 bytes=144

[PATCH COVERAGE]
patchFiles=14 embeddedFiles=13 omittedFiles=1 embeddedPatchBytes=63216

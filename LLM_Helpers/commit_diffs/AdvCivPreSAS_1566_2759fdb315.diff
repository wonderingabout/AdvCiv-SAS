# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1566
# Commit: 2759fdb3156b5654a41c7b3e85218f423687ed78
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 31cd543f1375ace3c6dcc8dad75ebe0dc8f28a0d
# Title: v0.96 release candidate and partly revert previous commit
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
-	-	Assets/CvGameCoreDLL.dll
3	2	CvGameCoreDLL/CvTeamAI.cpp
1	1	CvGameCoreDLL/Project/CvGameCoreDLL.rc
-	-	manual.odt
-	-	manual_0.95e.pdf => manual_0.96.pdf
 rename manual_0.95e.pdf => manual_0.96.pdf (54%)

[PATCHES]

[PATCH OMITTED] file=a/Assets/CvGameCoreDLL.dll b/Assets/CvGameCoreDLL.dll reason=binary/generated-file changedLines=0 bytes=176

diff --git a/CvGameCoreDLL/CvTeamAI.cpp b/CvGameCoreDLL/CvTeamAI.cpp
index 465df3638..6ea1956a3 100644
--- a/CvGameCoreDLL/CvTeamAI.cpp
+++ b/CvGameCoreDLL/CvTeamAI.cpp
@@ -957,8 +957,9 @@ bool CvTeamAI::AI_isWarPossible() const
 {
 	if (getAtWarCount(false) > 0)
 		return true;
-	if (GC.getGame().isOption(GAMEOPTION_ALWAYS_WAR)
-			&& isHuman()) // advc.001j: That option only applies to humans
+	/*  advc (comment): The option applies only to humans, but still implies that
+		all the non-human civs will have a (human) war enemy. */
+	if (GC.getGame().isOption(GAMEOPTION_ALWAYS_WAR))
 		return true;
 	return (!GC.getGame().isOption(GAMEOPTION_ALWAYS_PEACE) &&
 			!GC.getGame().isOption(GAMEOPTION_NO_CHANGING_WAR_PEACE));

diff --git a/CvGameCoreDLL/Project/CvGameCoreDLL.rc b/CvGameCoreDLL/Project/CvGameCoreDLL.rc
index c6a2635b0..1c14ad79b 100644
--- a/CvGameCoreDLL/Project/CvGameCoreDLL.rc
+++ b/CvGameCoreDLL/Project/CvGameCoreDLL.rc
@@ -54,7 +54,7 @@ END
 //
 
 VS_VERSION_INFO VERSIONINFO
- FILEVERSION 0,9,5,0
+ FILEVERSION 0,9,6,0
  PRODUCTVERSION 3,1,9,0
  FILEFLAGSMASK 0x17L
 #ifdef _DEBUG

[PATCH OMITTED] file=a/manual.odt b/manual.odt reason=binary/generated-file changedLines=0 bytes=120

[PATCH OMITTED] file=a/manual_0.95e.pdf b/manual_0.96.pdf reason=binary/generated-file changedLines=0 bytes=218

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=2 omittedFiles=3 embeddedPatchBytes=1108

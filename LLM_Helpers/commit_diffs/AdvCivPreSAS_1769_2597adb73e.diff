# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 1769
# Commit: 2597adb73eab6c878262f9b23236fb9a7a3cec1b
# History segment: pre-SAS AdvCiv history
# Parent used for diff: fa1e2fe0517ea97354f87f36e195faf733b07b7f
# Title: Fix Autolog crash upon founding corporation
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	Assets/Config/BUG Core.xml
1	1	Assets/Python/Contrib/autologEventManager.py
1	1	Assets/Python/K-Mod/ChangePlayer.py

[PATCHES]

diff --git a/Assets/Config/BUG Core.xml b/Assets/Config/BUG Core.xml
index ce3fdbc50..caa69f12e 100644
--- a/Assets/Config/BUG Core.xml	
+++ b/Assets/Config/BUG Core.xml	
@@ -119,5 +119,5 @@
 	<shortcut module="InputUtil" function="toggleDebugMode" keys="Ctrl Z"/>
 	<!-- advc.088: If the key combination is changed here, it'll also have to
 		 be changed in CvGlobals::suppressCycling. -->
-	<shortcut module="InputUtil" function="unselectAll" keys="Shift Alt U"/>
+	<shortcut module="InputUtil" function="unselectAll" keys="Alt Shift U"/>
 </mod>

diff --git a/Assets/Python/Contrib/autologEventManager.py b/Assets/Python/Contrib/autologEventManager.py
index f06f9a20d..5cbcaa4cf 100644
--- a/Assets/Python/Contrib/autologEventManager.py
+++ b/Assets/Python/Contrib/autologEventManager.py
@@ -729,7 +729,7 @@ class AutoLogEvent(AbstractAutoLogEvent):
 			player = PyPlayer(iFounder)
 			iCityId = gc.getGame().getHeadquarters(iCorporation).getID()
 			# advc.001: was ==0
-			if (player.getTeamID() == CyGame.getActiveTeam()):
+			if (player.getTeamID() == CyGame().getActiveTeam()):
 				messageEnd = gc.getPlayer(iFounder).getCity(iCityId).getName()
 			else:
 				messageEnd = BugUtil.getPlainText("TXT_KEY_AUTOLOG_DISTANT_LAND")

diff --git a/Assets/Python/K-Mod/ChangePlayer.py b/Assets/Python/K-Mod/ChangePlayer.py
index c6df711bd..011eaf145 100644
--- a/Assets/Python/K-Mod/ChangePlayer.py
+++ b/Assets/Python/K-Mod/ChangePlayer.py
@@ -58,7 +58,7 @@ class ChangePlayer :
 	def onKbdEvent(self, argsList ):
 		'keypress handler'
 		eventType,key,mx,my,px,py = argsList
-		# advc.127c: Check modifier keys upfront. Alt mustn't be pressed. Tbd.: Would be better to do this through a shortcut element in one of the BUG config files (see e.g. 'BUG Core.xml').
+		# advc.127c: Check cheat level. Check modifier keys upfront. Alt mustn't be pressed. Tbd.: Would be better to do this through a shortcut element in one of the BUG config files (see e.g. 'BUG Core.xml').
 		if getChtLvl() > 0 and eventType == 6 and self.customEM.bShift and self.customEM.bCtrl and not self.customEM.bAlt:
 			theKey=int(key)
 			if theKey == int(InputTypes.KB_P):

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2142

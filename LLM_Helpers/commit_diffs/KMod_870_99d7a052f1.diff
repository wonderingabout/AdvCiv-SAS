# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 870
# Commit: 99d7a052f1e2ecdc47b18c7f55957002d05175bd
# History segment: K-Mod history
# Parent used for diff: 6c4abcd100e607cd29c1e6a85292649b887ca970
# Title: Fixed an flag update problem when switching in and out of debug mode.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	0	CvGameCoreDLL/CvGame.cpp
9	0	CvGameCoreDLL/CvMap.cpp
1	0	CvGameCoreDLL/CvMap.h
2	1	CvGameCoreDLL/CvPlot.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 7e6205bff..f3dcdffb0 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -4668,6 +4668,7 @@ void CvGame::toggleDebugMode()
 	GC.getMapINLINE().updateVisibility();
 	GC.getMapINLINE().updateSymbols();
 	GC.getMapINLINE().updateMinimapColor();
+	GC.getMapINLINE().setFlagsDirty(); // K-Mod
 	updateColoredPlots(); // K-Mod
 
 	gDLL->getInterfaceIFace()->setDirty(GameData_DIRTY_BIT, true);

diff --git a/CvGameCoreDLL/CvMap.cpp b/CvGameCoreDLL/CvMap.cpp
index 7377fbf2c..a3bcc641c 100644
--- a/CvGameCoreDLL/CvMap.cpp
+++ b/CvGameCoreDLL/CvMap.cpp
@@ -338,6 +338,15 @@ void CvMap::doTurn()
 	}
 }
 
+// K-Mod
+void CvMap::setFlagsDirty()
+{
+	for (int i = 0; i < numPlotsINLINE(); i++)
+	{
+		plotByIndexINLINE(i)->setFlagDirty(true);
+	}
+}
+// K-Mod end
 
 void CvMap::updateFlagSymbols()
 {

diff --git a/CvGameCoreDLL/CvMap.h b/CvGameCoreDLL/CvMap.h
index 732af5f77..8bb00a0fb 100644
--- a/CvGameCoreDLL/CvMap.h
+++ b/CvGameCoreDLL/CvMap.h
@@ -92,6 +92,7 @@ public:
 
 	void doTurn();																			
 
+	void setFlagsDirty(); // K-Mod
 	DllExport void updateFlagSymbols();
 
 	DllExport void updateFog();

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index 0ea906da2..a31fe458b 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -8154,7 +8154,8 @@ void CvPlot::updateFlagSymbol()
 	PlayerTypes ePlayer = NO_PLAYER;
 	PlayerTypes ePlayerOffset = NO_PLAYER;
 
-	CvUnit* pCenterUnit = getCenterUnit();
+	//CvUnit* pCenterUnit = getCenterUnit();
+	CvUnit* pCenterUnit = getDebugCenterUnit(); // K-Mod
 
 	//get the plot's unit's flag
 	if (pCenterUnit != NULL)

[PATCH COVERAGE]
patchFiles=4 embeddedFiles=4 omittedFiles=0 embeddedPatchBytes=1688

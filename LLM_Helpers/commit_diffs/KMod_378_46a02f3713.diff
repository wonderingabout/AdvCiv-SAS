# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 378
# Commit: 46a02f3713e0aaea64e5c45eb2955182ac242623
# History segment: K-Mod history
# Parent used for diff: 88f00305fc8110c6e53475c3b811d84c5622e4f1
# Title: For human players, "border danger" is no longer counted on plots that are defended. (This means that combat units ordered to move through enemy lands will no...
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	CvGameCoreDLL/CvCityAI.cpp
13	3	CvGameCoreDLL/CvPlayerAI.cpp
2	18	CvGameCoreDLL/CvSelectionGroup.cpp
3	10	CvGameCoreDLL/CvSelectionGroupAI.cpp
2	1	CvGameCoreDLL/CvUnitAI.cpp

[PATCHES]

diff --git a/CvGameCoreDLL/CvCityAI.cpp b/CvGameCoreDLL/CvCityAI.cpp
index 9fd9ceb80..076720be5 100644
--- a/CvGameCoreDLL/CvCityAI.cpp
+++ b/CvGameCoreDLL/CvCityAI.cpp
@@ -7232,7 +7232,7 @@ void CvCityAI::AI_getYieldMultipliers( int &iFoodMultiplier, int &iProductionMul
 	// K-mod
 	if (kPlayer.AI_getFlavorValue(FLAVOR_PRODUCTION) > 0 || kPlayer.AI_getFlavorValue(FLAVOR_MILITARY) >= 10)
 	{
-		iProductionMultiplier *= 110 + 2 * kPlayer.AI_getFlavorValue(FLAVOR_PRODUCTION) + kPlayer.AI_getFlavorValue(FLAVOR_MILITARY);
+		iProductionMultiplier *= 113 + 2 * kPlayer.AI_getFlavorValue(FLAVOR_PRODUCTION) + kPlayer.AI_getFlavorValue(FLAVOR_MILITARY);
 		iProductionMultiplier /= 100;
 	}
 	// K-Mod end

diff --git a/CvGameCoreDLL/CvPlayerAI.cpp b/CvGameCoreDLL/CvPlayerAI.cpp
index 393c86271..9389ba613 100644
--- a/CvGameCoreDLL/CvPlayerAI.cpp
+++ b/CvGameCoreDLL/CvPlayerAI.cpp
@@ -4361,9 +4361,10 @@ int CvPlayerAI::AI_getPlotDanger(CvPlot* pPlot, int iRange, bool bTestMoves) con
 	    {
             iCount += iBorderDanger;
 	    } */
-		// K-Mod. I don't want auto-workers on the frontline; and Cities can be attacked too!
-		// but on the other hand, I don't think two border tiles are really more dangerous than one border tile...
-		iCount++;
+		// K-Mod. I don't want auto-workers on the frontline. So unless the plot is defended, count border danger for humans too.
+		// but on the other hand, I don't think two border tiles are really more dangerous than one border tile.
+		if (!isHuman() || pPlot->plotCount(PUF_canDefend, -1, -1, getID(), NO_TEAM) == 0)
+			iCount++;
 		// K-Mod end
 	}
 
@@ -6660,6 +6661,8 @@ int CvPlayerAI::AI_techUnitValue( TechTypes eTech, int iPathLength, bool &bEnabl
 							if (!kTeam.isHasTech((TechTypes)kLoopUnit.getPrereqAndTechs(iI)))
 							{
 								iMissingTechs++;
+								if (!canResearch((TechTypes)kLoopUnit.getPrereqAndTechs(iI)))
+									iMissingTechs++;
 							}
 						}
 						FAssert(iMissingTechs > 0);
@@ -11605,6 +11608,13 @@ int CvPlayerAI::AI_maxUnitCostPerMil(CvArea* pArea, int iBuildProb) const
 		iMaxUnitSpending += 5;
 	}
 
+	if (!bTotalWar)
+	{
+		iMaxUnitSpending += AI_isDoStrategy(AI_STRATEGY_ALERT1) ? 10 + iBuildProb / 4 : 0;
+		iMaxUnitSpending += AI_isDoStrategy(AI_STRATEGY_ALERT2) ? 10 + iBuildProb / 4 : 0;
+		// note. the boost from alert1 + alert2 matches the boost from total war. (see below).
+	}
+
 	if (AI_isDoStrategy(AI_STRATEGY_FINAL_WAR))
 	{
 		iMaxUnitSpending += 300;

diff --git a/CvGameCoreDLL/CvSelectionGroup.cpp b/CvGameCoreDLL/CvSelectionGroup.cpp
index 3e84b3c94..8748dec4a 100644
--- a/CvGameCoreDLL/CvSelectionGroup.cpp
+++ b/CvGameCoreDLL/CvSelectionGroup.cpp
@@ -237,16 +237,8 @@ void CvSelectionGroup::doTurn()
 
 		if (AI_isControlled())
 		{
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                      08/20/09                                jdog5000      */
-/*                                                                                              */
-/* Unit AI, Efficiency                                                                          */
-/************************************************************************************************/
 			//if ((getActivityType() != ACTIVITY_MISSION) || (!canFight() && (GET_PLAYER(getOwnerINLINE()).AI_getPlotDanger(plot(), 2) > 0)))
-			if ((getActivityType() != ACTIVITY_MISSION) || (!canFight() && (GET_PLAYER(getOwnerINLINE()).AI_getAnyPlotDanger(plot(), 2))))
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                       END                                                  */
-/************************************************************************************************/	
+			if ((getActivityType() != ACTIVITY_MISSION) || (!canFight() && (GET_PLAYER(getOwnerINLINE()).AI_getAnyPlotDanger(plot(), 2)))) //bbai
 			{
 				setForceUpdate(true);
 			}
@@ -266,16 +258,8 @@ void CvSelectionGroup::doTurn()
 					}
 				}
 
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                      08/20/09                                jdog5000      */
-/*                                                                                              */
-/* Unit AI, Efficiency                                                                          */
-/************************************************************************************************/
 				//if (bNonSpy && GET_PLAYER(getOwnerINLINE()).AI_getPlotDanger(plot(), 2) > 0)
-				if (bNonSpy && GET_PLAYER(getOwnerINLINE()).AI_getAnyPlotDanger(plot(), 2))
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                       END                                                  */
-/************************************************************************************************/
+				if (bNonSpy && GET_PLAYER(getOwnerINLINE()).AI_getAnyPlotDanger(plot(), 2)) //bbai
 				{
 					clearMissionQueue();
 				}

diff --git a/CvGameCoreDLL/CvSelectionGroupAI.cpp b/CvGameCoreDLL/CvSelectionGroupAI.cpp
index 281f1139e..a07e395d5 100644
--- a/CvGameCoreDLL/CvSelectionGroupAI.cpp
+++ b/CvGameCoreDLL/CvSelectionGroupAI.cpp
@@ -210,19 +210,12 @@ bool CvSelectionGroupAI::AI_update()
 		return false;
 	}
 
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                      04/28/10                                jdog5000      */
-/*                                                                                              */
-/* Unit AI                                                                                      */
-/************************************************************************************************/
-	//if( !(isHuman()) && !(getHeadUnit()->isCargo()) && getActivityType() == ACTIVITY_SLEEP )
-	if (getActivityType() == ACTIVITY_SLEEP && !isHuman() && !getHeadUnit()->isCargo()) // K-Mod (just for efficiency)
+	// K-Mod / BBAI
+	if (getActivityType() == ACTIVITY_SLEEP && !isHuman() && !getHeadUnit()->isCargo())
 	{
 		setForceUpdate(true);
 	}
-/************************************************************************************************/
-/* BETTER_BTS_AI_MOD                       END                                                  */
-/************************************************************************************************/
+	// end
 
 	if (isForceUpdate())
 	{

diff --git a/CvGameCoreDLL/CvUnitAI.cpp b/CvGameCoreDLL/CvUnitAI.cpp
index 9f3e83665..20e3ca50a 100644
--- a/CvGameCoreDLL/CvUnitAI.cpp
+++ b/CvGameCoreDLL/CvUnitAI.cpp
@@ -3230,7 +3230,8 @@ void CvUnitAI::AI_attackCityMove()
 				
 				if( (iJoiners*5) > getGroup()->getNumUnits() )
 				{
-					getGroup()->pushMission(MISSION_SKIP);
+					//getGroup()->pushMission(MISSION_SKIP);
+					getGroup()->pushMission(MISSION_SKIP, -1, -1, 0, false, false, MISSIONAI_GROUP); // K-Mod (for debug feedback)
 					return;
 				}

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=7159

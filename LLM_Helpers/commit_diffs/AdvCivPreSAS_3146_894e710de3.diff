# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3146
# Commit: 894e710de39602c8531b816677bf9444c3f166ad
# History segment: pre-SAS AdvCiv history
# Parent used for diff: abc39b359d2cfcb5070a2e44f5ba3247a8254c22
# Title: Coupla comment changes
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
2	2	CvGameCoreDLL/CvGameCoreDLL.h
3	2	CvGameCoreDLL/Project/Makefile.project

[PATCHES]

diff --git a/CvGameCoreDLL/CvGameCoreDLL.h b/CvGameCoreDLL/CvGameCoreDLL.h
index 82991ddce..3a180dc90 100644
--- a/CvGameCoreDLL/CvGameCoreDLL.h
+++ b/CvGameCoreDLL/CvGameCoreDLL.h
@@ -87,10 +87,10 @@ inline float MaxFloat() { return DWtoF(0x7f7fffff); }*/
 
 /*	advc: Get the limits of an integer type of at most 4 bytes length
 	(bool doesn't count either) -- since we don't have SIZE_MIN/MAX (cstdint),
-	nor boost::integer_traits<IntType>::const_max.
+	nor boost::integer_traits<T>::const_max.
 	We do have std::numeric_limits<T>::min and max, but those are functions,
 	so they can't e.g. be used in static assertions. std::numeric_limits<T>::is_signed
-	is a constant, but let's nevertheless take of that here too, so that
+	is a constant, but let's nevertheless take care of that here too, so that
 	integer_limits and numeric_limits won't have to be used side by side. */
 template<typename T>
 struct integer_limits;

diff --git a/CvGameCoreDLL/Project/Makefile.project b/CvGameCoreDLL/Project/Makefile.project
index 40b6ec1d4..44c871409 100644
--- a/CvGameCoreDLL/Project/Makefile.project
+++ b/CvGameCoreDLL/Project/Makefile.project
@@ -5,8 +5,9 @@ PROJECT_CFLAGS = /WX /W4
 PROJECT_Debug_CFLAGS = /Ob1
 # Should _not_ be set when inspecting disassembly as it will disable __forceinline
 #PROJECT_DebugOpt_CFLAGS = /DFASSERT_ENABLE /Oy-
-# Can use global optimizations along with debug symbols (also for profile builds);
-# it's just slow to compile.
+# Can use global optimizations along with debug symbols; it's just slow to compile.
+# (But not recommended for profiling with Very Sleepy; I've seen it e.g. claim
+# that _alldiv calls all sorts of GameCoreDLL functions.)
 #PROJECT_DebugOpt_CFLAGS = /GL
 # DLL-internal profiler: /DUSE_INTERNAL_PROFILER
 # Time Stamp Counter profiler: /DUSE_TSC_PROFILER

[PATCH COVERAGE]
patchFiles=2 embeddedFiles=2 omittedFiles=0 embeddedPatchBytes=1816

# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 787
# Commit: b46098c1a9a626e3fc1dfd81ab3c978ef86f94e8
# History segment: K-Mod history
# Parent used for diff: f26b0164e813359baae3e0a75bf2ba168dace3cf
# Title: Fixed a bug in the iFoodBonus count in CvGame::normalizeAddFoodBonuses.
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_001_K-Mod.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
15	1	CvGameCoreDLL/CvGame.cpp
2	2	CvGameCoreDLL/CvPlot.cpp
1	1	CvGameCoreDLL/CvPlot.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 318296fcd..a587b6168 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -1489,11 +1489,25 @@ void CvGame::normalizeAddFoodBonuses()
 									else
 									{
 										//iFoodBonus += 3;
+
 										// K-Mod. Bonus which only give 3 food with their improvement should not be worth 3 points. (ie. plains-cow should not be the only food resource.)
+										/* first attempt - this doesn't work, because "max yield" essentially means +2 food on any plot. That isn't what we want.
 										if (pLoopPlot->calculateMaxYield(YIELD_FOOD) >= 2*iFoodPerPop) // ie. >= 4
 											iFoodBonus += 3;
 										else
-											iFoodBonus += 2;
+											iFoodBonus += 2; */
+										int iNaturalFood = pLoopPlot->calculateBestNatureYield(YIELD_FOOD, kLoopPlayer.getTeam());
+										int iHighFoodThreshold = 2*iFoodPerPop; // ie. 4 food.
+										bool bHighFood = iNaturalFood + 1 >= iHighFoodThreshold; // (+1 just as a shortcut to save time for obvious cases.)
+
+										for (ImprovementTypes eImp = (ImprovementTypes)0; !bHighFood && eImp < GC.getNumImprovementInfos(); eImp=(ImprovementTypes)(eImp+1))
+										{
+											if (GC.getImprovementInfo(eImp).isImprovementBonusTrade(eBonus))
+											{
+												bHighFood = iNaturalFood + pLoopPlot->calculateImprovementYieldChange(eImp, YIELD_FOOD, (PlayerTypes)iI, false, false) >= iHighFoodThreshold;
+											}
+										}
+										iFoodBonus += bHighFood ? 3 : 2;
 										// K-Mod end
 									}
 								}

diff --git a/CvGameCoreDLL/CvPlot.cpp b/CvGameCoreDLL/CvPlot.cpp
index 45d44f52e..8dccfccbb 100644
--- a/CvGameCoreDLL/CvPlot.cpp
+++ b/CvGameCoreDLL/CvPlot.cpp
@@ -9597,7 +9597,7 @@ bool CvPlot::shouldUsePlotBuilder()
 	return false;
 }
 
-
+/* This function has been disabled by K-Mod, because it doesn't work correctly and so using it is just a magnet for bugs.
 int CvPlot::calculateMaxYield(YieldTypes eYield) const
 {
 	if (getTerrainType() == NO_TERRAIN)
@@ -9655,7 +9655,7 @@ int CvPlot::calculateMaxYield(YieldTypes eYield) const
 	}
 
 	return iMaxYield;
-}
+} */
 
 int CvPlot::getYieldWithBuild(BuildTypes eBuild, YieldTypes eYield, bool bWithUpgrade) const
 {

diff --git a/CvGameCoreDLL/CvPlot.h b/CvGameCoreDLL/CvPlot.h
index b863efebc..b30bc16c1 100644
--- a/CvGameCoreDLL/CvPlot.h
+++ b/CvGameCoreDLL/CvPlot.h
@@ -388,7 +388,7 @@ public:
 	int calculateYield(YieldTypes eIndex, bool bDisplay = false) const;												// Exposed to Python
 	bool hasYield() const;																																		// Exposed to Python
 	void updateYield();
-	int calculateMaxYield(YieldTypes eYield) const;
+	// int calculateMaxYield(YieldTypes eYield) const; // disabled by K-Mod
 	int getYieldWithBuild(BuildTypes eBuild, YieldTypes eYield, bool bWithUpgrade) const;
 
 	int getCulture(PlayerTypes eIndex) const;																									// Exposed to Python

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2941

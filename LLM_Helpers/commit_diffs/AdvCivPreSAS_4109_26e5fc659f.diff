# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 4109
# Commit: 26e5fc659fc89ddbef6f5409ec083335daaaeafb
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 05cbde45edcb50c3b3bfedc2ee0da7a0b01cc8df
# Title: A few minor misc. refactoring changes
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
1	1	CvGameCoreDLL/CvGame.cpp
1	4	CvGameCoreDLL/CvGameInterface.cpp
2	2	CvGameCoreDLL/CvPlayer.h

[PATCHES]

diff --git a/CvGameCoreDLL/CvGame.cpp b/CvGameCoreDLL/CvGame.cpp
index 5dd75d6f6..6853fbd9a 100644
--- a/CvGameCoreDLL/CvGame.cpp
+++ b/CvGameCoreDLL/CvGame.cpp
@@ -9755,7 +9755,7 @@ int CvGame::getCultureThreshold(CultureLevelTypes eLevel) const
 		static int const iNO_ESPIONAGE_CULTURE_LEVEL_MODIFIER = GC.getDefineINT("NO_ESPIONAGE_CULTURE_LEVEL_MODIFIER"); // advc.opt
 		iThreshold *= 100 + iNO_ESPIONAGE_CULTURE_LEVEL_MODIFIER;
 		iThreshold /= 100;
-	} // <advc.126>
+	}  // <advc.126>
 	int const iExempt = 50; // Don't adjust thresholds below "developing"
 	if (iThreshold >= iExempt)
 	{

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index 7a7cd19e7..c88ac9650 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -637,7 +637,7 @@ void CvGame::cycleSelectionGroups(bool bClear, bool bForward, bool bWorkers)
 	{	/*	K-Mod: I've weakend this condition so that the group cycle order
 			can be refreshed by automoves.
 			(Maybe I should create & use "sendCycleRefresh" instead.) */
-		if (pNextSelectionGroup ||
+		if (pNextSelectionGroup != NULL ||
 			GET_PLAYER(getActivePlayer()).hasAutoUnit())
 		{
 			CvMessageControl::getInstance().sendAutoMoves();
@@ -2449,10 +2449,7 @@ int CvGame::getNextSoundtrack(EraTypes eLastEra, int iLastSoundtrack) const
 			aiTracks.push_back(iTrack);
 	}
 	if (aiTracks.empty())
-	{
-		FAssert(!aiTracks.empty());
 		aiTracks.push_back(iLastSoundtrack);
-	}
 	return aiTracks[GC.getASyncRand().get(
 			aiTracks.size(), "Pick Song ASYNC")]; // </advc.002o>
 }

diff --git a/CvGameCoreDLL/CvPlayer.h b/CvGameCoreDLL/CvPlayer.h
index ad09f3ad7..c032cede9 100644
--- a/CvGameCoreDLL/CvPlayer.h
+++ b/CvGameCoreDLL/CvPlayer.h
@@ -103,10 +103,10 @@ public:
 	void killUnits();																								// Exposed to Python
 	// <advc.154>
 	CvSelectionGroup* getNextGroupInCycle(CvUnit* pUnit, bool bForward,
-			bool bWorkers, bool* pbWrap,
+			bool bWorkers, bool* pbWrap = NULL,
 			std::set<int>* pCycledGroup = NULL) const; // </advc.154>
 	CvSelectionGroup* cycleSelectionGroups(CvUnit* pUnit, bool bForward,
-			bool bWorkers, bool* pbWrap);
+			bool bWorkers, bool* pbWrap /* advc: */ = NULL);
 
 	bool hasTrait(TraitTypes eTrait) const { return GC.getInfo(getLeaderType()).hasTrait(eTrait); }					// Exposed to Python
 	// BETTER_BTS_AI_MOD, 12/30/08, jdog5000: START

[PATCH COVERAGE]
patchFiles=3 embeddedFiles=3 omittedFiles=0 embeddedPatchBytes=2386

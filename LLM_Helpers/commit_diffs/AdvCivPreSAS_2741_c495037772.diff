# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 2741
# Commit: c4950377722a25579baf28307568e14fb8fdd40a
# History segment: pre-SAS AdvCiv history
# Parent used for diff: cd2aa1694097a9e67b1489cd5a32d2d3683769bc
# Title: Move CITY_SCREEN_CLICK_WILL_EXIT option from XML to BUG menu
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
4	0	Assets/Config/BUG City Screen.xml
2	1	Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
0	6	Assets/XML/GlobalDefines_advc.xml
18	1	Assets/XML/Text/BUG City Screen Options.xml
12	7	CvGameCoreDLL/CvGameInterface.cpp

[PATCHES]

diff --git a/Assets/Config/BUG City Screen.xml b/Assets/Config/BUG City Screen.xml
index d5b3dce7c..99f7d001a 100644
--- a/Assets/Config/BUG City Screen.xml	
+++ b/Assets/Config/BUG City Screen.xml	
@@ -39,6 +39,10 @@
 				<!--choice id="All" get="isRawYieldsDefaultTilesAll"/-->
 			</list>
 
+			<!-- advc.004t: -->
+			<option id="ClickMapToExit" key="Click Map To Exit" 
+					type="boolean" default="False"/>
+			
 			<!-- FoodAssist by EmperorFool -->
 			<option id="FoodAssist" key="FoodAssist" 
 					type="boolean" default="False"

diff --git a/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py b/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
index d4156cc05..7acf1a4c5 100644
--- a/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
+++ b/Assets/Python/BUG/Tabs/BugCityScreenOptionsTab.py
@@ -123,7 +123,8 @@ class BugCityScreenOptionsTab(BugOptionsTab.BugOptionsTab):
 		
 	def createMiscellaneousPanel(self, screen, panel):
 		self.addLabel(screen, panel, "Misc", "Miscellaneous:")
-		
+		# advc.004t: New option
+		self.addCheckbox(screen, panel, "CityScreen__ClickMapToExit")
 		# advc.004: Moved up
 		self.addTextDropdown(screen, panel, panel, "CityScreen__Specialists", True)
 		#self.addCheckbox(screen, panel, "MiscHover__RemoveSpecialist")

diff --git a/Assets/XML/GlobalDefines_advc.xml b/Assets/XML/GlobalDefines_advc.xml
index 0e1b119da..ebf35153f 100644
--- a/Assets/XML/GlobalDefines_advc.xml
+++ b/Assets/XML/GlobalDefines_advc.xml
@@ -916,12 +916,6 @@
 		<iDefineIntVal>1</iDefineIntVal>
 	</Define>
 
-	<!-- [advc.004t] Copied from BtS GlobalDefines.xml where it's set to 1. -->
-	<Define>
-		<DefineName>CITY_SCREEN_CLICK_WILL_EXIT</DefineName>
-		<iDefineIntVal>0</iDefineIntVal>
-	</Define>
-
 	<!-- [advc.700] Rise & Fall parameters. The first four are applied at game
 		 start; changing them in a running game has no effect.  -->
 	<!-- Base value for the number of chapters; recommended: 7. Has to be

diff --git a/Assets/XML/Text/BUG City Screen Options.xml b/Assets/XML/Text/BUG City Screen Options.xml
index 61e5fda9d..e232323c4 100644
--- a/Assets/XML/Text/BUG City Screen Options.xml	
+++ b/Assets/XML/Text/BUG City Screen Options.xml	
@@ -80,6 +80,24 @@
 		<Spanish>Trabajadas|Ciudad|Territorio</Spanish>
 	</TEXT>
 
+	<!-- advc.004t: -->
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_CITYSCREEN__CLICKMAPTOEXIT_TEXT</Tag>
+		<English>Click Map to Exit</English>
+		<French>Click Map to Exit</French>
+		<German>Schlie�en bei Klick auf Karte</German>
+		<Italian>Click Map to Exit</Italian>
+		<Spanish>Click Map to Exit</Spanish>
+	</TEXT>
+	<TEXT>
+		<Tag>TXT_KEY_BUG_OPT_CITYSCREEN__CLICKMAPTOEXIT_HOVER</Tag>
+		<English>If checked, the city screen can be exited by left-clicking on a tile outside the city radius. (Otherwise, the city screen can still be exited via the middle mouse button, double-click outside the city radius, right-click on the city center tile, left-click on the mini-map, Esc, Enter and Space.)</English>
+		<French>If checked, the city screen can be exited by left-clicking on a tile outside the city radius. (Otherwise, the city screen can still be exited via the middle mouse button, double-click outside the city radius, right-click on the city center tile, left-click on the mini-map, Esc, Enter and Space.)</French>
+		<German>Schlie�t den Stadtbildschirm bei einem Linksklick auf ein Feld au�erhalb des Stadtradius. (Der Stadtbildschirm kann in jedem Fall auf folgenden Wegen geschlossen werden: Mittlere Maustaste, Doppelklick au�erhalb des Stadtradius, Rechtsklick aufs Stadtfeld, Linksklick auf die Mini-Karte rechts unten, Esc, Eingabe und Leertaste.)</German>
+		<Italian>If checked, the city screen can be exited by left-clicking on a tile outside the city radius. (Otherwise, the city screen can still be exited via the middle mouse button, double-click outside the city radius, right-click on the city center tile, left-click on the mini-map, Esc, Enter and Space.)</Italian>
+		<Spanish>If checked, the city screen can be exited by left-clicking on a tile outside the city radius. (Otherwise, the city screen can still be exited via the middle mouse button, double-click outside the city radius, right-click on the city center tile, left-click on the mini-map, Esc, Enter and Space.)</Spanish>
+	</TEXT>
+	
 	<!-- advc.004: "Detailed food info" sounds a bit grand (as does "Food
 		 Assist") -->
 	<TEXT>
@@ -90,7 +108,6 @@
 		<Italian>Informazioni alimentari dettagliate</Italian>
 		<Spanish>Comida Detallada</Spanish>
 	</TEXT>
-	
 	<!-- advc.004: Minor changes -->
 	<TEXT>
 		<Tag>TXT_KEY_BUG_OPT_CITYSCREEN__FOODASSIST_HOVER</Tag>

diff --git a/CvGameCoreDLL/CvGameInterface.cpp b/CvGameCoreDLL/CvGameInterface.cpp
index 7629f95dc..4d5dd14b2 100644
--- a/CvGameCoreDLL/CvGameInterface.cpp
+++ b/CvGameCoreDLL/CvGameInterface.cpp
@@ -2581,22 +2581,27 @@ void CvGame::handleCityScreenPlotPicked(CvCity* pCity, CvPlot* pPlot,
 	}
 	int iIndex = pCity->getCityPlotIndex(*pPlot);
 	if (pPlot->getOwner() == getActivePlayer() &&
-		pCity->getOwner() == getActivePlayer() && iIndex != -1)
+		pCity->getOwner() == getActivePlayer() && iIndex != NO_CITYPLOT)
 	{
 		CvMessageControl::getInstance().sendDoTask(pCity->getID(),
 				TASK_CHANGE_WORKING_PLOT, iIndex, -1, false, bAlt, bShift, bCtrl);
 	}
-	else if (GC.getDefineINT("CITY_SCREEN_CLICK_WILL_EXIT"))
+	else //if (GC.getDefineINT("CITY_SCREEN_CLICK_WILL_EXIT"))
+	if (BUGOption::isEnabled("CityScreen__ClickMapToExit", false)) // advc.004t
 		gDLL->UI().clearSelectedCities();
 }
 
 void CvGame::handleCityScreenPlotDoublePicked(CvCity* pCity, CvPlot* pPlot,
 	bool bAlt, bool bShift, bool bCtrl) const
-{	// advc.004t: Commented out
-	/*if (pCity != NULL) {
-		if (pCity->plot() == pPlot)
-			gDLL->UI().clearSelectedCities();
-	}*/
+{
+	if (pCity != NULL && pCity->plot() == pPlot)
+	{
+		/*	<advc.004t> Exit upon double click outside the radius, not upon
+			double click on the city center. */
+		/*gDLL->UI().clearSelectedCities()*/;
+	}
+	else if (pPlot != NULL && pCity->getCityPlotIndex(*pPlot) == NO_CITYPLOT)
+		gDLL->UI().clearSelectedCities(); // </advc.004t>
 }
 
 void CvGame::handleCityScreenPlotRightPicked(CvCity* pCity, CvPlot* pPlot,

[PATCH COVERAGE]
patchFiles=5 embeddedFiles=5 omittedFiles=0 embeddedPatchBytes=6183

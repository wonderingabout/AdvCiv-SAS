# AdvCiv-SAS reachable-history commit diff
# Email privacy: email-shaped addresses redacted from generated commit-history text.
# History metadata layout: 3 (history-segment label; title-only diff header; full message redirected to anonymized Git logs).
# Cache format: 2
# Cache policy: 8c87d6e4bd0f
# Practical commit count: 3809
# Commit: 9117a042bf68ab51b3036844e05d89ff3c1c25bd
# History segment: pre-SAS AdvCiv history
# Parent used for diff: 1a55c6b26d07a742b7cebcd046140f24e9cfe4aa
# Title: Fix arithmetic error in PerfectMogoose
# Full commit message/metadata: see _0_Common_Docs/git_logs/git_log_anonymized_email_002_Base_AdvCiv.txt.
# History segments describe ancestry/log partition, not authorship: SASBranch includes SAS work plus later upstream AdvCiv commits merged/imported into this branch.
# Practical commit counts can repeat on divergent/merged history; the full Git SHA is the canonical unique commit identifier.
# Diff policy: preserve textual source/docs/config history; summarize known generated/binary/noisy or exceptionally huge file patches.
# Git textconv/external diff drivers are disabled; redundant document formats such as ODT/PDF are summarized rather than converted.

[CHANGE SUMMARY - NUMSTAT + RENAME/MODE SUMMARY]
9	8	PrivateMaps/PerfectMongoose.py

[PATCHES]

diff --git a/PrivateMaps/PerfectMongoose.py b/PrivateMaps/PerfectMongoose.py
index 48ba2d8b7..189903ae2 100644
--- a/PrivateMaps/PerfectMongoose.py
+++ b/PrivateMaps/PerfectMongoose.py
@@ -2894,15 +2894,16 @@ def ShrinkMap(largeMap, lWidth, lHeight, sWidth, sHeight):
 		for x in range(sWidth):
 			weights      = 0.0
 			contributors = 0.0
-			yyStart = int(y * yScale)
-			yyStop = int((y + 1) * yScale)
-			if yyStop < ((y + 1) * yScale):
-				yyStop += 1
+			# <advc.001> Using the scale ratios here can lead to out of bounds
+			# indices due to floating-point inaccuracy.
+			yyStart = (y * lHeight) // sHeight
+			yyStop = ((y + 1) * lHeight + sHeight - 1) // sHeight
+			# </advc.001>
 			for yy in range(yyStart, yyStop):
-				xxStart = int(x * xScale)
-				xxStop = int((x + 1) * xScale)
-				if xxStop < ((x + 1) * xScale):
-					xxStop += 1
+				# <advc.001>
+				xxStart = (x * lWidth) // sWidth
+				xxStop = ((x + 1) * lWidth + sWidth - 1) // sWidth
+				# </advc.001>
 				for xx in range(xxStart, xxStop):
 					weight = GetWeight(x, y, xx, yy, xScale, yScale)
 					i = yy * lWidth + xx

[PATCH COVERAGE]
patchFiles=1 embeddedFiles=1 omittedFiles=0 embeddedPatchBytes=1104
